---
phase: 01-project-setup-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - .npmrc
  - .env.example
  - .gitignore
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/tsconfig.json
  - apps/mobile/metro.config.js
  - apps/mobile/tailwind.config.js
  - apps/mobile/nativewind-env.d.ts
  - apps/mobile/global.css
  - apps/mobile/src/app/_layout.tsx
  - apps/mobile/src/app/index.tsx
  - apps/mobile/vitest.config.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/server/src/config/env.ts
  - packages/server/src/config/supabase.ts
  - packages/server/src/routes/auth.ts
  - packages/server/src/routes/recipes.ts
  - packages/server/src/routes/pantry.ts
  - packages/server/src/routes/meal-plans.ts
  - packages/server/src/routes/shopping.ts
  - packages/server/src/routes/ai.ts
  - packages/server/src/routes/voice.ts
  - packages/server/src/middleware/auth.ts
  - packages/server/vitest.config.ts
  - packages/server/__tests__/health.test.ts
  - supabase/config.toml
  - supabase/migrations/00001_profiles.sql
  - supabase/seed.sql
autonomous: true
requirements:
  - FOUN-06

must_haves:
  truths:
    - "pnpm install succeeds at workspace root and all dependencies resolve"
    - "Hono server starts and /api/v1/health returns 200"
    - "All route stubs respond with 501 Not Implemented"
    - "Supabase local starts and profiles table exists with RLS enabled"
    - "Vitest runs in both mobile and server workspaces"
  artifacts:
    - path: "package.json"
      provides: "Workspace root with pnpm scripts"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definitions for apps/* and packages/*"
    - path: "packages/server/src/index.ts"
      provides: "Hono server entry point"
      exports: ["app"]
    - path: "supabase/migrations/00001_profiles.sql"
      provides: "Profiles table with RLS policies and auto-create trigger"
      contains: "CREATE TABLE profiles"
    - path: "packages/server/src/middleware/auth.ts"
      provides: "JWT verification middleware for protected routes"
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/routes/*.ts"
      via: "app.route() mounting"
      pattern: "app\\.route\\("
    - from: "packages/server/src/middleware/auth.ts"
      to: "@supabase/supabase-js"
      via: "getUser() with Bearer token"
      pattern: "supabase\\.auth\\.getUser"
---

<objective>
Scaffold the full pnpm monorepo with Expo SDK 55 mobile app, Hono API server, and Supabase local development. Create the foundational project structure, install all dependencies, configure Vitest for both workspaces, set up the Hono server with all route stubs and auth middleware, and create the Supabase profiles migration with RLS.

Purpose: Everything else in Phase 1 (and the entire project) depends on this foundation existing and working.
Output: Runnable monorepo where `pnpm install` works, server starts, Supabase runs locally, and tests execute.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo and Expo mobile app</name>
  <files>
    package.json, pnpm-workspace.yaml, .npmrc, .env.example, .gitignore,
    apps/mobile/package.json, apps/mobile/app.json, apps/mobile/tsconfig.json,
    apps/mobile/metro.config.js, apps/mobile/tailwind.config.js,
    apps/mobile/nativewind-env.d.ts, apps/mobile/global.css,
    apps/mobile/src/app/_layout.tsx, apps/mobile/src/app/index.tsx,
    apps/mobile/vitest.config.ts
  </files>
  <action>
    1. Create workspace root:
       - `package.json` with name "dinnertime", private: true, scripts: mobile (pnpm --filter @dinnertime/mobile), server (pnpm --filter @dinnertime/server), dev:server (pnpm --filter @dinnertime/server dev), test (pnpm -r test)
       - `pnpm-workspace.yaml` with packages: ['apps/*', 'packages/*']
       - `.npmrc` with `node-linker=hoisted` (CRITICAL for React Native compatibility -- pnpm's default isolated mode breaks Metro bundler)
       - `.env.example` with EXPO_PUBLIC_SUPABASE_URL, EXPO_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, PORT=3000
       - `.gitignore` including node_modules, .env, .expo, ios/, android/, dist/

    2. Create Expo mobile app:
       - Run `npx create-expo-app@latest apps/mobile --template default@sdk-55` to scaffold Expo SDK 55 app (uses /src directory structure by default)
       - Update apps/mobile/package.json name to "@dinnertime/mobile"
       - Install auth deps: `npx expo install expo-secure-store expo-apple-authentication` and `pnpm add @supabase/supabase-js @react-native-async-storage/async-storage aes-js react-native-get-random-values @react-native-google-signin/google-signin` and `pnpm add -D @types/aes-js`
       - Install state management: `pnpm add zustand @tanstack/react-query`
       - Install styling: `pnpm add nativewind react-native-reanimated react-native-safe-area-context` and `pnpm add -D tailwindcss@^3.4.17`
       - Configure NativeWind: create tailwind.config.js (content pointing to src/**/*.tsx), nativewind-env.d.ts, global.css with @tailwind directives
       - Create minimal src/app/_layout.tsx that just renders a Slot (will be expanded in Plan 02)
       - Create src/app/index.tsx as a simple placeholder screen
       - Create apps/mobile/vitest.config.ts configured for TypeScript (exclude react-native component files, focus on pure logic tests)

    3. Install Vitest at mobile workspace: `pnpm add -D vitest`
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm install && pnpm --filter @dinnertime/mobile vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>pnpm install succeeds with no errors. Mobile vitest config loads. Expo app directory structure exists with src/app/ layout.</done>
</task>

<task type="auto">
  <name>Task 2: Create Hono server with route stubs and Supabase migration</name>
  <files>
    packages/server/package.json, packages/server/tsconfig.json,
    packages/server/src/index.ts, packages/server/src/config/env.ts,
    packages/server/src/config/supabase.ts,
    packages/server/src/routes/auth.ts, packages/server/src/routes/recipes.ts,
    packages/server/src/routes/pantry.ts, packages/server/src/routes/meal-plans.ts,
    packages/server/src/routes/shopping.ts, packages/server/src/routes/ai.ts,
    packages/server/src/routes/voice.ts,
    packages/server/src/middleware/auth.ts,
    packages/server/vitest.config.ts, packages/server/__tests__/health.test.ts,
    supabase/config.toml, supabase/migrations/00001_profiles.sql, supabase/seed.sql
  </files>
  <action>
    1. Create Hono server package:
       - `packages/server/package.json` with name "@dinnertime/server", type "module", scripts: dev (tsx watch src/index.ts), start (node dist/index.js), test (vitest run)
       - Install: `pnpm add hono @hono/node-server @supabase/supabase-js zod` and `pnpm add -D typescript vitest @types/node tsx`
       - `packages/server/tsconfig.json` targeting ES2022, module NodeNext, moduleResolution NodeNext, outDir dist, rootDir src

    2. Create server source files:
       - `src/config/env.ts`: Export validated env vars (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, PORT) with runtime checks
       - `src/config/supabase.ts`: Export two Supabase clients -- `supabaseAdmin` (service role key, for privileged operations) and `createUserClient(token)` (anon key + user Bearer token, for RLS-respecting queries)
       - `src/middleware/auth.ts`: Export Hono middleware that extracts Bearer token from Authorization header, calls supabase.auth.getUser(), returns 401 if invalid, sets `c.set('user', user)` and `c.set('supabase', userClient)` on context for downstream handlers
       - `src/index.ts`: Create Hono app with basePath('/api/v1'), add logger() and cors() middleware, mount health check at GET /health returning { status: 'ok' }, mount all route files under /api/v1, serve on PORT with @hono/node-server

    3. Create route stubs (each as its own Hono router):
       - `src/routes/auth.ts`: POST /signup (stub), POST /login (stub), POST /logout (stub) -- these are NOT protected by auth middleware
       - `src/routes/recipes.ts`: GET / and POST / stubs, protected by auth middleware
       - `src/routes/pantry.ts`: GET / and POST / stubs, protected
       - `src/routes/meal-plans.ts`: GET / and POST / stubs, protected
       - `src/routes/shopping.ts`: GET / and POST / stubs, protected
       - `src/routes/ai.ts`: POST /suggest stubs, protected
       - `src/routes/voice.ts`: POST /transcribe stub, protected
       - All stubs return `c.json({ data: [], message: 'Not implemented' }, 501)`

    4. Create Vitest setup:
       - `packages/server/vitest.config.ts`
       - `packages/server/__tests__/health.test.ts`: Test that GET /api/v1/health returns 200 with { status: 'ok' } using Hono's test client (app.request())

    5. Create Supabase local config:
       - Run `supabase init` in the supabase/ directory (or create config.toml manually if CLI not available)
       - `supabase/migrations/00001_profiles.sql`: Create profiles table with columns: id (UUID PK referencing auth.users ON DELETE CASCADE), display_name TEXT, household_size INTEGER DEFAULT 2, dietary_preferences JSONB DEFAULT '[]', cuisine_preferences JSONB DEFAULT '[]', disliked_ingredients JSONB DEFAULT '[]', onboarding_complete BOOLEAN DEFAULT FALSE, created_at TIMESTAMPTZ DEFAULT NOW(), updated_at TIMESTAMPTZ DEFAULT NOW(). Enable RLS. Create policies: SELECT own row, UPDATE own row, INSERT own row. Create trigger function handle_new_user() that auto-inserts a profile row on auth.users INSERT.
       - `supabase/seed.sql`: Empty file with comment explaining it runs after migrations
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm install && pnpm --filter @dinnertime/server vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>Server health test passes. All route stubs are mounted and return 501. Auth middleware is wired to protected routes. Supabase migration SQL file exists with profiles table, RLS policies, and auto-create trigger. Server starts with `pnpm dev:server`.</done>
</task>

</tasks>

<verification>
1. `pnpm install` succeeds at workspace root
2. `pnpm --filter @dinnertime/server vitest run` -- health test passes
3. `pnpm --filter @dinnertime/mobile vitest run --passWithNoTests` -- vitest loads
4. `pnpm dev:server` starts Hono on port 3000, `curl http://localhost:3000/api/v1/health` returns 200
5. `cat supabase/migrations/00001_profiles.sql` contains CREATE TABLE, RLS policies, trigger
</verification>

<success_criteria>
- Monorepo structure exists: apps/mobile, packages/server, supabase/
- pnpm install resolves all dependencies
- Hono server starts and health check responds
- All 7 route stubs are mounted and return 501
- Auth middleware exists and verifies Bearer tokens
- Supabase migration creates profiles table with RLS
- Vitest runs in both workspaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-auth/01-01-SUMMARY.md`
</output>
