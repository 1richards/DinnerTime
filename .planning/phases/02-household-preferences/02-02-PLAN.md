---
phase: 02-household-preferences
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/mobile/src/stores/preferencesStore.ts
  - apps/mobile/src/hooks/usePreferences.ts
  - apps/mobile/src/stores/__tests__/preferencesStore.test.ts
  - apps/mobile/src/hooks/__tests__/useIngredientSearch.test.ts
  - apps/mobile/src/hooks/useIngredientSearch.ts
autonomous: true
requirements: [FOUN-03, FOUN-04, FOUN-05]

must_haves:
  truths:
    - "Household members can be created, read, updated, and deleted"
    - "Dietary restrictions and allergies are stored separately per member"
    - "Cuisine preferences and skill level can be updated on the profile"
    - "Ingredient search returns filtered results from the curated list"
    - "All preference changes auto-save to Supabase"
  artifacts:
    - path: "apps/mobile/src/stores/preferencesStore.ts"
      provides: "Zustand store for household preferences"
      exports: ["usePreferencesStore"]
    - path: "apps/mobile/src/hooks/usePreferences.ts"
      provides: "CRUD mutations for preferences with auto-save"
      exports: ["useAddMember", "useUpdateMember", "useDeleteMember", "useUpdateProfile"]
    - path: "apps/mobile/src/hooks/useIngredientSearch.ts"
      provides: "Ingredient search hook"
      exports: ["useIngredientSearch"]
    - path: "apps/mobile/src/stores/__tests__/preferencesStore.test.ts"
      provides: "Unit tests for preferences store"
    - path: "apps/mobile/src/hooks/__tests__/useIngredientSearch.test.ts"
      provides: "Unit tests for ingredient search"
  key_links:
    - from: "apps/mobile/src/stores/preferencesStore.ts"
      to: "apps/mobile/src/lib/supabase.ts"
      via: "supabase.from('household_members') and supabase.from('profiles')"
      pattern: "supabase\\.from\\("
    - from: "apps/mobile/src/hooks/usePreferences.ts"
      to: "apps/mobile/src/stores/preferencesStore.ts"
      via: "optimistic updates in store, Supabase writes via useMutation"
      pattern: "useMutation|usePreferencesStore"
    - from: "apps/mobile/src/hooks/useIngredientSearch.ts"
      to: "apps/mobile/src/data/ingredients.ts"
      via: "searchIngredients function"
      pattern: "searchIngredients"
---

<objective>
Build the preferences Zustand store, CRUD hooks with auto-save via useMutation, and ingredient search hook. Test-first for store logic and search filtering.

Purpose: Provide the data management layer that the settings UI (Plan 03) will consume. All Supabase interactions are encapsulated here.
Output: Tested preferences store, CRUD mutation hooks, ingredient search hook.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-preferences/02-CONTEXT.md
@.planning/phases/02-household-preferences/02-RESEARCH.md
@.planning/phases/02-household-preferences/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - types/preferences.ts (will exist after Plan 01) -->
export type SkillLevel = 'beginner' | 'intermediate' | 'confident' | 'adventurous';
export type AgeRange = 'toddler' | 'young_kid' | 'older_kid' | 'teen';
export type MemberType = 'adult' | 'kid';
export type DietaryOption = 'Vegetarian' | 'Vegan' | 'Gluten-Free' | 'Dairy-Free' | 'Nut Allergy' | 'Keto' | 'Paleo';
export type CuisineOption = 'Italian' | 'Mexican' | 'Chinese' | 'Japanese' | 'Indian' | 'Thai' | 'Mediterranean' | 'American' | 'Korean' | 'French';

export interface HouseholdMember {
  id: string;
  profile_id: string;
  name: string;
  member_type: MemberType;
  age_range: AgeRange | null;
  dietary_restrictions: DietaryOption[];
  dietary_allergies: DietaryOption[];
  disliked_ingredients: string[];
  created_at: string;
  updated_at: string;
}

<!-- From Plan 01 - data/ingredients.ts -->
export const INGREDIENTS: string[];
export function searchIngredients(query: string): string[];

<!-- Existing - stores/authStore.ts Profile interface (updated in Plan 01) -->
export interface Profile {
  id: string;
  display_name: string | null;
  household_size: number;
  dietary_preferences: string[];
  cuisine_preferences: string[];
  disliked_ingredients: string[];
  onboarding_complete: boolean;
  skill_level: SkillLevel;
  created_at: string;
  updated_at: string;
}

<!-- Existing - lib/supabase.ts -->
export const supabase: SupabaseClient;

<!-- Existing - hooks/useAuth.ts pattern for reference -->
// Uses { data, error } return pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preferences store and CRUD hooks with tests</name>
  <files>apps/mobile/src/stores/__tests__/preferencesStore.test.ts, apps/mobile/src/stores/preferencesStore.ts, apps/mobile/src/hooks/usePreferences.ts</files>
  <behavior>
    - Test: loadPreferences fetches members from household_members table and profile data, populates store
    - Test: addMember inserts into household_members, adds to store.members array
    - Test: updateMember updates a member by id, reflects in store
    - Test: deleteMember removes from household_members and store.members
    - Test: updateCuisinePreferences updates profiles.cuisine_preferences
    - Test: updateSkillLevel updates profiles.skill_level
    - Test: dietary_restrictions and dietary_allergies are separate arrays on each member
    - Test: updateMemberDislikes replaces the full disliked_ingredients array (not appends)
  </behavior>
  <action>
1. Create test file `apps/mobile/src/stores/__tests__/preferencesStore.test.ts`:
   - Mock supabase using vi.mock and vi.hoisted() (same pattern as auth-store.test.ts)
   - Test loadPreferences: mock supabase.from('household_members').select().eq() returning member array, and supabase.from('profiles').select().eq().single() returning profile. Assert store.members populated, store.cuisinePreferences populated, store.skillLevel populated.
   - Test addMember: mock supabase.from('household_members').insert().select().single(). Assert new member appears in store.members.
   - Test updateMember: mock supabase.from('household_members').update().eq(). Assert member fields updated in store.
   - Test deleteMember: mock supabase.from('household_members').delete().eq(). Assert member removed from store.members.
   - Test updateCuisinePreferences: mock supabase.from('profiles').update().eq(). Assert store.cuisinePreferences updated.
   - Test updateSkillLevel: mock supabase.from('profiles').update().eq(). Assert store.skillLevel updated.
   - Test dietary separation: verify a member can have dietary_restrictions: ['Vegetarian'] and dietary_allergies: ['Nut Allergy'] as separate fields.
   - Run tests -- they should all FAIL (RED).

2. Create `apps/mobile/src/stores/preferencesStore.ts`:
   - import { create } from 'zustand'
   - import { supabase } from '../lib/supabase'
   - import types from '../types/preferences'
   - Interface PreferencesState: members: HouseholdMember[], cuisinePreferences: CuisineOption[], skillLevel: SkillLevel, isLoading: boolean
   - Methods: loadPreferences(profileId), addMember(member), updateMember(id, updates), deleteMember(id), updateCuisinePreferences(profileId, prefs), updateSkillLevel(profileId, level)
   - Each mutation does optimistic local update first, then Supabase write. On error, roll back local state.
   - For array fields (dietary, dislikes, cuisine), always send the full array to Supabase (not diffs).

3. Create `apps/mobile/src/hooks/usePreferences.ts`:
   - Uses @tanstack/react-query useMutation wrapping each store method
   - useAddMember(): calls preferencesStore.addMember, returns mutation state
   - useUpdateMember(): calls preferencesStore.updateMember
   - useDeleteMember(): calls preferencesStore.deleteMember
   - useUpdateProfile(): generic profile field updater (cuisine, skill level)
   - Each mutation shows brief success state for toast (via onSuccess callback exposing a flag the UI can read)

4. Run tests -- they should all PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && npx vitest run src/stores/__tests__/preferencesStore.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Preferences store manages household members, cuisine preferences, and skill level with optimistic updates. All store tests pass. CRUD hooks wrap mutations with useMutation for error handling and loading states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ingredient search hook with tests</name>
  <files>apps/mobile/src/hooks/__tests__/useIngredientSearch.test.ts, apps/mobile/src/hooks/useIngredientSearch.ts</files>
  <behavior>
    - Test: empty query returns empty array
    - Test: query "chick" returns items starting with or containing "chick" (e.g., Chicken, Chickpeas)
    - Test: search is case-insensitive ("BROCCOLI" matches "Broccoli")
    - Test: returns at most 10 results
    - Test: excludes items already in the excluded list (already-disliked items)
    - Test: query with no matches returns empty array
  </behavior>
  <action>
1. Create test file `apps/mobile/src/hooks/__tests__/useIngredientSearch.test.ts`:
   - Test the pure search logic (not the hook itself, since hooks need React rendering context)
   - Import searchIngredients from '../../data/ingredients'
   - Test cases per behavior list above
   - Add an additional test for the exclusion filter: searchIngredients('chicken', ['Chicken']) should not include 'Chicken' in results
   - Run tests -- FAIL (RED) because searchIngredients doesn't handle exclusions yet.

2. Update `apps/mobile/src/data/ingredients.ts` searchIngredients function:
   - Add optional second parameter: excludedItems: string[] = []
   - Filter out items that are in excludedItems before returning results

3. Create `apps/mobile/src/hooks/useIngredientSearch.ts`:
   - import { useState, useDeferredValue } from 'react'
   - import { searchIngredients } from '../data/ingredients'
   - export function useIngredientSearch(excludedItems: string[]): { query: string, setQuery: (q: string) => void, results: string[] }
   - Use useDeferredValue on the query to avoid janky typing
   - Returns filtered results from searchIngredients(deferredQuery, excludedItems)

4. Run tests -- PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && npx vitest run src/hooks/__tests__/useIngredientSearch.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>Ingredient search filters from curated list, is case-insensitive, caps at 10 results, excludes already-selected items. All tests pass. useIngredientSearch hook uses useDeferredValue for smooth typing.</done>
</task>

</tasks>

<verification>
- All preference store tests pass: `cd apps/mobile && npx vitest run src/stores/__tests__/preferencesStore.test.ts`
- All ingredient search tests pass: `cd apps/mobile && npx vitest run src/hooks/__tests__/useIngredientSearch.test.ts`
- Full test suite still passes: `cd apps/mobile && pnpm test`
- TypeScript compiles: `cd apps/mobile && npx tsc --noEmit`
</verification>

<success_criteria>
- Preferences store supports full CRUD for household members with optimistic updates
- Dietary restrictions and allergies are tracked separately per member (per user decision)
- Cuisine preferences and skill level are updatable on the profile
- Ingredient search is fast (local filtering), case-insensitive, and excludes already-selected items
- All tests pass (8+ store tests, 6+ search tests)
- Hooks provide clean API for settings UI consumption
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-preferences/02-02-SUMMARY.md`
</output>
