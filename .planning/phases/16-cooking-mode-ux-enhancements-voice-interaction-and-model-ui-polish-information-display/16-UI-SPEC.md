---
phase: 16
slug: cooking-mode-ux-enhancements
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-20
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for Cooking Mode UX Enhancements.
> Ships the Claude.ai-artifact-style scrollable recipe layout, Phase 19 token
> adoption, voice-command feedback surfaces, and an optional dark cooking theme.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (NativeWind + Phase 19 tokens — not shadcn) |
| Preset | not applicable |
| Component library | none (custom primitives under `apps/mobile/src/components/ui/*`, Phase 19 shipped) |
| Icon library | SF Symbols via `expo-symbols` (`SymbolIcon`/`SymbolView`) — Phase 15 convention |
| Font | SF Pro (iOS system font; React Native default) |

Token sources consumed by this phase:
- `apps/mobile/src/design/tokens.ts` — typed color/typography/spacing/radius mirror
- `apps/mobile/src/design/typography.ts` — `textStyles` for StyleSheet consumers
- `apps/mobile/src/design/icons.ts` — `iconSize` + `iconPropsForText(scale)` helpers
- `apps/mobile/src/global.css` — CSS variables (light palette live; dark palette sketched in commented block)
- `apps/mobile/tailwind.config.js` — NativeWind className resolution

**Do NOT hardcode colors or hex values.** Every color in new cooking-mode code
must resolve to a token. Phase 16 introduces no new base tokens; it only adds
a dark-palette override layer behind a runtime flag (see Color section).

---

## Spacing Scale

Phase 19 8pt grid is authoritative. Declared tokens (must be multiples of 4):

| Token (Tailwind) | Value | Usage in cooking mode |
|-------|-------|-------|
| `p-1` / gap-1 | 4px | Icon-to-text gap inside toasts and chips |
| `p-2` / gap-2 | 8px | Timer chip internal padding; waveform-bar gaps |
| `p-3` | 12px | Ingredient-row vertical padding |
| `p-4` | 16px | Recipe horizontal gutter, step-card padding |
| `p-6` | 24px | Section break between Ingredients and Steps, step-card vertical padding |
| `p-8` | 32px | Top padding above first section below sticky header |
| `p-12` | 48px | Bottom content inset so last step clears nav bar |

Component-level spacing contract:
- Sticky header (exit + voice waveform + timer row + stop): **total height 64px when no timers active, 112px when timers active.** Timer chip row adds a fixed 48pt band.
- Step cards: **vertical gap between cards = 16px**; current-step card uses 24px internal padding, non-current cards use 16px.
- Nav bar (Back / Repeat / Next) bottom-anchored: **72pt height** (deliberate Phase 19 deviation — see Exceptions).

**Exceptions (documented deviations):**
1. **Nav buttons at 72pt, not 44pt.** `StepNavButtons` Back/Repeat/Next use 72pt (not Phase 19's 44pt minimum). Rationale: cooking-hands accessibility — wet/greasy fingertips need a larger target. Tokens (color, radius, icon sizing) stay Phase-19-compliant; only height is overridden. Document this in 16-0X-PLAN.md and in a code-level comment on the component.
2. **Empty-state glyph size (56px raw)** from existing `EmptyState` is preserved; not added by this phase but inherited if a loading/error fallback renders.

---

## Typography

Phase 19 5-step type scale is the authoritative source. Cooking mode uses 4 of
the 5 steps; `label` is reserved for the TIMER countdown chip label only.

| Role | Size | Weight | Line Height | Letter Spacing | Token |
|------|------|--------|-------------|----------------|-------|
| Display | 34pt | 700 | 41pt | -0.8 | `text-display` |
| Title | 22pt | 600 | 28pt | -0.3 | `text-title` |
| Body | 17pt | 400 | 22pt | 0 | `text-body` |
| Caption | 13pt | 400 | 18pt | 0 | `text-caption` |
| Label | 11pt | 600 | 16pt | +0.6 | `text-label` (uppercase) |

Role assignments in cooking mode:

| Element | Role | Notes |
|---------|------|-------|
| Current step text (highlighted) | `display` (34pt/700) | Counter-distance readability is the primary constraint |
| Non-current step text | `title` (22pt/600) | Visible but subordinate |
| Step number label ("STEP 3 of 7") | `label` (11pt/600 uppercase) | Muted `text-text-tertiary` |
| Ingredient name | `body` (17pt/400) | Strikethrough + `text-text-tertiary` when checked |
| Ingredient quantity/unit prefix | `body` (17pt/600) | Bold-weight override on the qty portion only |
| Recipe title (sticky header) | `title` (22pt/600) | Single-line, truncate with tail ellipsis |
| Timer chip countdown (e.g. "9m 23s") | `body` (17pt/600) | Semibold for glance-read |
| Timer chip label (e.g. "RICE") | `label` (11pt/600 uppercase) | Above the countdown |
| Command-confirmation toast | `body` (17pt/600) | Single line, centered |
| Contextual tip body | `caption` (13pt/400) | Existing Phase 10 tip surface, retinted to tokens |
| "TIP" tip-badge label | `label` (11pt/600 uppercase) | `text-warning` tint |
| AskSheet question (recap) | `caption` (13pt/400) | `text-text-secondary` |
| AskSheet answer | `body` (17pt/400) | `text-text-primary` |
| Nav button icons (Back/Repeat/Next) | SF Symbol size 28pt | `iconPropsForText('display')` returns 28; weight 600 |

**Validation hook (per CONTEXT decision):** During UAT on a physical iPhone at
counter-height distance, confirm the `display` scale for the current step is
readable. If underscale, only the current-step role may upscale — all other
roles stay locked to the Phase 19 tokens. Document any adjustment in the plan's
visual-verification checkpoint.

**Never introduce a new font size.** If a value is needed that's not in the
5-step scale, the request is out of scope for Phase 16 and belongs in a Phase
19.x amendment.

---

## Color

Phase 19 semantic tokens are the authoritative palette. Cooking mode ships two
surface variants (light default + dark optional), both of which resolve to the
same semantic tokens — only the underlying CSS variable values swap.

### Light (default cream surface — `bg-bg` / #FAF7F2)

| Role | Hex | Token | Usage in cooking mode |
|------|-----|-------|-------|
| Dominant (60%) | #FAF7F2 | `bg-bg` | Full-screen background, non-current step cards |
| Secondary (30%) | #FFFFFF | `bg-surface` | Current-step card, ingredient rows, AskSheet |
| Accent (10%) | #C65D3A | `brand` | Current-step left-edge rail, timer chip background (@15% alpha), waveform mic fill, Stop button fill, checked-ingredient checkmark, nav-button active icon tint |
| Destructive | #DC2626 | `destructive` | Exit confirm destructive action, "End cooking session" |

### Dark (optional "Spotify Now Playing" theme — runtime toggle)

Activated by `cookingStore.darkMode` — persisted Zustand + AsyncStorage. The
dark palette lives in a `View`-level style override (not `@media
prefers-color-scheme` — scoped to cooking screen only per CONTEXT decision).

| Role | Hex | Token (dark override) | Usage |
|------|-----|-------|-------|
| Dominant (60%) | #141210 | `bg-bg` (dark) | Full-screen background |
| Secondary (30%) | #1E1B18 | `bg-surface` (dark) | Current-step card, ingredient rows |
| Accent (10%) | #C65D3A | `brand` (unchanged — tested against dark) | Same list as light; contrast holds |
| Text primary | #F5F0E8 | `text-text-primary` (dark) | Step text, ingredient names |
| Text secondary | #C4B7A4 | `text-text-secondary` (dark) | Step labels, muted copy |
| Border | #40372D | `border` (dark) | Card edges, ingredient divider |

Light/dark palette alignment matches the commented sketch in
`apps/mobile/src/global.css` lines 46-66 — promote those values into a live
cooking-scoped theme provider.

### Accent reserved for:

- Current-step card left-edge 4pt rail (solid `brand`)
- Current-step step-number label ("STEP 3 of 7") — `brand` tint
- Timer chip background at 15% brand alpha (`bg-brand/15`), border solid `brand`, text `brand-pressed`
- Listening-state waveform mic icon fill
- Voice-enabled idle pulse dot
- Stop TTS button icon fill + border
- Checked-ingredient checkmark icon
- Nav-button active-state icon tint (pressed)
- Command-confirmation toast left-edge accent strip

**Accent is NEVER used for:** nav-button backgrounds (tokens stay `bg-surface`
+ `border`), non-current step text, ingredient names, timer bar surrounds, exit
button, AskSheet chrome, contextual-tip body, sticky-header background.

**Destructive tone reserved for:** exit-confirm destructive alert button only.
No destructive color in the main cooking UI — users exit via a benign X that
opens a confirm sheet.

**Semantic state tones (success/warning/info) reserved for:**
- `warning` (#D97706): contextual-tip "TIP" label + left rail + background tint at 10% (retints the existing amber-50/amber-200 hardcoded pair in `cook.tsx` line 245). **Must replace existing hardcoded amber classes** during Phase 16 sweep.
- `success`: ingredient-checked subtle affordance (optional — checkmark tint can be brand OR success; spec defers to implementation A/B during UAT). If both options are viable, pick `success` to avoid over-saturating brand accent.
- `info`: not used in cooking mode.

---

## Copywriting Contract

All copy is sentence-case (except `label` role which is uppercase by token
convention). No em-dashes in UI copy per project-wide voice.

### Primary CTAs

| Element | Copy |
|---------|------|
| Enter cooking mode (from recipe detail, unchanged) | `Cook` |
| Advance to next step (voice confirm) | `Next step` (toast) |
| Go back one step (voice confirm) | `Previous step` (toast) |
| Repeat current step (voice confirm) | `Repeating` (toast) |
| Timer set (voice confirm) | `Timer set · {duration}` (e.g. `Timer set · 10 min`) |
| Stop TTS (button label on press) | `Stop` (visible text OR aria-label on icon-only button) |
| Exit cooking mode (top-left button) | `Exit` (visible text next to X icon — current pattern preserved) |
| Exit confirm — destructive | `End cooking session` (destructive red) |
| Exit confirm — cancel | `Keep cooking` |
| Dark mode toggle label (Settings > Cooking) | `Dark cooking mode` |
| Dark mode toggle subtitle | `Darker background while cooking. Matches Spotify's Now Playing feel.` |
| Voice toggle accessibility label | `Voice commands: {on|off}` |

### Empty / Edge States

| Element | Heading | Body |
|---------|---------|------|
| Recipe not found | `Recipe unavailable` | `We couldn't load this recipe. Go back and try again.` |
| Recipe loading | `Loading recipe...` | (no body; spinner only) |
| No ingredients in recipe (rare) | (no empty-state — ingredient section simply omits) | — |
| Voice recognition unavailable (permission denied) | `Voice commands need mic access` | `Enable microphone in Settings to cook hands-free.` |

### Voice Command Toast Copy (1.5s auto-dismiss, medium haptic)

Every recognized command confirms via toast, never TTS echo. Copy is
present-tense imperative (matches what just happened).

| Intent | Toast text |
|--------|------------|
| Next | `Next step` |
| Back | `Previous step` |
| Repeat | `Repeating` |
| Set timer | `Timer set · {N} min` |
| Cancel timer | `Timer canceled` |
| Show ingredients | `Ingredients` |
| Ask fall-through (routed to `/cooking/ask`) | no toast — AskSheet opens instead |

### Error States (in-cooking, non-blocking)

| Scenario | Copy |
|---------|------|
| `/cooking/ask` network failure | `Couldn't reach the kitchen assistant. Try again in a moment.` (inline in AskSheet) |
| STT transcript empty / no match | no visible copy; telemetry logs `unrecognized` event |
| TTS speech engine failure | silent fail; next step still advances |
| Timer expired (while app foregrounded) | TTS: `{label} timer done.` (existing Phase 9 behavior; unchanged) |

### Destructive Confirmations

| Action | Trigger | Sheet/Alert copy |
|--------|---------|------|
| Exit cooking mode mid-recipe | Tap top-left Exit | Title: `End cooking session?` / Body: `Your place in the recipe won't be saved.` / Destructive button: `End cooking session` / Cancel button: `Keep cooking` |

No other destructive actions in cooking mode. Timer cancel is reversible (user
can re-add) and does not require confirmation.

### Voice feedback principle

**Silent confirmation, loud announcement.** Recognized commands confirm with
toast + haptic only (no TTS echo — prevents the "I said Next, TTS says 'Moving
to step 3', step reads aloud" cascade). TTS is reserved for reading recipe
step content and timer-done announcements.

---

## Interaction Contract

### Layout structure (Claude.ai artifact pattern)

```
+------------------------------------+
| Sticky Header (64 or 112pt)        |
|  Exit X  |  Recipe title  | Waveform mic
|  [Timer chips row — when active]
|  [Stop TTS button — when TTS speaking]
+------------------------------------+
| ScrollView (vertical, full recipe) |
|                                    |
|  INGREDIENTS                       | ← label role, 11pt uppercase
|  ☐ 2 cups rice                     | ← body role, tap to check
|  ☐ 1 lb chicken breast             |
|  ...                               |
|                                    |
|  STEPS                             |
|                                    |
|  STEP 1 of 7                       | ← non-current, 22pt/600
|  Heat olive oil in a large pan...  |
|                                    |
|  STEP 2 of 7         (current) ◂▏  | ← highlighted card, 34pt/700
|  Add diced onions and cook until...| ← brand left rail, white surface,
|  [TIP: Low heat prevents burning.] |    elevated (shadow) in light mode
|                                    |
|  STEP 3 of 7                       |
|  Stir in garlic and...             |
|  ...                               |
|                                    |
|  (bottom inset 48px)               |
+------------------------------------+
| Bottom Nav Bar (72pt)              |
|  [  Back  ][ Repeat ][   Next   ]  | ← secondary variant, 72pt height
+------------------------------------+
```

### State transitions

| Trigger | Visual change | Animation |
|---------|--------------|-----------|
| Step advance (next / voice / intent router) | Current-step highlight shifts one card down; scroll animates previous-current off-screen or to top, new-current centered in viewport | `ScrollView.scrollTo({ y, animated: true })` — 400ms iOS default; use Reanimated if jank observed |
| Step back | Mirror of advance, scroll direction reversed | Same |
| Repeat | No scroll; current-step card pulse (brand rail thickens 4pt→8pt→4pt) | 300ms Reanimated pulse |
| Ingredient tap | Row transitions to strike-through + tertiary text + success/brand check icon | 150ms opacity + text-decoration |
| Voice listening → speaking | Waveform bars animate to amplitude | Reanimated `withRepeat` loop; amplitude from STT partial-result callback |
| Voice idle → listening | Static dot → waveform (smooth 200ms crossfade) | Reanimated `withTiming` |
| TTS starts | Stop button slides in from sticky-header right edge | 200ms slide + fade |
| TTS ends | Stop button slides out | 200ms slide + fade |
| Toast appears | Fade + 4pt slide-down from below sticky header | 200ms fade-in, 1.3s hold, 200ms fade-out (matches existing `useToast` timing) |
| Timer chip at T-10s | Light haptic + chip background transitions to `warning` @20% alpha | 300ms color interpolation; haptic fires once at T=10.0s |
| Timer expires | Chip removed; TTS announcement `{label} timer done.` | 200ms chip fade-out |
| Dark-mode toggle | Full-screen background + surface + text tokens cross-fade | 250ms color interpolation on root `View` style; no flash |

### Gesture contract

- **Vertical scroll:** native `ScrollView` behavior. No pull-to-refresh.
- **Horizontal swipe on step card:** reserved for future (Phase 16 does NOT implement). Voice / nav buttons are the only step-advance inputs.
- **Long-press anywhere on cooking screen:** optional dark-mode toggle per CONTEXT decision. If implemented, 600ms press with light haptic on engage. Settings toggle is the primary entry; long-press is the secondary.
- **Tap Exit X:** opens iOS action sheet with destructive "End cooking session" + cancel. No direct dismiss.
- **`gestureEnabled: false`** on the screen (Phase 9 convention preserved) — no swipe-to-pop navigation.

### Haptic contract (via `expo-haptics`)

| Event | Feedback |
|-------|----------|
| Voice command recognized (Next/Back/Repeat/Timer) | `Haptics.ImpactFeedbackStyle.Medium` |
| Ingredient tap-to-check | `Haptics.ImpactFeedbackStyle.Light` |
| Step card long-press (dark-mode toggle, if shipped) | `Haptics.ImpactFeedbackStyle.Light` |
| Timer at T-10s warning | `Haptics.ImpactFeedbackStyle.Light` (once) |
| Timer expires | `Haptics.NotificationFeedbackType.Success` |
| Stop TTS button tapped | `Haptics.ImpactFeedbackStyle.Medium` |
| Exit confirm destructive tap | `Haptics.NotificationFeedbackType.Warning` |

Works when phone is on silent mode (haptics are independent of ringer).

### Accessibility

- `accessibilityLabel` on icon-only controls: Exit X, voice-toggle waveform, Stop TTS, Back/Repeat/Next nav buttons (even though nav buttons show text — SF Symbol + label both contribute).
- `accessibilityRole="button"` on all Pressables.
- `accessibilityState={{ selected: true }}` on the current step card.
- `accessibilityLiveRegion="polite"` on the toast surface (announces recognized commands to VoiceOver users).
- Ingredient checkboxes: `accessibilityRole="checkbox"` + `accessibilityState={{ checked }}`.
- Screen stays awake via `useKeepAwake()` (unchanged Phase 9 behavior).
- Minimum contrast: all text passes WCAG AA 4.5:1 at body size; verified for both light and dark palettes via `tokens.test.ts` extension.

---

## Component Inventory

### New primitives (ship in Phase 16)

| Component | Path | Purpose |
|-----------|------|---------|
| `ScrollableRecipe` | `apps/mobile/src/components/cooking/ScrollableRecipe.tsx` | Full-recipe scroll container with ingredient + step sections; owns scroll-to-current logic |
| `IngredientRow` | `apps/mobile/src/components/cooking/IngredientRow.tsx` | Checkable ingredient line; strike-through on tap; reads `cookingStore.ingredientChecks[id]` |
| `StepCard` | `apps/mobile/src/components/cooking/StepCard.tsx` | Single step card; accepts `isCurrent` prop; renders brand left-rail + `display` typography when current, `title` typography when not |
| `StickyCookingHeader` | `apps/mobile/src/components/cooking/StickyCookingHeader.tsx` | Exit X + recipe title + voice waveform + timer chip row + Stop-TTS button |
| `VoiceWaveform` | `apps/mobile/src/components/cooking/VoiceWaveform.tsx` | Animated mic icon — waveform bars when listening, pulse dot when idle-armed, gray static when voice off |
| `CommandToast` | `apps/mobile/src/components/cooking/CommandToast.tsx` | Phase-19-token-aware toast; replaces generic `useToast` for cooking-command confirmations (1.5s auto-dismiss, medium haptic companion) |
| `StopTTSButton` | `apps/mobile/src/components/cooking/StopTTSButton.tsx` | Icon-only button; renders only when `Speech.isSpeakingAsync() === true` |
| `ExitConfirmSheet` | — | iOS `ActionSheetIOS.showActionSheetWithOptions` — no component file needed; call site in `cook.tsx` |

### Primitives to rework (existing)

| Component | Current path | Phase 16 change |
|-----------|------|---------|
| `StepDisplay` | `apps/mobile/src/components/cooking/StepDisplay.tsx` | **Replace** with `ScrollableRecipe` + `StepCard` composition. Delete file after migration. |
| `StepNavButtons` | `apps/mobile/src/components/cooking/StepNavButtons.tsx` | Rework to 72pt tap targets, Phase 19 button tokens (secondary variant color, SF Symbol icons via `iconPropsForText('display')`). Document 72pt deviation inline. |
| `TimerBar` | `apps/mobile/src/components/cooking/TimerBar.tsx` | Retint hardcoded `#C2410C` to `colors.brandPressed` token; replace hardcoded font sizes with `text-body` and `text-label` roles; add T-10s warning-tone transition. |
| `VoiceStatusBadge` | `apps/mobile/src/components/cooking/VoiceStatusBadge.tsx` | Replace with `VoiceWaveform`. Delete file. |
| `AskSheet` | `apps/mobile/src/components/cooking/AskSheet.tsx` | Retoken colors + typography; add streaming-response incremental rendering as answer arrives. |
| Cooking-mode tip block (inline in `cook.tsx` lines 243-252) | `apps/mobile/src/app/recipes/[id]/cook.tsx` | Replace hardcoded amber classes with `bg-warning/10 border-warning` + `text-warning` label + `text-text-primary` body. |

### Settings additions

| Component | Path | Purpose |
|-----------|------|---------|
| Cooking section | `apps/mobile/src/app/(tabs)/settings.tsx` | New section "Cooking" with `Dark cooking mode` toggle row. Section header uses `label` role. Toggle row uses `body` role + native iOS `Switch`. |

### Reusable Phase 19 primitives consumed (no changes)

- `Button` (variant `secondary` for nav buttons, variant `destructive` for exit confirm action-sheet button — but action sheet uses native iOS styling, so only `secondary` is directly consumed)
- `Chip` (variant `display`, tone `default` for timer chips — or a dedicated TimerChip subclass if pulse/countdown animation requires it)
- `SymbolIcon` (all icons throughout)
- `EmptyState` (recipe-not-found fallback)
- `LoadingState` (recipe-fetch pending)
- `ErrorState` (AskSheet network failure)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none (project does not use shadcn — React Native app) | not applicable |
| Third-party registries | none declared | not applicable |

No registry blocks pulled in this phase. All components are hand-rolled against
Phase 19 tokens. Safety-gate vetting not required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

Every decision in this contract traces to an upstream artifact.

| Field | Source |
|-------|--------|
| Design system = NativeWind + Phase 19 tokens | ROADMAP Phase 19 (shipped 2026-04-18); detected `apps/mobile/src/design/tokens.ts` + `tailwind.config.js` |
| Icon library = SF Symbols via `expo-symbols` | ROADMAP Phase 15; detected `SymbolIcon` usage across codebase |
| 8pt spacing grid | Phase 19 `tokens.ts` `spacing` export (verified) |
| 5-step typography scale | Phase 19 `tokens.ts` `typography` export (verified) |
| Color palette (light) | Phase 19 `global.css` `--color-*` variables (verified) |
| Color palette (dark) | Phase 19 `global.css` commented-out dark sketch (lines 46-66) — promoted to live theme in this phase per CONTEXT D-03 |
| Current-step `display` / non-current `title` typography | CONTEXT "Step text typography" (Claude's discretion → locked here) |
| Nav buttons at 72pt deviation | CONTEXT "Nav button sizing" decision; additional_context bullet 7 |
| Cream-surface default + optional dark toggle | CONTEXT "Background surface — user toggle" decision |
| Toast-based voice feedback, no TTS echo | CONTEXT "Voice Command Feedback → Confirmation on recognized command" decision |
| Waveform mic listening indicator | CONTEXT "Listening-state indicator" decision |
| Stop TTS button in sticky header | CONTEXT "TTS interrupt" decision |
| Scrollable full-recipe layout with highlighted current step | CONTEXT "Cooking UI Information Density → Layout pivot" decision |
| Checkable ingredients at top of recipe | CONTEXT "Ingredients" decision |
| Sticky horizontal timer-chip header | CONTEXT "Timer display" decision |
| Exit-confirm destructive copy | CONTEXT `code_context` "Full-screen immersive routes" pattern (exit button + explicit confirm) |
| Toast timing 1.5s auto-dismiss | CONTEXT "Confirmation on recognized command" decision |
| Medium haptic on command / Light on T-10s | CONTEXT `code_context` "Haptics" pattern |
| Streaming `/cooking/ask` for <1.5s p95 | CONTEXT "/cooking/ask latency target" decision |
| New primitive names (ScrollableRecipe, StepCard, etc.) | Researcher discretion — named per Phase 15/19 component naming conventions |

## Defaults Claimed by Researcher

These values were not explicitly specified in CONTEXT and are locked here:

| Field | Value | Rationale |
|-------|-------|-----------|
| Sticky header height: 64pt baseline / 112pt with timers | 64 / 112 | 48pt timer chip band fits one `Chip` at `display` kind + 8pt gap + 8pt padding; matches 8pt grid |
| Scroll animation timing (step advance) | 400ms iOS default via `scrollTo({ animated: true })` | Native default reads as "brisk but not instant"; upgrade to Reanimated only if jank observed during UAT |
| Current-step pulse on Repeat | 300ms brand-rail thickness pulse | Subtle reinforcement that "something happened" without being noisy |
| Ingredient-check icon tone | `success` (tentative, spec-level A/B) | Prevents over-saturating `brand` accent; final call at UAT |
| T-10s warning-tone timer transition | `warning` @20% alpha background | Consistent with semantic-state token usage elsewhere |
| Dark-mode toggle copy | `Dark cooking mode` / `Darker background while cooking. Matches Spotify's Now Playing feel.` | Matches CONTEXT reference language |
| Exit confirm destructive copy | `End cooking session` / `Keep cooking` | Terminology matches "cooking session" naming elsewhere in app |

These defaults are overridable by the executor during implementation if the
visual-verification UAT checkpoint surfaces a better choice; any override must
be documented in the corresponding 16-0X-PLAN.md completion summary.
