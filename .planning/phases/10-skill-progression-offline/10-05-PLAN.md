---
phase: 10-skill-progression-offline
plan: 05
type: execute
wave: 3
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - apps/mobile/src/stores/progressionStore.ts
  - apps/mobile/src/stores/__tests__/progressionStore.test.ts
  - apps/mobile/src/hooks/useNetworkBanner.tsx
  - apps/mobile/src/components/OfflineBanner.tsx
  - apps/mobile/src/components/SuggestedForYou.tsx
  - apps/mobile/app/(tabs)/recipes.tsx
  - apps/mobile/app/recipes/[id]/index.tsx
  - apps/mobile/app/(cooking)/cook.tsx
  - apps/mobile/app/_layout.tsx
autonomous: false
requirements: [SKIL-01, SKIL-02, SKIL-03, SKIL-04, FOUN-07]
must_haves:
  truths:
    - "User sees a 'Suggested for you' section on the recipes tab with 3 ambition suggestions and rationales"
    - "User sees an offline banner when network is unavailable"
    - "User sees a contextual tip on the active step in cooking mode (when available)"
    - "User sees 'Creative variations' button on recipe detail for recipes cooked 3+ times"
    - "Recipes tab renders cached recipes when offline without any network calls"
    - "Device test passes on a physical iPhone: airplane mode → recipes still browseable, banner shows, markCooked queues"
  artifacts:
    - path: "apps/mobile/src/stores/progressionStore.ts"
      provides: "cookStats, ambitionSuggestions, fetchCookStats, fetchSuggestions, fetchVariations, fetchTip actions"
      min_lines: 80
    - path: "apps/mobile/src/components/OfflineBanner.tsx"
      provides: "Banner UI reading useNetworkStore"
    - path: "apps/mobile/src/components/SuggestedForYou.tsx"
      provides: "Horizontal card list of ambition suggestions"
  key_links:
    - from: "progressionStore"
      to: "/api/v1/progression/*"
      via: "authedFetch (mirror mealPlanStore pattern)"
      pattern: "api/v1/progression"
    - from: "OfflineBanner"
      to: "useNetworkStore"
      via: "selector hook"
      pattern: "useNetworkStore"
    - from: "cook.tsx active step"
      to: "/api/v1/cooking/tips"
      via: "progressionStore.fetchTip"
      pattern: "fetchTip|cooking/tips"
    - from: "recipe detail"
      to: "/api/v1/progression/variations/:id"
      via: "fetchVariations action"
      pattern: "progression/variations"
---

<objective>
Wire all Phase 10 backend endpoints and offline infrastructure into the mobile UI. Close SKIL-01..04 and FOUN-07 with a physical device verification.

Purpose: This is the user-facing payoff plan. Everything from 10-01 through 10-04 becomes visible and testable here.
Output: progressionStore, offline banner, recipes tab suggestions section, cook screen tips, recipe detail variations button, device test checkpoint.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-skill-progression-offline/10-RESEARCH.md
@.planning/phases/10-skill-progression-offline/10-02-SUMMARY.md
@.planning/phases/10-skill-progression-offline/10-03-SUMMARY.md
@.planning/phases/10-skill-progression-offline/10-04-SUMMARY.md
@apps/mobile/src/stores/mealPlanStore.ts
@apps/mobile/src/stores/shoppingStore.ts
@apps/mobile/app/(tabs)/recipes.tsx
@apps/mobile/app/recipes/[id]/index.tsx
@apps/mobile/app/(cooking)/cook.tsx
@apps/mobile/app/_layout.tsx
@apps/mobile/src/types/progression.ts

<interfaces>
authedFetch pattern (from mealPlanStore, phase 07-04):
```typescript
async function authedFetch(path: string, init?: RequestInit) {
  const token = (await supabase.auth.getSession()).data.session?.access_token;
  return fetch(`${getApiBaseUrl()}/api/v1${path}`, {
    ...init,
    headers: { ...init?.headers, Authorization: `Bearer ${token}` },
  });
}
```

Endpoints from 10-02 and 10-03:
- GET /progression/cook-stats
- GET /progression/suggestions
- GET /progression/variations/:recipeId → 400 BELOW_THRESHOLD
- GET /cooking/tips?recipe_id=&step_index=&step_text=

From 10-04 available:
- useNetworkStore.isOnline
- offlineQueue.enqueue (already hooked in markCooked and pantry edits)
- Persisted stores rehydrate automatically
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: progressionStore + OfflineBanner component</name>
  <files>apps/mobile/src/stores/progressionStore.ts, apps/mobile/src/stores/__tests__/progressionStore.test.ts, apps/mobile/src/components/OfflineBanner.tsx, apps/mobile/src/hooks/useNetworkBanner.tsx</files>
  <behavior>
    progressionStore:
    - `fetchCookStats()` → GET /progression/cook-stats → set cookStats[]
    - `fetchSuggestions()` → GET /progression/suggestions → set ambitionSuggestions[] (3 max)
    - `fetchVariations(recipeId)` → GET /progression/variations/:id → return string[] or null on BELOW_THRESHOLD (400)
    - `fetchTip(recipeId, stepIndex, stepText)` → GET /cooking/tips → return string or empty
    - All gracefully degrade when offline (return cached/empty, log warn)

    OfflineBanner: renders null when online, renders "You're offline — changes will sync" pill when offline.
  </behavior>
  <action>
    Write `stores/progressionStore.ts` mirroring mealPlanStore structure exactly: local `authedFetch` helper, Zustand create, actions, error boundary. Use types from `src/types/progression.ts`. Persist via persist middleware with name 'dinnertime-progression', partialize to {cookStats, ambitionSuggestions}, version: 1.

    Write `stores/__tests__/progressionStore.test.ts` — 4 action tests mocking global.fetch:
    1. fetchCookStats stores result
    2. fetchSuggestions stores 3 entries
    3. fetchVariations returns null on 400 BELOW_THRESHOLD
    4. fetchVariations returns string[] on 200

    Write `components/OfflineBanner.tsx` — reads `useNetworkStore(s => s.isOnline)`. When false, render styled View (NativeWind classes `bg-amber-100 py-2 px-4 ...`) with Text "You're offline — changes will sync when you're back online". When true, return null.

    Write `hooks/useNetworkBanner.tsx` — thin wrapper returning {isOnline, shouldShow} if any callers want the logic without the component.

    Test: `pnpm --filter mobile test -- --run progressionStore`.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --run progressionStore</automated>
  </verify>
  <done>progressionStore tests pass, OfflineBanner renders correctly in unit snapshot, offline banner hook exported.</done>
</task>

<task type="auto">
  <name>Task 2: Wire UI — recipes tab, cook screen, recipe detail, root layout</name>
  <files>apps/mobile/app/(tabs)/recipes.tsx, apps/mobile/app/(cooking)/cook.tsx, apps/mobile/app/recipes/[id]/index.tsx, apps/mobile/app/_layout.tsx, apps/mobile/src/components/SuggestedForYou.tsx</files>
  <action>
    1. `_layout.tsx`: Mount OfflineBanner near the top of the root navigator (above tabs). Add NetInfo listener bootstrap if networkStore doesn't self-initialize (verify from 10-04 — if module side-effect in networkStore.ts, no change needed here).

    2. `components/SuggestedForYou.tsx`: Horizontal scrolling card list; takes `suggestions: AmbitionSuggestion[]`; each card shows recipe title + rationale in 2 lines + tap → navigate to /recipes/[id]. Empty state: render null (don't show section).

    3. `(tabs)/recipes.tsx`: On mount, call `progressionStore.fetchSuggestions()` and `fetchCookStats()` (gated on isOnline — if offline, skip fetch, persisted state still renders). Add `<SuggestedForYou suggestions={suggestions} />` above the existing recipe list.

    4. `recipes/[id]/index.tsx` (detail screen): Add "Creative Variations" button. On tap → `fetchVariations(id)` → if result is string[], open a modal showing bulleted variations; if null (below threshold), show toast "Cook this 3+ times to unlock variations". Button shows a locked indicator when cookStats for this recipe shows cook_count < 3.

    5. `(cooking)/cook.tsx`: Add a "Tip" row under the active step. On step change (currentStep or activeStepIndex), call `fetchTip(recipeId, stepIndex, stepText)`; store result in local useState. If non-empty, render in subtle card; if empty, render nothing. Debounce/skip if already fetched for this (recipeId, stepIndex) in this session (local ref map). Fail silently on error — never block cooking on tips.

    6. Ensure recipes tab works offline: the existing recipes fetch should early-return if `!isOnline && recipes.length > 0` (use persisted data). Verify by turning on airplane mode and navigating to recipes tab in device test.

    Run mobile test suite: `pnpm --filter mobile test -- --run` — all green, zero regressions.

    Run typecheck: `pnpm --filter mobile typecheck`.
  </action>
  <verify>
    <automated>pnpm --filter mobile test -- --run &amp;&amp; pnpm --filter mobile typecheck</automated>
  </verify>
  <done>All UI wiring complete, mobile tests + typecheck green, offline banner mounted globally, suggestions/tips/variations reachable from existing screens.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Physical device verification — offline + skill progression</name>
  <files>Manual verification — no files modified</files>
  <action>Pause execution for human verification. See what-built and how-to-verify sections below for scope and steps.</action>
  <verify>Human confirms all device test steps pass</verify>
  <done>User types "approved" or describes issues found</done>
  <what-built>
    Phase 10 end-to-end:
    - Skill progression: recipes tab "Suggested for you", recipe detail "Creative variations" button, cook screen tip row
    - Offline: 5 stores persisted to AsyncStorage, network banner, offline write queue for markCooked and pantry edits
    - Claude has already run: pnpm test (mobile + server) fully green and typecheck green
  </what-built>
  <how-to-verify>
    Required hardware: physical iPhone with dev client installed (needed because netinfo is a native module).

    If dev client is not current, rebuild first:
    ```bash
    cd apps/mobile
    npx expo prebuild --clean
    pnpm --filter mobile ios --device
    ```

    Verification steps:

    1. **Skill progression (online)**
       - Cook a meal from meal plan via "Mark cooked" at least 3 times for the same recipe
       - Open Recipes tab → "Suggested for you" row should appear with 3 recipes + rationale
       - Open that cooked recipe's detail → "Creative variations" button should be enabled → tap → should see variations list
       - Open any recipe, enter cooking mode → on each step, a contextual tip should appear within 2-3 seconds (or nothing if Haiku returned empty — that's valid)

    2. **Offline (airplane mode)**
       - Enable airplane mode on the device
       - Expect: amber banner "You're offline — changes will sync when you're back online" appears near the top of the app
       - Navigate to Recipes tab → should still render the previously-loaded recipes from cache (no spinner, no error)
       - Open a recipe detail → should render from cache
       - Navigate to Pantry → should render cached items
       - Navigate to Plan tab → should render cached meal plan
       - Mark a meal as cooked while offline → UI should update optimistically, banner stays
       - Disable airplane mode → within a few seconds, queued markCooked should flush (verify by checking server logs or reopening app — cook count increments on recipes tab)

    3. **Regressions (quick)**
       - Voice cooking mode still works (phase 9 regression check)
       - Shopping list still opens (phase 8 regression check)
       - Login/logout still works (phase 1 regression check)

    Report back any failures with screenshots or logs.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- Mobile test suite green
- Mobile typecheck green
- Server test suite green (no regressions from 10-02/10-03 wiring)
- Device test checklist complete: skill progression visible, offline banner works, persisted stores render offline, write queue flushes on reconnect
</verification>

<success_criteria>
- [ ] Recipes tab has "Suggested for you" section populated from /progression/suggestions
- [ ] Recipe detail has "Creative variations" button gated on cook_count >= 3
- [ ] Cook screen shows contextual tips on active step
- [ ] Global offline banner reflects network state
- [ ] All 5 persisted stores render cached data offline
- [ ] offlineQueue flushes markCooked / pantry edits on reconnect
- [ ] Device test checkpoint approved
- [ ] SKIL-01, SKIL-02, SKIL-03, SKIL-04, FOUN-07 all observable end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-progression-offline/10-05-SUMMARY.md`
</output>
