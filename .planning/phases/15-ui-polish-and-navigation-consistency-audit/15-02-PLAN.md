---
phase: 15-ui-polish-and-navigation-consistency-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - apps/mobile/src/app/_layout.tsx
  - apps/mobile/src/app/scan/_layout.tsx
  - apps/mobile/src/app/recipes/_layout.tsx
  - apps/mobile/src/app/shopping/_layout.tsx
  - apps/mobile/src/app/scan/index.tsx
  - apps/mobile/src/app/scan/review.tsx
  - apps/mobile/src/app/scan/receipt.tsx
  - apps/mobile/src/app/scan/instacart.tsx
  - apps/mobile/src/app/recipes/review.tsx
  - apps/mobile/src/app/recipes/[id]/edit.tsx
  - apps/mobile/src/components/ui/HeaderCloseButton.tsx
autonomous: true
requirements:
  - "UI quality (post-v1)"
must_haves:
  truths:
    - "Every pushed screen shows a native chevron-only back button (no 'Back' text label) — matches Photos/Notes/Messages"
    - "Scan flow (index, review, receipt, instacart) presents as modal with X close that fully dismisses the stack via router.dismissAll()"
    - "Recipe import flow (import, import-url, import-photo, import-manual, review) presents as modal; destinations (discover, [id]/index, [id]/edit) remain push"
    - "Swipe-back gesture enabled on every pushed screen by default"
    - "Dirty-form guard active on recipes/[id]/edit.tsx, recipes/review.tsx, and scan/review.tsx — back attempts with unsaved edits trigger the Unsaved changes Alert"
    - "recipes/[id]/index.tsx hero-image floating back button is the ONLY hand-rolled back affordance surviving Phase 15 (recorded exception)"
  artifacts:
    - path: "apps/mobile/src/app/scan/_layout.tsx"
      provides: "Native modal presentation for scan stack; headerLeft X on entry screens"
      contains: "presentation: 'modal'"
    - path: "apps/mobile/src/app/recipes/_layout.tsx"
      provides: "Modal group for imports + push group for destinations; headerBackTitle: ''"
      contains: "presentation: 'modal'"
    - path: "apps/mobile/src/app/_layout.tsx"
      provides: "Root stack with headerBackTitle: '' applied to default screenOptions"
      contains: "headerBackTitle"
    - path: "apps/mobile/src/components/ui/HeaderCloseButton.tsx"
      provides: "Shared modal X close Pressable that calls router.dismissAll()"
      min_lines: 15
  key_links:
    - from: "apps/mobile/src/app/scan/_layout.tsx"
      to: "HeaderCloseButton"
      via: "headerLeft={() => <HeaderCloseButton />} on modal-root screens (index/receipt/instacart)"
      pattern: "HeaderCloseButton"
    - from: "apps/mobile/src/app/recipes/[id]/edit.tsx"
      to: "useDirtyFormGuard"
      via: "Called with isDirty state derived from draft !== recipe snapshot"
      pattern: "useDirtyFormGuard"
    - from: "apps/mobile/src/app/recipes/review.tsx"
      to: "useDirtyFormGuard"
      via: "Called with touched flag"
      pattern: "useDirtyFormGuard"
    - from: "apps/mobile/src/app/scan/review.tsx"
      to: "useDirtyFormGuard"
      via: "Called with any-item-touched flag"
      pattern: "useDirtyFormGuard"
---

<objective>
Migrate all stack layouts to native expo-router headers with chevron-only back button; convert scan/ and recipes/ import sub-routes to modal presentation with X close affordance; wire useDirtyFormGuard onto the three dirty-form screens.

Purpose: Phase 15's navigation-consistency mandate — every pushed/modal screen has a consistent header, scan flow feels like a self-contained task (modal), import flow feels like an interruption (modal), destinations remain push. Kills ~34 files worth of custom-header boilerplate by delegating to the native stack.

Output: 4 layout files reconfigured + 6 screen files lightly edited to mount the dirty-form guard or remove redundant header JSX + 1 shared HeaderCloseButton component.

Runs in parallel with Plan 03 (icon sweep). File ownership is DISJOINT:
- Plan 02 owns: app/_layout.tsx, scan/_layout.tsx, recipes/_layout.tsx, shopping/_layout.tsx, specific screen files that set Stack.Screen options or mount dirty-form guard, and the new HeaderCloseButton component.
- Plan 03 owns: everything else (icon swaps inside leaf component bodies, tab bar icons, emoji-to-SymbolIcon/FOOD_IMAGES conversion).
- If a screen file needs both a Stack.Screen options change AND an icon swap, Plan 02 edits the options block, Plan 03 edits the body — BUT same-file conflict risk is minimal because 02's edits in scan/review.tsx / recipes/review.tsx / recipes/[id]/edit.tsx are scoped to the top-of-component area (Stack.Screen options + useDirtyFormGuard hook) while 03's edits to those files are inside the render body.

**Phase 19 boundary callout:** This plan adds ZERO color or typography tokens. headerTintColor stays `#1F2937` (existing warmGray-900). headerStyle.backgroundColor stays `#FFFBF5` (warmWhite). Phase 19 will retintesr these via token swap. Do NOT replace `Button.tsx` / `ChipToggle.tsx` / `Input.tsx` / `SearchBar.tsx`.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-CONTEXT.md
@.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-RESEARCH.md
@.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-01-SUMMARY.md
@apps/mobile/src/app/_layout.tsx
@apps/mobile/src/app/scan/_layout.tsx
@apps/mobile/src/app/recipes/_layout.tsx
@apps/mobile/src/app/shopping/_layout.tsx
@apps/mobile/src/app/scan/index.tsx
@apps/mobile/src/app/scan/review.tsx
@apps/mobile/src/app/recipes/review.tsx
@apps/mobile/src/app/recipes/[id]/edit.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeaderCloseButton and migrate scan/recipes/shopping/root layouts to native headers with chevron-only back + modal presentation for scan and recipe imports</name>
  <files>
    apps/mobile/src/components/ui/HeaderCloseButton.tsx,
    apps/mobile/src/app/_layout.tsx,
    apps/mobile/src/app/scan/_layout.tsx,
    apps/mobile/src/app/recipes/_layout.tsx,
    apps/mobile/src/app/shopping/_layout.tsx
  </files>
  <action>
    Per D-02 (navigation headers), D-03 (modal vs push presentation), and D-04 (swipe-back gesture) from CONTEXT, and per 15-RESEARCH Patterns 2 and 3:

    **Step 1: HeaderCloseButton.tsx** — shared component used by every modal-root screen's `headerLeft`. Imports SymbolIcon from Plan 01, wraps a Pressable with `hitSlop={12}`, accessibilityLabel="Close", onPress={() => router.dismissAll()}. Per 15-RESEARCH Pitfall 4: USE `router.dismissAll()` NOT `router.back()` — the X on a modal root must exit the entire modal stack, even if the user has pushed sub-screens inside.

    ```tsx
    import { Pressable } from 'react-native';
    import { router } from 'expo-router';
    import { SymbolIcon } from './SymbolIcon';

    export function HeaderCloseButton() {
      return (
        <Pressable onPress={() => router.dismissAll()} hitSlop={12} accessibilityLabel="Close">
          <SymbolIcon name="xmark" size="body" weight="medium" tintColor="#1F2937" />
        </Pressable>
      );
    }
    ```

    **Step 2: app/_layout.tsx root stack** — add `headerBackTitle: ''` to whatever default screenOptions pattern is currently applied. Preserve everything else (sentinel banner, auth guards, OfflineBanner). Do NOT change `headerShown: false` on (tabs) or (auth) groups.

    **Step 3: scan/_layout.tsx** — migrate to modal presentation per 15-RESEARCH "Scan flow modal migration" section. Set these at the Stack level:
    ```
    screenOptions: {
      headerStyle: { backgroundColor: '#FFFBF5' },
      headerTintColor: '#1F2937',
      headerShadowVisible: false,
      headerBackTitle: '',
      headerTitleAlign: 'center',
      gestureEnabled: true,
      presentation: 'modal',   // cascades — override per-screen if needed
    }
    ```
    Per 15-RESEARCH Pitfall 2: `presentation: 'modal'` at Stack level cascades. Sub-screens inside the modal group (scan/review) should override to `presentation: 'card'` so they push normally inside the modal instead of presenting as a nested modal.

    Screen overrides:
    - `index`: title='Scan Your Kitchen', headerLeft={HeaderCloseButton}
    - `review`: title='Review Items', presentation: 'card' (push inside modal), back chevron auto-provided
    - `receipt`: title='Scan Receipt', headerLeft={HeaderCloseButton}
    - `instacart`: title='Import from Instacart', headerLeft={HeaderCloseButton}

    **Step 4: recipes/_layout.tsx** — per 15-RESEARCH "Recipe import flow" section:
    ```
    screenOptions: same defaults as scan (headerBackTitle: '', gestureEnabled: true, headerTitleAlign: 'center')
    // NO blanket 'modal' — set per screen
    ```
    Screen-by-screen:
    - `import`: title='Import Recipe', presentation: 'modal', headerLeft={HeaderCloseButton}
    - `import-url`: title='Paste URL', presentation: 'modal'  (sub of modal — chevron auto, dismisses via parent)
    - `import-photo`: title='Take Photo', presentation: 'modal'
    - `import-manual`: title='Type Recipe', presentation: 'modal'
    - `review`: title='Review Recipe', presentation: 'modal', headerLeft={HeaderCloseButton}
    - `discover`: title='Discover' (PUSH — destination)
    - `[id]/index`: headerShown: false (KEEP hero pattern — legitimate exception per 15-RESEARCH)
    - `[id]/edit`: title='Edit Recipe' (PUSH — destination, auto chevron)
    - `[id]/cook`: headerShown: false, gestureEnabled: false (cooking mode — untouched)

    Note: for import sub-routes (import-url/import-photo/import-manual), they're entered from `import` which is already a modal. iOS handles modal-inside-modal gracefully if you only set `presentation: 'modal'` on the ROOT entry. Verify behavior in simulator during Wave 3. If any screen shows doubled modal chrome, override those to `presentation: 'card'`.

    **Step 5: shopping/_layout.tsx** — preserve push behavior. Add `headerBackTitle: ''` to defaults. That's the whole change for this layout.

    **Do NOT:**
    - Replace existing Ionicons calls INSIDE screen bodies — that's Plan 03's scope
    - Touch (tabs)/_layout.tsx tab icons — Plan 03 owns those
    - Add color tokens or change headerStyle backgrounds to anything other than existing `#FFFBF5`
    - Remove the recipes/[id]/index.tsx floating-back hero Pressable (legitimate exception; Plan 03 migrates its icon but keeps the Pressable)
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit -p .</automated>
  </verify>
  <done>
    - HeaderCloseButton.tsx exists and is imported by scan/_layout.tsx and recipes/_layout.tsx where modal-root screens need an X
    - Root _layout.tsx sets `headerBackTitle: ''` in default screenOptions
    - scan/_layout.tsx has `presentation: 'modal'` cascading; scan/review overridden to `presentation: 'card'`
    - recipes/_layout.tsx has modal vs push routing per spec
    - shopping/_layout.tsx has `headerBackTitle: ''` added
    - Typecheck clean
    - No layout file imports from '@expo/vector-icons' (if the old layout had one, it's gone)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire useDirtyFormGuard into recipes/[id]/edit, recipes/review, scan/review and remove any redundant hand-rolled header JSX now covered by native stack</name>
  <files>
    apps/mobile/src/app/recipes/[id]/edit.tsx,
    apps/mobile/src/app/recipes/review.tsx,
    apps/mobile/src/app/scan/review.tsx,
    apps/mobile/src/app/scan/index.tsx,
    apps/mobile/src/app/scan/receipt.tsx,
    apps/mobile/src/app/scan/instacart.tsx
  </files>
  <action>
    Per D-04 (swipe-back) and 15-RESEARCH Pattern 4 + "Dirty-form swipe-back suspension" section:

    **Step 1: recipes/[id]/edit.tsx** — import `useDirtyFormGuard` from `@/components/ui/useDirtyFormGuard`. Derive `isDirty` from a `touched` ref/state flipped on any field edit, OR from a shallow compare `draft !== recipeSnapshot`. Pick whichever is already idiomatic in the file. Call `useDirtyFormGuard(isDirty)` at the top of the component.

    **Step 2: recipes/review.tsx** — same pattern. Per STATE ("Review screen uses local draft state separate from importedRecipe store" — Phase 05-04), there's already a local draft. Add a `touched` boolean flipped on first edit, pass to `useDirtyFormGuard(touched)`.

    **Step 3: scan/review.tsx** — same pattern. Per STATE ("review screen stays dumb renderer reading item.accepted" — Phase 14-02), dirty state is any item toggle after mount. Simplest: a `useRef(false)` flipped by the toggle/edit handlers, then mirrored to state for the guard (refs don't trigger the guard re-eval; `useState` does).

    **Step 4: Scan entry screens (scan/index.tsx, scan/receipt.tsx, scan/instacart.tsx)** — remove any hand-rolled header-like JSX that the native stack now provides. Specifically:
    - If the screen currently renders its own SafeAreaView-wrapped title + back affordance, delete that block; trust the native stack title from _layout.tsx.
    - Keep the FAB, the camera button, body content, etc. — those stay.
    - DO NOT touch Ionicons usages inside the body (that's Plan 03's scope).
    - DO NOT delete FOOD_IMAGES-dependent empty states (Plan 03 converts them).

    **Step 5: Re-verify the recipes/[id]/index.tsx exception** — open the file, confirm the floating back Pressable at line ~110-117 is the ONLY custom back affordance remaining. If the file has no Stack.Screen options block, add `<Stack.Screen options={{ headerShown: false }} />` at the top of the returned JSX to make the exception explicit. Plan 03 will still migrate the Ionicons chevron-back inside that Pressable to SymbolIcon.

    **Do NOT:**
    - Migrate any Ionicons usages in these files (Plan 03's scope)
    - Change body layout or business logic
    - Touch cook.tsx (already headerless, cooking-mode-owned)
    - Call `useDirtyFormGuard` on any file outside this task's allowlist (settings editors defer to Phase 23)
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit -p . && pnpm test --run src/components/ui/__tests__/useDirtyFormGuard.test.tsx</automated>
  </verify>
  <done>
    - useDirtyFormGuard imported and called in recipes/[id]/edit.tsx, recipes/review.tsx, scan/review.tsx with appropriate isDirty expression
    - Redundant hand-rolled headers removed from scan/index, scan/receipt, scan/instacart (if they existed)
    - recipes/[id]/index.tsx floating back Pressable survives (known exception, explicitly set via `<Stack.Screen options={{ headerShown: false }} />` or existing layout override)
    - Typecheck clean
    - useDirtyFormGuard unit tests still pass (Plan 01 coverage unchanged)
  </done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit -p .` exits 0
- `cd apps/mobile && pnpm test --run` exits 0 (Plan 01 tests still green; no new tests in this plan)
- Manual spot-check (not automated — Plan 04 handles Maestro): scan flow from pantry FAB presents as modal; swipe-down dismisses; X on scan/index dismisses all
- Manual spot-check: editing a recipe, swiping back triggers the Unsaved changes Alert
- Plan 02 touched NO Ionicons imports (Plan 03 owns those)
- Orange #F97316 preserved everywhere (no color swap)
- recipes/[id]/index.tsx floating back is the documented exception and still renders
</verification>

<success_criteria>
- Every pushed/modal screen gets consistent native header with chevron-only back (headerBackTitle: '')
- Scan flow is modal with X close; swipe-down dismisses the whole stack
- Recipe import sub-routes are modal; destinations are push
- Dirty-form guard active on 3 screens
- Zero new Ionicons touchpoints; zero color tokens; zero Button/Chip/Input/SearchBar edits
- File ownership disjoint from Plan 03 (Wave 2 parallel execution safe)
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-02-SUMMARY.md` documenting:
- Which screens became modal vs push (confirm against spec)
- How isDirty is derived in each of the three dirty-form screens
- Any Pitfall 2 (nested modal) or Pitfall 3 (usePreventRemove + modal drag) issues encountered
- Hand-rolled back Pressable count after this plan (must be 1: recipes/[id]/index hero)
</output>
