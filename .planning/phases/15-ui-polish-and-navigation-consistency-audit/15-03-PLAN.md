---
phase: 15-ui-polish-and-navigation-consistency-audit
plan: 03
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - apps/mobile/src/app/(tabs)/_layout.tsx
  - apps/mobile/src/app/(tabs)/kitchen.tsx
  - apps/mobile/src/app/(tabs)/pantry.tsx
  - apps/mobile/src/app/(tabs)/plan.tsx
  - apps/mobile/src/app/(tabs)/shopping.tsx
  - apps/mobile/src/app/recipes/[id]/index.tsx
  - apps/mobile/src/app/recipes/[id]/cook.tsx
  - apps/mobile/src/app/recipes/import.tsx
  - apps/mobile/src/app/recipes/discover.tsx
  - apps/mobile/src/app/shopping/orders.tsx
  - apps/mobile/src/app/onboarding/index.tsx
  - apps/mobile/src/app/scan/index.tsx
  - apps/mobile/src/app/scan/receipt.tsx
  - apps/mobile/src/app/scan/instacart.tsx
  - apps/mobile/src/app/recipes/import-photo.tsx
  - apps/mobile/src/components/pantry/EmptyPantry.tsx
  - apps/mobile/src/components/pantry/PantryItemCard.tsx
  - apps/mobile/src/components/pantry/LocationPicker.tsx
  - apps/mobile/src/components/pantry/ScanButton.tsx
  - apps/mobile/src/components/pantry/BulkImportSheet.tsx
  - apps/mobile/src/components/pantry/ReviewItemRow.tsx
  - apps/mobile/src/components/suggestions/SuggestionCard.tsx
  - apps/mobile/src/components/suggestions/SuggestionPreviewModal.tsx
  - apps/mobile/src/components/suggestions/SuggestionList.tsx
  - apps/mobile/src/components/recipes/RecipeCard.tsx
  - apps/mobile/src/components/recipes/FavoriteButton.tsx
  - apps/mobile/src/components/recipes/SearchBar.tsx
  - apps/mobile/src/components/recipes/AddToPlanSheet.tsx
  - apps/mobile/src/components/recipes/RecipeFilterSheet.tsx
  - apps/mobile/src/components/recipes/ServingSizeStepper.tsx
  - apps/mobile/src/components/plan/DayRow.tsx
  - apps/mobile/src/components/plan/SwapSheet.tsx
  - apps/mobile/src/components/plan/CookConfirm.tsx
  - apps/mobile/src/components/plan/EmptyPlanState.tsx
  - apps/mobile/src/components/shopping/ShoppingItemRow.tsx
  - apps/mobile/src/components/shopping/AddItemSheet.tsx
  - apps/mobile/src/components/cooking/TimerBar.tsx
  - apps/mobile/src/components/cooking/StepNavButtons.tsx
  - apps/mobile/src/components/cooking/VoiceStatusBadge.tsx
  - apps/mobile/src/components/cooking/AskSheet.tsx
  - apps/mobile/src/components/settings/IngredientSearch.tsx
  - apps/mobile/src/components/settings/MemberCard.tsx
autonomous: true
requirements:
  - "UI quality (post-v1)"
must_haves:
  truths:
    - "Zero Ionicons imports remain under apps/mobile/src (verify-no-ionicons.sh exits 0)"
    - "Zero decorative emoji unicode in apps/mobile/src/app empty states (verify-no-decorative-emoji.sh exits 0)"
    - "All 5 tab bar icons render as SF Symbols with focused/unfocused variants (fork.knife, calendar, basket, cart, gearshape)"
    - "Empty states across scan, pantry, shopping, plan, recipes use EmptyState primitive with FOOD_IMAGES or SF Symbols per 15-RESEARCH mapping table"
    - "Error state on suggestions uses ErrorState primitive"
    - "Kid-friendly '👶' emoji label converted to 'Kid-friendly' text label (CONTEXT Claude's Discretion: keep label, drop emoji)"
    - "Location emojis (🧊/🗄️/❄️) in PantryItemCard + LocationPicker converted to SF Symbols; 'fridge' uses 'snowflake' per Open Question #3 safe default"
    - "FavoriteButton uses 'heart.fill' (active) / 'heart' (inactive) with orange #F97316 tint preserved"
  artifacts:
    - path: "apps/mobile/src/app/(tabs)/_layout.tsx"
      provides: "5 tab bar SymbolIcon configurations (focused + unfocused variants)"
      contains: "SymbolIcon"
    - path: "apps/mobile/src/components/recipes/FavoriteButton.tsx"
      provides: "Heart toggle using SymbolIcon with orange tint"
      contains: "heart.fill"
    - path: "apps/mobile/src/components/pantry/EmptyPantry.tsx"
      provides: "EmptyState consumer using FOOD_IMAGES.hero[1]"
      contains: "EmptyState"
    - path: "apps/mobile/src/components/suggestions/SuggestionList.tsx"
      provides: "ErrorState consumer with SF Symbol"
      contains: "ErrorState"
  key_links:
    - from: "apps/mobile/src/app/(tabs)/_layout.tsx"
      to: "SymbolIcon"
      via: "tabBarIcon prop returns <SymbolIcon name={focused ? 'fork.knife.circle.fill' : 'fork.knife'} tintColor={color} />"
      pattern: "SymbolIcon"
    - from: "apps/mobile/src/components/pantry/EmptyPantry.tsx"
      to: "EmptyState + EMPTY_STATE_IMAGES"
      via: "<EmptyState visual={{kind:'image', uri: EMPTY_STATE_IMAGES.emptyPantry}} ... />"
      pattern: "EMPTY_STATE_IMAGES"
    - from: "apps/mobile/src/components/suggestions/SuggestionList.tsx"
      to: "ErrorState"
      via: "<ErrorState title=... retry={{label:'Try again', onPress: refetch}} />"
      pattern: "ErrorState"
---

<objective>
Sweep all decorative icons and emojis across ~34 files, replacing Ionicons with SymbolIcon wrapper and decorative emojis with SymbolIcon OR FOOD_IMAGES-backed EmptyState per the research mapping. Also route all existing empty/loading/error ad-hoc renderings through the Plan 01 primitives.

Purpose: Phase 15's iconography mandate — one icon family (SF Symbols) everywhere, iOS-native feel. Research inventory: 34 Ionicons files, ~13 decorative emoji locations. Every touchpoint converts in this plan so Plan 04's purity scripts pass.

Output: Every file listed in `files_modified` converts its Ionicons calls to SymbolIcon + migrates its decorative emojis/empty states to SymbolIcon or EmptyState primitive.

Runs in parallel with Plan 02 (nav migration). File ownership is DISJOINT:
- Plan 02 owns: all `_layout.tsx` files (app/, scan/, recipes/, shopping/), HeaderCloseButton.tsx, and the useDirtyFormGuard wiring in recipes/[id]/edit.tsx, recipes/review.tsx, scan/review.tsx
- Plan 03 owns: every other file listed here. For files touched by both plans (scan/index.tsx, scan/receipt.tsx, scan/instacart.tsx, recipes/[id]/edit.tsx, recipes/review.tsx, recipes/[id]/index.tsx): Plan 02 edits Stack.Screen options + dirty-form guard at top; Plan 03 edits icon swaps + empty states in the body. These edits are in separate regions and both commits can land without merge conflict if executed carefully.

IMPORTANT: If both plans execute concurrently and hit a merge conflict on the shared files, the execute orchestrator will serialize them. Running in Wave 2 order (02 first, then 03) is also acceptable — dependency graph says they're parallel-safe, but the execute layer may choose to sequence them.

**Phase 19 boundary callout:** DO NOT touch Button.tsx, ChipToggle.tsx, Input.tsx, or SearchBar.tsx's CORE design. SearchBar gets ONE icon swap (Ionicons `search` → SymbolIcon `magnifyingglass`) but its overall structure stays. Orange #F97316 is preserved everywhere — every SymbolIcon replacing an orange Ionicons uses `tintColor="#F97316"` verbatim. Do NOT introduce terracotta. Do NOT migrate RecipeFilterSheet lines 40-44 emoji array or RemixSheet lines 71-74 emoji array (DEFER per 15-RESEARCH Open Question #2 — Phase 19 chip rewrite owns those).
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-CONTEXT.md
@.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-RESEARCH.md
@.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-01-SUMMARY.md
@apps/mobile/src/constants/foodImages.ts

<icon_mapping>
<!-- Per 15-RESEARCH "Ionicons → SF Symbols name mapping" table. -->
<!-- Source of truth: apply verbatim. -->

Top 20 mappings:
- chevron-back → chevron.backward
- chevron-forward → chevron.forward
- close → xmark
- close-circle → xmark.circle.fill
- close-circle-outline → xmark.circle
- checkmark → checkmark
- checkmark-circle → checkmark.circle.fill
- checkmark-circle-outline → checkmark.circle
- heart → heart.fill          (active)
- heart-outline → heart        (inactive)
- camera → camera.fill
- camera-outline → camera
- cart → cart.fill
- cart-outline → cart
- calendar / calendar-outline → calendar (SF Symbols has no outline)
- basket-outline → basket
- restaurant / restaurant-outline → fork.knife.circle.fill / fork.knife
- settings / settings-outline → gearshape.fill / gearshape
- sparkles → sparkles
- time-outline → clock
- people-outline → person.2
- trash-outline → trash
- remove → minus
- add → plus
- search → magnifyingglass
- refresh → arrow.clockwise
- receipt-outline → doc.text
- timer-outline → timer
- play → play.fill
- pause → pause.fill
- alert-circle-outline → exclamationmark.circle
- filter / filter-outline → line.3.horizontal.decrease.circle

Location emojis (PantryItemCard, LocationPicker):
- 🧊 fridge → snowflake     (SAFE default per Open Question #3; refrigerator is iOS17+ only)
- 🗄️ pantry → archivebox
- ❄️ freezer → snowflake
- 📦 fallback → shippingbox

Decorative emoji replacements (per 15-RESEARCH Emoji inventory + FOOD_IMAGES reuse mapping):
- app/scan/index.tsx 📸 → EmptyState visual=image uri=EMPTY_STATE_IMAGES.scanReady
- app/scan/receipt.tsx 🧾 → EmptyState visual=symbol name='doc.text.viewfinder'
- app/scan/instacart.tsx 🛒 → EmptyState visual=symbol name='cart.circle'
- app/shopping/orders.tsx 📦 → EmptyState visual=symbol name='shippingbox'
- app/(tabs)/shopping.tsx 🛒 → EmptyState visual=symbol name='cart' (SF symbol is safer default than hero image for shopping list empty per research)
- app/recipes/import-photo.tsx 📷 → EmptyState visual=symbol name='camera'
- components/pantry/EmptyPantry.tsx 📷 → EmptyState visual=image uri=EMPTY_STATE_IMAGES.emptyPantry
- components/suggestions/SuggestionList.tsx 😕 → ErrorState with retry
- components/suggestions/SuggestionCard.tsx 👶 → drop emoji, keep text "Kid-friendly" (Claude Discretion)
- components/suggestions/SuggestionPreviewModal.tsx 👶 → drop emoji, keep text "Kid-friendly"
- components/plan/DayRow.tsx 👶 → drop emoji, keep text "Kid-friendly"
- components/plan/DayRow.tsx ✓ → SymbolIcon name='checkmark' (tiny)
- components/plan/EmptyPlanState.tsx (current Ionicons calendar-outline) → EmptyState visual=image uri=EMPTY_STATE_IMAGES.planEmpty (or keep as SymbolIcon if photo doesn't read well)
- app/onboarding/index.tsx line 220 ✓ → SymbolIcon name='checkmark'

DEFERRED to Phase 19 (DO NOT TOUCH):
- RecipeFilterSheet.tsx lines 40-44 emoji array (chip content)
- RemixSheet.tsx lines 71-74 emoji array (chip content)
</icon_mapping>

<consumer_files>
<!-- Plan 01's primitives these files will import. Reference without exploration. -->

From @/components/ui/SymbolIcon:
- SymbolIcon component, SymbolIconSize type ('body' | 'title' | 'largeTitle')
- Pass tintColor as prop, not className

From @/components/ui/EmptyState:
- EmptyState component
- visual={kind:'image'|'symbol', uri|name}, title, subtitle?, action?

From @/components/ui/LoadingState:
- LoadingState component
- variant?: 'spinner' | 'skeleton', label?

From @/components/ui/ErrorState:
- ErrorState component
- title, message?, retry?, variant?: 'banner' | 'full'

From @/constants/emptyStateImages:
- EMPTY_STATE_IMAGES (Record<EmptyStateImageKey, string>)
- Keys: 'scanReady' | 'emptyPantry' | 'shoppingListEmpty' | 'planEmpty'
</consumer_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tab bar icon migration + top-level screen icon migration (app/ tree)</name>
  <files>
    apps/mobile/src/app/(tabs)/_layout.tsx,
    apps/mobile/src/app/(tabs)/kitchen.tsx,
    apps/mobile/src/app/(tabs)/pantry.tsx,
    apps/mobile/src/app/(tabs)/plan.tsx,
    apps/mobile/src/app/(tabs)/shopping.tsx,
    apps/mobile/src/app/recipes/[id]/index.tsx,
    apps/mobile/src/app/recipes/[id]/cook.tsx,
    apps/mobile/src/app/recipes/import.tsx,
    apps/mobile/src/app/recipes/discover.tsx,
    apps/mobile/src/app/shopping/orders.tsx,
    apps/mobile/src/app/onboarding/index.tsx,
    apps/mobile/src/app/scan/index.tsx,
    apps/mobile/src/app/scan/receipt.tsx,
    apps/mobile/src/app/scan/instacart.tsx,
    apps/mobile/src/app/recipes/import-photo.tsx
  </files>
  <action>
    Per D-01 (icon system) from CONTEXT and 15-RESEARCH Ionicons inventory + mapping table:

    **Step 1: (tabs)/_layout.tsx** — 5 tab bar SymbolIcon configurations. Per 15-RESEARCH "Tab icon swap" code example. Each tabBarIcon returns SymbolIcon with focused/unfocused SF Symbol name pair and `tintColor={color}` (propagating the tab bar color). Remove `import { Ionicons } from '@expo/vector-icons'`.

    Mappings:
    - Kitchen tab: focused='fork.knife.circle.fill', unfocused='fork.knife'
    - Plan tab: 'calendar' (both focused and unfocused — SF Symbols has no calendar outline; differentiate via `weight`)
    - Pantry tab: focused='basket.fill', unfocused='basket'
    - Shopping tab: focused='cart.fill', unfocused='cart'
    - Settings: focused='gearshape.fill', unfocused='gearshape'

    Per Pitfall 1: wrap SymbolIcon in a sized View if alignment drifts — `style={{ width: size, height: size }}` on the parent View for tab bar icons.

    **Step 2: (tabs)/kitchen.tsx** — 4 Ionicons usages (add, sparkles, filter-related). Map via mapping table. Preserve orange #F97316 tintColor on the FAB.

    **Step 3: (tabs)/pantry.tsx** — camera icon → SymbolIcon name='camera.fill'. Preserve FAB orange.

    **Step 4: (tabs)/plan.tsx** — refresh → 'arrow.clockwise'.

    **Step 5: (tabs)/shopping.tsx** — 2 usages (receipt-outline → 'doc.text', add → 'plus'). Plus the 🛒 empty state emoji → EmptyState with SF Symbol 'cart' (per mapping: SF symbol, not hero image).

    **Step 6: recipes/[id]/index.tsx** — 5 Ionicons: time-outline → 'clock', chevron-back → 'chevron.backward', calendar-outline → 'calendar', sparkles → 'sparkles', trash-outline → 'trash'. IMPORTANT: the floating back button at ~lines 110-117 is a KNOWN EXCEPTION (Plan 02 documented it). Keep the Pressable, just migrate its Ionicons chevron-back to SymbolIcon.

    **Step 7: recipes/[id]/cook.tsx** — close → 'xmark'. (This screen has `headerShown: false` and is untouched by Plan 02.)

    **Step 8: recipes/import.tsx** — 3 Ionicons (method picker + chevron-forward). Map via table. Pitfall 5: if the `MethodCard` component in this file types its icon prop as `keyof typeof Ionicons.glyphMap`, change the type to `string` (SF Symbols has no typed enum).

    **Step 9: recipes/discover.tsx** — 7 Ionicons (refresh, time-outline, people-outline, chevron-forward, checkmark-circle ×2, close). Map via table.

    **Step 10: shopping/orders.tsx** — cart → 'cart', chevron-forward → 'chevron.forward'. Plus empty state 📦 → EmptyState with SF Symbol 'shippingbox'.

    **Step 11: onboarding/index.tsx** — line 220 `✓` → SymbolIcon name='checkmark' (small, inline).

    **Step 12: scan/index.tsx** — 📸 emoji (2 occurrences — consolidate into ONE EmptyState) → EmptyState visual={kind:'image', uri:EMPTY_STATE_IMAGES.scanReady}, title='Ready to scan your kitchen', subtitle per current copy, action={label:'Take photo', onPress:<existing handler>}.
    Plus any Ionicons in body → map via table. Do NOT touch Stack.Screen options block (Plan 02's scope).

    **Step 13: scan/receipt.tsx** — 🧾 → EmptyState visual={kind:'symbol', name:'doc.text.viewfinder'}. Plus any Ionicons → map.

    **Step 14: scan/instacart.tsx** — 🛒 → EmptyState visual={kind:'symbol', name:'cart.circle'}. Plus any Ionicons → map.

    **Step 15: recipes/import-photo.tsx** — 📷 → EmptyState visual={kind:'symbol', name:'camera'}. Plus any Ionicons → map.

    **Pitfall mitigations applied:**
    - Pitfall 1 (SymbolView flex sizing): For tab bar, wrap each SymbolIcon in a sized View.
    - Pitfall 5 (glyphMap typing): type SymbolIcon's `name` as `string` downstream; if any file references `keyof typeof Ionicons.glyphMap`, change to `string`.
    - Pitfall 7 (NativeWind tintColor): always pass tintColor as prop, never className.

    **Do NOT:**
    - Touch Stack.Screen options blocks or useDirtyFormGuard calls (Plan 02's domain)
    - Change body layout or business logic — only icon/emoji swaps + EmptyState adoption
    - Introduce any new color (tintColor uses existing palette: #F97316 orange, #9CA3AF warmGray, #1F2937 text)
    - Modify Button.tsx / ChipToggle.tsx / Input.tsx / SearchBar.tsx structure (Phase 19 scope); SearchBar icon swap is fine (search → magnifyingglass) but leave the rest
    - Touch (auth)/_layout.tsx or any auth screens unless they have an Ionicons import (check; likely none)
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit -p . &&  bash apps/mobile/scripts/verify-no-ionicons.sh || { echo "Task 1 partial: component files (Task 2) still have Ionicons — expected at this point"; true; }</automated>
  </verify>
  <done>
    - All 15 app/ files listed migrated; zero Ionicons imports among them
    - 5 tab bar icons render as SF Symbol pairs
    - Empty states in scan/index, scan/receipt, scan/instacart, shopping (tab + orders), recipes/import-photo use EmptyState primitive with correct visual (FOOD_IMAGES or SF Symbol per mapping)
    - Floating back button on recipes/[id]/index.tsx preserved (legitimate exception); only its Ionicons icon swapped to SymbolIcon
    - Typecheck clean
    - NOTE: verify-no-ionicons.sh may still fail here because Task 2 hasn't run yet; Task 2 closes the remaining gap
  </done>
</task>

<task type="auto">
  <name>Task 2: Component-tree icon + emoji migration (pantry, suggestions, recipes, plan, shopping, cooking, settings)</name>
  <files>
    apps/mobile/src/components/pantry/EmptyPantry.tsx,
    apps/mobile/src/components/pantry/PantryItemCard.tsx,
    apps/mobile/src/components/pantry/LocationPicker.tsx,
    apps/mobile/src/components/pantry/ScanButton.tsx,
    apps/mobile/src/components/pantry/BulkImportSheet.tsx,
    apps/mobile/src/components/pantry/ReviewItemRow.tsx,
    apps/mobile/src/components/suggestions/SuggestionCard.tsx,
    apps/mobile/src/components/suggestions/SuggestionPreviewModal.tsx,
    apps/mobile/src/components/suggestions/SuggestionList.tsx,
    apps/mobile/src/components/recipes/RecipeCard.tsx,
    apps/mobile/src/components/recipes/FavoriteButton.tsx,
    apps/mobile/src/components/recipes/SearchBar.tsx,
    apps/mobile/src/components/recipes/AddToPlanSheet.tsx,
    apps/mobile/src/components/recipes/RecipeFilterSheet.tsx,
    apps/mobile/src/components/recipes/ServingSizeStepper.tsx,
    apps/mobile/src/components/plan/DayRow.tsx,
    apps/mobile/src/components/plan/SwapSheet.tsx,
    apps/mobile/src/components/plan/CookConfirm.tsx,
    apps/mobile/src/components/plan/EmptyPlanState.tsx,
    apps/mobile/src/components/shopping/ShoppingItemRow.tsx,
    apps/mobile/src/components/shopping/AddItemSheet.tsx,
    apps/mobile/src/components/cooking/TimerBar.tsx,
    apps/mobile/src/components/cooking/StepNavButtons.tsx,
    apps/mobile/src/components/cooking/VoiceStatusBadge.tsx,
    apps/mobile/src/components/cooking/AskSheet.tsx,
    apps/mobile/src/components/settings/IngredientSearch.tsx,
    apps/mobile/src/components/settings/MemberCard.tsx
  </files>
  <action>
    Per D-01 (icon system) and 15-RESEARCH Ionicons + emoji inventories, migrate every component-tree Ionicons usage and relevant decorative emoji. Apply mappings verbatim per the `<icon_mapping>` block in <context>.

    **Pantry (6 files):**
    - `EmptyPantry.tsx`: Replace current 📷 + layout with `<EmptyState visual={{kind:'image', uri: EMPTY_STATE_IMAGES.emptyPantry}} title=... />`. Import EmptyState + EMPTY_STATE_IMAGES.
    - `PantryItemCard.tsx`: Location emojis 🧊/🗄️/❄️ in `locationIcons` map (lines 8-10) → replace with SymbolIcon renderer. Use snowflake/archivebox/snowflake per Open Question #3 safe default. Also migrate Ionicons: time-outline → 'clock', checkmark-circle-outline → 'checkmark.circle', trash-outline → 'trash'.
    - `LocationPicker.tsx`: Same location emoji migration (lines 12-14).
    - `ScanButton.tsx`: camera → 'camera.fill' (FAB emphasis); preserve orange #F97316 tintColor.
    - `BulkImportSheet.tsx`: 2 Ionicons (dynamic icon prop, chevron-forward). Per Pitfall 5, retype dynamic icon prop from `keyof typeof Ionicons.glyphMap` to `string`. Chevron-forward → 'chevron.forward'.
    - `ReviewItemRow.tsx`: close-circle-outline → 'xmark.circle'; dynamic icon prop retyped to string.

    **Suggestions (3 files):**
    - `SuggestionCard.tsx`: time-outline → 'clock', checkmark-circle → 'checkmark.circle.fill'. 👶 emoji DROP — keep text label "Kid-friendly" per CONTEXT Discretion.
    - `SuggestionPreviewModal.tsx`: 7 Ionicons → map via table. 👶 DROP, keep text.
    - `SuggestionList.tsx`: 😕 error emoji + current error rendering → `<ErrorState title='Something went wrong' message=... retry={{label:'Try again', onPress: <existing retry handler>}} variant='full' />`. Import ErrorState.

    **Recipes (6 files):**
    - `RecipeCard.tsx`: 3 Ionicons (sparkles → 'sparkles', time-outline → 'clock', people-outline → 'person.2'). Heart is in FavoriteButton — leave.
    - `FavoriteButton.tsx`: heart/heart-outline → SymbolIcon with conditional name={isFavorite ? 'heart.fill' : 'heart'}, tintColor="#F97316" (orange preserved). This is a HIGH-visibility change — Maestro flow `18-recipe-search-favorite.yaml` will need a screenshot rebase (Plan 04 handles).
    - `SearchBar.tsx`: search → 'magnifyingglass', close-circle → 'xmark.circle.fill'. DO NOT restructure SearchBar — Phase 19 rewrites the whole search-bar pattern. This is ONLY an icon swap.
    - `AddToPlanSheet.tsx`: 3 Ionicons → map.
    - `RecipeFilterSheet.tsx`: 4 Ionicons → map. DO NOT touch lines 40-44 emoji array (chip content — DEFERRED to Phase 19 per 15-RESEARCH Open Question #2).
    - `ServingSizeStepper.tsx`: remove → 'minus', add → 'plus'.

    **Plan (4 files):**
    - `DayRow.tsx`: 3 Ionicons → map. 👶 emoji DROP (keep text). ✓ (inline checkmark) → SymbolIcon name='checkmark' small.
    - `SwapSheet.tsx`: close → 'xmark'. Do NOT change modal structure — keep swipe-down behavior.
    - `CookConfirm.tsx`: close → 'xmark', checkmark-circle → 'checkmark.circle.fill', remove-circle-outline → 'xmark.circle' (approx semantics).
    - `EmptyPlanState.tsx`: calendar-outline (Ionicons currently) → EmptyState with FOOD_IMAGES.planEmpty (EMPTY_STATE_IMAGES.planEmpty). If photo doesn't read as "empty plan", fall back to `visual={kind:'symbol', name:'calendar'}`.

    **Shopping (2 files):**
    - `ShoppingItemRow.tsx`: 3 Ionicons (trash-outline → 'trash', close → 'xmark', checkmark → 'checkmark'). ShoppingItemRow is HIGH-visibility; use `size="body"` and preserve existing tint colors.
    - `AddItemSheet.tsx`: close → 'xmark'.

    **Cooking (4 files):**
    - `TimerBar.tsx`: timer-outline → 'timer', play/pause → 'play.fill'/'pause.fill'.
    - `StepNavButtons.tsx`: dynamic icon prop → retype to `string`; map invocations to SF Symbol names.
    - `VoiceStatusBadge.tsx`: dynamic icon prop → retype.
    - `AskSheet.tsx`: close → 'xmark'.

    **Settings (2 files):**
    - `IngredientSearch.tsx`: search → 'magnifyingglass', close-circle → 'xmark.circle.fill'. Mirror SearchBar treatment (icon-swap only, no structural changes).
    - `MemberCard.tsx`: trash-outline → 'trash'.

    **Cross-cutting:**
    - Remove every `import { Ionicons } from '@expo/vector-icons'` that becomes unused after the swap.
    - Every SymbolIcon usage that replaced an icon-only button (FAB, close X, heart toggle, trash) MUST have an `accessibilityLabel` prop per Pitfall 8 (Maestro selector resilience).
    - Preserve orange #F97316 on every FAB-level icon (Pantry FAB, Kitchen FAB, heart fill). This is NOT a color migration.

    **Do NOT:**
    - Touch RecipeFilterSheet lines 40-44 emoji array (Phase 19 chip rewrite)
    - Touch RemixSheet emoji array (Phase 19 chip rewrite — if RemixSheet is in the file list above, only migrate its Ionicons, leave emoji arrays alone) — NOTE: RemixSheet.tsx is NOT in files_modified; it defers entirely to Phase 19 per research
    - Rewrite Button.tsx, ChipToggle.tsx, Input.tsx structurally
    - Introduce terracotta or any new Tailwind color class
    - Alter component business logic, props APIs, or render structure beyond the swap
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit -p . && bash apps/mobile/scripts/verify-no-ionicons.sh && bash apps/mobile/scripts/verify-no-decorative-emoji.sh</automated>
  </verify>
  <done>
    - All 27 component files migrated to SymbolIcon
    - Zero Ionicons imports anywhere under apps/mobile/src (verify-no-ionicons.sh exits 0)
    - Zero decorative emojis under apps/mobile/src/app (verify-no-decorative-emoji.sh exits 0)
    - FavoriteButton uses heart.fill/heart with orange #F97316 preserved
    - Location emojis replaced with SymbolIcons in PantryItemCard + LocationPicker (snowflake for fridge, archivebox for pantry)
    - Kid-friendly 👶 emojis dropped across 3 files; text label preserved
    - SuggestionList error path uses ErrorState primitive with retry
    - EmptyPantry + EmptyPlanState use EmptyState with FOOD_IMAGES
    - RecipeFilterSheet and (untouched) RemixSheet emoji chip arrays preserved for Phase 19
    - Typecheck clean
    - Every icon-only button has accessibilityLabel
  </done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit -p .` exits 0
- `bash apps/mobile/scripts/verify-no-ionicons.sh` exits 0 (zero Ionicons imports in src)
- `bash apps/mobile/scripts/verify-no-decorative-emoji.sh` exits 0 (zero decorative emojis in src/app)
- Spot-grep: `grep -rn "from '@expo/vector-icons'" apps/mobile/src` returns nothing
- Spot-grep: `grep -rn "text-orange-500\|#F97316" apps/mobile/src/components/recipes/FavoriteButton.tsx` confirms orange preserved on heart
- Spot-check: `grep -rn "Kid-friendly" apps/mobile/src/components/suggestions apps/mobile/src/components/plan` shows text preserved
- Plan 02 artifacts untouched (layout files, HeaderCloseButton.tsx, useDirtyFormGuard hooks in dirty-form screens)
</verification>

<success_criteria>
- 34 files worth of Ionicons usages migrated to SymbolIcon (SF Symbols via expo-symbols)
- ~13 decorative emoji locations migrated: FOOD_IMAGES-backed EmptyState for scan-ready / empty-pantry / plan-empty / shopping-list-empty; SF Symbol EmptyState for instacart / receipt / shopping-orders / recipes-import-photo; ErrorState for suggestions error; text-only label for kid-friendly
- Orange #F97316 preserved on every FAB + FavoriteButton active heart
- Location emojis replaced with SF Symbols using safe iOS 15+ defaults (snowflake for fridge)
- All purity grep scripts (Plan 01) now exit 0
- Phase 19-owned files (Button.tsx, ChipToggle.tsx, Input.tsx, SearchBar structure, RecipeFilterSheet emoji array, RemixSheet entirely) untouched
- Plan 02 file ownership respected (no edits to _layout.tsx files, HeaderCloseButton.tsx, or dirty-form-guard blocks)
</success_criteria>

<output>
After completion, create `.planning/phases/15-ui-polish-and-navigation-consistency-audit/15-03-SUMMARY.md` documenting:
- Final count: files converted, Ionicons import count (should be 0), decorative emoji count (should be 0)
- Any SF Symbol names that rendered as blank boxes on iOS 15.1 and required a safer fallback (especially `refrigerator` vs `snowflake`)
- Any components where retyping `keyof typeof Ionicons.glyphMap` to `string` required downstream callsite fixes
- Breadcrumb for Phase 19: list of files where Button/Chip/Input/SearchBar structural rewrite will land, so Phase 19 can pick up cleanly
- Maestro flows that will need screenshot rebase (Plan 04 input): 18-recipe-search-favorite (heart), 07-pantry-add (FAB camera), 10-meal-plan-swap (close icon), 11-shopping-list-generate (FAB), 12-shopping-orders (empty state), 20-kitchen-segment-toggle (tab icon)
</output>
