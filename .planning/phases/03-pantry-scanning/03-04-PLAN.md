---
phase: 03-pantry-scanning
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - apps/mobile/src/app/scan/index.tsx
  - apps/mobile/src/app/scan/review.tsx
  - apps/mobile/src/app/scan/_layout.tsx
  - apps/mobile/src/app/(tabs)/pantry.tsx
  - apps/mobile/src/components/pantry/PantryItemCard.tsx
  - apps/mobile/src/components/pantry/PantryItemList.tsx
  - apps/mobile/src/components/pantry/ScanButton.tsx
  - apps/mobile/src/components/pantry/ReviewItemRow.tsx
  - apps/mobile/src/components/pantry/EmptyPantry.tsx
  - apps/mobile/src/components/pantry/LocationPicker.tsx
autonomous: false
requirements:
  - PANT-01
  - PANT-02
  - PANT-03
  - PANT-04
  - PANT-05
  - PANT-08

must_haves:
  truths:
    - "User can tap a scan button, choose fridge/pantry/freezer, take a photo, and see AI-detected items"
    - "User can accept, reject, edit names/quantities, and add missed items in the review screen"
    - "User can confirm reviewed items and see them appear in their pantry inventory"
    - "Pantry tab shows all available items grouped by category with confidence indicators"
    - "User can swipe or tap to mark items as used/depleted from the inventory list"
    - "Items not seen in 7+ days show a visual uncertainty indicator (muted styling, clock icon)"
    - "Empty pantry state guides user to take their first scan"
  artifacts:
    - path: "apps/mobile/src/app/scan/index.tsx"
      provides: "Scan screen with location picker and camera launch"
    - path: "apps/mobile/src/app/scan/review.tsx"
      provides: "Review screen for editing AI scan results before confirming"
    - path: "apps/mobile/src/app/(tabs)/pantry.tsx"
      provides: "Main pantry inventory screen (replaces placeholder)"
    - path: "apps/mobile/src/components/pantry/PantryItemCard.tsx"
      provides: "Single pantry item display with status actions"
    - path: "apps/mobile/src/components/pantry/EmptyPantry.tsx"
      provides: "Empty state with scan prompt"
  key_links:
    - from: "apps/mobile/src/app/scan/index.tsx"
      to: "apps/mobile/src/stores/pantryStore.ts"
      via: "calls startScan after photo capture"
      pattern: "usePantryStore.*startScan"
    - from: "apps/mobile/src/app/scan/review.tsx"
      to: "apps/mobile/src/stores/pantryStore.ts"
      via: "reads scanResults, calls confirmScan"
      pattern: "usePantryStore.*scanResults|confirmScan"
    - from: "apps/mobile/src/app/(tabs)/pantry.tsx"
      to: "apps/mobile/src/hooks/usePantryItems.ts"
      via: "uses enriched items with decay"
      pattern: "usePantryItems"
    - from: "apps/mobile/src/components/pantry/PantryItemCard.tsx"
      to: "apps/mobile/src/stores/pantryStore.ts"
      via: "calls markItemUsed/markItemDepleted"
      pattern: "markItemUsed|markItemDepleted"
---

<objective>
Build the complete pantry scanning UI: scan flow (location picker -> camera -> AI review -> confirm) and inventory display (item list with status management, uncertainty indicators, empty state).

Purpose: This is the user-facing experience for the core "photograph your kitchen" feature. Users need to capture photos, review AI results, manage their inventory, and mark items as used.
Output: Working scan flow screens, pantry inventory tab, all pantry UI components.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pantry-scanning/03-RESEARCH.md
@.planning/phases/03-pantry-scanning/03-01-SUMMARY.md
@.planning/phases/03-pantry-scanning/03-02-SUMMARY.md
@.planning/phases/03-pantry-scanning/03-03-SUMMARY.md

@apps/mobile/src/app/(tabs)/pantry.tsx
@apps/mobile/src/app/(tabs)/_layout.tsx
@apps/mobile/src/stores/pantryStore.ts
@apps/mobile/src/hooks/usePantryItems.ts
@apps/mobile/src/types/pantry.ts
@apps/mobile/src/components/ui/Button.tsx
@apps/mobile/src/components/ui/Input.tsx
</context>

<interfaces>
<!-- Store and hook APIs from Plan 03 -->

From apps/mobile/src/stores/pantryStore.ts:
```typescript
interface PantryState {
  items: PantryItem[];
  scanResults: ReviewItem[];
  isScanning: boolean;
  isLoading: boolean;
  loadItems: (profileId: string) => Promise<void>;
  startScan: (base64Image: string, location: SourceLocation) => Promise<void>;
  updateReviewItem: (id: string, updates: Partial<ReviewItem>) => void;
  addReviewItem: (item: ReviewItem) => void;
  removeReviewItem: (id: string) => void;
  confirmScan: (profileId: string, sourceLocation: SourceLocation) => Promise<void>;
  markItemUsed: (itemId: string) => Promise<void>;
  markItemDepleted: (itemId: string) => Promise<void>;
}
export const usePantryStore: UseBoundStore<StoreApi<PantryState>>;
```

From apps/mobile/src/hooks/usePantryItems.ts:
```typescript
interface EnrichedPantryItem extends PantryItem {
  effectiveConfidence: number;
  isUncertain: boolean;
}
export function usePantryItems(options?: { location?: SourceLocation }): EnrichedPantryItem[];
export function getEffectiveConfidence(item: PantryItem): number;
```

From apps/mobile/src/types/pantry.ts:
```typescript
export type SourceLocation = 'fridge' | 'pantry' | 'freezer';
export interface ReviewItem extends ScanResult { id: string; accepted: boolean; userEdited: boolean; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scan flow screens (location picker, camera, review)</name>
  <files>apps/mobile/src/app/scan/_layout.tsx, apps/mobile/src/app/scan/index.tsx, apps/mobile/src/app/scan/review.tsx, apps/mobile/src/components/pantry/LocationPicker.tsx, apps/mobile/src/components/pantry/ReviewItemRow.tsx</files>
  <action>
1. Create apps/mobile/src/app/scan/_layout.tsx:
   - Stack navigator layout for the scan flow
   - Screens: index (location picker + camera), review (edit results)
   - Use Stack from expo-router with headerShown options

2. Create apps/mobile/src/components/pantry/LocationPicker.tsx:
   - Three large tappable cards: "Fridge" (with fridge emoji), "Pantry" (shelf emoji), "Freezer" (snowflake emoji)
   - Horizontal row layout with NativeWind styling
   - Selected state with warm orange border (matching app theme)
   - onSelect callback returns SourceLocation

3. Create apps/mobile/src/app/scan/index.tsx (scan entry screen):
   - Show LocationPicker at top for user to select fridge/pantry/freezer
   - Large "Take Photo" button below (using existing Button component)
   - On button press: call ImagePicker.requestCameraPermissionsAsync(), then ImagePicker.launchCameraAsync({ base64: true, quality: 0.8, mediaTypes: ['images'] })
   - If photo captured: call usePantryStore.startScan(base64, selectedLocation)
   - Show loading spinner with "Analyzing your {location}..." text while isScanning=true
   - On scan complete (scanResults populated): router.push('/scan/review')
   - Handle permission denied with alert message
   - Handle cancel gracefully (no action)

4. Create apps/mobile/src/components/pantry/ReviewItemRow.tsx:
   - Displays a single ReviewItem with: checkbox (accepted toggle), name (editable TextInput), quantity + unit display, confidence badge (color-coded: green >0.8, yellow 0.5-0.8, red <0.5), category chip
   - Tap checkbox toggles accepted state via updateReviewItem
   - Tap name to edit inline, update via updateReviewItem on blur
   - Swipe left to remove (removeReviewItem)
   - Items that are not accepted show muted/strikethrough styling

5. Create apps/mobile/src/app/scan/review.tsx:
   - Header: "Review Items" with count of accepted items
   - FlatList of ReviewItemRow components, one per scanResult
   - "Add Item" button at bottom of list opens inline form (name, quantity, unit, category picker) -> calls addReviewItem
   - "Confirm" button at bottom: calls confirmScan with accepted items, navigates back to pantry tab with router.replace('/(tabs)/pantry')
   - "Discard" button (text style): clears scanResults and navigates back
   - Show category section headers if items span multiple categories
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -20; echo "---"; pnpm -r test 2>&1 | tail -10</automated>
  </verify>
  <done>Scan flow works: user selects location, takes photo, sees loading state, reviews AI results with edit/accept/reject/add capabilities, confirms items into inventory. All screens compile and route correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Pantry inventory tab and item components</name>
  <files>apps/mobile/src/app/(tabs)/pantry.tsx, apps/mobile/src/components/pantry/PantryItemCard.tsx, apps/mobile/src/components/pantry/PantryItemList.tsx, apps/mobile/src/components/pantry/ScanButton.tsx, apps/mobile/src/components/pantry/EmptyPantry.tsx</files>
  <action>
1. Create apps/mobile/src/components/pantry/EmptyPantry.tsx:
   - Centered layout with camera emoji/icon, "Your kitchen is empty" heading
   - Subtext: "Take a photo of your fridge, pantry, or freezer to get started"
   - "Scan Now" button that navigates to /scan
   - Warm, inviting styling matching app theme

2. Create apps/mobile/src/components/pantry/PantryItemCard.tsx:
   - Displays PantryItem (enriched with effectiveConfidence and isUncertain from hook)
   - Shows: name, quantity + unit, category badge, source location icon (fridge/pantry/freezer)
   - Uncertain items: muted opacity (0.6), clock icon, "Not seen in X days" subtitle text
   - Swipe actions (using React Native Gesture Handler or simple button row):
     - "Used" (green) -> calls markItemUsed
     - "Gone" (red) -> calls markItemDepleted
   - Alternative to swipe: long-press menu or tap to expand with action buttons (simpler implementation)

3. Create apps/mobile/src/components/pantry/PantryItemList.tsx:
   - SectionList grouped by category (produce, dairy, protein, etc.)
   - Section headers with category name and item count
   - Renders PantryItemCard for each item
   - Pull-to-refresh calls loadItems
   - Category order: produce, protein, dairy, grain, condiment, beverage, frozen, snack, other

4. Create apps/mobile/src/components/pantry/ScanButton.tsx:
   - Floating action button (FAB) positioned bottom-right
   - Camera icon with warm orange background
   - Navigates to /scan on press
   - Subtle shadow for elevation

5. Replace apps/mobile/src/app/(tabs)/pantry.tsx (currently a placeholder):
   - On mount: call loadItems with user's profile ID (from useAuthStore)
   - If items empty and not loading: show EmptyPantry
   - If items exist: show PantryItemList with ScanButton overlay
   - Header area with title "My Kitchen" and optional location filter tabs (All | Fridge | Pantry | Freezer)
   - Location filter uses usePantryItems({ location }) to filter displayed items
   - SafeAreaView wrapper following existing tab screen pattern
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -20; echo "---"; pnpm -r test 2>&1 | tail -10</automated>
  </verify>
  <done>Pantry tab shows inventory grouped by category. Empty state guides to first scan. Items show uncertainty indicators for stale items. Users can mark items used/depleted. FAB navigates to scan flow. Location filter tabs work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete pantry scanning feature</name>
  <action>Human verifies the full pantry scanning flow end-to-end on device or simulator.</action>
  <verify>User confirms the flow works as expected or reports issues.</verify>
  <done>User approves the pantry scanning feature visually and functionally.</done>
  <what-built>Complete pantry scanning feature: photo capture -> AI food recognition -> review/edit results -> persistent inventory with confidence decay and manual status management</what-built>
  <how-to-verify>
    1. Open the app on iOS simulator or device
    2. Navigate to the Pantry tab -- should show empty state with "Scan Now" button
    3. Tap "Scan Now" -> should see location picker (Fridge/Pantry/Freezer)
    4. Select a location and tap "Take Photo" -> camera should open
    5. Take a photo of some food items (or use a test image from simulator)
    6. Wait for AI analysis -> should see loading state "Analyzing your fridge..."
    7. Review screen should show detected items with confidence scores
    8. Try editing an item name, rejecting an item (uncheck), adding a missed item
    9. Tap "Confirm" -> should navigate to Pantry tab with items now visible
    10. Items should be grouped by category with source location indicators
    11. Try marking an item as "Used" -> should disappear from available list
    12. Do a second scan of same location -> existing items should update (reconciliation)
    13. Note: Confidence decay (7+ days) cannot be tested in real-time; verified by unit tests
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors across the project
- All existing tests pass (no regressions)
- Scan flow navigates correctly: location -> camera -> review -> confirm -> pantry tab
- Pantry tab displays items grouped by category
- Empty state shows when no items exist
</verification>

<success_criteria>
User can photograph their kitchen, review AI-detected items, confirm them into persistent inventory, view their pantry organized by category, and mark items as used/depleted. Stale items are visually distinguished. The complete PANT-01 through PANT-08 user experience is functional.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pantry-scanning/03-04-SUMMARY.md`
</output>
