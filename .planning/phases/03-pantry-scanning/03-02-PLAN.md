---
phase: 03-pantry-scanning
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/server/src/services/vision.ts
  - packages/server/src/services/pantry.ts
  - packages/server/src/routes/pantry.ts
  - packages/server/src/services/__tests__/vision.test.ts
  - packages/server/src/services/__tests__/pantry.test.ts
autonomous: true
requirements:
  - PANT-01
  - PANT-02
  - PANT-03
  - PANT-04
  - PANT-06

must_haves:
  truths:
    - "Claude Vision API is called with base64 image and tool_use schema, returning structured food items"
    - "Vision service handles all three source locations (fridge, pantry, freezer)"
    - "Reconciliation upserts items by normalized_name + source_location, never auto-deletes"
    - "Pantry routes handle GET (list), POST /scan (vision), POST /confirm (reconcile), PATCH /:id (update)"
  artifacts:
    - path: "packages/server/src/services/vision.ts"
      provides: "identifyFoodItems function calling Claude Vision"
      exports: ["identifyFoodItems"]
    - path: "packages/server/src/services/pantry.ts"
      provides: "reconcileItems function for inventory upsert"
      exports: ["reconcileItems"]
    - path: "packages/server/src/routes/pantry.ts"
      provides: "Full pantry API routes"
      exports: ["default"]
    - path: "packages/server/src/services/__tests__/vision.test.ts"
      provides: "Tests for vision service with mocked Anthropic SDK"
    - path: "packages/server/src/services/__tests__/pantry.test.ts"
      provides: "Tests for reconciliation logic with mocked Supabase"
  key_links:
    - from: "packages/server/src/routes/pantry.ts"
      to: "packages/server/src/services/vision.ts"
      via: "POST /scan calls identifyFoodItems"
      pattern: "identifyFoodItems"
    - from: "packages/server/src/routes/pantry.ts"
      to: "packages/server/src/services/pantry.ts"
      via: "POST /confirm calls reconcileItems"
      pattern: "reconcileItems"
    - from: "packages/server/src/services/vision.ts"
      to: "packages/server/src/config/anthropic.ts"
      via: "uses Anthropic client singleton"
      pattern: "import.*anthropic"
---

<objective>
Build the backend vision service (Claude API integration) and pantry reconciliation service using TDD, then wire them into pantry API routes.

Purpose: The backend is the intelligence layer -- it receives photos, identifies food via Claude Vision, and manages inventory reconciliation. This is the core AI feature of the app.
Output: Tested vision service, tested reconciliation service, working pantry API routes.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pantry-scanning/03-RESEARCH.md
@.planning/phases/03-pantry-scanning/03-01-SUMMARY.md

@packages/server/src/config/env.ts
@packages/server/src/config/anthropic.ts
@packages/server/src/routes/pantry.ts
@packages/server/src/middleware/auth.ts
@packages/server/vitest.config.ts
</context>

<interfaces>
<!-- Types created in Plan 01 that this plan depends on -->

From apps/mobile/src/types/pantry.ts:
```typescript
export type FoodCategory = 'produce' | 'dairy' | 'protein' | 'grain' | 'condiment' | 'beverage' | 'frozen' | 'snack' | 'other';
export type SourceLocation = 'fridge' | 'pantry' | 'freezer';
export type PantryItemStatus = 'available' | 'used' | 'depleted';
export interface ScanResult { name: string; quantity: number; unit: string; confidence: number; category: FoodCategory; }
```

From packages/server/src/config/anthropic.ts:
```typescript
export const anthropic: Anthropic; // Singleton client
```
</interfaces>

<feature>
  <name>Vision Service - Claude food item identification</name>
  <files>packages/server/src/services/vision.ts, packages/server/src/services/__tests__/vision.test.ts</files>
  <behavior>
    - identifyFoodItems(base64Image, 'fridge') returns array of ScanResult objects
    - Each ScanResult has name (string), quantity (number), unit (string), confidence (0-1), category (FoodCategory)
    - Calls Claude with tool_use and tool_choice forced to 'report_food_items'
    - Returns empty array if Claude returns no tool_use block
    - Prompt includes the source_location to give Claude context about what container is being scanned
    - Model used: 'claude-sonnet-4-20250514'
  </behavior>
  <implementation>
    Mock @anthropic-ai/sdk in tests (vi.mock). Test that the correct message structure is sent to Claude (image content block + text prompt). Test that tool_use response is parsed into ScanResult[]. Test the empty/error cases.
    
    Implementation follows the exact pattern from 03-RESEARCH.md Pattern 2. The tool schema defines report_food_items with items array. tool_choice forces the tool call. Parse the tool_use content block from the response.
  </implementation>
</feature>

<feature>
  <name>Pantry Reconciliation Service</name>
  <files>packages/server/src/services/pantry.ts, packages/server/src/services/__tests__/pantry.test.ts</files>
  <behavior>
    - reconcileItems(supabaseClient, profileId, confirmedItems, sourceLocation) upserts items into pantry_items
    - For each confirmed item: normalize name (lowercase, trim), check if exists by (profile_id, normalized_name, source_location)
    - If exists: update quantity, confidence, last_seen_at, updated_at
    - If new: insert with all fields including created_at
    - NEVER deletes items missing from a scan (scans are additive only)
    - Returns the full list of upserted items
  </behavior>
  <implementation>
    Mock Supabase client in tests. Test upsert logic: new item inserts, existing item updates last_seen_at. Test that items NOT in scan remain untouched. Test name normalization.
    
    Use Supabase upsert with onConflict on a unique constraint, OR implement as select-then-insert/update logic. The lookup index (profile_id, normalized_name, source_location) supports efficient matching. Consider adding a UNIQUE constraint in the migration or using application-level check.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vision service with TDD (RED -> GREEN)</name>
  <files>packages/server/src/services/__tests__/vision.test.ts, packages/server/src/services/vision.ts</files>
  <behavior>
    - Test 1: identifyFoodItems sends correct message structure to Claude (image block + text with source_location)
    - Test 2: identifyFoodItems parses tool_use response into ScanResult array
    - Test 3: identifyFoodItems returns empty array when no tool_use block in response
    - Test 4: identifyFoodItems uses claude-sonnet-4-20250514 model
    - Test 5: identifyFoodItems works for all three source locations (fridge, pantry, freezer)
  </behavior>
  <action>
RED: Create packages/server/src/services/__tests__/vision.test.ts. Mock @anthropic-ai/sdk using vi.mock. The mock should capture the arguments passed to messages.create and return a configurable response. Write tests for all 5 behaviors above. Run tests -- they must fail.

GREEN: Create packages/server/src/services/vision.ts implementing identifyFoodItems(base64Image: string, sourceLocation: 'fridge' | 'pantry' | 'freezer'): Promise<ScanResult[]>.
- Import anthropic from '../config/anthropic.js'
- Define the report_food_items tool schema with items array (name, quantity, unit, confidence, category -- all required)
- Call anthropic.messages.create with model 'claude-sonnet-4-20250514', max_tokens 4096, tools array, tool_choice { type: 'tool', name: 'report_food_items' }
- Message content: image block (type 'image', source base64, media_type 'image/jpeg') + text block with prompt mentioning sourceLocation
- Parse response: find content block with type 'tool_use', extract input.items
- Return empty array if no tool_use block found
- Define the ScanResult type locally in this file (or re-export from a shared types file) since server doesn't share types with mobile

Run tests -- they must pass.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/packages/server && pnpm vitest run src/services/__tests__/vision.test.ts -x</automated>
  </verify>
  <done>Vision service tested and implemented. Claude Vision called with correct tool_use schema. ScanResult[] returned from parsed response. All 5 test cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconciliation service + pantry routes with TDD (RED -> GREEN)</name>
  <files>packages/server/src/services/__tests__/pantry.test.ts, packages/server/src/services/pantry.ts, packages/server/src/routes/pantry.ts</files>
  <behavior>
    - Test 1: reconcileItems inserts new items with normalized name (lowercase, trimmed)
    - Test 2: reconcileItems updates existing items (quantity, confidence, last_seen_at) without deleting missing ones
    - Test 3: reconcileItems does not touch items not present in the confirmed scan
    - Test 4: normalizeName handles various inputs correctly ("Cheddar Cheese " -> "cheddar cheese")
  </behavior>
  <action>
RED: Create packages/server/src/services/__tests__/pantry.test.ts. Mock Supabase client. Write tests for reconciliation behaviors. Run tests -- must fail.

GREEN: Create packages/server/src/services/pantry.ts with:
- `normalizeName(name: string): string` -- lowercase, trim whitespace
- `reconcileItems(supabase, profileId, items: ConfirmedItem[], sourceLocation)`:
  - For each item, compute normalized_name
  - Query existing items by (profile_id, normalized_name, source_location)
  - If found: update quantity, confidence, last_seen_at = NOW(), status = 'available' (re-seeing it means it's back)
  - If not found: insert new row with all fields
  - Return array of all upserted items

Then expand packages/server/src/routes/pantry.ts (replace the existing stub) with these routes:
- GET / -- List pantry items for authenticated user, filter by status='available' by default, support ?location= query param, ?include_used=true for showing all. Order by category ascending.
- POST /scan -- Accept { image: string (base64), source_location: 'fridge'|'pantry'|'freezer' }. Call identifyFoodItems from vision service. Return { data: ScanResult[] }.
- POST /confirm -- Accept { items: ConfirmedItem[], source_location: string }. Call reconcileItems. Return { data: PantryItem[] }.
- PATCH /:id -- Accept partial updates { status?, quantity?, name? }. Update the pantry_item. Return { data: PantryItem }.

All routes use authMiddleware (already applied). Get supabase client and user from Hono context (c.get('supabase'), c.get('user')) following existing route patterns.

Run tests -- must pass.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/packages/server && pnpm vitest run src/services/__tests__/pantry.test.ts -x && pnpm vitest run -x</automated>
  </verify>
  <done>Reconciliation service tested with 4 test cases. Pantry routes fully implemented: GET /, POST /scan, POST /confirm, PATCH /:id. Scans are additive-only (no auto-delete). All server tests pass.</done>
</task>

</tasks>

<verification>
- `cd packages/server && pnpm vitest run -x` -- all tests pass including new vision + pantry tests
- Vision service correctly calls Claude with tool_use schema
- Reconciliation never deletes items (verified by test 3)
- Routes follow existing Hono patterns with authMiddleware
</verification>

<success_criteria>
Backend can receive a base64 image, call Claude Vision to identify food items, return structured results, and reconcile confirmed items into the database. All logic is tested.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pantry-scanning/03-02-SUMMARY.md`
</output>
