---
phase: 17-something-new-ai-powered-recipe-exploration-with-search-and-remix
plan: 04
type: execute
wave: 4
depends_on:
  - "17-01"
  - "17-02"
  - "17-03"
files_modified:
  - apps/mobile/.maestro/27-something-new-search.yaml
  - apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
autonomous: false
requirements:
  - P17-01
  - P17-02
  - P17-03
  - P17-05
must_haves:
  truths:
    - "A new Maestro flow `27-something-new-search.yaml` exercises the full happy-path: open Kitchen → Something New segment → tap pill → type query → submit → see results → tap card → see preview → tap Remix → Remix Sheet visible"
    - "Existing flow `20-kitchen-segment-toggle.yaml` assertions are rebased from `Suggestions` → `Something New` (line ~77)"
    - "All other Maestro flows that reference the Kitchen/Suggestions segment continue to pass without change"
    - "UAT checkpoint: Patrick verifies on iOS Simulator that the feel is correct (loading state, chip scroll, pantry toggle behavior, Remix flow, overflow menu)"
  artifacts:
    - path: "apps/mobile/.maestro/27-something-new-search.yaml"
      provides: "Phase 17 happy-path UAT flow"
      contains: "Something New"
    - path: "apps/mobile/.maestro/20-kitchen-segment-toggle.yaml"
      provides: "Rebased segment-toggle assertion for new label"
      contains: "Something New"
  key_links:
    - from: "27-something-new-search.yaml"
      to: "iOS Simulator dev client (com.dinnertime.app)"
      via: "Maestro test runner + `_ensure-logged-in.yaml` prereq"
      pattern: "launchApp"
    - from: "Maestro full suite"
      to: "Phase 17 feature"
      via: ".maestro/scripts/uat.sh all"
      pattern: "uat.sh"
---

<objective>
Land the end-to-end UAT for Phase 17: a new Maestro flow that exercises the full Something New search → preview → Remix journey, plus a one-line rebase of the existing segment-toggle flow. Then gate on a human simulator run so Patrick signs off on the feel (loading timing, chip scroll behavior, overflow menu presentation) before the phase is called done.

Purpose: P17-05 e2e verification + P17-01 regression coverage. This is the Nyquist Wave 4 close-out — every failing Wave 0 Maestro stub becomes a passing flow.

Output: One new Maestro YAML flow, one-line edit to an existing flow, a human-verified UAT checkpoint, and a green `uat.sh all` run.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-something-new-ai-powered-recipe-exploration-with-search-and-remix/17-CONTEXT.md
@.planning/phases/17-something-new-ai-powered-recipe-exploration-with-search-and-remix/17-VALIDATION.md
@CLAUDE.md

# Existing Maestro patterns — copy conventions from these
@apps/mobile/.maestro/06-recipe-discover.yaml
@apps/mobile/.maestro/08-home-suggestions.yaml
@apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
@apps/mobile/.maestro/18-recipe-search-favorite.yaml
@apps/mobile/.maestro/_ensure-logged-in.yaml
@apps/mobile/.maestro/README.md
@apps/mobile/.maestro/scripts/uat.sh

<interfaces>
Maestro selector gotchas (from CLAUDE.md):
- Text matchers are regex; avoid `=`, `(`, `?` in asserted text.
- Prefer plain UI labels: `"Something New"`, `"Save to Library"`, `"Remix"`.
- Accessibility labels are matchable via `id:` only sometimes; usually fall back to visible text.
- Take screenshots liberally — Claude can `Read` PNG files for debug.

Dev environment (from CLAUDE.md):
- Server: `set -a && source .env && set +a && cd packages/server && pnpm dev` (port 3000).
- Metro: `cd apps/mobile && npx expo start --dev-client --lan` (NOT --tunnel for simulator).
- After .env changes: `--clear` Metro cache.
- Simulator: `xcrun simctl boot "iPhone 17 Pro"` + `open -a Simulator`.
- UAT helper: `apps/mobile/.maestro/scripts/uat.sh {boot|smoke|all|shot|log|reset}`.

Required preflight (from README.md):
- `_ensure-logged-in.yaml` handles the dev-menu dismiss + session check.
- Every new flow starts with `runFlow: _ensure-logged-in.yaml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebase 20-kitchen-segment-toggle.yaml for new label</name>
  <files>
    apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
  </files>
  <action>
    Locate the line that asserts the Suggestions label (approximately line 77 per 17-VALIDATION.md) and rebase:

    - Before: `text: ".*Suggestions.*"`
    - After: `text: ".*Something New.*"` (or plain `text: "Something New"` if the match was literal)

    If the flow references "Suggestions" in other lines — tap targets, assertions, screenshot labels — rebase each occurrence consistently. Grep the file first:

    ```bash
    grep -n -i "suggestions" apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
    ```

    Every "Suggestions" in visible-text context becomes "Something New". "Suggestions" in filenames, comments referencing the old name, or screenshot filenames can be updated for clarity but are non-blocking.

    Do NOT change the Zustand-internal segment key (`suggestions`). Only user-visible labels and their Maestro assertions.
  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; ./.maestro/scripts/uat.sh flow 20-kitchen-segment-toggle</automated>
    Expected: flow 20 passes against the post-Plan-03 build.

    **Pre-reqs** (assume dev already started the stack before running; if not, the flow will fail at connection step):
    - Backend running on port 3000
    - Metro running with `--lan --clear`
    - Simulator booted with dev client installed
    - User logged in (handled by `_ensure-logged-in.yaml` prereq)
  </verify>
  <done>
    - `20-kitchen-segment-toggle.yaml` passes green against the new label.
    - `grep -i "Suggestions" apps/mobile/.maestro/20-kitchen-segment-toggle.yaml` returns zero visible-text matches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 27-something-new-search.yaml happy-path flow</name>
  <files>
    apps/mobile/.maestro/27-something-new-search.yaml
  </files>
  <action>
    Create a new Maestro flow exercising the Phase 17 happy path:

    ```yaml
    appId: com.dinnertime.app
    name: 27-something-new-search
    tags:
      - phase-17
      - kitchen
      - search
    ---
    # Preflight — dev-menu dismiss + session bootstrap
    - runFlow: _ensure-logged-in.yaml

    # Land on Kitchen tab (leftmost in 4-tab bar after Phase 12)
    - tapOn:
        text: "Kitchen"

    # Switch to Something New segment (label change from Phase 17)
    - tapOn:
        text: "Something New"
    - assertVisible: "Something New"
    - takeScreenshot: "27-01-segment-landing"

    # Tap sticky search pill → opens /search modal with context=something-new
    # (Use a stable selector — pill placeholder or id if available)
    - tapOn:
        id: "sticky-search-pill"
        # Fallback if id doesn't exist:
        # text: "Search dinner ideas"
    - assertVisible: "What are you craving"
    - takeScreenshot: "27-02-search-modal"

    # Type a query
    - inputText: "quick pasta"

    # Toggle "Only what's in my pantry" — pantry-only path
    - tapOn:
        text: "Only what's in my pantry"

    # Submit — dismisses modal, segment shows loading then results
    - tapOn:
        text: "Search"
    # Wait for network + AI response (Plan 01's /recipes/search)
    - assertNotVisible:
        text: "What are you craving"
        timeout: 3000
    - takeScreenshot: "27-03-loading-or-results"

    # Wait for results grid to render (allow up to 30s for AI cold-start)
    - extendedWaitUntil:
        visible:
          id: "something-new-results"
        timeout: 30000
    - takeScreenshot: "27-04-results"

    # Tap the first result card (use id or nth-match)
    - tapOn:
        id: "something-new-card-0"
    - assertVisible: "Save to Library"
    - assertVisible: "Remix"
    - takeScreenshot: "27-05-preview-sheet"

    # Tap Remix — opens RemixSheet
    - tapOn:
        text: "Remix"
    - assertVisible:
        text: "Make it quicker"
        # RemixSheet has 4 mode options; "Make it quicker" is one.
    - takeScreenshot: "27-06-remix-sheet"

    # Close Remix, back to preview, then save
    - swipe:
        direction: DOWN
    - assertVisible: "Save to Library"
    - tapOn:
        text: "Save to Library"
    - extendedWaitUntil:
        visible: "Saved to library"
        timeout: 15000
    - takeScreenshot: "27-07-saved"

    # Close preview, verify results still on segment (persistence works mid-session)
    - tapOn:
        text: "Done"
    - assertVisible: "Something New"
    - takeScreenshot: "27-08-back-to-segment"
    ```

    **testID requirements:** The flow references three testIDs that Plan 03 should have added:
    - `testID="sticky-search-pill"` on `StickySearchPill` on the Something New segment (may need to pass via prop — confirm Plan 03 did this; if not, edit Plan 03's components during this plan or use the fallback text selector).
    - `testID="something-new-results"` on the `SomethingNewResults` outer View.
    - `testID="something-new-card-0"` on the first result card in the grid.

    If Plan 03 did not add these testIDs, add them as part of Task 2 via minimal edits (but prefer text-based selectors where stable to avoid over-coupling).

    **Selectors to avoid:** The SearchBar contains a `:` or `?` in placeholder text — use plain substrings only. `"What are you craving"` (without `?`) is safe for the default placeholder `"What are you craving?"`.

    **Timing:**
    - AI search takes 5-20s cold-start; use `extendedWaitUntil: timeout: 30000`.
    - Save to library takes 1-3s; `timeout: 15000`.
    - Modal dismissal is immediate; `timeout: 3000` is sufficient.

    **Screenshots:** take liberally at every major step. They are free debugging gold.

    **Known flakes** (per 17-VALIDATION.md):
    - Dev-client early death under 15s — flow 27 should be retried on any failure at step 1 or 2.
    - Flow 22 (dirty-form-guard) is not a Phase 17 gate; ignore flakes there.
  </action>
  <verify>
    <automated>cd apps/mobile &amp;&amp; ./.maestro/scripts/uat.sh flow 27-something-new-search</automated>
    Expected: flow passes green on iOS Simulator. If the flow fails, `Read` the screenshots in `apps/mobile/.maestro/screenshots/` to diagnose.

    **Pre-reqs:**
    - Backend live on port 3000 with the Plan 01 `/recipes/search` route
    - User has a non-empty pantry (prime with a few items or the pantryOnly path will return zero recipes)
    - At least one prior household_member preferences set (guarantees the AI prompt is meaningful)
  </verify>
  <done>
    - `27-something-new-search.yaml` exists in `.maestro/`.
    - Flow passes end-to-end against the live simulator.
    - All 8 screenshot steps captured in the flow's screenshot directory.
    - Flow is tagged `phase-17` for filtering.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT on iOS Simulator</name>
  <files>N/A</files>
  <action>
    This is a human-verification checkpoint, not an automated task. Claude's role: surface the verification checklist below (same content as `<how-to-verify>`) and wait for the user's "approved" or a description of issues. Do NOT auto-approve. Do NOT edit source files during this task.

    See `<what-built>` for the feature summary and `<how-to-verify>` for the step-by-step verification flow.
  </action>
  <verify>
    <automated>echo "Human-verify checkpoint — no automated verification. See how-to-verify block."</automated>
    Manual verification only. Claude displays the checklist from `<how-to-verify>`; user ticks each box and types "approved" to resume execution.
  </verify>
  <done>
    User has typed "approved" (or equivalent sign-off) AFTER confirming all 9 verification sections pass (or documenting known flakes per 17-VALIDATION.md). Claude does NOT proceed to commit/summary until this signal arrives.
  </done>
  <what-built>
    Phase 17 "Something New" feature complete:
    - Segment renamed from Suggestions to Something New (cosmetic, Zustand key unchanged)
    - StickySearchPill → /search modal with context=something-new → TextInput + pantry Switch + Submit
    - Submit fires POST /api/v1/recipes/search (server-enforced pantry filter) → results populate segment
    - Persisted `searchResults`, `recentQueries`, `lastQuery`, `pantryOnly` via Zustand persist middleware
    - Recent-query chips render horizontally when history present; tap replays search
    - Result card tap → PreviewSheet with Save to Library + Remix buttons
    - Remix button opens RemixSheet with inline source kind (no intermediate save required)
    - Sparkles FAB replaced with HeaderEllipsis overflow menu (Regenerate from pantry + Clear search history, destructive)
    - First-time users see a "Get dinner ideas from my pantry" button below hint copy (D-08)
    - Legacy autoFetch + pantry-grounded SuggestionList fallback preserved (D-10)
  </what-built>
  <how-to-verify>
    **Start the dev stack:**
    ```bash
    # Terminal 1 — backend
    set -a && source .env && set +a && cd packages/server && pnpm dev
    # Expect: listening on 3000

    # Terminal 2 — metro
    cd apps/mobile && npx expo start --dev-client --lan --clear

    # Terminal 3 — simulator
    xcrun simctl boot "iPhone 17 Pro" || true
    open -a Simulator
    xcrun simctl install booted apps/mobile/ios/build/Build/Products/Debug-iphonesimulator/DinnerTime.app
    xcrun simctl launch booted com.dinnertime.app
    ```

    **Verify each behavior (tick each before approving):**

    1. **Segment rename (P17-01)**
       - Open Kitchen tab. Observe segment control reads "Something New | Recipe Box".
       - [ ] Label reads "Something New" (not "Suggestions").
       - [ ] Tapping it visually switches the active segment.

    2. **Empty first-time state (P17-02, D-08)**
       - Only relevant if your test account has no search history. If you've searched before, the persisted state will show. To reset: force-quit app → delete and reinstall, or clear AsyncStorage via dev-menu reset.
       - [ ] Search pill visible at top.
       - [ ] Hint copy "Discover new dinner ideas" + "Get dinner ideas from my pantry" button OR SuggestionList fallback is reachable.

    3. **Search happy path (P17-03)**
       - Tap the sticky search pill.
       - [ ] Modal opens with auto-focused text input + placeholder "What are you craving?".
       - [ ] Pantry toggle visible below input.
       - Type "quick weeknight pasta" → toggle pantry ON → tap Search.
       - [ ] Modal dismisses immediately (dismiss-first per D-09).
       - [ ] Segment shows loading skeleton.
       - [ ] Results cards appear within 20s.
       - [ ] Recent-query chip "quick weeknight pasta" appears at the top of the segment (horizontal scroll per D-11).

    4. **Pantry filter realism (P17-04)**
       - With pantry toggle ON, verify returned recipes look feasible with current pantry. (This is a subjective manual check — the server prompt embeds the pantry manifest.)
       - [ ] No recipe requires exotic ingredients you don't have.
       - [ ] Recipes feel relevant, not generic weeknight ideas.

    5. **Preview + Remix + Save (P17-05)**
       - Tap any result card.
       - [ ] PreviewSheet opens showing title, ingredients, steps.
       - [ ] Both "Save to Library" and "Remix" buttons visible at the bottom.
       - Tap "Remix".
       - [ ] RemixSheet opens with 4 mode options (Surprise me, Swap protein, Swap veggies, Make it quicker).
       - Tap "Make it quicker" → wait for variation → verify recipe updates.
       - Dismiss Remix → tap "Save to Library".
       - [ ] "Saved to library" confirmation renders.
       - Go to Recipe Box segment, filter by AI-generated if possible, verify recipe is there.

    6. **Overflow menu (P17-06)**
       - On Something New segment, verify NO sparkles FAB floats in the bottom-right.
       - Tap the ellipsis in the header action slot.
       - [ ] ActionSheetIOS shows "Regenerate from pantry" + "Clear search history" (red/destructive).
       - Tap "Regenerate from pantry" → confirm a new search fires with pantry-only constraint.
       - Open menu again → tap "Clear search history" → confirm recentQueries disappear, searchResults clear.

    7. **Persistence (P17-02)**
       - Perform one search, let results land.
       - Navigate to Pantry tab, then back to Kitchen > Something New.
       - [ ] Results still visible (no refetch).
       - Force-quit and relaunch the app.
       - [ ] Results + recent chips still there (AsyncStorage persist working).

    8. **autoFetch preservation (D-10, Pitfall 1)**
       - Go through a pantry scan → review → save. On Kitchen landing, SuggestionList should auto-fire (legacy Phase 4 behavior).
       - [ ] Auto-suggest fires only when triggered by a scan-flow redirect, NOT on every cold launch.

    9. **Existing flows regression**
       ```bash
       cd apps/mobile && ./.maestro/scripts/uat.sh all
       ```
       - [ ] `20-kitchen-segment-toggle.yaml` green.
       - [ ] `27-something-new-search.yaml` green.
       - [ ] `06-recipe-discover.yaml` green (legacy Discover deep link unchanged).
       - [ ] `08-home-suggestions.yaml` green (pantry-grounded Phase 4 flow still works).
       - Flow 22 (dirty-form-guard) is an acceptable flake per 17-VALIDATION.md.

    If any box is unchecked, note the failure and describe what you see. Do NOT approve until all are ticked or known flakes documented.
  </how-to-verify>
  <resume-signal>Type "approved" to sign off, or describe any issues found. If issues: reply with screenshots or dev-tools observations so Claude can diagnose.</resume-signal>
</task>

</tasks>

<verification>
Full-stack verification:

```bash
# Unit + integration
cd apps/mobile && pnpm test -- --run && pnpm tsc --noEmit
cd packages/server && pnpm test -- --run

# E2E
cd apps/mobile && ./.maestro/scripts/uat.sh all
```

Expected: everything green (except the documented flow-22 flake). `pnpm -r test` passes cleanly across both workspaces.

**Final Dimension 8 Nyquist check** (from 17-VALIDATION.md):
- [ ] Every functional requirement (P17-01 through P17-06) has ≥1 automated test green.
- [ ] Every `must_have` in PLAN files traces back to a test.
- [ ] Wave 0 red stubs are now all green.
- [ ] Full suite latency under 15 minutes end-to-end (observed).
- [ ] Manual-only verifications (pantry realism, 3G feel, chip scroll on small devices, FAB aesthetic) explicitly documented in this checkpoint, not hidden.
</verification>

<success_criteria>
- `27-something-new-search.yaml` is a new file that passes against the simulator.
- `20-kitchen-segment-toggle.yaml` is rebased and passes.
- Human UAT checkpoint approved with all 9 behavior sections verified.
- Full Maestro suite passes (minus acceptable flakes).
- Full unit/integration suites pass across both workspaces.
- No regressions on adjacent Maestro flows (especially flow 06 — recipe-discover).
</success_criteria>

<output>
After completion, create `.planning/phases/17-something-new-ai-powered-recipe-exploration-with-search-and-remix/17-04-SUMMARY.md` documenting:
- Final Maestro flow pass/fail summary (`uat.sh all` output)
- Any testIDs added during Task 2 that retroactively edited Plan 03 components
- Screenshots captured (paths) for Patrick's reference
- Any subjective findings from the human UAT (3G feel, pantry realism, etc.)
- Open items deferred to future phases (if any emerged during UAT)
</output>
