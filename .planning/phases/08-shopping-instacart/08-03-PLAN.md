---
phase: 08-shopping-instacart
plan: 03
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/server/src/services/ingredientCategories.ts
  - packages/server/src/services/__tests__/ingredientCategories.test.ts
autonomous: true
requirements: [SHOP-03]
must_haves:
  truths:
    - "classifyStatic returns correct category for common ingredients (tomato→produce, milk→dairy, chicken→protein)"
    - "classifyStatic falls through tokens (e.g., 'ground beef' → 'beef' → protein)"
    - "classifyStatic returns null for unknown items"
    - "classifyBatchWithHaiku resolves unknowns via Claude tool-use with strict GroceryCategory enum"
    - "classifyItems hybrid function returns a category for every input (static first, Haiku fallback)"
  artifacts:
    - path: packages/server/src/services/ingredientCategories.ts
      provides: "STATIC_MAP, classifyStatic, classifyBatchWithHaiku, classifyItems"
      exports: ["STATIC_MAP", "classifyStatic", "classifyBatchWithHaiku", "classifyItems"]
  key_links:
    - from: packages/server/src/services/ingredientCategories.ts
      to: packages/server/src/types/shopping.ts
      via: "imports GroceryCategory"
      pattern: "GroceryCategory"
---

<objective>
Build hybrid ingredient category classification: a static map of ~150 common ingredients for zero-cost O(1) lookup, with Claude Haiku fallback for unknowns, composed into a single `classifyItems` entry point that routes normalize their batch through.

Purpose: SHOP-03 requires categorized shopping lists. Static map handles ~80% of cases; Haiku handles the rest. Per-item classification is persisted on `shopping_list_items.category` so this runs ONCE per list (research Pitfall 5).
Output: Service file + test file.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-shopping-instacart/08-RESEARCH.md
@.planning/phases/08-shopping-instacart/08-01-SUMMARY.md
@packages/server/src/types/shopping.ts
@packages/server/src/services/ingredientMatching.ts

<interfaces>
From Plan 01:
```typescript
export type GroceryCategory =
  | 'produce' | 'dairy' | 'protein' | 'pantry' | 'bakery'
  | 'frozen' | 'beverages' | 'condiments' | 'spices' | 'other';
```

Existing: `normalizeIngredientName(name)` lowercases and strips plural suffixes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD STATIC_MAP + classifyStatic</name>
  <files>packages/server/src/services/ingredientCategories.ts, packages/server/src/services/__tests__/ingredientCategories.test.ts</files>
  <behavior>
    - classifyStatic('tomato') → 'produce'
    - classifyStatic('milk') → 'dairy'
    - classifyStatic('chicken') → 'protein'
    - classifyStatic('ground beef') → 'protein' (token fallback on 'beef')
    - classifyStatic('unicorn meat') → null
    - classifyStatic is case-insensitive because caller passes normalizedName
  </behavior>
  <action>
Create `STATIC_MAP: Record<string, GroceryCategory>` covering at least 150 common ingredients across all 10 categories. Minimum coverage per category:

- produce: tomato, onion, garlic, lettuce, spinach, carrot, potato, pepper, cucumber, broccoli, lemon, lime, apple, banana, avocado (+ ~15 more)
- dairy: milk, butter, cheese, yogurt, cream, sour cream, cream cheese, parmesan, mozzarella, cheddar (+ ~5 more)
- protein: chicken, beef, pork, tofu, egg, shrimp, salmon, tuna, turkey, bacon, sausage, lamb (+ ~5 more)
- pantry: rice, flour, pasta, bean, lentil, oil, vinegar, sugar, salt, soy sauce, oats, quinoa, stock, broth (+ ~5 more)
- bakery: bread, bagel, tortilla, bun, croissant, pita, baguette
- frozen: ice cream, frozen peas, frozen corn, frozen berries
- beverages: juice, soda, coffee, tea, wine, beer, sparkling water
- condiments: ketchup, mustard, mayo, mayonnaise, hot sauce, ranch, bbq sauce, sriracha, pesto, jam
- spices: cumin, paprika, pepper (black), oregano, basil, thyme, cinnamon, chili powder, turmeric, ginger

Implement `classifyStatic(normName: string): GroceryCategory | null` per research Pattern 3:
1. Full lookup in STATIC_MAP
2. If miss, split by space and try each token
3. Return null on all-miss

Write tests FIRST covering happy path, token fallback, and null miss.

Avoid: Do NOT include every ingredient — the Haiku fallback exists precisely for long-tail items. ~150 entries is the target sweet spot.
  </action>
  <verify>
    <automated>pnpm -C packages/server test ingredientCategories.test.ts -- --run</automated>
  </verify>
  <done>Static classification tests pass; STATIC_MAP has ≥150 entries across all 10 categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD classifyBatchWithHaiku + classifyItems hybrid</name>
  <files>packages/server/src/services/ingredientCategories.ts, packages/server/src/services/__tests__/ingredientCategories.test.ts</files>
  <behavior>
    - classifyBatchWithHaiku(['dragon fruit', 'kimchi']): mocked Haiku returns {'dragon fruit':'produce', 'kimchi':'condiments'} → function returns that map
    - classifyBatchWithHaiku uses claude-haiku-4-latest with forced tool_choice on 'classify_ingredients' tool
    - classifyBatchWithHaiku with empty input returns empty map without calling Claude
    - classifyItems: for mix of known + unknown, static handles knowns, Haiku only called with unknowns, result merged
    - classifyItems: when all items are known statically, Claude is NOT invoked (mock.calls.length === 0)
  </behavior>
  <action>
Extend tests with vi.hoisted mock of the anthropic client.

Implement `classifyBatchWithHaiku(unknownItems: string[]): Promise<Record<string, GroceryCategory>>`:
- Early return `{}` if input is empty
- Single Claude Haiku call with tool `classify_ingredients`:
  ```
  input_schema: {
    type: 'object',
    properties: {
      classifications: {
        type: 'array',
        items: {
          type: 'object',
          properties: {
            name: { type: 'string' },
            category: { type: 'string', enum: ['produce','dairy','protein','pantry','bakery','frozen','beverages','condiments','spices','other'] }
          },
          required: ['name','category']
        }
      }
    },
    required: ['classifications']
  }
  ```
- Parse tool_use block → convert array to Record<name, category>

Implement `classifyItems(items: Array<{normalizedName: string}>): Promise<Record<string, GroceryCategory>>`:
1. Walk items, use classifyStatic for each; collect unknowns
2. Call classifyBatchWithHaiku(unknowns) only if unknowns.length > 0
3. Merge and return full map keyed by normalizedName (unknowns absent from AI response default to 'other')

Avoid: Do NOT use freeform text output — the enum constraint in tool schema is the Pitfall 5 mitigation. Do NOT cache to an external table in this plan (deferred).
  </action>
  <verify>
    <automated>pnpm -C packages/server test ingredientCategories.test.ts -- --run</automated>
  </verify>
  <done>Hybrid classification tests pass; static-only path does not invoke Claude mock.</done>
</task>

</tasks>

<verification>
- All categorization tests pass
- Enum constraint present in Haiku tool schema
- Zero-unknown path skips Claude call
</verification>

<success_criteria>
SHOP-03 classification logic exists, is tested, and is ready for the route layer to call on list generation.
</success_criteria>

<output>
Create `.planning/phases/08-shopping-instacart/08-03-SUMMARY.md`
</output>
