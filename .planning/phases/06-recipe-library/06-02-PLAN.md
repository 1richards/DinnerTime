---
phase: 06-recipe-library
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/server/src/services/recipeStore.ts
  - packages/server/src/routes/recipes.ts
  - packages/server/src/services/__tests__/recipeStore.test.ts
  - packages/server/src/routes/__tests__/recipes.patch.test.ts
  - packages/server/src/routes/__tests__/recipes.delete.test.ts
requirements: [RECP-06, RECP-07, RECP-08]
autonomous: true
must_haves:
  truths:
    - "Server PATCH /api/v1/recipes/:id updates whitelisted fields and returns updated row"
    - "Server DELETE /api/v1/recipes/:id removes the row and returns 204"
    - "GET /api/v1/recipes accepts ?q= (keyword) and ?favorites=true query params"
    - "ILIKE search escapes user-supplied % and _ wildcards"
    - "PATCH only allows whitelisted fields; unknown fields are ignored"
  artifacts:
    - path: packages/server/src/routes/recipes.ts
      provides: "PATCH /:id, DELETE /:id, extended GET / with q & favorites params"
      contains: "recipes.patch"
    - path: packages/server/src/services/recipeStore.ts
      provides: "updateRecipe, deleteRecipe, getRecipes (with search/favorites)"
      contains: "export async function updateRecipe"
  key_links:
    - from: packages/server/src/routes/recipes.ts
      to: packages/server/src/services/recipeStore.ts
      via: updateRecipe / deleteRecipe import
      pattern: "updateRecipe|deleteRecipe"
    - from: packages/server/src/services/recipeStore.ts
      to: Supabase recipes table
      via: .update / .delete / .ilike
      pattern: "\\.ilike|\\.update|\\.delete"
---

<objective>
Add server-side CRUD completion (PATCH, DELETE), keyword search, and favorites filtering to the recipes API. Everything is TDD: tests first, then implementation.

Purpose: Unblocks RECP-06 (view/edit/delete), RECP-07 (search), RECP-08 (favorite filter) for the mobile client.
Output: Three new/updated server endpoints with test coverage.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-recipe-library/06-RESEARCH.md
@.planning/phases/06-recipe-library/06-01-SUMMARY.md
@packages/server/src/routes/recipes.ts
@packages/server/src/services/recipeStore.ts
@packages/server/src/services/__tests__/recipeStore.test.ts

<interfaces>
<!-- Existing Hono route patterns in packages/server/src/routes/recipes.ts -->
```typescript
// Auth middleware provides:
//   c.get('supabase') -> SupabaseClient (scoped to user)
//   c.get('user')     -> { id: string, ... }

// Existing service signatures (packages/server/src/services/recipeStore.ts):
export async function getRecipes(supabase: SupabaseClient, userId: string): Promise<Recipe[]>;
export async function getRecipeById(supabase: SupabaseClient, userId: string, id: string): Promise<Recipe | null>;
export async function saveRecipe(supabase: SupabaseClient, userId: string, parsed: ParsedRecipe): Promise<Recipe>;
export async function findRecipeBySourceUrl(supabase: SupabaseClient, userId: string, url: string): Promise<Recipe | null>;
```

<!-- Supabase query chainables (v2.101): .select(), .eq(), .ilike(), .update(), .delete(), .single(), .order() -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend getRecipes + add updateRecipe/deleteRecipe (TDD, service layer)</name>
  <files>packages/server/src/services/recipeStore.ts, packages/server/src/services/__tests__/recipeStore.test.ts</files>
  <behavior>
    - getRecipes with no options returns all user recipes ordered by created_at desc
    - getRecipes with { q: 'pasta' } applies `.ilike('title', '%pasta%')`
    - getRecipes escapes wildcards: q='50%' becomes pattern `%50\\%%` (supabase literal)
    - getRecipes with { favoritesOnly: true } applies `.eq('is_favorite', true)`
    - getRecipes with both options combines both filters
    - updateRecipe with valid whitelisted patch calls `.update(patch).eq('id', id).eq('profile_id', userId).select().single()` and returns the row
    - updateRecipe returns null when row not found (PGRST116 error code)
    - deleteRecipe calls `.delete().eq('id', id).eq('profile_id', userId)` and resolves without error on success
    - deleteRecipe throws on supabase error
  </behavior>
  <action>
    RED step:
    1. Open `packages/server/src/services/__tests__/recipeStore.test.ts`. It already has tests for getRecipes/saveRecipe/getRecipeById -- extend it (do NOT rewrite).
    2. Add describe blocks: `describe('getRecipes with options', ...)`, `describe('updateRecipe', ...)`, `describe('deleteRecipe', ...)`.
    3. Mock the Supabase client using the same pattern as existing tests in that file. Cover the behaviors above.
    4. Run `pnpm --filter server test -- --run services/recipeStore` -- tests must FAIL (new functions don't exist yet).

    GREEN step:
    5. Update `packages/server/src/services/recipeStore.ts`:
       - Change `getRecipes` signature to `getRecipes(supabase, userId, opts?: { q?: string; favoritesOnly?: boolean })`. Default opts to `{}`.
       - Build query with `.from('recipes').select('*').eq('profile_id', userId)`, conditionally chain `.ilike('title', '%' + escapedQ + '%')` and `.eq('is_favorite', true)`, then `.order('created_at', { ascending: false })`.
       - Escape q with `q.replace(/[%_\\\\]/g, '\\\\$&amp;')` to neutralize wildcards (Pitfall 2).
       - Add `updateRecipe(supabase, userId, id, patch)`: `.update(patch).eq('id', id).eq('profile_id', userId).select().single()`. On `error.code === 'PGRST116'` return null. Otherwise throw error.
       - Add `deleteRecipe(supabase, userId, id)`: `.delete().eq('id', id).eq('profile_id', userId)`. Throw on error.
       - Export all three.
    6. Run tests again -- must PASS.

    Keep existing getRecipes callers working: the signature change is backward-compatible because opts is optional.
  </action>
  <verify>
    <automated>pnpm --filter server test -- --run services/recipeStore</automated>
  </verify>
  <done>All new tests pass. Functions exported with specified signatures. Wildcards escaped in ILIKE input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PATCH /:id and DELETE /:id routes, extend GET / with query params (TDD)</name>
  <files>packages/server/src/routes/recipes.ts, packages/server/src/routes/__tests__/recipes.patch.test.ts, packages/server/src/routes/__tests__/recipes.delete.test.ts</files>
  <behavior>
    - PATCH /:id with { title: 'New', is_favorite: true } returns 200 + updated data
    - PATCH /:id with unknown field { hacker: 'x' } strips it (calls updateRecipe with empty patch or only whitelisted fields)
    - PATCH /:id on nonexistent recipe returns 404
    - PATCH /:id on supabase failure returns 500
    - DELETE /:id on existing recipe returns 204 with empty body
    - DELETE /:id on supabase failure returns 500
    - GET /?q=pasta calls getRecipes with { q: 'pasta' }
    - GET /?favorites=true calls getRecipes with { favoritesOnly: true }
    - GET /?q=pasta&amp;favorites=true calls with both
  </behavior>
  <action>
    RED step:
    1. Create `packages/server/src/routes/__tests__/recipes.patch.test.ts` and `recipes.delete.test.ts`. Use the existing test harness pattern in `packages/server/src/routes/__tests__/` (check one existing file like `recipes.test.ts` or similar for Hono test client setup + supabase mock).
    2. Write tests covering the behaviors above. Mock `updateRecipe`, `deleteRecipe`, `getRecipes` from the service layer.
    3. Also extend the existing GET /api/v1/recipes test file (search for it in `packages/server/src/routes/__tests__/`) with tests for `?q=` and `?favorites=true` query-param passing.
    4. Run failing tests.

    GREEN step:
    5. In `packages/server/src/routes/recipes.ts`:
       - Import `updateRecipe, deleteRecipe` from `../services/recipeStore.js`.
       - Update the `recipes.get('/')` handler to parse `c.req.query('q')` and `c.req.query('favorites')` and pass `{ q, favoritesOnly: favorites === 'true' }` to `getRecipes`.
       - Add `recipes.patch('/:id', ...)`:
         - Read body with `await c.req.json()`.
         - Whitelist allowed fields: `['title', 'description', 'ingredients', 'steps', 'prep_time_minutes', 'cook_time_minutes', 'total_time_minutes', 'servings', 'is_favorite', 'image_url']`.
         - Build patch via `Object.fromEntries(Object.entries(body).filter(([k]) =&gt; allowed.includes(k)))`.
         - Call `updateRecipe(supabase, user.id, id, patch)`. If null, return 404. Otherwise return `{ data }`. Try/catch returns 500 on error.
       - Add `recipes.delete('/:id', ...)`: calls `deleteRecipe`, returns `c.body(null, 204)`. Try/catch returns 500.
    6. Run tests -- must PASS.

    Do NOT touch the import routes (url/photo/text) -- they stay untouched in this plan.
  </action>
  <verify>
    <automated>pnpm --filter server test -- --run routes/recipes</automated>
  </verify>
  <done>PATCH, DELETE, and extended GET endpoints exist with passing tests. Whitelist prevents arbitrary field writes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter server test -- --run routes/recipes services/recipeStore` green
- Manual: `curl -X PATCH .../api/v1/recipes/:id -d '{"is_favorite":true}'` would return 200 (validated via tests, no manual check required)
</verification>

<success_criteria>
Server exposes full CRUD + search + favorites for recipes. Mobile store plan (06-03) can consume these endpoints.
</success_criteria>

<output>
Create `.planning/phases/06-recipe-library/06-02-SUMMARY.md` documenting: exact exported service signatures, whitelisted PATCH fields, wildcard escape approach, test file paths.
</output>
