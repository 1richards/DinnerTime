---
phase: 29-something-new-lightweight-first-generation-29s-3-5s
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - apps/mobile/src/hooks/useHydratedRecipeContent.ts
  - apps/mobile/src/hooks/__tests__/useHydratedRecipeContent.test.ts
  - apps/mobile/src/stores/suggestionsStore.ts
  - apps/mobile/src/stores/__tests__/suggestionsStore.test.ts
  - apps/mobile/src/lib/perfBudgets.ts
autonomous: true
requirements: [D4, D7, D8-client]

must_haves:
  truths:
    - "After searchRecipes sets lightweight searchResults, each preview is background-hydrated (throttled 2-at-a-time) and patched into searchResults[i] with ingredients+steps as it lands"
    - "searchRecipes sends light:true to /search and is wrapped in withBudget('suggestions.search', ...)"
    - "A preview's hydration status is queryable (loading | resolved | failed) so the UI can gate Save/Cook (Plan 29-04)"
    - "On store rehydrate, any persisted un-hydrated preview re-triggers hydration (D7) — no relaunch with permanently-empty cards"
  artifacts:
    - path: "apps/mobile/src/hooks/useHydratedRecipeContent.ts"
      provides: "Background hydration hook mirroring useGeneratedRecipeImage (Map cache + MAX_CONCURRENT=2 FIFO limiter + AsyncStorage)"
      exports: ["useHydratedRecipeContent", "MAX_CONCURRENT", "prefetchHydration", "hydrationStatusFor"]
    - path: "apps/mobile/src/stores/suggestionsStore.ts"
      provides: "light:true on /search, withBudget wrap, background-hydrate-all on results land + on rehydrate"
      contains: "light: true"
  key_links:
    - from: "suggestionsStore.searchRecipes"
      to: "POST /recipes/search { light: true }"
      via: "fetch body"
      pattern: "light: true"
    - from: "suggestionsStore (results land / rehydrate)"
      to: "useHydratedRecipeContent cache (prefetchHydration)"
      via: "background-hydrate each preview, patch searchResults[i]"
      pattern: "prefetchHydration|hydrateAll"
---

<objective>
Wire the CLIENT background hydration (D4) that makes cards appear in 3-5s then fill in. Add `useHydratedRecipeContent` — a hook that mirrors `useGeneratedRecipeImage.ts` EXACTLY (module-level Map cache, the MAX_CONCURRENT=2 FIFO limiter, AsyncStorage persistence, inflight coalescing, `{ ingredients, steps, status }` return). After `searchRecipes` sets lightweight `searchResults`, background-hydrate each preview (throttled 2-at-a-time) and patch `searchResults[i]` as each lands. Send `light:true` to /search. Wrap the round-trip in `withBudget('suggestions.search', ...)`. Handle persistence safety (D7): on store rehydrate, re-trigger hydration for any persisted un-hydrated preview. Record hydration timing via logAiEvent (D8-client).

Purpose: This is the user-visible win — instant cards + background fill — plus the status signal that Plan 29-04 gates Save/Cook on.
Output: hydration hook + suggestionsStore wiring + new perf budget.

DEPENDS ON 29-01 (server sends light contract) + 29-02 (POST /recipes/hydrate exists). The hook calls /recipes/hydrate; the store sends light:true to /search.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-something-new-lightweight-first-generation-29s-3-5s/29-CONTEXT.md

<interfaces>
<!-- Executor: mirror useGeneratedRecipeImage EXACTLY. Key landmarks: -->

apps/mobile/src/hooks/useGeneratedRecipeImage.ts (the template):
- module Map cache `const cache = new Map<string, Entry>()` (65)
- `const MAX_CONCURRENT = 2; let _inFlight = 0; const _waitQueue: Array<()=>void> = []` (78-80)
- `acquireSlot()` / `releaseSlot()` FIFO limiter (82-101)
- `fetchGeneratedUrlThrottled` (103-112) wraps the fetch in acquire/release
- AsyncStorage: `STORAGE_KEY`, `hydrateFromStorage()` (195-216), `persistToStorage()` (218-230), `hydrationListeners` set
- hook returns `{ url, status: 'loading'|'resolved'|'failed' }` (253-354)
- `prefetchGeneratedRecipeImage(title, opts)` (366-386) — fire-and-forget, no-op if cached
- `getApiBaseUrl()` (47-49), `getAuthToken()` (51-58)
- `logAiEvent` + `sanitizePayload` from '../ai/telemetry' (24); emitImageEvent (33-45) is the timing-event template

Server contract (from Plan 29-02):
- POST `${API}/api/v1/recipes/hydrate` body `{ title, description, difficulty, prep_time_minutes, cook_time_minutes, total_time_minutes, cuisine?, ingredient_names? }`
- → 200 `{ data: { ingredients: ParsedIngredient[], steps: string[], calories_per_serving, protein_grams_per_serving, servings } }`

suggestionsStore.ts:
- searchResults state (28), searchRecipes (122-165) sends `{ query, pantryOnly }` to /search,
- appendSearchResults (167-231) sends `{ query, pantryOnly, count:2, excludeTitles }`,
- partialize (249-254) persists searchResults/recentQueries/lastQuery/pantryOnly,
- persist config name 'dinnertime-suggestions', has `version: 1`.

perfBudgets.ts: `withBudget(name, budgetMs, fn)` (56), existing `RECIPE_LOAD_MS = 3500` (37).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useHydratedRecipeContent hook (D4) — mirror useGeneratedRecipeImage</name>
  <read_first>
    - apps/mobile/src/hooks/useGeneratedRecipeImage.ts (FULL — copy structure: Map cache 65, MAX_CONCURRENT limiter 78-112, AsyncStorage 188-230, hook 253-354, prefetch 366-386, emitImageEvent 33-45)
    - apps/mobile/src/ai/telemetry.ts (logAiEvent 167, sanitizePayload 130)
    - apps/mobile/src/types/recipe.ts (ParsedIngredient 1-6, ParsedRecipe 32)
  </read_first>
  <files>apps/mobile/src/hooks/useHydratedRecipeContent.ts, apps/mobile/src/hooks/__tests__/useHydratedRecipeContent.test.ts</files>
  <behavior>
    - `useHydratedRecipeContent(preview)` returns `{ ingredients: ParsedIngredient[], steps: string[], status: 'loading'|'resolved'|'failed' }`.
    - At most MAX_CONCURRENT=2 hydrate HTTP calls run simultaneously (FIFO queue), identical to the image limiter.
    - Two cards for the same preview (same cache key) share ONE inflight fetch (coalescing).
    - A resolved hydration persists to AsyncStorage; a failed one does NOT (retry next session) — mirror image hook.
    - `prefetchHydration(preview)` fires the fetch without mounting the hook; no-op if already cached/inflight.
    - `hydrationStatusFor(preview)` (sync) returns the current cached status without subscribing — used by the store + Plan 29-04 gating.
  </behavior>
  <action>
    1. Copy useGeneratedRecipeImage.ts to useHydratedRecipeContent.ts and adapt: Entry shape `{ content: { ingredients: ParsedIngredient[]; steps: string[] } | null; inflight: Promise<...|null> | null; attempted: boolean }`. cacheKey = `norm(title) + '#' + sorted(ingredient_names).join('|')` (define a `HydratePreview` input type carrying title/description/difficulty/times/cuisine/ingredient_names).
    2. `fetchHydrated(preview)` POSTs to `/api/v1/recipes/hydrate` (body per the 29-02 contract), returns `{ ingredients, steps } | null` (null on !res.ok or throw). Wrap in `fetchHydratedThrottled` using the SAME acquireSlot/releaseSlot limiter — export `MAX_CONCURRENT` (= 2).
    3. AsyncStorage: STORAGE_KEY = 'dinnertime-hydration-cache'; hydrateFromStorage/persistToStorage persist ONLY resolved (non-null content) entries. Reuse the hydrationListeners + `hydrated` flag pattern verbatim.
    4. Hook `useHydratedRecipeContent(preview)`: same evaluate() state machine as the image hook — cache hit → resolved; attempted+null → failed; inflight → loading + .then; no entry → kick off throttled fetch, status loading. On resolve set `{ ingredients, steps, status:'resolved' }`.
    5. Timing (D8-client): add `emitHydrationEvent(startedAt, success)` modeled on emitImageEvent — `logAiEvent({ name: 'recipe.hydrate.visible', session_id: 'something-new', task_name: 'recipe.hydrate', model: 'gemini-flash', payload: sanitizePayload({ ms: Date.now()-startedAt, success }) })`. NO titles/ingredients in payload (PII guard — ms+success only).
    6. Export `prefetchHydration(preview)` (mirror prefetchGeneratedRecipeImage: no-op if `cache.has(key)`, else kick off throttled fetch + persist on resolve) and `hydrationStatusFor(preview): 'loading'|'resolved'|'failed'|'idle'` reading the module cache synchronously.
    7. Tests: mock global fetch + AsyncStorage (follow useGeneratedRecipeImage test patterns / vitest.setup). Assert: (a) hook resolves to provided ingredients/steps with status 'resolved'; (b) two prefetchHydration calls for the same preview → fetch called ONCE (coalescing); (c) limiter caps in-flight at 2 (fire 4 prefetches against a controllable fetch, assert only 2 resolve their fetch start before the first releases) — copy the image-hook limiter test if one exists, else a counting mock; (d) failed fetch → status 'failed', NOT persisted.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm vitest run src/hooks/__tests__/useHydratedRecipeContent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const MAX_CONCURRENT" apps/mobile/src/hooks/useHydratedRecipeContent.ts` present and equals 2.
    - `grep -n "recipes/hydrate" apps/mobile/src/hooks/useHydratedRecipeContent.ts` present (calls the 29-02 endpoint).
    - `grep -n "export function prefetchHydration\|export function hydrationStatusFor\|export function useHydratedRecipeContent" apps/mobile/src/hooks/useHydratedRecipeContent.ts` — all three exported.
    - `grep -n "acquireSlot\|releaseSlot\|_waitQueue" apps/mobile/src/hooks/useHydratedRecipeContent.ts` shows the FIFO limiter mirrored.
    - Coalescing test (fetch called once for duplicate prefetch) passes.
  </acceptance_criteria>
  <done>Hydration hook mirrors the image hook (2-concurrent throttle, Map+AsyncStorage cache, coalescing, status) and calls /recipes/hydrate; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: suggestionsStore — light:true, withBudget, background-hydrate-all, rehydrate safety (D4 + D7 + D8-client)</name>
  <read_first>
    - apps/mobile/src/stores/suggestionsStore.ts (searchRecipes 122-165, appendSearchResults 167-231, partialize 249-254, persist onRehydrate hook availability)
    - apps/mobile/src/hooks/useHydratedRecipeContent.ts (Task 1 — prefetchHydration, hydrationStatusFor)
    - apps/mobile/src/lib/perfBudgets.ts (withBudget 56, RECIPE_LOAD_MS 37 as the const-naming template)
    - apps/mobile/src/stores/__tests__/suggestionsStore.test.ts + suggestionsStore.persist.test.ts (existing harness)
  </read_first>
  <files>apps/mobile/src/stores/suggestionsStore.ts, apps/mobile/src/stores/__tests__/suggestionsStore.test.ts, apps/mobile/src/lib/perfBudgets.ts</files>
  <behavior>
    - searchRecipes POSTs `{ query, pantryOnly, light: true }`; appendSearchResults POSTs `{ ..., light: true }`.
    - The /search round-trip is wrapped in `withBudget('suggestions.search', SUGGESTIONS_SEARCH_MS, ...)`.
    - After searchResults are set (initial AND append), every preview is background-hydrated via prefetchHydration (throttled by the hook's limiter) and, as each resolves, searchResults[i] is patched with ingredients+steps.
    - On store rehydrate from AsyncStorage, any persisted preview whose ingredients OR steps are empty re-triggers hydration (D7) so a relaunched preview never stays permanently empty.
  </behavior>
  <action>
    1. perfBudgets.ts: add `export const SUGGESTIONS_SEARCH_MS = 5000;` with a doc comment (5s = upper bound of the 3-5s target band for the light /search round-trip). Mirror the RECIPE_LOAD_MS comment style.
    2. searchRecipes: add `light: true` to the JSON body. Wrap the `fetch(...)` round-trip in `withBudget('suggestions.search', SUGGESTIONS_SEARCH_MS, () => fetch(...))`. After `set({ searchResults: ... })`, call a new private helper `hydrateAll(get, set)` that iterates the just-set searchResults and for each calls `prefetchHydration(previewFrom(r))`, then attaches a `.then` (or polls via a small async loop) that, when the hook cache resolves, patches that index: `set(s => ({ searchResults: s.searchResults.map((x,i) => i===idx ? { ...x, ingredients, steps, calories_per_serving, protein_grams_per_serving } : x) }))`. SIMPLEST robust approach: have prefetchHydration RETURN the inflight promise resolving to `{ ingredients, steps } | null` (adjust the hook export to return the promise) so the store can await per-index and patch. Match by stable identity = title (Something New titles are unique).
    3. appendSearchResults: add `light: true` to the body; after appending, run the same per-new-item hydration+patch.
    4. D7 rehydrate safety: add an `onRehydrateStorage`/`onFinishHydration` callback in the persist config (zustand persist `onRehydrateStorage: () => (state) => { ... }`). In it, if `state?.searchResults?.length`, for each preview where `ingredients.length===0 || steps.length===0`, trigger hydration + patch (reuse hydrateAll). Guard against running before the hook module is ready — fire on next tick (`setTimeout(0)`-style or queueMicrotask) is acceptable.
    5. `previewFrom(r)` helper: maps a searchResults ParsedRecipe to the HydratePreview input `{ title, description, difficulty, prep_time_minutes, cook_time_minutes, total_time_minutes, ingredient_names: r.ingredients?.map(i=>i.name) }`.
    6. Tests: (a) searchRecipes sends `light:true` (assert fetch body); (b) after a mocked /search returns light previews (empty steps) AND a mocked hydrate returns full content, searchResults[i].steps becomes non-empty (patch works); (c) withBudget is invoked (spy/mock perfBudgets); (d) D7: simulate rehydrate with a persisted empty-steps preview → assert hydration is triggered (prefetchHydration/fetch called). Mock the hydration hook module in the store test.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm vitest run src/stores/__tests__/suggestionsStore.test.ts src/stores/__tests__/suggestionsStore.persist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "light: true" apps/mobile/src/stores/suggestionsStore.ts` present in BOTH searchRecipes and appendSearchResults bodies.
    - `grep -n "withBudget('suggestions.search'\|SUGGESTIONS_SEARCH_MS" apps/mobile/src/stores/suggestionsStore.ts` present.
    - `grep -n "export const SUGGESTIONS_SEARCH_MS" apps/mobile/src/lib/perfBudgets.ts` present.
    - `grep -n "onRehydrateStorage\|onFinishHydration" apps/mobile/src/stores/suggestionsStore.ts` present (D7).
    - `grep -n "prefetchHydration\|hydrateAll" apps/mobile/src/stores/suggestionsStore.ts` present.
    - Patch test (searchResults[i].steps fills after hydrate) passes.
  </acceptance_criteria>
  <done>Store sends light:true, measures via withBudget, background-hydrates+patches every preview, and re-hydrates persisted empty previews on rehydrate; tests green.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && pnpm vitest run src/hooks/__tests__/useHydratedRecipeContent.test.ts src/stores/__tests__/suggestionsStore.test.ts src/stores/__tests__/suggestionsStore.persist.test.ts` — green.
- Reasoning: light previews now fill in the background; persisted previews self-heal on relaunch (D7).
</verification>

<success_criteria>
- Cards render from light previews in 3-5s, then ingredients+steps fill in (throttled 2-at-a-time).
- searchRecipes is light-mode + withBudget-measured.
- No relaunch leaves a preview permanently empty (D7).
</success_criteria>

<output>
After completion, create `.planning/phases/29-something-new-lightweight-first-generation-29s-3-5s/29-03-SUMMARY.md`
</output>
