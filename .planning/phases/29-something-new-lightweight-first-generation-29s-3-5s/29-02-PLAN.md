---
phase: 29-something-new-lightweight-first-generation-29s-3-5s
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - packages/server/src/services/recipeHydration.ts
  - packages/server/src/services/__tests__/recipeHydration.test.ts
  - packages/server/src/routes/recipes.ts
  - packages/server/src/routes/__tests__/recipes.hydrate.test.ts
autonomous: true
requirements: [D3]

must_haves:
  truths:
    - "POST /recipes/hydrate accepts a light preview and returns full ingredients[] + steps[] + nutrition"
    - "Hydration reuses the proven single-call recipe.parseText engine (same as applyRemixVariation), not a new AI surface"
    - "Re-hydrating the same preview within the cache window returns from cache (no second Gemini call)"
  artifacts:
    - path: "packages/server/src/services/recipeHydration.ts"
      provides: "hydrateRecipePreview() — title+context → full ParsedRecipe via callAIParseRecipeText"
      exports: ["hydrateRecipePreview"]
    - path: "packages/server/src/routes/recipes.ts"
      provides: "POST /hydrate route"
      contains: "app.post"
  key_links:
    - from: "routes/recipes.ts POST /hydrate"
      to: "recipeHydration.hydrateRecipePreview"
      via: "service call returning { ingredients, steps, nutrition }"
      pattern: "hydrateRecipePreview"
---

<objective>
Add the SERVER hydration endpoint (D3): `POST /recipes/hydrate` that takes a light preview and returns full `{ ingredients, steps, nutrition }`. It REUSES the proven single-call full-recipe primitive — `applyRemixVariation` (recipeParser.ts:435-476) already does "title + light context → full ParsedRecipe with ingredients+steps+nutrition" via ONE `callAIParseRecipeText('recipe.parseText', ...)` call. The hydrate handler is modeled on it. The result is content-address cached (mirror discoveryCache / recipeImageGen) so re-hydrating the same preview is cheap.

Purpose: This is the background-fill engine the client (Plan 29-03) calls per-preview, throttled 2-at-a-time, to fill cards after they render.
Output: `recipeHydration.ts` service + `POST /recipes/hydrate` route + cache.

DEPENDS ON 29-01: shares `routes/recipes.ts` (different region — appends a new route). Wave 2 so the /search edits land first and there's no merge conflict on recipes.ts.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-something-new-lightweight-first-generation-29s-3-5s/29-CONTEXT.md

<interfaces>
<!-- Executor: use these directly. -->

applyRemixVariation (recipeParser.ts:435-476) — the engine to mirror:
```ts
export async function applyRemixVariation(
  base: { title; description?; ingredients?; steps?; total_time_minutes? },
  variation: { title; description },
): Promise<ParsedRecipe> {
  // builds a prompt, then:
  const input = await callAIParseRecipeText('recipe.parseText', prompt);
  return toolOutputToRecipe(input, 'ai');
}
```
`callAIParseRecipeText('recipe.parseText', prompt)` (recipeParser.ts ~254) routes to
google/gemini flash (taskRouting.ts:30) and returns a structured object;
`toolOutputToRecipe(input, 'ai')` (recipeParser.ts) maps it to a full ParsedRecipe
including calories_per_serving / protein_grams_per_serving. The remix prompt
(455-472) shows the exact nutrition-instruction wording to reuse.

discoveryCache.ts content-address pattern (getOrComputeDiscovery + createHash sha256)
is the template for the hydration cache.

ParsedRecipe (recipeParser.ts ParsedRecipe / mobile types/recipe.ts:32):
{ title, description, ingredients: ParsedIngredient[], steps: string[],
  prep/cook/total_time_minutes, servings, calories_per_serving,
  protein_grams_per_serving, difficulty, practiced_skills, skill_note, ... }

Existing route auth pattern (recipes.ts): `const supabase = c.get('supabase'); const user = c.get('user');`
JSON-body parse with try/catch → 400 on bad JSON (see /search 205-209).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recipeHydration service — preview → full recipe via recipe.parseText + cache (D3)</name>
  <read_first>
    - packages/server/src/services/recipeParser.ts (applyRemixVariation 435-476 — the EXACT pattern to mirror; callAIParseRecipeText ~250-256; toolOutputToRecipe)
    - packages/server/src/services/discoveryCache.ts (getOrComputeDiscovery + createHash content-address pattern to mirror for the hydration cache)
    - packages/server/src/services/__tests__/recipeParser.test.ts (clientFactory mock conventions)
  </read_first>
  <files>packages/server/src/services/recipeHydration.ts, packages/server/src/services/__tests__/recipeHydration.test.ts</files>
  <behavior>
    - `hydrateRecipePreview(preview)` where preview = `{ title, description?, difficulty?, prep_time_minutes?, cook_time_minutes?, total_time_minutes?, cuisine?, ingredient_names?: string[] }` returns a full ParsedRecipe with non-empty ingredients[] and steps[].
    - It calls `callAIParseRecipeText('recipe.parseText', prompt)` exactly ONCE per cache miss (mirror applyRemixVariation), then `toolOutputToRecipe(input, 'ai')`.
    - The prompt seeds the model with the preview's title + description + times + ingredient_names so the hydration is faithful to the card the user saw.
    - A second call with the SAME preview within the TTL returns the cached result and does NOT invoke the AI again (assert the mock call count stays 1).
  </behavior>
  <action>
    1. Create `recipeHydration.ts`. Define `HydratePreviewInput` = `{ title: string; description?: string | null; difficulty?: string | null; prep_time_minutes?: number | null; cook_time_minutes?: number | null; total_time_minutes?: number | null; cuisine?: string | null; ingredient_names?: string[] }`.
    2. `export async function hydrateRecipePreview(preview: HydratePreviewInput): Promise<ParsedRecipe>`. Build a prompt mirroring applyRemixVariation's wording: state the title, description, total time, and the `ingredient_names` list as "Known ingredients (expand to full quantities)", then instruct: "Produce the COMPLETE recipe — full structured ingredients (name, quantity, unit, notes), ordered steps, servings >= 4. Convert fractions to decimals." INCLUDE the same calories/protein nutrition instruction block copied from applyRemixVariation (lines 472). Call `callAIParseRecipeText('recipe.parseText', prompt)` then `return toolOutputToRecipe(input, 'ai')`.
    3. Add a content-address cache mirroring discoveryCache: a module-level `Map<string, { value: ParsedRecipe; expiresAt: number }>` + a sha256 key from `title|total_time|sorted(ingredient_names)` (createHash('sha256')). Wrap hydrateRecipePreview body in a getOrCompute pattern with a TTL (export `HYDRATION_CACHE_TTL_MS = 30 * 60 * 1000` — 30 min; hydration is deterministic-ish and re-hydration is pure waste). Coalesce concurrent identical calls via an inflight `Map<string, Promise<ParsedRecipe>>` (copy the discoveryCache inflight pattern). Export `__resetHydrationCache()` for tests.
    4. Tests: (a) mock clientFactory so `generateStructured` returns a recipe object with ingredients+steps; assert hydrateRecipePreview returns ingredients.length>0 && steps.length>0; (b) call twice with the same preview, assert the underlying `generateStructured` mock was called exactly once (cache hit); (c) two DIFFERENT previews → two AI calls (key differs).
  </action>
  <verify>
    <automated>cd packages/server && pnpm vitest run src/services/__tests__/recipeHydration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export async function hydrateRecipePreview" packages/server/src/services/recipeHydration.ts` present.
    - `grep -n "callAIParseRecipeText('recipe.parseText'" packages/server/src/services/recipeHydration.ts` present (reuses the proven engine).
    - `grep -n "createHash\|HYDRATION_CACHE_TTL_MS\|inflight" packages/server/src/services/recipeHydration.ts` shows content-address cache + coalescing.
    - Cache-hit test (mock called once across two identical calls) passes.
  </acceptance_criteria>
  <done>hydrateRecipePreview returns full ingredients+steps+nutrition via one recipe.parseText call, cached + coalesced; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /recipes/hydrate route (D3)</name>
  <read_first>
    - packages/server/src/routes/recipes.ts (/search 192-336 for auth + JSON-parse-400 conventions; /discover 345-423 for a simple POST shape)
    - packages/server/src/routes/__tests__/recipes.search.test.ts (Hono test-client + supabase/auth mock harness to copy)
    - packages/server/src/services/recipeHydration.ts (from Task 1)
  </read_first>
  <files>packages/server/src/routes/recipes.ts, packages/server/src/routes/__tests__/recipes.hydrate.test.ts</files>
  <behavior>
    - `POST /api/v1/recipes/hydrate` with a valid preview body returns `{ data: { ingredients, steps, calories_per_serving, protein_grams_per_serving, servings } }` (the full-content fields the client patches onto the preview).
    - Missing `title` → 400.
    - Bad JSON → 400.
    - AI/service failure → 500 with `{ error }` (mirror /search catch).
  </behavior>
  <action>
    1. In recipes.ts, add `recipes.post('/hydrate', async (c) => { ... })`. Register it ABOVE the `recipes.get('/:id')` route (so '/hydrate' isn't captured as an id param) — same positioning rationale as /search and /discover which sit before /:id.
    2. Parse JSON with the try/catch → 400 'Invalid JSON body' pattern. Validate `typeof body.title === 'string' && body.title.trim().length > 0` else 400 'title is required'.
    3. Call `const full = await hydrateRecipePreview({ title, description, difficulty, prep_time_minutes, cook_time_minutes, total_time_minutes, cuisine, ingredient_names })` (pull fields from body defensively). Return `c.json({ data: { ingredients: full.ingredients, steps: full.steps, calories_per_serving: full.calories_per_serving ?? null, protein_grams_per_serving: full.protein_grams_per_serving ?? null, servings: full.servings ?? null } })`. (Auth still required — keep `c.get('user')` so the route stays behind the auth middleware; user id not needed for the AI call but the guard must run.)
    4. Wrap in try/catch → `c.json({ error: message }, 500)` mirroring /search.
    5. Tests (recipes.hydrate.test.ts): (a) valid preview → 200, response.data.ingredients.length>0 && steps.length>0 (mock hydrateRecipePreview or the underlying clientFactory); (b) missing title → 400; (c) bad JSON → 400. Reuse the auth/supabase mock harness from recipes.search.test.ts.
  </action>
  <verify>
    <automated>cd packages/server && pnpm vitest run src/routes/__tests__/recipes.hydrate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recipes.post('/hydrate'" packages/server/src/routes/recipes.ts` present (note: file uses `recipes.post(...)` not `app.post`).
    - The /hydrate route is declared BEFORE `recipes.get('/:id'` in the file (grep line numbers confirm hydrate < :id).
    - `grep -n "hydrateRecipePreview" packages/server/src/routes/recipes.ts` present.
    - 400-on-missing-title and 200-happy-path tests pass.
  </acceptance_criteria>
  <done>POST /recipes/hydrate returns full ingredients+steps+nutrition for a preview; validation + error paths covered; tests green.</done>
</task>

</tasks>

<verification>
- `cd packages/server && pnpm vitest run` — full server suite green (hydration + route + no regressions on existing recipes routes).
- Reasoning: /hydrate is additive — no existing contract changes.
</verification>

<success_criteria>
- A working POST /recipes/hydrate that turns a light preview into full ingredients+steps+nutrition.
- Implementation reuses recipe.parseText (no new AI prompt surface invented).
- Content-address cache + inflight coalescing so repeat hydration is free.
</success_criteria>

<output>
After completion, create `.planning/phases/29-something-new-lightweight-first-generation-29s-3-5s/29-02-SUMMARY.md`
</output>
