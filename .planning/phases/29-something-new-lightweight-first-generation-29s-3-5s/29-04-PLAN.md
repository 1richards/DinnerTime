---
phase: 29-something-new-lightweight-first-generation-29s-3-5s
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - apps/mobile/src/components/suggestions/SomethingNewResults.tsx
  - apps/mobile/src/app/recipes/discover.tsx
  - apps/mobile/src/app/(tabs)/kitchen.tsx
autonomous: false
requirements: [D5, D6]

must_haves:
  truths:
    - "Save / Cook Now / Save+Favorite are disabled (or await in-flight hydration) until that recipe's hydration status === 'resolved' — an un-hydrated save can never fire (POST /recipes 400s without ingredients+steps)"
    - "PreviewSheet shows a loading affordance for un-hydrated STEPS (existing stepsLoading prop wired) AND for un-hydrated INGREDIENTS (new affordance) — no bald empty-state"
    - "A tapped card whose hydration is still in flight either awaits hydration before saving or presents a clear loading state, never a 400"
  artifacts:
    - path: "apps/mobile/src/components/suggestions/SomethingNewResults.tsx"
      provides: "PreviewRecipeCard gates save/cook/favorite on hydration status"
      contains: "status"
    - path: "apps/mobile/src/app/recipes/discover.tsx"
      provides: "PreviewSheet ingredientsLoading affordance + stepsLoading wired"
      contains: "ingredientsLoading"
  key_links:
    - from: "PreviewRecipeCard / kitchen PreviewSheet save handlers"
      to: "useHydratedRecipeContent status"
      via: "disable until status==='resolved' OR await hydration"
      pattern: "status === 'resolved'|hydrationStatusFor"
---

<objective>
Close the CRITICAL correctness gap and add the loading affordances. `POST /api/v1/recipes` HARD-REQUIRES non-empty ingredients AND steps (recipes.ts:454 → 400). Every save path spreads `...recipe` (SomethingNewResults.tsx:306/335/370, kitchen.tsx:573/603/796). With lightweight-first generation, a freshly-rendered card has EMPTY ingredients/steps until hydration lands — so Save/Cook/Favorite MUST be gated on `status==='resolved'` (D5, non-negotiable). Also wire the existing `stepsLoading` prop and add a new ingredients loader in PreviewSheet (D6) so un-hydrated content shows a spinner, not "No ingredients listed."

Purpose: Without D5, the lightweight-first flow ships a broken Save that 400s. This plan is the safety net.
Output: Gated save/cook/favorite across all 3 save surfaces + PreviewSheet loading affordances.

DEPENDS ON 29-03: consumes `useHydratedRecipeContent` / `hydrationStatusFor` for the status signal.

Has a checkpoint: human-verify the full flow on the simulator (Maestro / manual) since this is the flagship UX and the 400-gate is behavior the test suite alone can't fully prove.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-something-new-lightweight-first-generation-29s-3-5s/29-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Save surfaces to gate (all spread ...recipe → 400 if ingredients/steps empty): -->

SomethingNewResults.tsx PreviewRecipeCard (244-433):
- handleSave (300-316): `saveRecipe({ ...recipe, image_url: heroUri, source_type:'ai' })`
- handleSaveAndFavorite (329-359)
- handleCookNow (364-382)
- pantryMatchCount (269-272) reduces over `recipe.ingredients` — empty until hydrated.
- previewActions passes saved/favorited/working to RecipeCard.

discover.tsx PreviewSheet (exported 344; props 344-372):
- `stepsLoading = false` prop (365) ALREADY renders "Generating steps…" + spinner at 735-739.
- Ingredients render 692-729: `recipe.ingredients.length === 0 ? <Text>No ingredients listed.</Text> : <ScaledIngredientList .../>` — NO loading branch.
- onSave/onCookNow/onCookLater/onAdHocFavorite are passed in by the parent.

kitchen.tsx (the Something New PreviewSheet host):
- handlePreviewSave (564-592), handlePreviewCookNow (596-...), onCookLater inline (770-784), onAdHocFavorite inline (787-818) — all `saveRecipe({ ...previewRecipe, ... })`.
- PreviewSheet mounted 754-820; previewRecipe state (326).
- previewGeneratedUri hook already exists (347-352).

29-03 exports: `useHydratedRecipeContent(preview)` → { ingredients, steps, status }; `hydrationStatusFor(preview)` (sync); `prefetchHydration(preview)`.
NOTE: With Plan 29-03, suggestionsStore patches searchResults[i] with ingredients+steps as hydration lands — so `recipe.ingredients.length>0 && recipe.steps.length>0` is itself a reliable "hydrated" signal for the store-backed cards. Use status where the recipe identity is a live preview; use the ingredients/steps non-empty check as the simplest gate where a status lookup is awkward.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate Save/Cook/Favorite on hydration (D5) across all 3 save surfaces</name>
  <read_first>
    - apps/mobile/src/components/suggestions/SomethingNewResults.tsx (PreviewRecipeCard 244-433: handleSave 300, handleSaveAndFavorite 329, handleCookNow 364, previewActions 397-405, pantryMatchCount 269-272)
    - apps/mobile/src/app/(tabs)/kitchen.tsx (handlePreviewSave 564, handlePreviewCookNow 596, PreviewSheet 754-820, onAdHocFavorite 787, onCookLater 770)
    - apps/mobile/src/hooks/useHydratedRecipeContent.ts (status + hydrationStatusFor)
    - apps/mobile/src/app/recipes/discover.tsx (RecipeCard previewActions consumption — confirm how `working`/disabled propagate to the buttons)
  </read_first>
  <files>apps/mobile/src/components/suggestions/SomethingNewResults.tsx, apps/mobile/src/app/(tabs)/kitchen.tsx</files>
  <action>
    1. PreviewRecipeCard (SomethingNewResults.tsx): derive `const hydrated = recipe.ingredients.length > 0 && recipe.steps.length > 0;` (the store patches these in as hydration lands per Plan 29-03 — this is the live signal). Optionally cross-check `hydrationStatusFor(previewFrom(recipe))` for an in-flight indicator, but the ingredients/steps non-empty check is the authoritative gate (it's exactly what the server requires).
    2. In handleSave/handleSaveAndFavorite/handleCookNow: at the TOP, `if (!hydrated) { /* await in-flight hydration then proceed */ }`. Implement the await path: call `await prefetchHydration(previewFrom(recipe))` which resolves to `{ ingredients, steps } | null`; if it resolves non-null, use those for the save body (`saveRecipe({ ...recipe, ingredients, steps, image_url: heroUri, source_type:'ai' })`); if null, `Alert.alert('Still preparing', 'This recipe is still loading — try again in a moment.')` and bail. This guarantees no 400.
    3. Pass a `disabled`/loading affordance to RecipeCard's previewActions so the buttons visibly show a subtle loading state while `!hydrated` (e.g. extend previewActions with `hydrating: !hydrated` and have RecipeCard show a spinner or dim the action; if RecipeCard already keys off `working`, set a synthetic working state). Keep it subtle — card still tappable to open the sheet.
    4. pantryMatchCount: guard `recipe.ingredients.reduce(...)` already handles empty arrays (returns 0) — leave as-is; it self-corrects when ingredients hydrate in. Add a code comment noting it reads 0 until hydration.
    5. kitchen.tsx PreviewSheet host: in handlePreviewSave, handlePreviewCookNow, the inline onCookLater (770) and onAdHocFavorite (787) — apply the SAME gate. Compute `const hydrated = !!previewRecipe && previewRecipe.ingredients.length>0 && previewRecipe.steps.length>0;`. Each handler: if `!hydrated`, `await prefetchHydration(previewFrom(previewRecipe))`, use the resolved ingredients/steps (or bail with the same Alert on null) before calling saveRecipe/addToPlan.
    6. Import `prefetchHydration` + a shared `previewFrom` helper (export `previewFrom` from useHydratedRecipeContent.ts in Plan 29-03, or define a tiny local mapper here mapping ParsedRecipe → HydratePreview). Keep one definition; prefer importing from the hook module.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm vitest run src/components/suggestions src/stores/__tests__/suggestionsStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ingredients.length > 0 && .*steps.length > 0\|hydrationStatusFor\|prefetchHydration" apps/mobile/src/components/suggestions/SomethingNewResults.tsx` present in the save handlers.
    - `grep -n "prefetchHydration\|ingredients.length > 0" apps/mobile/src/app/(tabs)/kitchen.tsx` present in handlePreviewSave/handlePreviewCookNow/onCookLater/onAdHocFavorite.
    - No save handler calls `saveRecipe({ ...recipe })` without first ensuring non-empty ingredients+steps (manual grep review of each of the 3+ surfaces).
  </acceptance_criteria>
  <done>Every Save/Cook/Favorite path either is disabled until hydrated or awaits in-flight hydration before firing — no path can 400 on empty ingredients/steps.</done>
</task>

<task type="auto">
  <name>Task 2: PreviewSheet loading affordances — wire stepsLoading + add ingredientsLoading (D6)</name>
  <read_first>
    - apps/mobile/src/app/recipes/discover.tsx (PreviewSheet props 344-372: stepsLoading 365/418; ingredients render 692-729; steps render 733-751 with the existing spinner block 735-739; styles sheetStepsLoading 1165, sheetEmpty 1158)
    - apps/mobile/src/app/(tabs)/kitchen.tsx (PreviewSheet mount 754-820 — where stepsLoading/ingredientsLoading get passed for Something New)
  </read_first>
  <files>apps/mobile/src/app/recipes/discover.tsx, apps/mobile/src/app/(tabs)/kitchen.tsx</files>
  <action>
    1. discover.tsx PreviewSheet: add an `ingredientsLoading?: boolean` prop (default false) alongside `stepsLoading` (props list ~344-372 and the type block ~418). Reuse the existing `sheetStepsLoading` style + ActivityIndicator pattern.
    2. Ingredients render (692-729): change the empty branch to mirror steps — `recipe.ingredients.length === 0 ? (ingredientsLoading ? (<View style={styles.sheetStepsLoading}><ActivityIndicator size="small" color={colors.brand} /><Text style={styles.sheetEmpty}>Gathering ingredients…</Text></View>) : (<Text style={styles.sheetEmpty}>No ingredients listed.</Text>)) : (<ScaledIngredientList .../>)`.
    3. kitchen.tsx PreviewSheet mount (754-820): pass `stepsLoading={!!previewRecipe && previewRecipe.steps.length === 0}` and `ingredientsLoading={!!previewRecipe && previewRecipe.ingredients.length === 0}`. (As Plan 29-03 patches the store-backed previewRecipe, these flip false when content lands. If previewRecipe is a stale snapshot, also kick `prefetchHydration(previewFrom(previewRecipe))` on sheet open so an un-hydrated card hydrates while the sheet is up.)
    4. Ensure no empty-state FLASH: when ingredients/steps are empty AND hydration is in flight (status loading), the loader shows — not "No ingredients listed." The loading branch above achieves this because the parent passes ingredientsLoading/stepsLoading=true whenever the arrays are empty (un-hydrated previews are always either loading or will be; a genuinely content-less recipe is not a Something New case).
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm vitest run src/app/recipes 2>/dev/null || cd apps/mobile && pnpm tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ingredientsLoading" apps/mobile/src/app/recipes/discover.tsx` present (prop + render branch).
    - `grep -n "stepsLoading={" apps/mobile/src/app/(tabs)/kitchen.tsx` present (wired for Something New PreviewSheet).
    - `grep -n "ingredientsLoading={" apps/mobile/src/app/(tabs)/kitchen.tsx` present.
    - `grep -n "Gathering ingredients\|Generating steps" apps/mobile/src/app/recipes/discover.tsx` shows both loaders.
  </acceptance_criteria>
  <done>Un-hydrated ingredients and steps both show a spinner-with-label loader in PreviewSheet; no bald empty-state flash.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the lightweight-first Something New flow on the simulator</name>
  <action>Deploy/run locally and verify the end-to-end flow (timing, background fill, D5 no-400 save, D6 loaders, D7 rehydrate) per the steps below. This is the data-driven + human-gated confirmation of 29s→3-5s that the test suite cannot assert alone.</action>
  <what-built>
    Lightweight-first Something New: cards render in ~3-5s from light previews, ingredients+steps fill in via background hydration (2-at-a-time), Save/Cook/Favorite are gated until hydrated, and PreviewSheet shows ingredient/step loaders while content fills.
  </what-built>
  <how-to-verify>
    1. Start backend + Metro per CLAUDE.md (simulator: `EXPO_PUBLIC_API_URL=http://localhost:3000`, `npx expo start --dev-client --lan`). Server must be running the 29-01/29-02 changes.
    2. Boot the iPhone 17 Pro simulator, open the app, go to the Kitchen tab → Something New, run a pantry-only search (or a query like "quick weeknight pastas").
    3. CONFIRM cards appear in ~3-5s (NOT ~29s) — watch the Fly/server log for the `recipes.search` line with `gemini_ms` in the 3-5s range and `light: true`.
    4. CONFIRM each card's ingredients/steps fill in shortly after the card appears (background hydration). Open a card immediately after it renders — the PreviewSheet should show "Gathering ingredients…" / "Generating steps…" loaders, then populate.
    5. CRITICAL (D5): tap Save (or Cook Now) on a card the INSTANT it appears, before hydration finishes. It must NOT 400 — it should either wait then save successfully, or show "Still preparing…". Verify the recipe lands in Recipe Box WITH ingredients+steps.
    6. D7: force-quit and relaunch the app while previews are on screen. Confirm the persisted previews re-hydrate (no permanently-empty cards) and Save still works on them.
    7. Take Maestro screenshots at the loading and resolved states (free debugging gold per CLAUDE.md).
  </how-to-verify>
  <resume-signal>Type "approved" if cards render in 3-5s, hydration fills content, save never 400s, and relaunch re-hydrates. Otherwise describe the failure (timing, empty cards, 400, missing loader).</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/mobile && pnpm vitest run` — mobile suite green.
- Human checkpoint confirms the 3-5s render, background fill, D5 no-400 save, D6 loaders, D7 rehydrate.
</verification>

<success_criteria>
- D5: No save/cook/favorite path can fire on an un-hydrated recipe (no 400).
- D6: Ingredients + steps both show loaders while un-hydrated; no empty-state flash.
- Flagship Something New flow renders fast and fills in correctly (human-verified).
</success_criteria>

<output>
After completion, create `.planning/phases/29-something-new-lightweight-first-generation-29s-3-5s/29-04-SUMMARY.md`
</output>
