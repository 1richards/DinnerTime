---
phase: 28-recipe-load-telemetry-performance-10s-3-5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/services/recipeStore.ts
  - packages/server/src/routes/recipes.ts
  - packages/server/src/services/__tests__/recipeStore.test.ts
  - packages/server/src/routes/__tests__/recipes.get.test.ts
autonomous: true
requirements: [T1, O1]
must_haves:
  truths:
    - "Server GET /recipes emits a structured log line with db_query_ms, row_count, and payload_bytes"
    - "The list query selects an explicit lightweight column set that excludes steps and step_image_urls"
    - "The list query is bounded by an explicit LIMIT so worst-case payload is capped"
    - "ingredients is STILL returned by the list query (image-hook fingerprint + detail fallback both depend on it)"
    - "Existing recipe list/search/favorites behavior is unchanged (q + favorites still work)"
  artifacts:
    - path: "packages/server/src/services/recipeStore.ts"
      provides: "getRecipes lightweight explicit column select + LIMIT + timing return"
      contains: "RECIPE_LIST_COLUMNS"
    - path: "packages/server/src/routes/recipes.ts"
      provides: "GET / sub-stage timing log (db_query_ms, row_count, payload_bytes)"
      contains: "payload_bytes"
  key_links:
    - from: "packages/server/src/routes/recipes.ts GET /"
      to: "getRecipes timing"
      via: "structured console.log with stage=recipes.list"
      pattern: "recipes\\.list"
    - from: "getRecipes select"
      to: "recipes table"
      via: "explicit column list (no steps/step_image_urls)"
      pattern: "RECIPE_LIST_COLUMNS"
---

<objective>
Light up server-side sub-stage timing on GET /recipes (T1) and trim the list query to a lightweight explicit column set with a hard LIMIT (O1), WITHOUT breaking client-side search, the offline cache, the image-gen fingerprint, or recipe detail.

Purpose: Quantify whether the list DB query / payload is a real cost (the diagnosis says it is "mild over-fetch" — telemetry confirms), and shrink the worst-case payload by dropping the large `steps` + `step_image_urls` JSONB from the list path.
Output: getRecipes returns lightweight rows + timing metadata; GET / logs a structured `recipes.list` line; existing tests updated + new column/limit assertions.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-recipe-load-telemetry-performance-10s-3-5s/28-CONTEXT.md
@.planning/debug/perf-recipe-load-image-caching.md

<interfaces>
<!-- Current getRecipes (packages/server/src/services/recipeStore.ts:133-156) -->
```typescript
// CURRENT — SELECT * (all columns incl. steps + step_image_urls JSONB)
export async function getRecipes(
  supabase: SupabaseClient,
  profileId: string,
  opts: GetRecipesOptions = {}
): Promise<RecipeRow[]> {
  let query = supabase.from('recipes').select().eq('profile_id', profileId);
  if (opts.q && opts.q.trim().length > 0) { /* ilike */ }
  if (opts.favoritesOnly) { query = query.eq('is_favorite', true); }
  const { data, error } = await query.order('created_at', { ascending: false });
  // ...
  return (data ?? []) as RecipeRow[];
}
```

<!-- RecipeCard (apps/mobile/src/components/recipes/RecipeCard.tsx) renders ONLY: -->
<!-- id, title, description, total/prep/cook_time_minutes, servings, image_url, -->
<!-- is_favorite, labels, calories/protein/fat_grams_per_serving, source_type, -->
<!-- created_at, AND recipe.ingredients (passed to useGeneratedRecipeImage for the -->
<!-- cold-gen fingerprint). It does NOT read recipe.steps or recipe.step_image_urls. -->

<!-- GET / handler (packages/server/src/routes/recipes.ts:62-79) -->
```typescript
recipes.get('/', async (c) => {
  const supabase = c.get('supabase');
  const user = c.get('user');
  const q = c.req.query('q');
  const favorites = c.req.query('favorites');
  try {
    const data = await getRecipes(supabase, user.id, { q, favoritesOnly: favorites === 'true' });
    return c.json({ data });
  } catch (error) { /* 500 */ }
});
```
</interfaces>

<correctness_guard>
CRITICAL — do NOT break detail or search:
- Recipe DETAIL reads from the SAME mobile `recipes` array as the list
  (apps/mobile/src/app/recipes/[id]/index.tsx:34 `recipes.find(r => r.id === id)`;
  renders `recipe.steps.map` line 332). Dropping `steps` from the list payload makes
  detail show empty steps UNTIL the client re-hydrates. The client-side hydration
  fetch is added in Plan 28-03 (detail GET /:id → mergeRecipeLocal). This plan ONLY
  trims the LIST query; getRecipeById (recipeStore.ts:206-224) KEEPS its full `select()`
  so the detail re-fetch returns full steps. DO NOT touch getRecipeById's select.
- Client search + filters run in a useMemo over the FULL in-memory `recipes` array
  (kitchen.tsx:425-440). Therefore O1 is a payload TRIM + generous LIMIT, NOT incremental
  client paging. Keep "load-all (capped)" so client search/offline cache stay intact.
- KEEP `ingredients` in the list column set — useGeneratedRecipeImage fingerprints on it
  for any remaining cold-gen path, and detail falls back to it before re-hydration.
</correctness_guard>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getRecipes — explicit lightweight column set + LIMIT + timing</name>
  <files>packages/server/src/services/recipeStore.ts, packages/server/src/services/__tests__/recipeStore.test.ts</files>
  <read_first>
    packages/server/src/services/recipeStore.ts:114-156 (GetRecipesOptions + getRecipes).
    packages/server/src/services/__tests__/recipeStore.test.ts:8-120 (makeChain pattern + existing getRecipes assertions — they assert `chain.select` toHaveBeenCalled with NO args today; you will change select to take a column string).
  </read_first>
  <behavior>
    - Test: getRecipes calls `.select(RECIPE_LIST_COLUMNS)` where the string includes 'id','title','ingredients','image_url' and EXCLUDES 'steps' and 'step_image_urls'.
    - Test: getRecipes applies `.limit(RECIPE_LIST_LIMIT)` (a number, e.g. 200) before/after order — assert chain.limit called with 200.
    - Test: existing behavior preserved — `.eq('profile_id', ...)`, ilike on q (escaped), eq('is_favorite', true) on favoritesOnly, order created_at desc all still asserted.
    - Test: getRecipes returns `{ rows, queryMs, rowCount }` (or rows unchanged + a sibling timing export) — pick ONE shape and make the route consume it. Assert rowCount === rows.length.
  </behavior>
  <action>
    In packages/server/src/services/recipeStore.ts:
    1. Add an exported const column list near the top of the file (after RecipeRow):
       `export const RECIPE_LIST_COLUMNS = 'id, profile_id, title, description, ingredients, prep_time_minutes, cook_time_minutes, total_time_minutes, servings, source_type, source_url, image_url, is_favorite, labels, calories_per_serving, protein_grams_per_serving, fat_grams_per_serving, difficulty, practiced_skills, skill_note, created_at, updated_at';`
       — explicitly OMITS `steps` and `step_image_urls` (the large JSONB). Keep `ingredients` (needed by image fingerprint + detail fallback).
    2. Add `export const RECIPE_LIST_LIMIT = 200;` (generous cap — bounds worst-case payload without breaking client-side search over the full library; the typical user has <100 recipes).
    3. Change getRecipes to return timing + rows. New return type:
       `Promise<{ rows: RecipeRow[]; queryMs: number; rowCount: number }>`.
       Implementation: `const query = supabase.from('recipes').select(RECIPE_LIST_COLUMNS).eq('profile_id', profileId);` then the existing q/favorites branches, then `const t0 = Date.now(); const { data, error } = await query.order('created_at', { ascending: false }).limit(RECIPE_LIST_LIMIT); const queryMs = Date.now() - t0;` Throw on error as today. Return `{ rows: (data ?? []) as RecipeRow[], queryMs, rowCount: (data ?? []).length }`.
    4. Update ALL internal callers of getRecipes in recipeStore.ts (none — it's only called from routes). Confirm via grep that recipes.ts /search (line 255) and /discover (line 355) call `getRecipes(...)` and use `.map(r => r.title)` — those MUST be updated to read `.rows`. Update them: `const library = await getRecipes(supabase, user.id); const existingTitles = library.rows.map((r) => r.title);` in BOTH /search and /discover.
    Note: `select(RECIPE_LIST_COLUMNS)` keeps the RecipeRow shape minus steps/step_image_urls. The RecipeRow type still declares those fields; callers that don't read them are unaffected. Do not change RecipeRow.
  </action>
  <verify>
    <automated>cd packages/server && pnpm vitest run src/services/__tests__/recipeStore.test.ts</automated>
  </verify>
  <done>
    getRecipes selects RECIPE_LIST_COLUMNS (no steps/step_image_urls), applies LIMIT 200, returns { rows, queryMs, rowCount }; /search + /discover updated to read .rows; recipeStore.test.ts green with new column + limit assertions.
  </done>
</task>

<task type="auto">
  <name>Task 2: GET /recipes sub-stage timing log + route wiring</name>
  <files>packages/server/src/routes/recipes.ts, packages/server/src/routes/__tests__/recipes.get.test.ts</files>
  <read_first>
    packages/server/src/routes/recipes.ts:62-79 (GET / handler).
    packages/server/src/middleware/requestLogging.ts (the existing one-JSON-line-per-request logger emits total latency_ms — this task ADDS a second structured line for sub-stage detail; do not modify requestLogging.ts).
    packages/server/src/routes/__tests__/recipes.get.test.ts (existing GET test — update the getRecipes mock to return the new { rows, queryMs, rowCount } shape).
  </read_first>
  <action>
    In packages/server/src/routes/recipes.ts GET / handler:
    1. Destructure the new shape: `const { rows, queryMs, rowCount } = await getRecipes(supabase, user.id, { q, favoritesOnly: favorites === 'true' });`
    2. Compute payload bytes from the serialized response: `const json = JSON.stringify({ data: rows }); const payloadBytes = Buffer.byteLength(json, 'utf8');`
    3. Emit ONE structured stdout line (mirrors requestLogging.ts JSON-line convention) with the request_id for correlation:
       ```
       console.log(JSON.stringify({
         stage: 'recipes.list',
         request_id: c.get('request_id') ?? null,
         profile_id: user.id,
         db_query_ms: queryMs,
         row_count: rowCount,
         payload_bytes: payloadBytes,
       }));
       ```
    4. Return the already-serialized body to avoid double-stringify: `return c.body(json, 200, { 'Content-Type': 'application/json' });` (keeps the `{ data }` contract byte-identical to the old `c.json({ data })`).
    5. Update recipes.get.test.ts: the getRecipes mock must now resolve `{ rows: [...], queryMs: 5, rowCount: N }` instead of an array; assert the response body still has `{ data: [...] }` and (optionally) that console.log was called with a string containing 'recipes.list'.
  </action>
  <verify>
    <automated>cd packages/server && pnpm vitest run src/routes/__tests__/recipes.get.test.ts src/routes/__tests__/recipes.search.test.ts src/routes/__tests__/recipes.discover.test.ts</automated>
  </verify>
  <done>
    GET /recipes logs a `recipes.list` JSON line with db_query_ms + row_count + payload_bytes; response body still `{ data }`; get/search/discover route tests green (mocks updated to .rows shape).
  </done>
</task>

</tasks>

<verification>
- `cd packages/server && pnpm vitest run src/services/__tests__/recipeStore.test.ts src/routes/__tests__/recipes.get.test.ts src/routes/__tests__/recipes.search.test.ts src/routes/__tests__/recipes.discover.test.ts` → all green.
- `grep -n "RECIPE_LIST_COLUMNS" packages/server/src/services/recipeStore.ts` → defined + used in getRecipes select; string does NOT contain 'steps' or 'step_image_urls'.
- `grep -n "payload_bytes\|recipes.list" packages/server/src/routes/recipes.ts` → present in GET / handler.
- `grep -n "library.rows\|getRecipes(" packages/server/src/routes/recipes.ts` → /search + /discover read `.rows`.
- getRecipeById select() UNCHANGED (still full select) — `grep -n "getRecipeById" packages/server/src/services/recipeStore.ts` then confirm its `.select()` has no column arg.
</verification>

<success_criteria>
- T1 satisfied: GET /recipes emits sub-stage timing (DB query ms, row count, payload bytes).
- O1 (partial — server half) satisfied: list query is an explicit lightweight column set excluding steps/step_image_urls, with a LIMIT; ingredients retained; detail's getRecipeById still full.
- No regression: recipe service + route test suites pass.
</success_criteria>

<output>
After completion, create `.planning/phases/28-recipe-load-telemetry-performance-10s-3-5s/28-01-SUMMARY.md`
</output>
