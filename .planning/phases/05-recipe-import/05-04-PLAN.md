---
phase: 05-recipe-import
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - apps/mobile/src/app/recipes/import.tsx
  - apps/mobile/src/app/recipes/import-url.tsx
  - apps/mobile/src/app/recipes/import-photo.tsx
  - apps/mobile/src/app/recipes/import-manual.tsx
  - apps/mobile/src/app/recipes/review.tsx
  - apps/mobile/src/app/recipes/_layout.tsx
  - apps/mobile/src/app/(tabs)/recipes.tsx
  - apps/mobile/src/components/recipes/RecipeCard.tsx
  - apps/mobile/src/components/recipes/IngredientList.tsx
autonomous: false
requirements: [RECP-01, RECP-02, RECP-03, RECP-04, RECP-05]
must_haves:
  truths:
    - "User can navigate to recipe import from the Recipes tab"
    - "User can paste a URL and see a parsed recipe for review"
    - "User can take a photo and see a parsed recipe for review"
    - "User can type freeform text and see a parsed recipe for review"
    - "User can review, edit, and save an imported recipe"
    - "Saved recipes appear in the Recipes tab list"
  artifacts:
    - path: "apps/mobile/src/app/recipes/import.tsx"
      provides: "Import method picker screen (URL / Photo / Manual)"
      min_lines: 30
    - path: "apps/mobile/src/app/recipes/import-url.tsx"
      provides: "URL paste input screen with import action"
      min_lines: 40
    - path: "apps/mobile/src/app/recipes/import-photo.tsx"
      provides: "Photo capture screen using expo-image-picker"
      min_lines: 40
    - path: "apps/mobile/src/app/recipes/import-manual.tsx"
      provides: "Freeform text entry screen with AI parse action"
      min_lines: 40
    - path: "apps/mobile/src/app/recipes/review.tsx"
      provides: "Review/edit screen for parsed recipe before saving"
      min_lines: 80
    - path: "apps/mobile/src/app/(tabs)/recipes.tsx"
      provides: "Recipe list with import FAB button"
      min_lines: 50
    - path: "apps/mobile/src/components/recipes/RecipeCard.tsx"
      provides: "Recipe card component for list display"
      min_lines: 30
  key_links:
    - from: "apps/mobile/src/app/(tabs)/recipes.tsx"
      to: "apps/mobile/src/stores/recipeStore.ts"
      via: "useRecipeStore().fetchRecipes on mount"
      pattern: "useRecipeStore"
    - from: "apps/mobile/src/app/recipes/import-url.tsx"
      to: "apps/mobile/src/stores/recipeStore.ts"
      via: "useRecipeStore().importFromUrl"
      pattern: "importFromUrl"
    - from: "apps/mobile/src/app/recipes/review.tsx"
      to: "apps/mobile/src/stores/recipeStore.ts"
      via: "useRecipeStore().saveRecipe"
      pattern: "saveRecipe"
    - from: "apps/mobile/src/app/recipes/import.tsx"
      to: "apps/mobile/src/app/recipes/import-url.tsx"
      via: "router.push navigation"
      pattern: "router\\.push.*import-url"
---

<objective>
Build the complete recipe import UI flow: method picker, three import screens, review/edit screen, and recipe list tab.

Purpose: This is the user-facing layer that makes recipe import accessible. Three entry points (URL, photo, manual) all converge on a shared review screen before saving.
Output: Complete import flow UI, recipe list tab, visual verification.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recipe-import/05-RESEARCH.md
@.planning/phases/05-recipe-import/05-03-SUMMARY.md

@apps/mobile/src/app/(tabs)/recipes.tsx
@apps/mobile/src/app/scan/
@apps/mobile/src/stores/recipeStore.ts
@apps/mobile/src/types/recipe.ts
</context>

<interfaces>
<!-- Recipe store from Plan 03 -->
From apps/mobile/src/stores/recipeStore.ts:
```typescript
export const useRecipeStore = create<RecipeState>((set, get) => ({
  recipes: Recipe[],
  isLoading: boolean,
  isImporting: boolean,
  error: string | null,
  importedRecipe: ParsedRecipe | null,
  isDuplicate: boolean,
  existingRecipe: Recipe | null,

  importFromUrl: async (url: string) => void,
  importFromPhoto: async (base64: string) => void,
  importFromText: async (text: string) => void,
  saveRecipe: async (recipe: ParsedRecipe) => void,
  fetchRecipes: async () => void,
  clearImport: () => void,
}));
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create recipe route group layout and import flow screens</name>
  <files>apps/mobile/src/app/recipes/_layout.tsx, apps/mobile/src/app/recipes/import.tsx, apps/mobile/src/app/recipes/import-url.tsx, apps/mobile/src/app/recipes/import-photo.tsx, apps/mobile/src/app/recipes/import-manual.tsx</files>
  <action>
Create apps/mobile/src/app/recipes/_layout.tsx:
- Stack navigator layout for recipe sub-screens (same pattern as scan/_layout.tsx)
- Screens: import, import-url, import-photo, import-manual, review

Create apps/mobile/src/app/recipes/import.tsx:
- Import method picker with three large tappable cards:
  1. "Paste URL" with link icon -- navigates to import-url
  2. "Take Photo" with camera icon -- navigates to import-photo
  3. "Type It In" with edit icon -- navigates to import-manual
- Use NativeWind styling, warm palette matching existing app
- Header: "Import Recipe" with back navigation

Create apps/mobile/src/app/recipes/import-url.tsx:
- TextInput for pasting URL with placeholder "https://example.com/recipe"
- "Import" button that calls useRecipeStore().importFromUrl(url)
- Loading state while isImporting is true (ActivityIndicator)
- Error display if error state is set
- Duplicate handling: if isDuplicate is true, show message "This recipe is already in your collection" with option to view existing or re-import
- On successful import (importedRecipe is set), navigate to review screen via router.push('/recipes/review')

Create apps/mobile/src/app/recipes/import-photo.tsx:
- Follow scan flow camera pattern: launch expo-image-picker with launchCameraAsync (quality: 0.8)
- Also offer "Choose from Library" option using launchImageLibraryAsync
- On image captured, convert to base64 and call useRecipeStore().importFromPhoto(base64)
- Loading state with "Extracting recipe..." message while isImporting
- On success, navigate to review screen

Create apps/mobile/src/app/recipes/import-manual.tsx:
- Large multiline TextInput for freeform recipe text
- Placeholder: "Paste or type your recipe here...\n\nExample:\nPasta Carbonara\n1 lb spaghetti\n4 eggs\n..."
- "Parse Recipe" button that calls useRecipeStore().importFromText(text)
- Loading state while isImporting
- On success, navigate to review screen
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && npx tsc --noEmit 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>Recipe route layout and all four import screens compile. Navigation flow: recipes tab -> import picker -> (url|photo|manual) -> review.</done>
</task>

<task type="auto">
  <name>Task 2: Create review screen, recipe card, and update recipes tab</name>
  <files>apps/mobile/src/app/recipes/review.tsx, apps/mobile/src/components/recipes/RecipeCard.tsx, apps/mobile/src/components/recipes/IngredientList.tsx, apps/mobile/src/app/(tabs)/recipes.tsx</files>
  <action>
Create apps/mobile/src/app/recipes/review.tsx:
- Reads importedRecipe from useRecipeStore()
- Displays all parsed fields in editable form:
  - Title: TextInput (editable)
  - Description: TextInput multiline (editable)
  - Ingredients: FlatList of ingredient rows, each showing quantity + unit + name + notes. Tappable to edit individual fields.
  - Steps: numbered list of TextInput fields (editable). Add/remove step buttons.
  - Times: prep, cook, total as numeric inputs
  - Servings: numeric input
  - Source: read-only display of source_type and source_url
- "Save Recipe" button at bottom that calls useRecipeStore().saveRecipe(editedRecipe)
- On save success, navigate back to recipes tab via router.replace('/(tabs)/recipes')
- If importedRecipe is null (navigated here without importing), show empty state and redirect back

Create apps/mobile/src/components/recipes/RecipeCard.tsx:
- Compact card showing: title, source_type badge (URL/Photo/Manual), cook time, servings count
- Tappable (onPress prop for future navigation to detail view -- just log for now)
- NativeWind styling: rounded-xl, shadow-sm, bg-white, padding, matching SuggestionCard style

Create apps/mobile/src/components/recipes/IngredientList.tsx:
- Renders ParsedIngredient[] as formatted list
- Each row: "{quantity} {unit} {name}" with notes in lighter text
- Handles null quantity/unit gracefully (just shows name)

Update apps/mobile/src/app/(tabs)/recipes.tsx:
- Replace placeholder content with actual recipe list
- On mount, call useRecipeStore().fetchRecipes() via useEffect
- FlatList of RecipeCard components
- Empty state: "No recipes yet. Import your first recipe!" with button navigating to /recipes/import
- Floating action button (FAB) in bottom-right: "+" icon that navigates to /recipes/import
- Follow same FAB pattern as ScanButton in pantry tab
- Loading state with ActivityIndicator while isLoading
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && npx tsc --noEmit 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>Review screen allows editing all recipe fields before saving. Recipe tab shows saved recipes in a list with import FAB. RecipeCard and IngredientList components render correctly. All files compile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete recipe import flow end-to-end</name>
  <action>
Human verifies the complete recipe import flow built in Tasks 1-2, connected to the server API from Plan 02 and store from Plan 03.

Steps to verify:
1. Open the app and navigate to the Recipes tab
2. Verify empty state shows "No recipes yet" with import prompt
3. Tap the "+" FAB button to open import method picker
4. Test URL import: paste a recipe URL (try https://www.allrecipes.com or similar), verify parsed recipe appears on review screen with title, ingredients, steps
5. Test photo import: take a photo of a cookbook page or recipe card, verify parsed recipe appears on review screen
6. Test manual import: type or paste a recipe in freeform text, verify AI parses it into structured format on review screen
7. On review screen: verify all fields are editable (title, description, ingredients, steps, times, servings)
8. Save a recipe and verify it appears in the Recipes tab list
9. Verify RecipeCard shows title, source type badge, and time info
  </action>
  <verify>Human confirms all import methods work and recipes save correctly</verify>
  <done>All three import paths (URL, photo, manual) produce parseable recipes. Review screen allows editing. Saved recipes appear in the list. User approves.</done>
</task>

</tasks>

<verification>
- All TypeScript files compile without errors
- Recipe import flow navigates correctly through all screens
- All three import methods produce editable parsed recipes
- Save persists to database and recipe appears in list
</verification>

<success_criteria>
User can import a recipe via URL, photo, or manual text entry, review/edit it, save it, and see it in their recipe collection. Complete end-to-end flow verified by human.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recipe-import/05-04-SUMMARY.md`
</output>
