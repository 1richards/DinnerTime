---
phase: 04-fridge-to-dinner-suggestions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/types/suggestions.ts
  - apps/mobile/src/stores/suggestionsStore.ts
  - apps/mobile/src/stores/__tests__/suggestionsStore.test.ts
autonomous: true
requirements: [MEAL-01, MEAL-04]

must_haves:
  truths:
    - "Suggestions store can fetch suggestions via authenticated API call"
    - "Suggestions store tracks loading, error, and data states"
    - "Suggestion type definitions match the server response schema"
  artifacts:
    - path: "apps/mobile/src/types/suggestions.ts"
      provides: "DinnerSuggestion and SuggestionsResponse types"
      exports: ["DinnerSuggestion", "SuggestionsResponse"]
    - path: "apps/mobile/src/stores/suggestionsStore.ts"
      provides: "Zustand store for fetching and holding suggestions"
      exports: ["useSuggestionsStore"]
    - path: "apps/mobile/src/stores/__tests__/suggestionsStore.test.ts"
      provides: "Unit tests for suggestions store"
      min_lines: 40
  key_links:
    - from: "apps/mobile/src/stores/suggestionsStore.ts"
      to: "/api/v1/ai/suggest"
      via: "fetch with Bearer token"
      pattern: "fetch.*api/v1/ai/suggest"
    - from: "apps/mobile/src/stores/suggestionsStore.ts"
      to: "apps/mobile/src/types/suggestions.ts"
      via: "import types"
      pattern: "import.*suggestions"
---

<objective>
Create the mobile-side types and Zustand store for dinner suggestions, following the exact patterns from pantryStore.ts.

Purpose: Provides the state management layer for the suggestions UI (Plan 03) to consume.
Output: Type definitions and a working Zustand store with fetch, loading, error, and clear states.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fridge-to-dinner-suggestions/04-RESEARCH.md

<interfaces>
<!-- Patterns to follow exactly from existing codebase -->

From apps/mobile/src/stores/pantryStore.ts (auth + fetch pattern):
```typescript
const getApiBaseUrl = (): string => {
  return process.env.EXPO_PUBLIC_API_URL ?? 'http://localhost:3000';
};

const getAuthToken = async (): Promise<string> => {
  const { data, error } = await supabase.auth.getSession();
  if (error || !data.session) throw new Error('Not authenticated');
  return data.session.access_token;
};

// Usage in store action:
const token = await getAuthToken();
const response = await fetch(`${getApiBaseUrl()}/api/v1/pantry/scan`, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${token}`,
  },
  body: JSON.stringify({}),
});
```

From apps/mobile/src/lib/supabase.ts:
```typescript
import { supabase } from '../lib/supabase';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create suggestion types</name>
  <files>apps/mobile/src/types/suggestions.ts</files>
  <behavior>
    - DinnerSuggestion has: title, description, ingredients_used (string[]), ingredients_needed (string[]), estimated_time_minutes (number), difficulty ('easy'|'medium'|'hard'), kid_friendly (boolean), cuisine_type (string), why_suggested (string)
    - SuggestionsResponse has: suggestions (DinnerSuggestion[]), pantry_item_count (number), generated_at (string)
  </behavior>
  <action>Create apps/mobile/src/types/suggestions.ts with DinnerSuggestion interface and SuggestionsResponse interface. These must exactly match the server-side tool_use schema from the research document. All fields are required (no optionals).</action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Type file exists with both exported interfaces matching server response schema</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create suggestions Zustand store with tests</name>
  <files>apps/mobile/src/stores/suggestionsStore.ts, apps/mobile/src/stores/__tests__/suggestionsStore.test.ts</files>
  <behavior>
    - fetchSuggestions() sets isLoading=true, calls POST /api/v1/ai/suggest with Bearer token, sets suggestions + pantryItemCount + generatedAt on success, sets isLoading=false
    - fetchSuggestions() on API error sets error message and isLoading=false
    - fetchSuggestions() on 400 (too few items) sets specific error from server
    - clearSuggestions() resets suggestions to [], error to null
    - Store has: suggestions (DinnerSuggestion[]), isLoading (boolean), error (string|null), pantryItemCount (number), generatedAt (string|null)
  </behavior>
  <action>
    Create suggestionsStore.ts following the pantryStore.ts pattern exactly:
    - Import supabase from ../lib/supabase
    - Reuse the getApiBaseUrl and getAuthToken helper pattern (define locally in the file, same as pantryStore)
    - SuggestionsState interface with: suggestions, isLoading, error, pantryItemCount, generatedAt
    - fetchSuggestions() action: POST to /api/v1/ai/suggest, parse response.data into state
    - clearSuggestions() action: reset to initial state
    - Do NOT use React Query -- keep it pure Zustand to match pantryStore pattern

    Create suggestionsStore.test.ts:
    - Mock fetch globally (vi.stubGlobal)
    - Mock supabase auth (vi.mock('../lib/supabase'))
    - Test: fetchSuggestions sets loading then sets suggestions on success
    - Test: fetchSuggestions sets error on API failure
    - Test: clearSuggestions resets state
    - Follow the vi.hoisted pattern from vision.test.ts for mock setup
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && pnpm vitest run src/stores/__tests__/suggestionsStore.test.ts -x</automated>
  </verify>
  <done>All store tests pass, store exports useSuggestionsStore with fetchSuggestions and clearSuggestions</done>
</task>

</tasks>

<verification>
cd /Users/patrickrichards/DinnerTime/apps/mobile && pnpm vitest run src/stores/__tests__/suggestionsStore.test.ts -x
</verification>

<success_criteria>
- DinnerSuggestion and SuggestionsResponse types exported and match server schema
- useSuggestionsStore works with fetchSuggestions and clearSuggestions
- All store tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-fridge-to-dinner-suggestions/04-02-SUMMARY.md`
</output>
