---
phase: 04-fridge-to-dinner-suggestions
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - apps/mobile/src/components/suggestions/SuggestionCard.tsx
  - apps/mobile/src/components/suggestions/SuggestionSkeleton.tsx
  - apps/mobile/src/components/suggestions/SuggestionList.tsx
  - apps/mobile/src/app/(tabs)/index.tsx
  - apps/mobile/src/app/scan/review.tsx
autonomous: false

requirements: [MEAL-01, MEAL-02, MEAL-03, MEAL-04]

must_haves:
  truths:
    - "User sees dinner suggestion cards on the home tab after tapping Get Dinner Ideas"
    - "Each suggestion card shows title, description, time, difficulty, ingredients used/needed, and kid-friendly badge"
    - "Loading state shows skeleton placeholders while Claude generates suggestions"
    - "Empty pantry state shows friendly message with scan prompt instead of calling API"
    - "After confirming a pantry scan, user is offered Get Dinner Ideas flow"
    - "Error state shows retry option"
  artifacts:
    - path: "apps/mobile/src/components/suggestions/SuggestionCard.tsx"
      provides: "Individual meal suggestion card component"
      min_lines: 30
    - path: "apps/mobile/src/components/suggestions/SuggestionSkeleton.tsx"
      provides: "Loading placeholder skeleton for suggestion cards"
      min_lines: 15
    - path: "apps/mobile/src/components/suggestions/SuggestionList.tsx"
      provides: "Scrollable list of suggestion cards with loading/empty/error states"
      min_lines: 30
    - path: "apps/mobile/src/app/(tabs)/index.tsx"
      provides: "Home tab with suggestions UI replacing placeholder"
      min_lines: 40
    - path: "apps/mobile/src/app/scan/review.tsx"
      provides: "Post-scan alert offering dinner ideas navigation"
  key_links:
    - from: "apps/mobile/src/app/(tabs)/index.tsx"
      to: "apps/mobile/src/stores/suggestionsStore.ts"
      via: "useSuggestionsStore hook"
      pattern: "useSuggestionsStore"
    - from: "apps/mobile/src/app/(tabs)/index.tsx"
      to: "apps/mobile/src/components/suggestions/SuggestionList.tsx"
      via: "component import"
      pattern: "SuggestionList"
    - from: "apps/mobile/src/app/scan/review.tsx"
      to: "(tabs)/index"
      via: "router.replace after scan confirm"
      pattern: "router\\.replace.*tabs"
---

<objective>
Build the suggestions UI on the home tab and wire up the post-scan "Get Dinner Ideas" flow.

Purpose: This is the user-facing experience of Phase 4 -- seeing AI-generated dinner ideas and the seamless fridge-to-dinner flow.
Output: Working home tab with suggestion cards, loading/error/empty states, and post-scan navigation.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fridge-to-dinner-suggestions/04-RESEARCH.md
@.planning/phases/04-fridge-to-dinner-suggestions/04-01-SUMMARY.md
@.planning/phases/04-fridge-to-dinner-suggestions/04-02-SUMMARY.md

<interfaces>
<!-- Types created in Plan 02 -->
From apps/mobile/src/types/suggestions.ts:
```typescript
export interface DinnerSuggestion {
  title: string;
  description: string;
  ingredients_used: string[];
  ingredients_needed: string[];
  estimated_time_minutes: number;
  difficulty: 'easy' | 'medium' | 'hard';
  kid_friendly: boolean;
  cuisine_type: string;
  why_suggested: string;
}

export interface SuggestionsResponse {
  suggestions: DinnerSuggestion[];
  pantry_item_count: number;
  generated_at: string;
}
```

From apps/mobile/src/stores/suggestionsStore.ts:
```typescript
export const useSuggestionsStore: {
  suggestions: DinnerSuggestion[];
  isLoading: boolean;
  error: string | null;
  pantryItemCount: number;
  generatedAt: string | null;
  fetchSuggestions: () => Promise<void>;
  clearSuggestions: () => void;
}
```

From apps/mobile/src/stores/pantryStore.ts:
```typescript
export const usePantryStore: {
  items: PantryItem[];
  loadItems: (profileId: string) => Promise<void>;
}
```

From apps/mobile/src/stores/authStore.ts:
```typescript
export const useAuthStore: {
  profile: { id: string; display_name: string; } | null;
}
```

From apps/mobile/src/components/ui/Button.tsx:
```typescript
export function Button({ title, onPress, variant, loading, disabled, className }: ButtonProps)
// variants: 'primary' | 'outline' | 'ghost'
```

Styling: NativeWind (Tailwind classes). Warm palette: bg-warmWhite, text-warmGray-900, bg-primary (orange).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build suggestion card components</name>
  <files>apps/mobile/src/components/suggestions/SuggestionCard.tsx, apps/mobile/src/components/suggestions/SuggestionSkeleton.tsx, apps/mobile/src/components/suggestions/SuggestionList.tsx</files>
  <action>
    Create the suggestions/ component directory with three components:

    **SuggestionCard.tsx** -- Renders a single DinnerSuggestion:
    - White card with rounded corners and shadow (bg-white rounded-xl p-4 shadow-sm)
    - Title (text-lg font-bold), cuisine tag, and difficulty badge in header row
    - Description text (text-sm text-warmGray-600)
    - Clock icon + estimated_time_minutes display (e.g., "30 min")
    - Kid-friendly badge (yellow pill) shown only when kid_friendly=true
    - "Using from pantry:" section listing ingredients_used as green chips
    - "May need:" section listing ingredients_needed as orange chips (only if non-empty)
    - why_suggested as italic footer text (text-xs text-warmGray-400)
    - Use Text/View only (no external icon library) -- use emoji for clock, kid, difficulty indicators

    **SuggestionSkeleton.tsx** -- Loading placeholder:
    - 3 skeleton cards with animated opacity pulse (use Animated API from react-native for simple opacity animation)
    - Same card dimensions as SuggestionCard for visual consistency
    - Gray rounded rectangles for title, description, chips areas

    **SuggestionList.tsx** -- Container component:
    - Props: none (reads from useSuggestionsStore directly)
    - Loading state: show SuggestionSkeleton
    - Error state: show error message with "Try Again" button that calls fetchSuggestions
    - Empty state (no suggestions fetched yet): show welcome message with "Get Dinner Ideas" button
    - Data state: FlatList of SuggestionCard components
    - Also check pantry items count -- if usePantryStore.items.length < 3, show "Scan your fridge first" message with navigation to pantry tab instead of fetch button
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Three component files exist, TypeScript compiles, components handle all states (loading, error, empty, data, insufficient pantry)</done>
</task>

<task type="auto">
  <name>Task 2: Wire home tab and post-scan navigation</name>
  <files>apps/mobile/src/app/(tabs)/index.tsx, apps/mobile/src/app/scan/review.tsx</files>
  <action>
    **Home tab (index.tsx)** -- Replace placeholder with suggestions UI:
    - Keep SafeAreaView with bg-warmWhite
    - Header: greeting with display_name (existing pattern) + gear icon to settings (existing)
    - Body: render SuggestionList component (handles all states internally)
    - On mount: load pantry items if not already loaded (call usePantryStore.loadItems with profile.id)
    - Do NOT auto-fetch suggestions on mount -- user triggers via button in SuggestionList
    - When navigated to from scan review (MEAL-04), suggestions should auto-trigger: check if route has a query param or use a Zustand flag. Simplest approach: add an `autoFetch` flag on suggestionsStore that scan/review sets before navigating, and SuggestionList checks it on mount via useEffect.

    **Scan review (review.tsx)** -- Add post-scan dinner ideas flow (MEAL-04):
    - After successful confirmScan, replace the current `router.replace('/(tabs)/pantry')` with an Alert:
      - Title: "Pantry Updated!"
      - Message: "{acceptedCount} items added. Want dinner ideas?"
      - Buttons: "Later" (navigates to /(tabs)/pantry), "Get Dinner Ideas" (sets suggestionsStore autoFetch=true, navigates to /(tabs)/)
    - Import useSuggestionsStore for setting the autoFetch flag
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Home tab renders SuggestionList, post-scan offers dinner ideas flow, TypeScript compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete fridge-to-dinner flow</name>
  <action>Human verifies the complete suggestion flow end-to-end.</action>
  <what-built>Complete fridge-to-dinner suggestions flow: home tab with AI-powered dinner suggestion cards (loading, error, empty, and data states), plus post-scan "Get Dinner Ideas" navigation from scan review screen.</what-built>
  <how-to-verify>
    1. Start the dev server: cd packages/server && pnpm dev
    2. Start the mobile app in Expo dev client
    3. Navigate to Home tab -- should see welcome message with "Get Dinner Ideas" button (or "Scan your fridge first" if pantry is empty)
    4. If pantry has items: tap "Get Dinner Ideas" -- should show skeleton loading, then suggestion cards
    5. Each card should show: title, description, time, difficulty, cuisine, ingredients from pantry (green), ingredients needed (orange), why suggested
    6. If household has kids: at least one suggestion should show kid-friendly badge
    7. Do a pantry scan: after confirming items, should see alert offering "Get Dinner Ideas"
    8. Tap "Get Dinner Ideas" from post-scan alert -- should navigate to home and auto-fetch suggestions
    9. Test error state: stop the server and try fetching -- should show error with retry button
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript compiles: cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json
2. Server tests pass: cd /Users/patrickrichards/DinnerTime/packages/server && pnpm test
3. Mobile tests pass: cd /Users/patrickrichards/DinnerTime/apps/mobile && pnpm test
4. Full suite: cd /Users/patrickrichards/DinnerTime && pnpm -r test
</verification>

<success_criteria>
- Home tab shows suggestion cards with all required fields
- Loading skeleton displays while waiting for Claude response
- Empty pantry state shows scan prompt (not a broken API call)
- Post-scan flow offers dinner ideas navigation (MEAL-04)
- Error state shows retry option
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-fridge-to-dinner-suggestions/04-03-SUMMARY.md`
</output>
