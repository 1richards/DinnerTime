---
phase: 20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/shopping/__fixtures__/shopping-list.ts
  - apps/mobile/src/shopping/__tests__/telemetry.test.ts
  - apps/mobile/src/shopping/__tests__/openInstacartCart.test.ts
  - apps/mobile/src/shopping/__tests__/classifyHandoffError.test.ts
  - apps/mobile/src/components/shopping/__tests__/HandoffSheet.test.tsx
  - apps/mobile/src/stores/__tests__/settingsStore.test.ts
  - apps/mobile/src/stores/settingsStore.ts
  - packages/server/src/routes/__tests__/telemetry.test.ts
  - packages/server/src/__tests__/migrations.test.ts
  - supabase/migrations/00024_shopping_events.sql
  - .planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/DEVICE-TEST-20.md
autonomous: true
requirements:
  - SHOP-DC-01
  - SHOP-DC-02
  - SHOP-DC-03
  - SHOP-DC-04
  - SHOP-DC-05
  - SHOP-DC-06

must_haves:
  truths:
    - "All 5 Wave 0 mobile test files exist and fail with clear 'expected / actual' diffs, not 'cannot find module' errors — production stubs exist just enough to import"
    - "migrations.test.ts extended with 00024 assertions fails red because the migration file asserts real contracts (RLS policies, FK to auth.users, event_type TEXT, session_id NOT NULL)"
    - "settingsStore exists as a persisted Zustand store with shoppingHandoffMode default 'draft_cart' and a setter; all existing tests still pass"
    - "shopping_events Supabase migration 00024 mirrors 00020_cooking_events.sql exactly, RLS policies present, no UPDATE/DELETE policies (append-only)"
    - "DEVICE-TEST-20.md exists with checklist covering universal link → Instacart app, items actually appearing in cart, feature-flag rollback"
  artifacts:
    - path: "apps/mobile/src/shopping/__fixtures__/shopping-list.ts"
      provides: "test fixture with 3-5 items covering qty/unit/checked states for HandoffSheet + shopping.tsx tests"
    - path: "apps/mobile/src/shopping/__tests__/telemetry.test.ts"
      provides: "red stub — clones structure of cooking/telemetry.test.ts, asserts logShoppingEvent / flushShoppingTelemetry / sanitizePayload"
    - path: "apps/mobile/src/shopping/__tests__/openInstacartCart.test.ts"
      provides: "red stub — asserts Linking.openURL called first, WebBrowser.openBrowserAsync fallback on throw"
    - path: "apps/mobile/src/shopping/__tests__/classifyHandoffError.test.ts"
      provides: "red stub — maps network / 502 / 401 errors to 'network' | 'instacart_api' | 'auth' variants"
    - path: "apps/mobile/src/components/shopping/__tests__/HandoffSheet.test.tsx"
      provides: "red RNTL stub — renders 3 discriminated-union states (sending | success | error) + retry CTA"
    - path: "apps/mobile/src/stores/__tests__/settingsStore.test.ts"
      provides: "persistence + setter contract for shoppingHandoffMode"
    - path: "apps/mobile/src/stores/settingsStore.ts"
      provides: "Zustand persisted store with shoppingHandoffMode: 'draft_cart' | 'legacy'"
      exports: ["useSettingsStore"]
    - path: "packages/server/src/routes/__tests__/telemetry.test.ts"
      provides: "extended with POST /telemetry/shopping happy/empty/schema/auth/insert-failed cases"
    - path: "packages/server/src/__tests__/migrations.test.ts"
      provides: "extended with 00024_shopping_events static assertions"
    - path: "supabase/migrations/00024_shopping_events.sql"
      provides: "shopping_events table, RLS policies, indexes — clones 00020_cooking_events.sql"
      contains: "CREATE TABLE shopping_events"
    - path: ".planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/DEVICE-TEST-20.md"
      provides: "manual UAT checklist for physical iPhone handoff verification"
  key_links:
    - from: "apps/mobile/src/shopping/__tests__/telemetry.test.ts"
      to: "apps/mobile/src/shopping/telemetry.ts (created in 20-01)"
      via: "import { logShoppingEvent, flushShoppingTelemetry, sanitizePayload, __resetForTests } from '../telemetry'"
      pattern: "from ['\"]\\.\\./telemetry['\"]"
    - from: "apps/mobile/src/components/shopping/__tests__/HandoffSheet.test.tsx"
      to: "apps/mobile/src/components/shopping/HandoffSheet.tsx (created in 20-03)"
      via: "import { HandoffSheet } from '../HandoffSheet'"
      pattern: "from ['\"]\\.\\./HandoffSheet['\"]"
    - from: "packages/server/src/__tests__/migrations.test.ts"
      to: "supabase/migrations/00024_shopping_events.sql"
      via: "readMigration('00024_shopping_events.sql')"
      pattern: "00024_shopping_events"
---

<objective>
Wave 0 scaffolding for Phase 20. Lock the test contracts (7 new/extended test files) and data-layer foundations (00024 shopping_events migration, settingsStore with the feature flag) before any production code ships in Waves 1–2. Also drop DEVICE-TEST-20 skeleton so physical-iPhone UAT has a documented checklist from day one.

Purpose: This is a Nyquist wave — every downstream task in this phase has an `<automated>` command that must resolve to a real test file. Without Wave 0, waves 1–3 would be creating tests and implementation in the same commit, which violates the Nyquist contract laid out in 20-VALIDATION.md.

Output:
- 5 red mobile test stubs (telemetry, openInstacartCart, classifyHandoffError, HandoffSheet, settingsStore)
- 2 extended test files (server telemetry.test.ts with shopping channel cases; migrations.test.ts with 00024 assertions)
- 1 fixture file (shopping-list)
- 1 new Zustand store (settingsStore — minimal: flag + setter + persist middleware, default 'draft_cart')
- 1 new Supabase migration (00024_shopping_events — clones 00020_cooking_events)
- 1 DEVICE-TEST-20.md skeleton

This plan is the ONLY wave with new test files. Waves 1–3 flip red → green.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 20 planning artifacts
@.planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/20-CONTEXT.md
@.planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/20-RESEARCH.md
@.planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/20-VALIDATION.md

# Phase 16 telemetry pattern (clone target)
@apps/mobile/src/cooking/telemetry.ts
@apps/mobile/src/cooking/__tests__/telemetry.test.ts
@packages/server/src/routes/telemetry.ts
@packages/server/src/routes/__tests__/telemetry.test.ts
@supabase/migrations/00020_cooking_events.sql
@packages/server/src/__tests__/migrations.test.ts

# Existing Phase 8 surfaces
@apps/mobile/src/stores/shoppingStore.ts
@apps/mobile/src/app/(tabs)/shopping.tsx
@apps/mobile/src/types/shopping.ts

<interfaces>
<!-- Extracted from cooking telemetry so Wave 0 stubs can copy shapes 1:1 -->

From apps/mobile/src/cooking/telemetry.ts (clone target):
```typescript
export type CookingEventName =
  | 'stt_final' | 'stt_error' | 'intent_routed' | 'intent_route'
  | 'ask_start' | 'ask_first_chunk' | 'ask_complete' | 'ask_latency'
  | 'tts_echo_swallowed' | 'command_unrecognized' | (string & {});

interface LogInput {
  name: CookingEventName;
  session_id: string;
  recipe_id?: string | null;
  step_index?: number | null;
  payload?: Record<string, unknown>;
}

export function wireSupabaseAuth(getter: TokenGetter): void;
export function sanitizePayload(dirty: Record<string, unknown>): Record<string, unknown>;
export function logCookingEvent(e: LogInput): void;
export async function flushTelemetry(): Promise<void>;
export const __resetForTests: () => void & {
  getQueueLength: () => number;
  setTokenGetter: (fn: TokenGetter) => void;
};
```

Shopping-side analog (target for Wave 1 plan 20-01):
```typescript
// apps/mobile/src/shopping/telemetry.ts — to be built in 20-01
export type ShoppingEventName =
  | 'shopping.draft_cart_started'
  | 'shopping.draft_cart_succeeded'
  | 'shopping.draft_cart_failed'
  | 'shopping.handoff_opened_app'
  | 'shopping.handoff_opened_web'
  | 'shopping.handoff_dismissed'
  | (string & {});

interface LogInput {
  name: ShoppingEventName;
  session_id: string;
  shopping_list_id?: string | null;
  shopping_order_id?: string | null;
  payload?: Record<string, unknown>;
}

export function wireSupabaseAuth(getter: () => Promise<string | null>): void;
export function sanitizePayload(dirty: Record<string, unknown>): Record<string, unknown>;
export function logShoppingEvent(e: LogInput): void;
export async function flushShoppingTelemetry(): Promise<void>;
// ALLOWED_PAYLOAD_KEYS whitelist = Phase 16's 9 keys + 5 shopping-specific:
//   existing: answer_length, confidence, error_code, first_chunk_ms, intent_type,
//             length, ms, session_id, total_ms
//   new:      item_count, list_id, order_id, app_installed, variant
```

From apps/mobile/src/stores/cookingStore.ts (persist pattern reference):
```typescript
// Zustand + persist + AsyncStorage pattern — settingsStore mirrors this
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import AsyncStorage from '@react-native-async-storage/async-storage';
```

From packages/server/src/__tests__/migrations.test.ts (static-assertion pattern):
```typescript
// Read migration file, assert column shapes via regex, assert RLS policies present.
// Static layer ALWAYS runs; live-supabase layer conditional on env vars.
function readMigration(name: string): string { /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 00024_shopping_events migration + extend migrations.test.ts</name>
  <files>
    supabase/migrations/00024_shopping_events.sql,
    packages/server/src/__tests__/migrations.test.ts
  </files>
  <action>
Clone `supabase/migrations/00020_cooking_events.sql` to `supabase/migrations/00024_shopping_events.sql` with these substitutions:
  - Table name `cooking_events` → `shopping_events`
  - Drop `recipe_id` / `step_index` columns
  - Add `shopping_list_id UUID REFERENCES shopping_lists(id) ON DELETE SET NULL`
  - Add `shopping_order_id UUID REFERENCES shopping_orders(id) ON DELETE SET NULL`
  - Keep identical: id BIGSERIAL PK, profile_id UUID FK auth.users CASCADE, session_id TEXT NOT NULL, event_type TEXT NOT NULL, payload JSONB NOT NULL DEFAULT '{}'::jsonb, client_ts TIMESTAMPTZ NOT NULL, server_ts TIMESTAMPTZ NOT NULL DEFAULT now()
  - Keep identical: 2 indexes (profile_id+server_ts DESC; session_id), RLS enabled, SELECT + INSERT policies auth.uid() = profile_id, NO UPDATE/DELETE policies
  - Update COMMENT ON TABLE to reference Phase 20 + 20-RESEARCH.md Pattern 2

Then extend `packages/server/src/__tests__/migrations.test.ts`. Add a new `describe('00024_shopping_events.sql (static)', () => { ... })` block after the existing 00020 assertions (if 00020 tests exist) or after the last describe. Assert:
  - Static SQL regex: `CREATE TABLE shopping_events`, `profile_id UUID NOT NULL REFERENCES auth\.users\(id\) ON DELETE CASCADE`, `shopping_list_id UUID REFERENCES shopping_lists\(id\) ON DELETE SET NULL`, `shopping_order_id UUID REFERENCES shopping_orders\(id\) ON DELETE SET NULL`, `event_type TEXT NOT NULL`, `payload JSONB NOT NULL DEFAULT '\{\}'::jsonb`, `ENABLE ROW LEVEL SECURITY`, two CREATE POLICY statements (one SELECT, one INSERT with WITH CHECK), absence of any UPDATE/DELETE policy (assert regex `CREATE POLICY[^;]*FOR (UPDATE|DELETE)` does NOT match).

Per CLAUDE.md + D-04 in 20-RESEARCH.md: reuse Phase 16 telemetry pipeline pattern verbatim. Keep append-only semantics.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm -C packages/server test --run src/__tests__/migrations.test.ts 2>&1 | grep -E "(00024|shopping_events|passed|failed)"</automated>
  </verify>
  <done>
    `supabase/migrations/00024_shopping_events.sql` exists with all required columns, indexes, RLS, no UPDATE/DELETE policies. `migrations.test.ts` describe block for 00024 has at least 6 static assertions and all pass (green — this is infrastructure, not a red stub).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Red mobile test stubs — telemetry, openInstacartCart, classifyHandoffError, HandoffSheet, settingsStore + fixture</name>
  <files>
    apps/mobile/src/shopping/__fixtures__/shopping-list.ts,
    apps/mobile/src/shopping/__tests__/telemetry.test.ts,
    apps/mobile/src/shopping/__tests__/openInstacartCart.test.ts,
    apps/mobile/src/shopping/__tests__/classifyHandoffError.test.ts,
    apps/mobile/src/components/shopping/__tests__/HandoffSheet.test.tsx,
    apps/mobile/src/stores/__tests__/settingsStore.test.ts
  </files>
  <behavior>
    - `shopping-list.ts` fixture exports `makeFixtureList()` returning `{ list: ShoppingList, items: ShoppingListItem[] }` with 4 items across 2 categories, 1 checked + 3 unchecked, all with quantity+unit set. Use real `ShoppingList` / `ShoppingListItem` types from `src/types/shopping.ts` so downstream consumers don't drift.
    - `telemetry.test.ts` asserts: (a) `logShoppingEvent` enqueues + auto-flushes at 10-event batch, (b) `flushShoppingTelemetry` POSTs to `/api/v1/telemetry/shopping` with `{ events: [...] }` body, (c) `sanitizePayload` strips non-whitelisted keys but keeps the 5 shopping-specific additions (`item_count`, `list_id`, `order_id`, `app_installed`, `variant`) plus the 9 Phase-16 keys, (d) queue cap 200 drops oldest, (e) 5xx re-queues batch at front, (f) `__resetForTests` exposes `.getQueueLength()` and `.setTokenGetter()`. Use `vi.useFakeTimers()` + fetch mock identical to `cooking/__tests__/telemetry.test.ts`.
    - `openInstacartCart.test.ts` asserts: (a) `openInstacartCart(url)` calls `Linking.openURL(url)` first, (b) on Linking resolve returns without calling WebBrowser, (c) on Linking throw calls `WebBrowser.openBrowserAsync(url)` as fallback. Mock `react-native` Linking and `expo-web-browser`.
    - `classifyHandoffError.test.ts` asserts: (a) `TypeError` with message matching /network|fetch/i → `'network'`, (b) `{ status: 502 }` or `{ status: 500 }` → `'instacart_api'`, (c) `{ status: 401 }` or `{ status: 403 }` → `'auth'`, (d) unknown shape → `'network'` (default). Import `classifyHandoffError` from `../classifyHandoffError`.
    - `HandoffSheet.test.tsx` (RNTL) asserts all 3 states from a discriminated union `{ kind: 'sending' | 'success' | 'error' }`: (a) `kind: 'sending'` renders `"Sending to Instacart cart"` copy + spinner (role='progressbar' or a testID='handoff-sending-spinner'), (b) `kind: 'success'` with `{ url, itemCount: 4, appInstalled: false }` renders `"4 items ready"` + primary button labeled `"Open in Instacart"` + secondary `"View shopping list"` — tapping primary calls `onOpenCart()` prop, (c) `kind: 'error', variant: 'network'` renders retry button labeled `"Try again"` calling `onRetry` prop; variant 'auth' renders a distinct copy string; variant 'instacart_api' renders a distinct copy string. Use `@testing-library/react-native`.
    - `settingsStore.test.ts` asserts: (a) default state is `shoppingHandoffMode: 'draft_cart'`, (b) `setShoppingHandoffMode('legacy')` updates the value, (c) zustand persist middleware wired to AsyncStorage under key `'dinnertime-settings'` (mock `@react-native-async-storage/async-storage` + verify setItem called with the right key).

    Each test file must fail with ASSERTION errors (not module-not-found). To guarantee this, also create minimal stubs of the production files in this plan:
      - `apps/mobile/src/shopping/telemetry.ts` exports stub functions that throw `new Error('Phase 20-01 not implemented')`
      - `apps/mobile/src/shopping/openInstacartCart.ts` stub throwing
      - `apps/mobile/src/shopping/classifyHandoffError.ts` stub returning literal `'network'` (so a subset of assertions PASS and a subset FAIL — classic red-test pattern)
      - `apps/mobile/src/components/shopping/HandoffSheet.tsx` stub rendering `<Text>stub</Text>` (the assertion "Sending to Instacart cart" will fail the sending-state case)
      - settingsStore stub is built real in Task 3 below so this test will pass after Task 3 ships. That's fine — it's the only test that goes green in Wave 0; the other 4 stay red.

    File structure and fixture imports MUST match the extracted interfaces in <context>. Clone cooking telemetry.test.ts verbatim where applicable; the 5-shopping-key additions are the only payload-whitelist divergence.
  </behavior>
  <action>
Create each file per the behavior block above. Use `cooking/__tests__/telemetry.test.ts` as the structural reference for `shopping/__tests__/telemetry.test.ts` — copy-paste and rename `cooking` → `shopping`, `CookingEventName` → `ShoppingEventName`, `/telemetry/cooking` → `/telemetry/shopping`. For the sanitize whitelist test, extend the expected-keys list to 14 entries (9 original + 5 new).

For the RNTL tests, ensure `react-native-safe-area-context` and testing-library are already wired (they are — Phase 15/16 already use them).

Create each production stub file MINIMALLY so the test files import without a "cannot find module" error. Each stub must have a `// TODO(phase-20-01)` or `// TODO(phase-20-03)` marker inline so later waves know what to replace.

Addresses review constraint: every plan's test files must fail red before production code, per 20-VALIDATION.md Wave 0 Requirements.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm -C apps/mobile test --run src/shopping src/components/shopping src/stores/__tests__/settingsStore.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>
    5 new test files exist + 4 red production stubs + 1 fixture file. Running the scoped command shows test suites fail with assertion errors (not import errors). settingsStore.test.ts stays red until Task 3 adds the real store. telemetry.test.ts, openInstacartCart.test.ts, classifyHandoffError.test.ts, HandoffSheet.test.tsx are all red with clear "expected X, received Y" diffs. Zero regression in unrelated existing suites (`pnpm -C apps/mobile test --run` overall count does not decrease in green files outside the scope).
  </done>
</task>

<task type="auto">
  <name>Task 3: settingsStore real implementation + extend server telemetry.test.ts + DEVICE-TEST-20 skeleton</name>
  <files>
    apps/mobile/src/stores/settingsStore.ts,
    packages/server/src/routes/__tests__/telemetry.test.ts,
    .planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/DEVICE-TEST-20.md
  </files>
  <action>
**settingsStore.ts (NEW — real implementation, not a stub):**
Create a Zustand store with persist middleware. Surface area is intentionally minimal — the ONLY key-value in Wave 0 is `shoppingHandoffMode`:
```typescript
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import AsyncStorage from '@react-native-async-storage/async-storage';

export type ShoppingHandoffMode = 'draft_cart' | 'legacy';

interface SettingsState {
  shoppingHandoffMode: ShoppingHandoffMode;
  setShoppingHandoffMode: (mode: ShoppingHandoffMode) => void;
}

export const useSettingsStore = create<SettingsState>()(
  persist(
    (set) => ({
      shoppingHandoffMode: 'draft_cart',
      setShoppingHandoffMode: (mode) => set({ shoppingHandoffMode: mode }),
    }),
    {
      name: 'dinnertime-settings',
      storage: createJSONStorage(() => AsyncStorage),
    }
  )
);
```
This pattern mirrors cookingStore persist pattern. `'draft_cart'` default implements SHOP-DC-05. This makes settingsStore.test.ts flip GREEN.

**telemetry.test.ts (EXTEND — add shopping channel cases):**
The existing `packages/server/src/routes/__tests__/telemetry.test.ts` covers POST `/telemetry/cooking`. Add five parallel cases for POST `/telemetry/shopping` (the route lands in Wave 1 20-01):
  - 401 when no auth token
  - 204 when events[] is empty
  - 400 when body is invalid JSON or fails zod schema
  - 200 with `{ inserted: N }` when batch is valid — assert supabase.from('shopping_events').insert called with rows having profile_id = user.id, event_type = e.name, client_ts = e.timestamp, shopping_list_id, shopping_order_id
  - 500 when supabase insert returns error

Stay structurally identical to the existing cooking cases — same auth middleware mock, same supabase spy pattern. This test file is EXTENDED, not replaced; the cooking cases stay untouched. Expected state: the 5 new shopping cases go red until 20-01 ships the `/shopping` route handler.

**DEVICE-TEST-20.md (NEW — physical iPhone UAT skeleton):**
Create `.planning/phases/20-.../DEVICE-TEST-20.md` with sections:
  1. Purpose — explain why simulator-only UAT (Maestro 29 in 20-04) is insufficient: simulator can't install real Instacart app, so universal-link app-routing cannot be verified.
  2. Required device state: iPhone 15+ iOS 17+, Instacart app installed + logged in, DinnerTime dev client installed, Cloudflare tunnel URL in `apps/mobile/.env`.
  3. Checklist table with rows (use a markdown table — 6 columns: `ID | Behavior | Steps | Expected | Pass? | Notes`):
     - UNIVLINK-01 — Tap "Open in Instacart" with app installed → Instacart app launches with cart populated (items match DinnerTime list)
     - UNIVLINK-02 — Uninstall Instacart app → tap "Open in Instacart" → Safari View Controller opens instacart.com with cart populated
     - HANDOFF-01 — Sheet state progression: button tap → spinner "Sending to Instacart cart…" → success with correct item count
     - HANDOFF-02 — Error states: airplane mode ON → tap order → `network` variant error + retry
     - ROLLBACK-01 — Hidden Settings toggle flips to `legacy` → next order uses Phase 8 inline WebBrowser flow (no sheet)
     - TELEMETRY-01 — Complete one handoff → query `shopping_events` in Supabase SQL editor → verify `shopping.draft_cart_started` + `shopping.draft_cart_succeeded` rows present with sanitized payload
  4. Reference to UAT.md reporting format (mirror Phase 16 DEVICE-TEST-16.md structure). Leave Status: 'not started' as frontmatter.

Use YAML frontmatter: `phase: 20`, `device_test_version: 1`, `status: skeleton`, `created: 2026-04-22`.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm -C apps/mobile test --run src/stores/__tests__/settingsStore.test.ts 2>&1 | grep -E "(passed|failed)" && pnpm -C packages/server test --run src/routes/__tests__/telemetry.test.ts 2>&1 | grep -E "shopping|passed|failed" | head -20 && test -f .planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/DEVICE-TEST-20.md && echo "DEVICE-TEST-20 exists"</automated>
  </verify>
  <done>
    `settingsStore.test.ts` → GREEN (all 3 cases pass; this is the one test file that goes green in Wave 0 because the real store ships here). Server `telemetry.test.ts` → mix of green (existing cooking cases) + red (5 new shopping cases waiting for 20-01). DEVICE-TEST-20.md exists with all 6 checklist rows + frontmatter. No changes to existing cooking test cases (run full server suite — cooking cases still pass).
  </done>
</task>

</tasks>

<verification>
- `pnpm -C packages/server test --run` — 00024 migration assertions PASS; 5 new `/telemetry/shopping` cases FAIL (expected — route lands in 20-01)
- `pnpm -C apps/mobile test --run src/shopping src/components/shopping src/stores/__tests__/settingsStore.test.ts` — 4 red stubs + 1 green (settingsStore)
- `ls supabase/migrations/ | grep 00024` returns `00024_shopping_events.sql`
- `ls apps/mobile/src/shopping/` returns at least: `__fixtures__/`, `__tests__/`, `telemetry.ts`, `openInstacartCart.ts`, `classifyHandoffError.ts`
- `ls apps/mobile/src/components/shopping/` includes `HandoffSheet.tsx` (stub) + `__tests__/HandoffSheet.test.tsx`
- `ls apps/mobile/src/stores/settingsStore.ts`
- `ls .planning/phases/20-*/DEVICE-TEST-20.md`
</verification>

<success_criteria>
- Every test file from 20-VALIDATION.md Wave 0 Requirements exists on disk (5 mobile + 2 server extensions + 1 migration)
- settingsStore.ts ships as real code (not a stub) — test goes green; default `'draft_cart'` implements SHOP-DC-05 flag default
- 00024_shopping_events.sql clones 00020_cooking_events.sql schema semantics 1:1 (append-only, RLS auth.uid() = profile_id, no UPDATE/DELETE)
- DEVICE-TEST-20.md skeleton covers universal-link + rollback + telemetry behaviors that Maestro 29 (20-04) cannot reach
- Zero regression in existing cooking telemetry tests (server + mobile)
- All production-code stubs have `// TODO(phase-20-NN)` markers pointing at the wave that replaces them
</success_criteria>

<output>
After completion, create `.planning/phases/20-shopping-refactor-push-to-instacart-draft-cart-instead-of-creating-orders/20-00-SUMMARY.md`
</output>
