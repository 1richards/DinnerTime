---
phase: 01-project-setup-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/mobile/eas.json
  - apps/mobile/app.json
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-06

user_setup:
  - service: supabase
    why: "Local Supabase for development and auth provider config"
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "Output of `supabase start` (API URL)"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "Output of `supabase start` (anon key)"
      - name: SUPABASE_URL
        source: "Same as EXPO_PUBLIC_SUPABASE_URL"
      - name: SUPABASE_ANON_KEY
        source: "Same as EXPO_PUBLIC_SUPABASE_ANON_KEY"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Output of `supabase start` (service_role key)"
    dashboard_config:
      - task: "Enable Apple provider in Auth settings (requires Apple Developer account with Sign in with Apple capability and Services ID)"
        location: "Supabase Dashboard -> Authentication -> Providers -> Apple"
      - task: "Enable Google provider with Web Client ID (requires Google Cloud Console OAuth 2.0 Web Application credential)"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
  - service: apple-developer
    why: "Apple Sign In requires Apple Developer account configuration"
    env_vars: []
    dashboard_config:
      - task: "Enable Sign in with Apple capability for app bundle ID"
        location: "Apple Developer Portal -> Certificates, Identifiers & Profiles -> Identifiers"
      - task: "Create a Services ID for Supabase callback URL"
        location: "Apple Developer Portal -> Certificates, Identifiers & Profiles -> Identifiers -> Services IDs"
  - service: google-cloud
    why: "Google Sign In requires OAuth 2.0 credentials"
    env_vars:
      - name: EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs -> Web Application type"
    dashboard_config:
      - task: "Create OAuth 2.0 Web Application credential (MUST be Web type, not iOS -- Supabase validates using the web client ID)"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> Create Credentials -> OAuth Client ID"

must_haves:
  truths:
    - "EAS dev client build configuration exists for iOS development"
    - "User can visually confirm the auth and app shell look warm and inviting"
    - "Full auth flow works end-to-end: register, onboarding, tabs, logout, login"
  artifacts:
    - path: "apps/mobile/eas.json"
      provides: "EAS build profiles including development client"
    - path: "apps/mobile/app.json"
      provides: "Updated app config with bundle ID and plugins"
  key_links:
    - from: "apps/mobile/eas.json"
      to: "EAS Build service"
      via: "eas build --profile development --platform ios"
      pattern: "developmentClient"
---

<objective>
Configure EAS build for dev client (required since Expo Go cannot run native modules like expo-secure-store and Apple Auth), and verify the complete Phase 1 deliverable visually.

Purpose: Ensure the app can actually be built and run on a device/simulator, and that the visual design meets the warm/inviting requirement.
Output: EAS config ready for dev client builds. User-verified auth flow and app shell.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-auth/01-RESEARCH.md
@.planning/phases/01-project-setup-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure EAS build and finalize app config</name>
  <files>
    apps/mobile/eas.json,
    apps/mobile/app.json
  </files>
  <action>
    1. Create `apps/mobile/eas.json` with build profiles:
       - `development`: developmentClient true, distribution "internal", ios simulator true
       - `preview`: distribution "internal" (for TestFlight-like testing)
       - `production`: autoIncrement true

    2. Update `apps/mobile/app.json` (or app.config.ts if that exists):
       - Set bundle identifier (e.g., "com.dinnertime.app")
       - Add expo-apple-authentication plugin
       - Add @react-native-google-signin/google-signin plugin with iosUrlScheme (reversed web client ID)
       - Ensure expo-secure-store plugin is listed
       - Set scheme for deep linking (e.g., "dinnertime")

    3. Verify the full test suite still passes after config changes.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm -r test 2>&1 | tail -15</automated>
  </verify>
  <done>eas.json exists with development/preview/production profiles. app.json has correct bundle ID, plugins for Apple Auth and Google Sign In. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 auth flow and visual design</name>
  <files>N/A</files>
  <action>User verifies the complete Phase 1 deliverable end-to-end.</action>
  <what-built>
    Complete Phase 1 deliverable: pnpm monorepo with Expo SDK 55 mobile app, Hono API server, Supabase local dev setup, full authentication flow (email + Apple + Google), onboarding wizard, and 5-tab app shell with warm visual design.
  </what-built>
  <how-to-verify>
    1. Start Supabase local: `cd supabase && supabase start` (requires Docker running)
    2. Copy the output env vars to `.env` file at project root
    3. Start server: `pnpm dev:server` -- confirm it logs "Server running on http://localhost:3000"
    4. Verify health: `curl http://localhost:3000/api/v1/health` -- should return { status: "ok" }
    5. Build dev client: `cd apps/mobile && eas build --profile development --platform ios` (or run on simulator if local builds are configured)
    6. Open the app and verify:
       - Login screen appears with warm colors (orange/amber theme)
       - Email/password fields and social sign-in buttons are visible
       - Register screen is accessible and styled consistently
       - After creating an account, onboarding wizard appears
       - Onboarding collects name, household size, preferences
       - After onboarding, 5-tab interface is visible (Home, Recipes, Pantry, Shopping, Cook)
       - Each tab shows placeholder content
       - Logging out returns to login screen
       - Logging back in goes directly to tabs (skips onboarding)
    7. Visual check: Does the app feel warm and inviting? Food-themed, not sterile?
  </how-to-verify>
  <verify>User confirms visual and functional correctness</verify>
  <done>User has approved the auth flow and visual design, or issues have been identified for fixing.</done>
  <resume-signal>Type "approved" if the auth flow and visual design look good, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. eas.json exists with development profile
2. app.json has correct plugins and bundle ID
3. Full test suite passes: `pnpm -r test`
4. User confirms auth flow works end-to-end
5. User confirms warm, inviting visual design
</verification>

<success_criteria>
- EAS development build profile configured
- App builds and runs on iOS simulator or device
- User has verified the complete auth flow: register -> onboarding -> tabs -> logout -> login
- User has confirmed the visual design feels warm and food-themed
- All Phase 1 requirements validated: FOUN-01 (account creation), FOUN-02 (session persistence), FOUN-06 (cloud sync)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-auth/01-03-SUMMARY.md`
</output>
