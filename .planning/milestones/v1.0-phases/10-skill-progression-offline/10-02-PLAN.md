---
phase: 10-skill-progression-offline
plan: 02
type: tdd
wave: 2
depends_on: [10-01]
files_modified:
  - packages/server/src/services/progression.ts
  - packages/server/src/services/__tests__/progression.test.ts
  - packages/server/src/routes/progression.ts
  - packages/server/src/routes/__tests__/progression.test.ts
  - packages/server/src/services/ingredientMatching.ts
  - packages/server/src/index.ts
autonomous: true
requirements: [SKIL-01, SKIL-02, SKIL-04]
must_haves:
  truths:
    - "When a meal is marked cooked, a row is appended to recipe_cooks in the same flow"
    - "Server can return per-recipe cook stats for a profile (count + last_cooked_at)"
    - "Server returns 3 ambition suggestions ranked by Claude Sonnet given history + candidates"
    - "Server returns creative variations only for recipes with cook_count >= 3"
  artifacts:
    - path: "packages/server/src/services/progression.ts"
      provides: "getCookStats, rankAmbition, getRecipeVariations service functions"
      min_lines: 120
    - path: "packages/server/src/routes/progression.ts"
      provides: "GET /cook-stats, GET /suggestions, GET /variations/:recipeId"
      exports: ["progressionRoutes"]
    - path: "packages/server/src/services/__tests__/progression.test.ts"
      provides: "Unit tests for all three service functions with mocked Anthropic + Supabase"
  key_links:
    - from: "markCooked service"
      to: "recipe_cooks table"
      via: "INSERT in same transaction/flow"
      pattern: "recipe_cooks.*insert"
    - from: "progression routes"
      to: "auth middleware"
      via: "existing requireAuth pattern"
      pattern: "requireAuth|supabase.auth"
    - from: "rankAmbition"
      to: "Anthropic Sonnet 4"
      via: "tool_use with recommendations schema"
      pattern: "messages\\.create"
---

<objective>
Build the server-side progression layer: cook history logging (hook into existing markCooked), cook stats aggregation, Claude Sonnet ambition ranker, and creative variations endpoint. TDD throughout.

Purpose: This is the AI/data backbone for SKIL-01/02/04. SKIL-03 (cooking tips) is handled in a sibling plan.
Output: progression service + route + hook into markCooked, all TDD-covered.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-skill-progression-offline/10-RESEARCH.md
@.planning/phases/10-skill-progression-offline/10-01-SUMMARY.md
@packages/server/src/services/ingredientMatching.ts
@packages/server/src/services/suggestions.ts
@packages/server/src/services/shoppingList.ts
@packages/server/src/services/recipeDiscovery.ts
@packages/server/src/routes/meal-plans.ts
@packages/server/src/routes/shopping.ts
@packages/server/src/index.ts
@packages/server/src/types/progression.ts

<interfaces>
From 10-01 (types already defined):
```typescript
export interface RecipeCookStats { recipe_id, title, cook_count, last_cooked_at }
export interface AmbitionSuggestion { recipe_id, title, rationale }
export interface AmbitionRankRequest { history, candidates }
```

Existing `markCooked` lives in phase 07-03 `ingredientMatching.ts` (per STATE.md). Extend it: after the status guard and pantry deduction, `INSERT INTO recipe_cooks (profile_id, recipe_id)` using the same authenticated Supabase client. Treat insert failure as non-fatal (log + swallow) — cook succeeded, stats are best-effort.

Pattern reference for Claude tool_use: `packages/server/src/services/shoppingList.ts::suggestVariations` and `packages/server/src/services/mealPlanner.ts::generateMealPlan`.

Complexity heuristic (from research Pattern 4):
```
complexity = steps.length + ingredients.length + Math.floor(total_time_minutes / 15)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: progression service (RED → GREEN → REFACTOR)</name>
  <files>packages/server/src/services/progression.ts, packages/server/src/services/__tests__/progression.test.ts, packages/server/src/services/ingredientMatching.ts</files>
  <behavior>
    - `logRecipeCook(supabase, profileId, recipeId)` inserts one row into recipe_cooks; returns void; logs but does not throw on failure
    - `getCookStats(supabase, profileId)` returns RecipeCookStats[] via one query joining recipe_cooks with recipes (title), grouped by recipe_id, with cook_count and last_cooked_at
    - `computeComplexity(recipe)` returns steps.length + ingredients.length + floor(total_time/15)
    - `rankAmbition(anthropic, { history, candidates })` calls Sonnet 4 with tool schema { recommendations: [{recipe_id, rationale}] }, filters out recipes with cook_count >= 2 from history, returns top 3 AmbitionSuggestion
    - `getRecipeVariations(anthropic, supabase, profileId, recipeId)` returns string[] variations ONLY if that recipe's cook_count >= 3; otherwise throws `{ code: 'BELOW_THRESHOLD' }`
    - `markCooked` in ingredientMatching.ts calls `logRecipeCook` after successful status update; failure non-fatal
  </behavior>
  <action>
    RED: Write `packages/server/src/services/__tests__/progression.test.ts` using the project's existing Anthropic mock pattern (recipeDiscovery/shoppingList tests). Test cases:
    1. `logRecipeCook` inserts row (mock Supabase .from().insert())
    2. `logRecipeCook` swallows errors (Supabase returns error → no throw, console.warn called)
    3. `getCookStats` returns aggregated stats (mock returns 3 raw rows across 2 recipes → result has 2 entries with correct counts)
    4. `computeComplexity` — 3 table-driven cases
    5. `rankAmbition` — mocked Sonnet returns tool_use with 3 recommendations; verify result shape; verify history recipes with cook_count >= 2 are excluded from candidates before prompt assembly
    6. `getRecipeVariations` — cook_count < 3 throws BELOW_THRESHOLD; cook_count >= 3 returns string[]

    Run tests — all fail. Commit: `test(10-02): add failing tests for progression service`.

    GREEN: Implement `packages/server/src/services/progression.ts` with all five functions. Then edit `packages/server/src/services/ingredientMatching.ts::markCooked`: after the existing status-update success path, call `await logRecipeCook(supabase, profileId, entry.recipe_id)` inside a try/catch that logs and continues. Do NOT add markCooked-specific tests here — existing 07-03 tests cover status logic; add one new test to progression.test.ts asserting markCooked invokes logRecipeCook (can spy or re-mock).

    Run tests — all pass. Commit: `feat(10-02): implement progression service + markCooked logging`.

    REFACTOR if needed (extract prompt builder as pure helper), commit `refactor(10-02): ...`.

    Pitfall guards:
    - Use `isInternetReachable !== false` semantics do NOT apply here (server-side)
    - When Sonnet returns a recipe_id not in candidates, drop it (don't hallucinate)
    - Empty history → return first 3 candidates by lowest complexity (fallback)
  </action>
  <verify>
    <automated>pnpm --filter server test -- --run progression</automated>
  </verify>
  <done>All 6+ tests pass; progression.ts ≥120 lines; markCooked calls logRecipeCook; Sonnet mock verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: progression Hono route + app mount</name>
  <files>packages/server/src/routes/progression.ts, packages/server/src/routes/__tests__/progression.test.ts, packages/server/src/index.ts</files>
  <behavior>
    - GET /api/v1/progression/cook-stats → 200 with RecipeCookStats[] for authenticated profile
    - GET /api/v1/progression/suggestions → 200 with 3 AmbitionSuggestion entries; assembles history from cook stats and candidates from recipes library
    - GET /api/v1/progression/variations/:recipeId → 200 with string[] when eligible; 400 BELOW_THRESHOLD when cook_count < 3; 404 if recipe not owned
    - All routes require auth; unauthenticated → 401
  </behavior>
  <action>
    RED: Create `packages/server/src/routes/__tests__/progression.test.ts` mirroring `packages/server/src/routes/__tests__/meal-plans.test.ts` pattern. Use the Hono test client + existing Supabase test stub. Test cases:
    1. 401 without auth header
    2. GET /cook-stats returns aggregated stats
    3. GET /suggestions calls rankAmbition and returns shape
    4. GET /variations/:id returns 400 BELOW_THRESHOLD for low cook count
    5. GET /variations/:id returns 200 with variations for eligible recipe
    6. GET /variations/:id returns 404 when recipe not owned (Supabase returns null)

    Run — fail. Commit: `test(10-02): add failing tests for progression routes`.

    GREEN: Create `packages/server/src/routes/progression.ts` exporting `progressionRoutes` Hono sub-app. Reuse `requireAuth` pattern from `routes/meal-plans.ts`. For /suggestions: load cook stats, load recipe library (cap 100, mirror meal-plans pattern), call rankAmbition, return. For /variations: load recipe (404 if not owned), call getRecipeVariations, catch BELOW_THRESHOLD → 400 with error code.

    Mount in `packages/server/src/index.ts`: `app.route('/api/v1/progression', progressionRoutes)` alongside existing routes.

    Run — pass. Commit: `feat(10-02): add progression routes and mount`.

    Pitfalls:
    - Always map service `BELOW_THRESHOLD` → HTTP 400 (not 500); mirror meal-plans error mapping style
    - Empty cook stats → /suggestions still returns 3 (falls through to complexity fallback in service)
  </action>
  <verify>
    <automated>pnpm --filter server test -- --run progression.route &amp;&amp; pnpm --filter server test -- --run</automated>
  </verify>
  <done>All route tests pass; full server test suite green; route mounted in index.ts; 3 endpoints live.</done>
</task>

</tasks>

<verification>
- `pnpm --filter server test -- --run` full green
- Manual curl check (optional): `curl -H "Authorization: Bearer <token>" http://localhost:3000/api/v1/progression/cook-stats` returns JSON
- No regressions in meal-plans or shopping test suites
</verification>

<success_criteria>
- [ ] progression service with 5 exported functions + tests
- [ ] markCooked now logs to recipe_cooks (non-fatal)
- [ ] 3 progression routes mounted and auth-gated
- [ ] All tests green on server package
- [ ] SKIL-01, SKIL-02, SKIL-04 backend truths observable
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-progression-offline/10-02-SUMMARY.md`
</output>
