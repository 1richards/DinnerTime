---
phase: 10-skill-progression-offline
plan: 03
type: tdd
wave: 2
depends_on: [10-01]
files_modified:
  - packages/server/src/services/cookingTips.ts
  - packages/server/src/services/__tests__/cookingTips.test.ts
  - packages/server/src/routes/cooking.ts
  - packages/server/src/routes/__tests__/cooking.test.ts
autonomous: true
requirements: [SKIL-03]
must_haves:
  truths:
    - "Cache hit returns the stored tip without calling Claude"
    - "Cache miss calls Claude Haiku, stores the tip, and returns it"
    - "Haiku returning empty string is honored (no tip) and NOT cached as empty"
    - "GET /api/v1/cooking/tips returns tip for (recipe_id, step_index) or 404"
  artifacts:
    - path: "packages/server/src/services/cookingTips.ts"
      provides: "getOrGenerateTip function with Haiku + cache"
      min_lines: 60
    - path: "packages/server/src/routes/cooking.ts"
      provides: "Extended with GET /tips endpoint (existing file from phase 09-03)"
      contains: "tips"
  key_links:
    - from: "getOrGenerateTip"
      to: "recipe_step_tips table"
      via: "SELECT then INSERT on miss"
      pattern: "recipe_step_tips"
    - from: "getOrGenerateTip"
      to: "Claude Haiku"
      via: "anthropic.messages.create with haiku model"
      pattern: "haiku"
---

<objective>
Implement per-step cooking tips with server-side Haiku cache. SKIL-03 only.

Purpose: Cost control (Pitfall 6) — tips are lazy and per-step, cached to avoid duplicate Haiku calls. Separated from 10-02 so two devs could run parallel and so blast radius of Haiku prompt tuning is isolated.
Output: cookingTips service + GET /cooking/tips endpoint extending existing phase 09 cooking route.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-skill-progression-offline/10-RESEARCH.md
@.planning/phases/10-skill-progression-offline/10-01-SUMMARY.md
@packages/server/src/routes/cooking.ts
@packages/server/src/routes/__tests__/cooking.test.ts
@packages/server/src/services/suggestions.ts

<interfaces>
Pattern from research (Section "Pattern 3"):
```typescript
export async function getOrGenerateTip(
  supabase: SupabaseClient,
  recipeId: string,
  stepIndex: number,
  stepText: string
): Promise<string>
```

Haiku prompt guard (Pitfall 5): "If uncertain about the technique, return an empty string. Never hedge with 'traditionally' / 'some say' / 'might' — return empty instead."

Existing cooking route already mounted at `/api/v1/cooking` (phase 09-03) with POST /ask. Extend same file with GET /tips?recipe_id=...&step_index=...&step_text=...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cookingTips service with cache (RED → GREEN)</name>
  <files>packages/server/src/services/cookingTips.ts, packages/server/src/services/__tests__/cookingTips.test.ts</files>
  <behavior>
    - Cache hit: Supabase returns row → return cached tip, no Anthropic call
    - Cache miss: query returns null → call Haiku → insert row → return tip
    - Haiku empty string response: return empty string, do NOT insert (don't cache uncertainty)
    - Haiku throws: propagate error (route layer decides 5xx)
    - Uses model `claude-haiku-4-20250514` or project's configured Haiku constant (check packages/server/src/config/anthropic.ts if exists)
  </behavior>
  <action>
    RED: Write `packages/server/src/services/__tests__/cookingTips.test.ts`. Mock Anthropic SDK (mirror `shoppingList.test.ts`) and Supabase. Test cases:
    1. Cache hit — Supabase .maybeSingle returns {tip: 'cached'}; Anthropic NOT called; returns 'cached'
    2. Cache miss — Supabase returns null; Anthropic returns text 'Braising means...'; verify .insert called with {recipe_id, step_index, tip}; returns 'Braising means...'
    3. Haiku empty response — Anthropic returns ''; verify .insert NOT called; returns ''
    4. Haiku whitespace-only response — normalize to empty, same as case 3
    5. System prompt contains the uncertainty guard string (grep assertion)

    Commit: `test(10-03): add failing tests for cookingTips`.

    GREEN: Create `packages/server/src/services/cookingTips.ts`. Implementation:
    - `getOrGenerateTip(supabase, recipeId, stepIndex, stepText)` — select from recipe_step_tips → return cached → else call Haiku with system prompt:
      ```
      You are a cooking coach. Given a single recipe step, return ONE short tip (max 2 sentences) that explains technique, timing, or sensory cues. If you're uncertain about the technique in the step, return an empty string — never hedge with "traditionally", "some say", or "might". Return plain text only, no preamble.
      ```
    - Max tokens: 120. Temperature: 0.3.
    - Trim response; if empty, skip insert and return empty.
    - Lazy Anthropic singleton — reuse pattern from suggestions.ts.

    Commit: `feat(10-03): implement cookingTips service`.
  </action>
  <verify>
    <automated>pnpm --filter server test -- --run cookingTips</automated>
  </verify>
  <done>All 5 tests pass; service ≥60 lines; Haiku uncertainty guard verifiable in prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /api/v1/cooking/tips route</name>
  <files>packages/server/src/routes/cooking.ts, packages/server/src/routes/__tests__/cooking.test.ts</files>
  <behavior>
    - GET /api/v1/cooking/tips with query params (recipe_id, step_index, step_text) returns 200 {tip: string}
    - 400 INVALID_REQUEST on missing/invalid params
    - 404 if recipe not owned by auth user
    - 401 without auth
    - Does NOT break existing POST /ask behavior
  </behavior>
  <action>
    RED: Extend `packages/server/src/routes/__tests__/cooking.test.ts` (existing from phase 09-03) with new cases:
    1. 401 without auth
    2. 400 missing recipe_id
    3. 404 recipe not owned (supabase returns null)
    4. 200 with {tip: 'cached'} when service returns cached
    5. 200 with {tip: ''} when Haiku returned empty

    Ensure existing POST /ask tests still pass untouched.

    Commit: `test(10-03): extend cooking route tests for GET /tips`.

    GREEN: Add `c.get('/tips', ...)` handler to `routes/cooking.ts`. Validate params with the same style as existing /ask handler. Ownership check: SELECT id FROM recipes WHERE id = recipe_id AND profile_id = auth.uid() LIMIT 1. Call `getOrGenerateTip`. Return {tip}. Map service errors to HTTP (Anthropic failure → 502, mirror 09-03 style).

    Commit: `feat(10-03): add GET /cooking/tips route`.
  </action>
  <verify>
    <automated>pnpm --filter server test -- --run cooking &amp;&amp; pnpm --filter server test -- --run</automated>
  </verify>
  <done>All new and existing cooking tests green; full server suite green; endpoint live.</done>
</task>

</tasks>

<verification>
- `pnpm --filter server test -- --run` passes
- Phase 09-03 POST /ask tests untouched and still green
- Haiku prompt contains uncertainty guard (Pitfall 5)
</verification>

<success_criteria>
- [ ] cookingTips service with cache hit/miss/empty logic + tests
- [ ] GET /api/v1/cooking/tips route
- [ ] No regressions in existing cooking route tests
- [ ] SKIL-03 backend truth observable
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-progression-offline/10-03-SUMMARY.md`
</output>
