---
phase: 09-voice-cooking-mode
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - apps/mobile/src/cooking/useStepSpeaker.ts
  - apps/mobile/src/cooking/useVoiceListener.ts
  - apps/mobile/src/cooking/askAssistant.ts
  - apps/mobile/src/cooking/__tests__/useStepSpeaker.test.ts
  - apps/mobile/src/cooking/__tests__/askAssistant.test.ts
  - apps/mobile/vitest.setup.ts
autonomous: true
requirements: [VOIC-02, VOIC-04, VOIC-05]
user_setup: []
must_haves:
  truths:
    - "useStepSpeaker calls Speech.speak when enabled and the step text changes"
    - "useStepSpeaker calls Speech.stop on unmount AND when step text changes (prevents overlap)"
    - "useVoiceListener only forwards FINAL STT results to its callback (Pitfall 2 — no interim false-fires)"
    - "useVoiceListener pauses while Speech.speaking is in progress (Pitfall 4 — TTS/STT loop prevention)"
    - "askAssistant POSTs to /api/v1/cooking/ask with auth token and returns answer string"
  artifacts:
    - path: apps/mobile/src/cooking/useStepSpeaker.ts
      provides: "Hook wrapping expo-speech for step read-aloud"
      contains: "useStepSpeaker"
    - path: apps/mobile/src/cooking/useVoiceListener.ts
      provides: "Hook wrapping expo-speech-recognition"
      contains: "useVoiceListener"
    - path: apps/mobile/src/cooking/askAssistant.ts
      provides: "Fetch client for /cooking/ask endpoint"
      contains: "askAssistant"
  key_links:
    - from: apps/mobile/src/cooking/askAssistant.ts
      to: "/api/v1/cooking/ask"
      via: "authedFetch POST with { recipe_id, current_step_index, question }"
      pattern: "cooking/ask"
    - from: apps/mobile/src/cooking/useVoiceListener.ts
      to: "@jamsch/expo-speech-recognition"
      via: "ExpoSpeechRecognitionModule.start + useSpeechRecognitionEvent"
      pattern: "expo-speech-recognition"
    - from: apps/mobile/src/cooking/useStepSpeaker.ts
      to: "expo-speech"
      via: "Speech.speak / Speech.stop"
      pattern: "expo-speech"
---

<objective>
Build the thin framework-side wrappers that the cooking screen will compose: TTS hook, STT listener hook, and HTTP client for the assistant endpoint. Keep each module small enough to mock in isolation so the screen test in 09-05 can swap them out.

Purpose: Isolate native-module surface behind hooks (Pitfall 7 — only one file touches expo-speech-recognition so a pre-1.0 API break is a one-file fix).
Output: Three modules with vitest coverage for the pure ones, module mocks for the native ones.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-voice-cooking-mode/09-RESEARCH.md
@apps/mobile/src/types/cooking.ts
@apps/mobile/src/stores/cookingStore.ts
@apps/mobile/src/stores/shoppingStore.ts
@apps/mobile/src/stores/mealPlanStore.ts
@apps/mobile/vitest.config.ts
@apps/mobile/vitest.setup.ts

<interfaces>
authedFetch pattern (from mealPlanStore/shoppingStore — Decision [Phase 07-04]):
- Reads Supabase session token via `supabase.auth.getSession()`
- Prefixes base URL + /api/v1
- Throws on non-2xx with parsed server error body

For this plan, create a local `authedFetch` helper in askAssistant.ts (or reuse an existing one if you find a shared helper — check `src/lib/api.ts` or similar first; otherwise inline, matching mealPlanStore pattern).

From apps/mobile/src/types/cooking.ts (created in 09-01): CookingIntent, Timer, CookingState.

Module mocks to add to apps/mobile/vitest.setup.ts (if not already mocked):
- `expo-speech` -> `{ speak: vi.fn(), stop: vi.fn(), isSpeakingAsync: vi.fn().mockResolvedValue(false) }`
- `expo-keep-awake` -> `{ useKeepAwake: vi.fn() }`
- `@jamsch/expo-speech-recognition` -> `{ ExpoSpeechRecognitionModule: { start: vi.fn(), stop: vi.fn(), requestPermissionsAsync: vi.fn().mockResolvedValue({ granted: true }) }, useSpeechRecognitionEvent: vi.fn() }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add module mocks to vitest.setup.ts</name>
  <files>apps/mobile/vitest.setup.ts</files>
  <action>
    Read existing vitest.setup.ts. Add `vi.mock()` calls for the three native modules listed in the interfaces block above. Place them alongside any existing mocks (likely AsyncStorage, expo-router). Each mock should expose the exact surface used by the hooks in this plan — see `interfaces` block. Do NOT mock them per-test; these are global so downstream screen tests inherit them.
  </action>
  <verify>
    <automated>cd apps/mobile && npm test -- --run --reporter=basic 2>&1 | head -40</automated>
  </verify>
  <done>Existing test suite still passes, new global mocks defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useStepSpeaker hook + test</name>
  <files>apps/mobile/src/cooking/useStepSpeaker.ts, apps/mobile/src/cooking/__tests__/useStepSpeaker.test.ts</files>
  <behavior>
    - useStepSpeaker(text: string | undefined, enabled: boolean): void
    - Test: mount with enabled=true, text='Chop onions' -> Speech.speak called once with 'Chop onions' and options { language: 'en-US', rate: 0.95, pitch: 1.0, onError: fn }
    - Test: rerender with text='Heat pan' -> Speech.stop called before second Speech.speak (overlap prevention)
    - Test: enabled=false -> Speech.speak never called
    - Test: unmount -> Speech.stop called in cleanup
    - Test: text=undefined -> no calls
  </behavior>
  <action>
    Implement per 09-RESEARCH.md Pattern 2 using React `useEffect`. Use `@testing-library/react-native`'s `renderHook` (or a minimal test harness component if renderHook isn't available — check existing tests first). Assert Speech.speak mock call args.
  </action>
  <verify>
    <automated>cd apps/mobile && npm test -- useStepSpeaker</automated>
  </verify>
  <done>Hook exists, all 5 behaviors tested green.</done>
</task>

<task type="auto">
  <name>Task 3: useVoiceListener hook + askAssistant client with tests</name>
  <files>apps/mobile/src/cooking/useVoiceListener.ts, apps/mobile/src/cooking/askAssistant.ts, apps/mobile/src/cooking/__tests__/askAssistant.test.ts</files>
  <action>
    **useVoiceListener.ts** — per 09-RESEARCH.md Pattern 6. Accept `(onFinalTranscript: (t: string) => void, enabled: boolean, hints: string[])`. Subscribes to 'result' (filtering `isFinal === true` — Pitfall 2), 'error' (console.warn), 'end' (auto-restart via setTimeout when still enabled). On mount when enabled: call `requestPermissionsAsync()` then `ExpoSpeechRecognitionModule.start({ lang: 'en-US', continuous: true, interimResults: false, iosTaskHint: 'confirmation', contextualStrings: hints })`. On unmount or enabled->false: call `ExpoSpeechRecognitionModule.stop()`.

    Also export a utility `pauseForSpeech(): Promise<void>` OR integrate pause-during-TTS directly: before forwarding a final transcript, if `Speech.isSpeakingAsync()` resolves true, return early (Pitfall 4 mitigation). Keep it simple — the screen will also call Speech.stop on action dispatch.

    No dedicated test for useVoiceListener in this task — it's deeply RN/native coupled and will be exercised via the cook.tsx screen test in 09-05. Mark TODO for manual device test.

    **askAssistant.ts** — exports `async function askAssistant(recipeId: string, currentStepIndex: number, question: string): Promise<string>`. Use the same authedFetch helper pattern as mealPlanStore (resolve base URL from env, inject Bearer token from supabase session, POST JSON). On non-2xx, throw Error with server error code. Return `answer` string.

    Test (askAssistant.test.ts): mock global.fetch, assert POST to `/api/v1/cooking/ask` with correct body and Authorization header, returns answer string; throws on 500.
  </action>
  <verify>
    <automated>cd apps/mobile && npm test -- askAssistant useVoiceListener</automated>
  </verify>
  <done>Both files exist, askAssistant tests green, useVoiceListener compiles and imports expo-speech-recognition mock without runtime errors in test environment.</done>
</task>

</tasks>

<verification>
- npm test in apps/mobile passes for all new test files
- No regressions in other mobile store/component tests
- tsc --noEmit clean
</verification>

<success_criteria>
The cooking screen (next plan) can import these three modules and wire them up without needing to know any expo-speech/expo-speech-recognition specifics.
</success_criteria>

<output>
Create `.planning/phases/09-voice-cooking-mode/09-04-SUMMARY.md` describing hook APIs and which pitfalls each one mitigates.
</output>
