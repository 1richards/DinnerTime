---
phase: 11-hybrid-ai-client
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/server/src/services/cookingTips.ts
  - packages/server/src/services/ingredientCategories.ts
  - packages/server/src/routes/cooking.ts
  - packages/server/src/services/__tests__/cookingTips.test.ts
  - packages/server/src/services/__tests__/ingredientCategories.test.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
must_haves:
  truths:
    - "cookingTips.getOrGenerateTip uses getClientFor('cooking.tips').generateText (or generateStructured if the current tool use is essential)"
    - "ingredientCategories.classify uses getClientFor('ingredient.categorize').generateStructured with the classify_ingredients tool"
    - "routes/cooking.ts /ask endpoint uses getClientFor('cooking.voiceAsk').generateText — NO anthropic import remains in the file"
    - "All three consumers route via Gemini 3.1 flash-lite preview per TASK_ROUTES"
    - "cookingTips tests mock factory, verify cache hit still short-circuits AI, verify empty string uncertainty path not cached"
    - "ingredientCategories tests mock factory, verify enum-constrained output handling and input coverage guard"
    - "No /ask route integration test break"
  artifacts:
    - path: "packages/server/src/services/cookingTips.ts"
      provides: "Per-step tips via AIClient, Supabase cache unchanged"
      contains: "getClientFor"
    - path: "packages/server/src/services/ingredientCategories.ts"
      provides: "Grocery classification via AIClient"
      contains: "getClientFor"
    - path: "packages/server/src/routes/cooking.ts"
      provides: "/ask voice Q&A endpoint using AIClient.generateText"
      contains: "getClientFor"
  key_links:
    - from: "packages/server/src/services/cookingTips.ts"
      to: "packages/server/src/ai/clientFactory.ts"
      via: "import { getClientFor }"
      pattern: "getClientFor\\('cooking\\.tips"
    - from: "packages/server/src/services/ingredientCategories.ts"
      to: "packages/server/src/ai/clientFactory.ts"
      via: "import { getClientFor }"
      pattern: "getClientFor\\('ingredient\\.categorize"
    - from: "packages/server/src/routes/cooking.ts"
      to: "packages/server/src/ai/clientFactory.ts"
      via: "import { getClientFor }"
      pattern: "getClientFor\\('cooking\\.voiceAsk"
---

<objective>
Migrate the three classification/short-text consumers — `cookingTips.ts`, `ingredientCategories.ts`, and the `/ask` voice endpoint in `routes/cooking.ts` — onto the AIClient abstraction. All three route to Gemini 3.1 Flash Lite. Preserve cache semantics, enum-constrained classification, and the short-answer voice contract.

Purpose: Complete the migration of every AI call site in the server so Plan 11-05 can safely delete the `config/anthropic.ts` singleton.

Output: Three files migrated, two test files updated, zero remaining `anthropic.messages.create` call sites outside of the Anthropic adapter internal code.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-hybrid-ai-client/11-01-SUMMARY.md
@.planning/phases/11-hybrid-ai-client/11-02-SUMMARY.md
@.planning/phases/11-hybrid-ai-client/11-RESEARCH.md

@packages/server/src/ai/types.ts
@packages/server/src/ai/clientFactory.ts
@packages/server/src/services/cookingTips.ts
@packages/server/src/services/ingredientCategories.ts
@packages/server/src/routes/cooking.ts

<interfaces>
Three call patterns:

```typescript
// cookingTips — plain text output
const ai = getClientFor('cooking.tips');
const tip = await ai.generateText({
  system: TIP_SYSTEM_PROMPT,
  user: `Step: ${stepText}`,
  maxTokens: 120,
});

// ingredientCategories — structured classification
const ai = getClientFor('ingredient.categorize');
const { classifications } = await ai.generateStructured({
  user: `Classify: ${names.join(', ')}`,
  tool: classifyIngredientsTool, // StructuredTool with enum on category
  maxTokens: 1024,
});

// routes/cooking.ts /ask — plain text voice answer
const ai = getClientFor('cooking.voiceAsk');
const answer = await ai.generateText({
  system: systemPromptBuiltFromRecipe,
  user: question,
  maxTokens: 300,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate cookingTips.ts + test (preserve cache + uncertainty empty-string path)</name>
  <files>
    packages/server/src/services/cookingTips.ts,
    packages/server/src/services/__tests__/cookingTips.test.ts
  </files>
  <behavior>
    - getOrGenerateTip(supabase, recipeId, stepIndex, stepText): cache hit (row exists in recipe_step_tips) → returns cached tip, NO AI call.
    - Cache miss → calls getClientFor('cooking.tips').generateText with TIP_SYSTEM_PROMPT as system and the step text as user, maxTokens 120.
    - Returned text (trimmed) — if non-empty, upsert into cache and return.
    - Returned text empty (uncertainty path) → return '' and do NOT cache.
    - Remove HAIKU_MODEL constant and all anthropic imports.
  </behavior>
  <action>
    1. cookingTips.ts:
       - Delete `import { anthropic } from '../config/anthropic.js';` and the `HAIKU_MODEL` constant.
       - Add `import { getClientFor } from '../ai/clientFactory.js';`
       - Replace the `anthropic.messages.create` call + response.content parsing with:
         ```typescript
         const ai = getClientFor('cooking.tips');
         const tip = (await ai.generateText({
           system: TIP_SYSTEM_PROMPT,
           user: `Step: ${stepText}`,
           maxTokens: 120,
         })).trim();
         ```
       - Preserve all cache logic (select → hit short-circuit → miss → AI call → empty-string uncertainty guard → upsert).

    2. cookingTips.test.ts:
       - Swap the @anthropic-ai/sdk mock for the '../../ai/clientFactory.js' factory mock.
       - Use `mockGenerateText.mockResolvedValue('Keep the pan at medium heat...')` for the happy path.
       - Preserve tests for: cache hit (no AI call), cache miss with non-empty result (upserts + returns), cache miss with empty result (returns '' + does NOT upsert), Supabase error propagation.
       - Assert `getClientFor` called with 'cooking.tips' on the miss path, and NOT called on the hit path.

    3. Run `cd packages/server && npm test -- cookingTips` — must be green.
  </action>
  <verify>
    <automated>cd packages/server && npm test -- cookingTips 2>&1 | tail -40 && echo '---' && grep -c "anthropic" src/services/cookingTips.ts</automated>
  </verify>
  <done>cookingTips.ts migrated with cache semantics preserved; test green; zero anthropic refs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate ingredientCategories.ts + test (preserve enum-constrained classification + input coverage)</name>
  <files>
    packages/server/src/services/ingredientCategories.ts,
    packages/server/src/services/__tests__/ingredientCategories.test.ts
  </files>
  <behavior>
    - Exported classify function (whatever it's named — `classifyIngredients` or similar) takes an array of ingredient names, returns a Record<name, GroceryCategory> (or matching existing return type).
    - Calls getClientFor('ingredient.categorize').generateStructured with the existing classifyIngredientsTool (converted to StructuredTool<{classifications: Array<{name: string; category: GroceryCategory}>}>).
    - The `category` field retains its enum constraint in the JsonSchema — critical because Gemini will respect `enum: [...]` on string fields.
    - Input coverage guard: if Gemini returns fewer classifications than inputs, fill missing items with 'other' (preserve existing behavior).
  </behavior>
  <action>
    1. ingredientCategories.ts:
       - Swap imports to getClientFor + StructuredTool types.
       - Convert `classifyIngredientsTool` to `StructuredTool<{ classifications: Array<{name: string; category: GroceryCategory}> }>` with `schema` field — the enum on `category` stays exactly as-is.
       - Replace anthropic.messages.create + tool_use parsing with `const { classifications } = await getClientFor('ingredient.categorize').generateStructured({ user: promptText, tool: classifyIngredientsTool, maxTokens: 1024 });`
       - Preserve the input-coverage guard (fill missing with 'other').

    2. ingredientCategories.test.ts:
       - Swap to factory mock.
       - Use `mockGenerateStructured.mockResolvedValue({ classifications: [...] })`.
       - Preserve tests for: full coverage happy path, partial coverage (missing names filled with 'other'), empty input edge case.
       - Assert `getClientFor` called with 'ingredient.categorize'.

    3. Run `cd packages/server && npm test -- ingredientCategories` — must be green.
  </action>
  <verify>
    <automated>cd packages/server && npm test -- ingredientCategories 2>&1 | tail -40 && echo '---' && grep -c "anthropic" src/services/ingredientCategories.ts</automated>
  </verify>
  <done>ingredientCategories.ts migrated; enum schema preserved; coverage guard intact; test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate routes/cooking.ts /ask endpoint to AIClient.generateText</name>
  <files>
    packages/server/src/routes/cooking.ts
  </files>
  <behavior>
    - POST /ask still accepts the same body and returns { answer: string } with same error handling and auth middleware unchanged.
    - System prompt is built identically via buildSystemPrompt(recipe, currentStep) — unchanged.
    - The AI call is replaced by:
      ```typescript
      const ai = getClientFor('cooking.voiceAsk');
      const answer = await ai.generateText({ system: systemPrompt, user: question, maxTokens: 300 });
      ```
    - The SHORT_ANSWER_RULE is appended to systemPrompt as today (no change to prompt composition).
    - Remove `import { anthropic } from '../config/anthropic.js';` — routes/cooking.ts must have zero anthropic imports.
    - cookingTips import + getOrGenerateTip usage unchanged.
  </behavior>
  <action>
    1. Read the full current `routes/cooking.ts` file to locate the /ask handler.
    2. Remove `import { anthropic } from '../config/anthropic.js';`
    3. Add `import { getClientFor } from '../ai/clientFactory.js';`
    4. Inside the /ask handler, replace the `anthropic.messages.create({ model: 'claude-sonnet-4-latest', ... })` block + response text extraction with the `getClientFor('cooking.voiceAsk').generateText(...)` pattern above. The systemPrompt variable that the existing code passes as `system` maps to our `system` field. The user question body field maps to `user`. max_tokens=300 maps to `maxTokens: 300`.
    5. Preserve all error handling (400 on missing body, 404 on recipe not found, etc.) and the JSON response shape.
    6. If there is no direct test for /ask, add a minimal smoke test in a NEW file `packages/server/src/routes/__tests__/cooking.test.ts`:
       - Mock '../../ai/clientFactory.js' with mockGenerateText + vi.mock for '../middleware/auth.js' (pass-through) and '../services/cookingTips.js'.
       - Mock supabase recipe fetch.
       - POST /cooking/ask with a valid body, assert 200 response with { answer } where answer === mockGenerateText resolved value.
       - Assert getClientFor called with 'cooking.voiceAsk'.
       - If adding this test is too complex due to route wiring, skip it and rely on the type check + the final phase smoke test in 11-05.
    7. Run `cd packages/server && npx tsc --noEmit` — must be clean.
    8. Run `cd packages/server && grep -c "anthropic" src/routes/cooking.ts` — must be 0.
  </action>
  <verify>
    <automated>cd packages/server && npx tsc --noEmit 2>&1 | tail -20 && echo '---' && grep -c "anthropic" src/routes/cooking.ts</automated>
  </verify>
  <done>/ask endpoint uses AIClient abstraction; routes/cooking.ts has zero anthropic refs; tsc clean.</done>
</task>

</tasks>

<verification>
- `cd packages/server && npm test -- cookingTips ingredientCategories` — both green
- `cd packages/server && npx tsc --noEmit` — clean
- `grep -rln "@anthropic-ai/sdk\|from '.*config/anthropic'" packages/server/src/services packages/server/src/routes | grep -v "adapters/anthropicAdapter"` — zero matches (only the adapter should reference the SDK at this point)
</verification>

<success_criteria>
- cookingTips, ingredientCategories, routes/cooking.ts all route via AIClient abstraction
- Cache semantics and enum-constrained classification preserved
- No anthropic singleton imports outside of the Anthropic adapter itself
- All tests in this plan green; full server tsc clean
</success_criteria>

<output>
Create `.planning/phases/11-hybrid-ai-client/11-04-SUMMARY.md` noting: (a) whether a new routes/cooking.test.ts was added, (b) any behavioral subtleties discovered in the cache path during migration, (c) confirmation that the only remaining anthropic SDK reference in the repo is inside anthropicAdapter.ts.
</output>
