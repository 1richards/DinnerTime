---
phase: 05-recipe-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00004_recipes.sql
  - packages/server/src/services/recipeParser.ts
  - apps/mobile/src/types/recipe.ts
autonomous: true
requirements: [RECP-05]
must_haves:
  truths:
    - "recipes table exists in Supabase with correct schema and RLS policies"
    - "ParsedRecipe and ParsedIngredient types are defined and exported for both server and mobile"
    - "cheerio dependency is installed in server package"
  artifacts:
    - path: "supabase/migrations/00004_recipes.sql"
      provides: "recipes table with JSONB ingredients/steps, RLS, indexes"
      contains: "CREATE TABLE recipes"
    - path: "packages/server/src/services/recipeParser.ts"
      provides: "ParsedRecipe interface and parse_recipe tool definition"
      exports: ["ParsedRecipe", "ParsedIngredient"]
    - path: "apps/mobile/src/types/recipe.ts"
      provides: "Mobile-side recipe types matching server schema"
      exports: ["Recipe", "ParsedRecipe", "ParsedIngredient", "ImportSource"]
  key_links:
    - from: "supabase/migrations/00004_recipes.sql"
      to: "packages/server/src/services/recipeParser.ts"
      via: "DB columns match ParsedRecipe fields"
      pattern: "ingredients JSONB.*steps JSONB"
---

<objective>
Create the recipe data foundation: database table, shared type contracts, and dependency installation.

Purpose: All three import paths (URL, photo, manual) and the mobile UI need a shared data schema. This plan establishes those contracts first so subsequent plans implement against defined interfaces.
Output: recipes migration, server-side ParsedRecipe types with tool definition, mobile recipe types, cheerio installed.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-recipe-import/05-RESEARCH.md

@supabase/migrations/00003_pantry_items.sql
@packages/server/src/services/vision.ts
@apps/mobile/src/types/suggestions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recipes database migration and install cheerio</name>
  <files>supabase/migrations/00004_recipes.sql</files>
  <action>
Create the recipes table migration following the exact pattern from 00003_pantry_items.sql:

```sql
CREATE TABLE recipes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  profile_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  description TEXT,
  ingredients JSONB NOT NULL DEFAULT '[]',
  steps JSONB NOT NULL DEFAULT '[]',
  prep_time_minutes INTEGER,
  cook_time_minutes INTEGER,
  total_time_minutes INTEGER,
  servings INTEGER,
  source_type TEXT NOT NULL CHECK (source_type IN ('url', 'photo', 'manual')),
  source_url TEXT,
  image_url TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

Add indexes: idx_recipes_profile (profile_id), idx_recipes_profile_title (profile_id, title).

Add RLS policies following pantry_items pattern exactly:
- SELECT: auth.uid() = profile_id
- INSERT: auth.uid() = profile_id (WITH CHECK)
- UPDATE: auth.uid() = profile_id (USING + WITH CHECK)
- DELETE: auth.uid() = profile_id

Add updated_at trigger using existing public.update_updated_at() function.

Also install cheerio in the server package:
```bash
cd packages/server && npm install cheerio
```
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && test -f supabase/migrations/00004_recipes.sql && grep -q "CREATE TABLE recipes" supabase/migrations/00004_recipes.sql && grep -q "ROW LEVEL SECURITY" supabase/migrations/00004_recipes.sql && cd packages/server && node -e "require('cheerio')" && echo "PASS"</automated>
  </verify>
  <done>recipes migration file exists with correct schema, RLS policies, indexes, and trigger. cheerio is installed and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Define recipe type contracts for server and mobile</name>
  <files>packages/server/src/services/recipeParser.ts, apps/mobile/src/types/recipe.ts</files>
  <action>
Create packages/server/src/services/recipeParser.ts with:
1. Export ParsedIngredient interface: { name: string, quantity: number | null, unit: string | null, notes: string | null }
2. Export ParsedRecipe interface: { title: string, description: string | null, ingredients: ParsedIngredient[], steps: string[], prep_time_minutes: number | null, cook_time_minutes: number | null, total_time_minutes: number | null, servings: number | null, source_url: string | null, source_type: 'url' | 'photo' | 'manual', image_url: string | null }
3. Define the parse_recipe Claude tool definition (following vision.ts foodItemsTool pattern exactly) with input_schema matching ParsedRecipe fields. Tool requires ['title', 'ingredients', 'steps'].
4. Export a parseDuration helper function for ISO 8601 durations (PT1H30M -> 90 minutes). Simple regex-based, no library needed.
5. Leave the actual parsing functions as stubs with TODO comments -- Plan 02 will implement them.

Create apps/mobile/src/types/recipe.ts with:
1. Export Recipe interface matching the DB row shape (id, profile_id, title, description, ingredients as ParsedIngredient[], steps as string[], prep/cook/total time, servings, source_type, source_url, image_url, created_at, updated_at)
2. Export ParsedRecipe and ParsedIngredient matching server-side definitions (mobile mirrors server types, does not share them -- per Phase 3 decision)
3. Export ImportSource type: 'url' | 'photo' | 'manual'
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && cd packages/server && npx tsc --noEmit src/services/recipeParser.ts 2>&1 | head -20 && cd ../../apps/mobile && npx tsc --noEmit src/types/recipe.ts 2>&1 | head -20 && echo "PASS"</automated>
  </verify>
  <done>ParsedRecipe, ParsedIngredient types exported from both server and mobile. parse_recipe tool definition exists. parseDuration helper implemented. Types compile without errors.</done>
</task>

</tasks>

<verification>
- recipes migration SQL is syntactically valid with all RLS policies
- cheerio package is in server package.json dependencies
- Server types compile (npx tsc --noEmit)
- Mobile types compile (npx tsc --noEmit)
- parse_recipe tool schema matches ParsedRecipe interface
</verification>

<success_criteria>
All type contracts are defined and ready for Plan 02 (parser implementation) and Plan 03 (mobile store). Database migration is ready to apply. cheerio is installed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-recipe-import/05-01-SUMMARY.md`
</output>
