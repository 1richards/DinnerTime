---
phase: 12-combine-home-recipes
plan: 02
type: execute
wave: 2
depends_on:
  - "12-combine-home-recipes/01"
files_modified:
  - apps/mobile/src/app/scan/review.tsx
  - apps/mobile/src/app/recipes/review.tsx
  - apps/mobile/src/app/recipes/import-url.tsx
autonomous: true
requirements:
  - UI rationalization (post-v1)

must_haves:
  truths:
    - "Post-scan 'Get Dinner Ideas' flow navigates to Kitchen and autoFetch fires on Suggestions segment"
    - "Post-save recipe flows (URL import + review/edit) navigate to Kitchen with ?segment=library so user sees their new recipe in the library"
    - "No code references to '/(tabs)/recipes' remain anywhere in apps/mobile/src"
    - "No code references to old tab paths cause TypeScript errors"
  artifacts:
    - path: "apps/mobile/src/app/scan/review.tsx"
      provides: "Post-scan navigation updated to Kitchen"
      contains: "(tabs)/kitchen"
    - path: "apps/mobile/src/app/recipes/review.tsx"
      provides: "Post-save recipe review navigation lands on Library segment"
      contains: "segment=library"
    - path: "apps/mobile/src/app/recipes/import-url.tsx"
      provides: "Post-URL-import navigation lands on Library segment"
      contains: "segment=library"
  key_links:
    - from: "apps/mobile/src/app/recipes/review.tsx"
      to: "kitchen.tsx segment param"
      via: "router.replace('/(tabs)/kitchen?segment=library')"
      pattern: "segment=library"
    - from: "apps/mobile/src/app/scan/review.tsx"
      to: "kitchen.tsx Suggestions segment"
      via: "router.replace('/(tabs)/kitchen') (default segment is suggestions)"
      pattern: "\\(tabs\\)/kitchen"
---

<objective>
Update the four call sites that still reference the old `/(tabs)` index or `/(tabs)/recipes` paths. Save flows (recipe review + URL import) must land on Kitchen's Library segment via `?segment=library` so the user sees their newly-saved recipe immediately (Research Pitfall 3). Post-scan "Get Dinner Ideas" flow stays on the default Suggestions segment (autoFetch needs no param).

Purpose: Close the "no orphaned navigation routes" success criterion. Without this plan, save flows silently regress to the wrong segment and the app typecheck fails.

Output: 3 files updated, 4 call sites changed, full typecheck clean, grep sweep confirms zero residual references.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-combine-home-recipes/12-01-SUMMARY.md
@.planning/phases/12-combine-home-recipes/12-RESEARCH.md

# Call-site files to modify
@apps/mobile/src/app/scan/review.tsx
@apps/mobile/src/app/recipes/review.tsx
@apps/mobile/src/app/recipes/import-url.tsx

<interfaces>
<!-- kitchen.tsx was created in Plan 01. It reads this param on mount: -->
From apps/mobile/src/app/(tabs)/kitchen.tsx:
```ts
const params = useLocalSearchParams<{ segment?: 'suggestions' | 'library' }>();
// initial segment = params.segment === 'library' ? 'library' : 'suggestions'
```

<!-- Call site locations (from Research Runtime State Inventory): -->
- apps/mobile/src/app/scan/review.tsx:93 → router.replace('/(tabs)')       (Get Dinner Ideas)
- apps/mobile/src/app/recipes/import-url.tsx:72 → router.replace('/(tabs)/recipes')
- apps/mobile/src/app/recipes/review.tsx:56, 145, 157 → router.replace('/(tabs)/recipes')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update scan/review.tsx post-scan navigation to Kitchen</name>
  <files>apps/mobile/src/app/scan/review.tsx</files>
  <action>
Find the "Get Dinner Ideas" path at approximately line 93 that does `router.replace('/(tabs)')` and change to:

```ts
router.replace('/(tabs)/kitchen');
```

Why not `?segment=suggestions`: Suggestions is the hard-coded default in kitchen.tsx's initial state, and explicitly passing it is redundant. More importantly, `SuggestionList` reads `useSuggestionsStore.autoFetch` flag internally — the existing upstream code sets `autoFetch=true` before the navigate call, and that still works because `SuggestionList` mounts with the parent. Research Pitfall 2 confirms.

Leave the other route in the same file unchanged — line 87's `router.replace('/(tabs)/pantry')` is still correct (pantry tab still exists).

Confirm there's exactly one change by reading the file before and after.
  </action>
  <verify>
    <automated>grep -q "router.replace('/(tabs)/kitchen')" apps/mobile/src/app/scan/review.tsx && grep -q "router.replace('/(tabs)/pantry')" apps/mobile/src/app/scan/review.tsx && ! grep -q "router.replace('/(tabs)')" apps/mobile/src/app/scan/review.tsx</automated>
  </verify>
  <done>scan/review.tsx points "Get Dinner Ideas" at /(tabs)/kitchen; pantry path unchanged; bare /(tabs) no longer present.</done>
</task>

<task type="auto">
  <name>Task 2: Update recipe save flows to land on Library segment</name>
  <files>apps/mobile/src/app/recipes/review.tsx, apps/mobile/src/app/recipes/import-url.tsx</files>
  <action>
Update all 4 call sites that save a recipe and redirect the user back to their library. Each must include `?segment=library` so kitchen.tsx opens on the Library segment — otherwise the user saves a recipe and sees Suggestions, visually "losing" the recipe (Research Pitfall 3).

**`apps/mobile/src/app/recipes/review.tsx`** — three sites around lines 56, 145, 157. Each is currently:
```ts
router.replace('/(tabs)/recipes');
```
Change all three to:
```ts
router.replace('/(tabs)/kitchen?segment=library');
```

**`apps/mobile/src/app/recipes/import-url.tsx`** — one site around line 72. Currently:
```ts
router.replace('/(tabs)/recipes');
```
Change to:
```ts
router.replace('/(tabs)/kitchen?segment=library');
```

After all edits, confirm zero residual `/(tabs)/recipes` references anywhere in the mobile codebase (this is the "no orphaned routes" success criterion from the phase goal). Also confirm full typecheck passes.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && ! grep -r "(tabs)/recipes" apps/mobile/src --include="*.tsx" --include="*.ts" && grep -c "segment=library" apps/mobile/src/app/recipes/review.tsx | grep -q "^3$" && grep -q "segment=library" apps/mobile/src/app/recipes/import-url.tsx && cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Zero /(tabs)/recipes references remain in apps/mobile/src. recipes/review.tsx has exactly 3 segment=library redirects; recipes/import-url.tsx has 1. Full TypeScript check passes (no errors in any file).</done>
</task>

</tasks>

<verification>
1. Grep sweep: `grep -r "(tabs)/recipes" apps/mobile/src --include="*.tsx" --include="*.ts"` returns zero matches.
2. Post-save redirects: `grep -rn "segment=library" apps/mobile/src/app/recipes/` returns 4 lines total (3 in review.tsx, 1 in import-url.tsx).
3. Post-scan redirect: `grep -n "(tabs)/kitchen" apps/mobile/src/app/scan/review.tsx` returns 1 line.
4. Full `cd apps/mobile && npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- All 4 route-call-sites updated to target `/(tabs)/kitchen` (with `?segment=library` on save flows)
- Zero orphaned references to `/(tabs)/recipes` in mobile source
- Full TypeScript typecheck passes for apps/mobile (previously blocked on these 4 sites)
- kitchen.tsx correctly honors the `?segment=library` param (integration point with Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/12-combine-home-recipes/12-02-SUMMARY.md` per template.
</output>
