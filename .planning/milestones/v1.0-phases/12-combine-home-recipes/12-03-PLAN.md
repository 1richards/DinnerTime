---
phase: 12-combine-home-recipes
plan: 03
type: execute
wave: 3
depends_on:
  - "12-combine-home-recipes/01"
  - "12-combine-home-recipes/02"
files_modified:
  - apps/mobile/.maestro/03-import-url.yaml
  - apps/mobile/.maestro/04-import-manual.yaml
  - apps/mobile/.maestro/05-recipe-detail-edit.yaml
  - apps/mobile/.maestro/06-recipe-discover.yaml
  - apps/mobile/.maestro/08-home-suggestions.yaml
  - apps/mobile/.maestro/18-recipe-search-favorite.yaml
  - apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
autonomous: false
requirements:
  - UI rationalization (post-v1)

must_haves:
  truths:
    - "All Maestro flows that previously tapped 'Recipes' tab now tap 'Kitchen' + 'Library' segment"
    - "All Maestro flows that previously asserted 'My Recipes' now assert a stable post-merge marker"
    - "New 20-kitchen-segment-toggle.yaml flow verifies segment state preservation end-to-end"
    - "Full Maestro suite passes against a running dev client on iOS Simulator"
    - "Human-verified: segment toggle preserves Library search + filter state across session"
  artifacts:
    - path: "apps/mobile/.maestro/20-kitchen-segment-toggle.yaml"
      provides: "New Maestro flow covering segment state preservation"
      contains: "Suggestions"
    - path: "apps/mobile/.maestro/03-import-url.yaml"
      provides: "Updated import-URL flow targeting Kitchen + Library"
      contains: "Kitchen"
  key_links:
    - from: "Maestro flows (03, 04, 05, 06, 18)"
      to: "kitchen.tsx Library segment"
      via: "tapOn Kitchen then tapOn Library"
      pattern: "tapOn.*Library"
    - from: "Maestro flow 20"
      to: "segment state preservation"
      via: "tap Library, type search, switch segments, assert text persists"
      pattern: "Search recipes"
---

<objective>
Update the 6 Maestro flows that target the old Home/Recipes tabs so they navigate through the new Kitchen tab, and fill in the stub 20-kitchen-segment-toggle.yaml flow created in Plan 01 with real assertions. End the phase with a human-verified UAT checkpoint on the iOS Simulator.

Purpose: Without updated flows, the UAT harness (per CLAUDE.md "Before reporting a UI feature complete, validate it with Maestro") reports false failures against the new UI. This plan is the UAT closure of Phase 12.

Output: 6 flows rewritten + 1 flow filled in + green Maestro run + human visual checkpoint confirming segment state preservation and FAB swap behavior.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-combine-home-recipes/12-01-SUMMARY.md
@.planning/phases/12-combine-home-recipes/12-02-SUMMARY.md
@.planning/phases/12-combine-home-recipes/12-RESEARCH.md

# Maestro flows to rewrite
@apps/mobile/.maestro/03-import-url.yaml
@apps/mobile/.maestro/04-import-manual.yaml
@apps/mobile/.maestro/05-recipe-detail-edit.yaml
@apps/mobile/.maestro/06-recipe-discover.yaml
@apps/mobile/.maestro/08-home-suggestions.yaml
@apps/mobile/.maestro/18-recipe-search-favorite.yaml
@apps/mobile/.maestro/20-kitchen-segment-toggle.yaml

# Supporting context
@apps/mobile/.maestro/_ensure-logged-in.yaml
@apps/mobile/.maestro/01-login.yaml
@apps/mobile/.maestro/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 6 Maestro flows for the new Kitchen tab</name>
  <files>apps/mobile/.maestro/03-import-url.yaml, apps/mobile/.maestro/04-import-manual.yaml, apps/mobile/.maestro/05-recipe-detail-edit.yaml, apps/mobile/.maestro/06-recipe-discover.yaml, apps/mobile/.maestro/08-home-suggestions.yaml, apps/mobile/.maestro/18-recipe-search-favorite.yaml</files>
  <action>
Read each flow first to understand its exact current assertions, then apply these mechanical rewrites per Research Pitfall 4:

For flows 03, 04, 05, 06, 18 (the ones that navigate to the Recipes tab):
- Replace any step that taps "Recipes" with a two-step sequence: tap "Kitchen" then tap "Library".
- Replace any assertVisible of "My Recipes" with a stable post-merge marker. Prefer `assertVisible: "Search recipes"` (the SearchBar placeholder). Fallback to `assertVisible: "Kitchen"` against the compact header if the search bar is not yet open at that point in the flow.
- Keep all other steps (screenshots, tapOn recipe titles, individual recipe UI assertions) unchanged.

Per-flow notes:
- 03-import-url.yaml: lines ~9 and ~73. Standard rewrite at both sites.
- 04-import-manual.yaml: lines ~9 and ~59. Standard rewrite at both sites.
- 05-recipe-detail-edit.yaml: lines ~11 and ~16. Standard rewrite.
- 06-recipe-discover.yaml: lines ~9 and ~14. The Discover button now lives in the action row on the Library segment.
- 08-home-suggestions.yaml: rename the flow `name:` header from "home suggestions" to "kitchen: suggestions segment renders after login" (cosmetic). The `assertVisible: ".*What should we cook tonight.*"` assertion still passes because Kitchen opens on Suggestions by default. Only rewrite steps if the flow contains a literal tap on "Home".
- 18-recipe-search-favorite.yaml: lines ~11 and ~16. Standard rewrite. This flow also types in the search bar, so verify the order is tap Kitchen, tap Library, tap the search-bar toggle, then type.

After all edits, do NOT run the flows yet — Task 2 fills in flow 20 and Task 3 runs the full suite in a single pass so any failure is attributable to one run.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/patrickrichards/DinnerTime && for f in 03-import-url 04-import-manual 05-recipe-detail-edit 06-recipe-discover 18-recipe-search-favorite; do grep -q Kitchen "apps/mobile/.maestro/$f.yaml" && grep -q Library "apps/mobile/.maestro/$f.yaml" || { echo "FAIL: $f"; exit 1; }; done'</automated>
  </verify>
  <done>All 5 rewrite flows contain both "Kitchen" and "Library" tokens; flow 08 renamed or unchanged depending on whether it tapped Home; no residual "My Recipes" assertions remain in flows 03-06 and 18.</done>
</task>

<task type="auto">
  <name>Task 2: Fill in 20-kitchen-segment-toggle.yaml with real assertions</name>
  <files>apps/mobile/.maestro/20-kitchen-segment-toggle.yaml</files>
  <action>
Replace the stub TODO content created in Plan 01 with a real segment-toggle flow. The flow should prove (a) segments exist and switch, (b) Library state (search query) survives a round-trip toggle, (c) FABs swap correctly between segments.

Structure the flow as follows:

```yaml
appId: com.dinnertime.app
name: "20 - kitchen: segment toggle preserves state"
---
- runFlow: _ensure-logged-in.yaml

# 1. Verify default segment is Suggestions
- assertVisible: "What should we cook tonight"
- takeScreenshot: 20-01-suggestions-default

# 2. Switch to Library, verify search bar placeholder visible
- tapOn: "Library"
- assertVisible: "Search recipes"
- takeScreenshot: 20-02-library-active

# 3. Open search and type a query
- tapOn:
    id: "search-toggle"
    optional: true
- inputText: "chicken"
- takeScreenshot: 20-03-library-search-typed

# 4. Switch to Suggestions, verify hero/greeting visible
- tapOn: "Suggestions"
- assertVisible: "What should we cook tonight"
- takeScreenshot: 20-04-back-to-suggestions

# 5. Switch back to Library, verify search query persisted
- tapOn: "Library"
- assertVisible: "chicken"
- takeScreenshot: 20-05-library-state-preserved
```

Notes on fragility and selectors (per CLAUDE.md Maestro selector guidance):
- Use plain text labels ("Library", "Suggestions", "Kitchen"). Avoid regex with special characters.
- `assertVisible: "chicken"` will find the typed text in the search input.
- If the `search-toggle` id does not exist on the SearchToggleButton, drop those two lines or rely on SearchBar being visible by default. An alternative is to apply a filter chip via `tapOn: "Filter"` then `tapOn: "Favorites only"` and assert the filter count badge survives the toggle.
- Screenshots are "free debugging gold" — take them liberally.

Reference `apps/mobile/.maestro/README.md` to confirm you follow the inventory conventions.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/patrickrichards/DinnerTime && grep -q "Library" apps/mobile/.maestro/20-kitchen-segment-toggle.yaml && grep -q "Suggestions" apps/mobile/.maestro/20-kitchen-segment-toggle.yaml && grep -q "What should we cook tonight" apps/mobile/.maestro/20-kitchen-segment-toggle.yaml && ! grep -q "TODO" apps/mobile/.maestro/20-kitchen-segment-toggle.yaml'</automated>
  </verify>
  <done>Flow 20 contains real assertions for both segments and a round-trip toggle that proves state preservation; no TODO marker remains; uses plain-text selectors throughout.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Maestro suite on iOS Simulator</name>
  <files>apps/mobile/.maestro</files>
  <action>
Execute the full Maestro test suite against a running dev client on the iOS Simulator. Follow the runbook in CLAUDE.md UAT section.

Preparation:
```
cd apps/mobile
xcrun simctl boot "iPhone 17 Pro" || true
open -a Simulator
xcrun simctl install booted ios/build/Build/Products/Debug-iphonesimulator/DinnerTime.app
```

Start Metro in LAN mode (NOT tunnel — per CLAUDE.md, tunnel mode makes the simulator try `.exp.direct` URLs that fail behind iOS ATS):
```
npx expo start --dev-client --lan
```

Let Metro settle for ~5 seconds, then in a second shell run the full suite:
```
cd apps/mobile
maestro test .maestro/
```

Or use the helper if present: `apps/mobile/.maestro/.scripts/uat.sh all`.

Expected outcome: all flows pass. Previously 16 flows green on `main`; Phase 12 adds 1 more (flow 20), target is 17/17.

If any flow fails:
1. Read the Maestro output and identify the flow and step that failed.
2. If it's a Kitchen-related flow, the fix is in the .yaml file — edit and re-run.
3. If a non-Kitchen flow regresses (e.g., a pantry flow unexpectedly broke), the fix is in kitchen.tsx or _layout.tsx — Plan 01's scope.
4. DO NOT commit flow changes that paper over real bugs. If flow 03 can't find the saved recipe after save, the kitchen.tsx `?segment=library` handling is broken and Plan 01 needs revisiting.

Record pass/fail counts in the summary.
  </action>
  <verify>
    <automated>bash -c 'cd /Users/patrickrichards/DinnerTime/apps/mobile && maestro test .maestro/ 2>&1 | tail -40 | tee /tmp/maestro-12-03.log && ! grep -qiE "(FAILED|Flow FAILED|failed with)" /tmp/maestro-12-03.log'</automated>
  </verify>
  <done>Full Maestro suite passes. 17/17 flows green (16 existing + 1 new kitchen-segment-toggle). No Kitchen-related regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT checkpoint on iOS Simulator</name>
  <files>apps/mobile (visual verification only)</files>
  <action>
Pause for the user to verify the unified Kitchen tab visually on the iOS Simulator. The automated Maestro suite confirms the happy paths; this checkpoint catches visual regressions and confirms the CONTEXT-locked UX decisions (segment state preservation, FAB swap, hero greeting, tab order). See `<what-built>` and `<how-to-verify>` below.
  </action>
  <what-built>
The unified Kitchen tab is live:
- Tab bar is now 4 tabs (Kitchen, Plan, Pantry, Shopping), with Kitchen leftmost.
- Kitchen defaults to the Suggestions segment showing the daily hero image + "Hey, {name}!" greeting.
- Segmented control (Suggestions | Library) switches between views.
- Library segment preserves search, filter, and scroll state across toggles.
- Import FAB (+) only appears on Library. Regenerate FAB (sparkles) only appears on Suggestions and calls fetchSuggestions.
- Save flows (URL import, recipe review) land on Library via `?segment=library`.
- Post-scan "Get Dinner Ideas" flow lands on Suggestions with autoFetch.
- 17/17 Maestro flows green.
  </what-built>
  <how-to-verify>
With the simulator running and Metro serving (`npx expo start --dev-client --lan` from Task 3):

1. Tab bar check: Launch app, log in. Confirm the tab bar shows exactly 4 tabs in order: Kitchen | Plan | Pantry | Shopping. Kitchen icon is the restaurant (plate+utensils) Ionicon.
2. Default segment + hero: Kitchen opens on Suggestions. Hero image visible with "Hey, {name}!" and "What should we cook tonight?" subtitle. Sparkles FAB visible bottom-right. No Import (+) FAB.
3. Switch to Library: Tap "Library" segment. Hero disappears, header becomes "Kitchen" / "{N} recipes". Import (+) FAB visible bottom-right. Sparkles FAB gone. Search bar and filter chips accessible.
4. State preservation: On Library, tap the search icon, type "chicken". Tap "Suggestions" segment — hero is back. Tap "Library" again — your "chicken" query is STILL in the search bar. (This is the CONTEXT locked decision; failure = display:none regressed to conditional render.)
5. Scroll preservation: On Library, scroll down a few recipes. Switch to Suggestions, switch back. Library scroll position should be preserved.
6. Settings gear: Top-right gear icon visible and functional on BOTH segments.
7. Save flow: Tap Import (+) FAB → Paste a URL → Save. Should return to Kitchen with Library segment active (not Suggestions) and the new recipe visible.
8. Scan flow: Go to Pantry → Scan → after review, tap "Get Dinner Ideas". Should land on Kitchen → Suggestions with dinner cards populated.
9. Regenerate: On Suggestions, tap the sparkles FAB. Suggestions should refetch (loading state then new cards).
  </how-to-verify>
  <verify>Manual visual verification — see <how-to-verify> above.</verify>
  <done>User types "approved" after all 9 checks pass on the iOS Simulator.</done>
  <resume-signal>Type "approved" if all 9 checks pass. Describe the specific failing check if any fail (include screenshot if possible — the simulator's screenshot is one click from the File menu).</resume-signal>
</task>

</tasks>

<verification>
1. Maestro flows on disk: 6 updated flows + 1 new flow = 7 files touched, all valid YAML.
2. Full suite green: 17/17 Maestro flows pass.
3. Human checkpoint passed: all 9 UAT checks approved.
4. No residual "My Recipes" or "Home" tab references in .maestro/: `grep -r "My Recipes\\|tapOn.*Home" apps/mobile/.maestro/*.yaml` returns nothing.
</verification>

<success_criteria>
- 6 existing Maestro flows rewritten to navigate via Kitchen + Library
- New 20-kitchen-segment-toggle.yaml flow covers segment state preservation
- Full Maestro suite (17 flows) passes on iOS Simulator
- Human UAT checkpoint confirms segment toggle, FAB swap, save flow, scan flow, regenerate flow all work
- Phase 12 success criteria fully met: tab bar is 4 tabs, Home + Recipes consolidated, all existing features accessible, no orphaned routes
</success_criteria>

<output>
After completion, create `.planning/phases/12-combine-home-recipes/12-03-SUMMARY.md` per template and update STATE.md current position to "Phase 12 complete".
</output>
