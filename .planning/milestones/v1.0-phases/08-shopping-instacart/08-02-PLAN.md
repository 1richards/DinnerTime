---
phase: 08-shopping-instacart
plan: 02
type: tdd
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/server/src/services/shoppingList.ts
  - packages/server/src/services/__tests__/shoppingList.test.ts
autonomous: true
requirements: [SHOP-01, SHOP-02, SHOP-07]
must_haves:
  truths:
    - "consolidateIngredients sums matching-unit quantities and nulls unit on conflict"
    - "subtractPantry removes fully-stocked items and reduces partial ones"
    - "suggestVariations returns 3-5 structured swap suggestions via mocked Claude Haiku tool use"
  artifacts:
    - path: packages/server/src/services/shoppingList.ts
      provides: "consolidateIngredients, subtractPantry, suggestVariations"
      exports: ["consolidateIngredients", "subtractPantry", "suggestVariations"]
    - path: packages/server/src/services/__tests__/shoppingList.test.ts
      provides: "Unit tests covering consolidation, subtraction, and variations"
  key_links:
    - from: packages/server/src/services/shoppingList.ts
      to: packages/server/src/services/ingredientMatching.ts
      via: "imports normalizeIngredientName"
      pattern: "from ['\"].*ingredientMatching"
    - from: packages/server/src/services/shoppingList.ts
      to: "anthropic SDK client"
      via: "lazy singleton from existing anthropic client module"
      pattern: "anthropic\\.messages\\.create"
---

<objective>
Build the pure TypeScript shopping-list service functions (consolidate, pantry subtract, AI variations) under TDD. These are the mathematical core of SHOP-01, SHOP-02, SHOP-07.

Purpose: Pure functions unit-tested in isolation — zero Supabase coupling. Route layer (Plan 05) will compose them.
Output: One service file + one test file covering all three behaviors.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-shopping-instacart/08-RESEARCH.md
@.planning/phases/08-shopping-instacart/08-01-SUMMARY.md
@packages/server/src/services/ingredientMatching.ts
@packages/server/src/types/shopping.ts
@packages/server/src/types/mealPlan.ts

<interfaces>
From packages/server/src/services/ingredientMatching.ts (Phase 07-03):
```typescript
export function normalizeIngredientName(name: string): string;
// Collapses case, strips trailing 'es'/'s' so 'Tomatoes' -> 'tomato'
```

From packages/server/src/types/shopping.ts (Plan 01):
```typescript
export interface ConsolidatedItem { name; normalizedName; quantity; unit; sources; category? }
export interface VariationSuggestion { instead_of; swap; rationale }
```

Reuse the existing anthropic lazy singleton (introduced Phase 03). Do not instantiate a new client — import getAnthropicClient or similar from the established module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD consolidateIngredients + subtractPantry</name>
  <files>packages/server/src/services/shoppingList.ts, packages/server/src/services/__tests__/shoppingList.test.ts</files>
  <behavior>
    - consolidateIngredients: two entries each with "tomato" 2 cups → one item quantity=4 unit="cups"
    - consolidateIngredients: "2 cups chicken broth" + "1 can chicken broth" → quantity=max(2,1)=2 unit=null, sources contains both recipe titles
    - consolidateIngredients: "Tomatoes" + "tomato" collapse to same normalized key (leverages normalizeIngredientName)
    - consolidateIngredients: sources dedupes duplicate recipe titles
    - subtractPantry: pantry has 5 of item, need 3 → item removed entirely
    - subtractPantry: pantry has 2 of item, need 5 → item remains with quantity 3
    - subtractPantry: pantry name "tomato" matches needed "Tomatoes" via normalization
    - subtractPantry: item with no pantry match passes through unchanged
  </behavior>
  <action>
Write the test file FIRST (RED), then implement the two functions to pass.

Implementation per research Pattern 2:
- `consolidateIngredients(entries: MealPlanEntry[]): ConsolidatedItem[]` iterates entries, builds Map keyed by normalized name, sums when units match, else takes max and nulls unit.
- `subtractPantry(needed: ConsolidatedItem[], pantry: PantryItem[]): ConsolidatedItem[]` builds pantry Map by normalized name, subtracts quantity, drops items where remaining <= 0.

Use types from `packages/server/src/types/shopping.ts` and `packages/server/src/types/mealPlan.ts`. Check existing PantryItem type in services/pantry.ts (Phase 03) and import from there.

Avoid: Do NOT attempt unit conversion (cups → tbsp). Unit mismatch → null unit per research Pitfall 2. Do NOT mutate the input arrays.
  </action>
  <verify>
    <automated>pnpm -C packages/server test shoppingList.test.ts -- --run</automated>
  </verify>
  <done>All consolidation and subtraction tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD suggestVariations with mocked Claude Haiku</name>
  <files>packages/server/src/services/shoppingList.ts, packages/server/src/services/__tests__/shoppingList.test.ts</files>
  <behavior>
    - suggestVariations: given 3 items, calls anthropic.messages.create with model 'claude-haiku-4-latest' and tool_choice forcing 'suggest_swaps'
    - suggestVariations: parses tool_use block and returns VariationSuggestion[] of 3-5 items
    - suggestVariations: each suggestion has instead_of, swap, rationale strings
    - suggestVariations: throws if Claude returns no tool_use block
  </behavior>
  <action>
Extend the test file with vi.mock for the anthropic client module (use vi.hoisted per Phase 01 decision: "vi.hoisted for Vitest mock variables").

Implement `suggestVariations(items: ConsolidatedItem[]): Promise<VariationSuggestion[]>` per research "Reorder with AI variations" code example:
- Use `claude-haiku-4-latest` model
- Tool schema: `suggest_swaps` with `swaps: array maxItems 5 of {instead_of, swap, rationale}`
- `tool_choice: { type: 'tool', name: 'suggest_swaps' }`
- Parse the first `tool_use` content block → return `input.swaps`
- Throw `Error('no tool_use in response')` if missing

Avoid: Do NOT import the full anthropic SDK type-expensively in the test — use `vi.mock` on the local client module. Do NOT hardcode swap responses; mock must return a realistic tool_use block.
  </action>
  <verify>
    <automated>pnpm -C packages/server test shoppingList.test.ts -- --run</automated>
  </verify>
  <done>Variation tests pass with mocked Haiku response; tool schema matches research example.</done>
</task>

</tasks>

<verification>
- `pnpm -C packages/server test shoppingList.test.ts -- --run` all green
- No real network calls (anthropic mocked)
</verification>

<success_criteria>
SHOP-01 (consolidate), SHOP-02 (subtract), SHOP-07 (variations) core logic exists and is test-covered.
</success_criteria>

<output>
Create `.planning/phases/08-shopping-instacart/08-02-SUMMARY.md`
</output>
