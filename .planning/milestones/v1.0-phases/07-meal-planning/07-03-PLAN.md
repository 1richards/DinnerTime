---
phase: 07-meal-planning
plan: 03
type: tdd
wave: 3
depends_on: ["07-02"]
files_modified:
  - packages/server/src/services/mealPlanner.ts
  - packages/server/src/services/__tests__/mealPlanner.test.ts
  - packages/server/src/services/ingredientMatching.ts
  - packages/server/src/services/__tests__/ingredientMatching.test.ts
  - packages/server/src/routes/meal-plans.ts
  - packages/server/src/routes/__tests__/meal-plans.test.ts
autonomous: true
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07]
must_haves:
  truths:
    - "POST /meal-plans/generate produces a 7-day plan via generateMealPlan"
    - "GET /meal-plans/current returns active plan for the current Monday week_start"
    - "POST /meal-plans/:id/entries/:day/regenerate replaces a single entry, excluding current title"
    - "POST /meal-plans/:id/entries/:day/cook deducts matched pantry items and marks entry cooked"
    - "Cook endpoint is idempotent — second call on already-cooked entry returns 409 with no pantry delta"
  artifacts:
    - path: packages/server/src/routes/meal-plans.ts
      provides: "Four endpoints: GET /current, POST /generate, POST /:id/entries/:day/regenerate, POST /:id/entries/:day/cook"
      exports: [default]
    - path: packages/server/src/services/ingredientMatching.ts
      provides: "matchIngredientsToPantry(entryIngredients, pantryItems) -> match list + deductions"
      exports: [matchIngredientsToPantry, normalizeIngredientName]
    - path: packages/server/src/services/mealPlanner.ts
      provides: "regenerateDay, markCooked service functions"
      exports: [regenerateDay, markCooked]
  key_links:
    - from: packages/server/src/routes/meal-plans.ts
      to: packages/server/src/services/mealPlanner.ts
      via: "Import generateMealPlan, regenerateDay, markCooked"
      pattern: "from.*mealPlanner"
    - from: packages/server/src/services/mealPlanner.ts
      to: packages/server/src/services/ingredientMatching.ts
      via: "markCooked calls matchIngredientsToPantry"
      pattern: "matchIngredientsToPantry"
---

<objective>
Complete the server side of Phase 7: swap (PLAN-05), cook-deduct (PLAN-07), route wiring (PLAN-01/06), and shared ingredient matching utility.

Purpose: Without these, the mobile store has nothing to call. Ingredient matching is factored into its own tested module because both cook-deduct now and Phase 8 shopping later will need it.
Output: Two new service functions + one utility + complete route module, all tested.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-meal-planning/07-RESEARCH.md
@.planning/phases/07-meal-planning/07-01-SUMMARY.md
@.planning/phases/07-meal-planning/07-02-SUMMARY.md
@packages/server/src/routes/meal-plans.ts
@packages/server/src/routes/recipes.ts
@packages/server/src/routes/__tests__/recipes.test.ts
@packages/server/src/services/pantry.ts
@packages/server/src/services/mealPlanner.ts

<interfaces>
Route wiring follows the pattern in packages/server/src/routes/recipes.ts:
- `const app = new Hono();`
- `app.use('*', authMiddleware);`
- `const supabase = c.get('supabase');`
- `const profileId = c.get('profileId');`
- Return `c.json(...)` with appropriate status codes

Existing 501 stub at packages/server/src/routes/meal-plans.ts must be fully replaced.

Ingredient matching must reuse `normalized_name` logic already applied to pantry items in Phase 03 (see packages/server/src/services/pantry.ts for the normalization helper — reuse, do NOT redefine).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD ingredientMatching utility</name>
  <files>packages/server/src/services/ingredientMatching.ts, packages/server/src/services/__tests__/ingredientMatching.test.ts</files>
  <behavior>
    Pure function: `matchIngredientsToPantry(entryIngredients: MealPlanIngredient[], pantryItems: PantryItem[]): MatchResult`

    MatchResult = { matches: Array<{ pantryItemId, ingredientName, deductQuantity, willDeplete }>, unmatched: string[] }

    Tests (RED first):
    - Test 1: Exact name match deducts full quantity
    - Test 2: Normalized name match (case + whitespace + singular/plural) still matches ("Tomatoes" matches pantry "tomato")
    - Test 3: Quantity deduction: pantry has 4, recipe needs 2 → deductQuantity=2, willDeplete=false
    - Test 4: Pantry quantity <= recipe needs → willDeplete=true (pantry item will be marked 'used')
    - Test 5: Unmatched ingredients listed in unmatched array and NOT in matches
    - Test 6: Ingredient with undefined quantity → deduct 1 unit by default
    - Test 7: Multiple pantry items with same normalized name → match against first
    - Test 8: normalizeIngredientName trims, lowercases, removes trailing 's' naively

    Reuse any existing normalization helper from packages/server/src/services/pantry.ts if present; otherwise define normalizeIngredientName here and document migration path.
  </behavior>
  <action>
    RED → GREEN → REFACTOR on ingredientMatching. Commit at RED and GREEN boundaries as in 07-02.
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/server test ingredientMatching</automated>
  </verify>
  <done>ingredientMatching.ts exports matchIngredientsToPantry + normalizeIngredientName; all 8 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD regenerateDay + markCooked service functions</name>
  <files>packages/server/src/services/mealPlanner.ts, packages/server/src/services/__tests__/mealPlanner.test.ts</files>
  <behavior>
    Extend mealPlanner.ts with two new exports.

    `regenerateDay(supabase, profileId, planId, dayOfWeek): Promise<MealPlanEntry>`:
    - Load existing entry (verify plan belongs to profileId via RLS or explicit check)
    - Fetch fresh pantry + prefs + recipe library (Pitfall 2 — do NOT use snapshot)
    - Build a scoped prompt asking for ONE alternative for that day, excluding currentEntry.title
    - Tool schema narrowed to single-day shape (or reuse full tool and take day index match)
    - Update the entry row in place (preserve id + day_of_week)
    - Return updated entry

    `markCooked(supabase, profileId, planId, dayOfWeek): Promise<{ entry, pantryDelta }>`:
    - Load entry. If status === 'cooked' → throw { code: 'ALREADY_COOKED', status: 409 }
    - Load pantry items for profileId
    - Call matchIngredientsToPantry with entry.ingredients
    - For each match: UPDATE pantry_items SET quantity = quantity - deductQuantity (and status='used' if willDeplete)
    - UPDATE entry SET status='cooked', cooked_at=NOW()
    - Return updated entry + array of pantry deltas

    Tests (RED first):
    - Test 1: regenerateDay fetches fresh pantry (mock supabase verifies pantry_items .select called AFTER plan fetch)
    - Test 2: regenerateDay prompt includes the excluded title
    - Test 3: regenerateDay updates only the target entry (no other days touched)
    - Test 4: markCooked idempotency — second call returns 409 ALREADY_COOKED
    - Test 5: markCooked deducts matched pantry items by correct quantities
    - Test 6: markCooked marks pantry item status='used' when deductQuantity >= current quantity
    - Test 7: markCooked sets entry.status='cooked' and cooked_at is non-null
    - Test 8: markCooked returns pantryDelta array with {pantryItemId, newQuantity, status}
  </behavior>
  <action>
    RED → GREEN → REFACTOR. Append to existing mealPlanner.test.ts. Reuse Anthropic mock pattern from Task 2 of 07-02.

    Commits: `test(07-03): failing tests for regenerateDay/markCooked` then `feat(07-03): regenerate + cook-deduct`.
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/server test mealPlanner</automated>
  </verify>
  <done>All 8 tests green; mealPlanner.ts exports regenerateDay + markCooked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD Hono routes in meal-plans.ts</name>
  <files>packages/server/src/routes/meal-plans.ts, packages/server/src/routes/__tests__/meal-plans.test.ts</files>
  <behavior>
    Replace the 501 stub with four endpoints, all under authMiddleware.

    Endpoints:
    1. `GET /current` → returns active plan for current Monday week_start (computed server-side via native Date — Monday = 1, Sunday = 0 so adjust). Returns 404 if no plan.
    2. `POST /generate` body `{ week_start: string }` → calls generateMealPlan, returns 201 with MealPlan. 400 on empty-pantry error. 502 on invalid-length error.
    3. `POST /:id/entries/:day/regenerate` → validates day param is 0..6, calls regenerateDay, returns 200 with updated entry.
    4. `POST /:id/entries/:day/cook` → calls markCooked, returns 200 with { entry, pantryDelta }. Returns 409 ALREADY_COOKED.

    Tests (RED first) — follow the pattern in recipes.test.ts (Hono test client, mocked Supabase + mocked service layer):
    - Test 1: GET /current unauthenticated → 401
    - Test 2: GET /current returns 404 when no plan exists for current week
    - Test 3: POST /generate with valid body → 201 and body contains 7 entries
    - Test 4: POST /generate with empty pantry → 400 EMPTY_PANTRY
    - Test 5: POST /:id/entries/5/regenerate → 200 with updated entry
    - Test 6: POST /:id/entries/9/regenerate → 400 (day out of range)
    - Test 7: POST /:id/entries/0/cook → 200 with pantryDelta populated
    - Test 8: POST /:id/entries/0/cook called twice → second call 409 ALREADY_COOKED

    Mock the mealPlanner service module via vi.mock so route tests only verify wiring.
  </behavior>
  <action>
    RED → GREEN. Delete 501 stub, implement handlers. Register the router in server entry if not already registered (check existing patterns for how ai/pantry/recipes are mounted — meal-plans.ts is likely already mounted since the stub existed).

    Week start computation: `function mondayOf(date: Date): string` — compute Monday 00:00 UTC, return YYYY-MM-DD. Test this helper inline or extract.

    Commits: `test(07-03): failing route tests for meal-plans` then `feat(07-03): implement meal-plans routes`.
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/server test meal-plans</automated>
  </verify>
  <done>meal-plans.ts has 4 working endpoints, all 8 route tests pass, 501 stub removed.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @dinnertime/server test` all green
- `pnpm --filter @dinnertime/server lint` clean
- Manual grep: `grep -r "501" packages/server/src/routes/meal-plans.ts` returns nothing
</verification>

<success_criteria>
- Ingredient matching, regenerate, markCooked, and all four routes are implemented and tested
- Cook is idempotent (409 on second call)
- Swap re-fetches pantry (not snapshot)
- Week-start is computed as Monday server-side
</success_criteria>

<output>
Create `.planning/phases/07-meal-planning/07-03-SUMMARY.md` after completion.
</output>
</content>
</invoke>