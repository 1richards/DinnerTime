---
phase: 07-meal-planning
plan: 05
type: execute
wave: 4
depends_on: ["07-03", "07-04"]
files_modified:
  - apps/mobile/src/app/(tabs)/_layout.tsx
  - apps/mobile/src/app/(tabs)/plan.tsx
  - apps/mobile/src/components/plan/DayRow.tsx
  - apps/mobile/src/components/plan/SwapSheet.tsx
  - apps/mobile/src/components/plan/CookConfirm.tsx
  - apps/mobile/src/components/plan/EmptyPlanState.tsx
autonomous: false
requirements: [PLAN-01, PLAN-05, PLAN-06, PLAN-07]
must_haves:
  truths:
    - "User can navigate to a Plan tab and see either the empty state or the current week's plan"
    - "User can tap Generate and receive a 7-day plan (loading state shown during generation)"
    - "User can see 7 day rows labeled Mon..Sun with title, time, difficulty, kid-friendly badge"
    - "User can tap Swap on any day and receive an alternative meal"
    - "User can tap Cooked on any day and see the entry marked cooked + pantry deduction feedback"
    - "User can tap an entry row to see details (description, why_suggested, ingredients)"
  artifacts:
    - path: apps/mobile/src/app/(tabs)/plan.tsx
      provides: "Plan tab screen — orchestrates store, renders DayRow list, generate button, empty state"
      min_lines: 80
    - path: apps/mobile/src/components/plan/DayRow.tsx
      provides: "Single day row with label, title, meta, swap/cook buttons"
      min_lines: 40
    - path: apps/mobile/src/components/plan/SwapSheet.tsx
      provides: "Bottom sheet confirming swap with loading spinner"
    - path: apps/mobile/src/components/plan/CookConfirm.tsx
      provides: "Modal/sheet confirming cook action and showing pantry deltas after success"
    - path: apps/mobile/src/components/plan/EmptyPlanState.tsx
      provides: "Empty state prompting user to generate first plan"
    - path: apps/mobile/src/app/(tabs)/_layout.tsx
      provides: "Plan tab registered in tab bar"
      contains: "name=\"plan\""
  key_links:
    - from: apps/mobile/src/app/(tabs)/plan.tsx
      to: apps/mobile/src/stores/mealPlanStore.ts
      via: "useMealPlanStore for state + actions"
      pattern: "useMealPlanStore"
    - from: apps/mobile/src/components/plan/DayRow.tsx
      to: "swapDay + markCooked store actions"
      via: "props callbacks from plan.tsx"
      pattern: "onSwap|onCook"
---

<objective>
Ship the Plan tab UI: weekly view, generate, swap, and cook flows — wired to the already-tested store and server.

Purpose: This is the user-visible outcome of Phase 7. All prior plans are plumbing; this makes the "generate my week" experience real.
Output: Plan tab registered in tabs, 7-day calendar-style list, swap + cook interactions, visual verification checkpoint.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-meal-planning/07-RESEARCH.md
@.planning/phases/07-meal-planning/07-03-SUMMARY.md
@.planning/phases/07-meal-planning/07-04-SUMMARY.md
@apps/mobile/src/app/(tabs)/_layout.tsx
@apps/mobile/src/app/(tabs)/recipes.tsx
@apps/mobile/src/app/(tabs)/index.tsx
@apps/mobile/src/stores/mealPlanStore.ts
@apps/mobile/src/types/mealPlan.ts

<interfaces>
Tab registration: the existing _layout.tsx uses `<Tabs.Screen name="..." options={{...}} />` entries. Add a new entry `name="plan"` with a calendar icon (Ionicons 'calendar-outline'). Insert between index and recipes or between recipes and pantry — match user flow ordering.

NativeWind patterns from recipes.tsx: use className with Tailwind classes, FlatList for lists, SafeAreaView wrapper where needed.

Do NOT install react-native-calendars (research anti-pattern). Use a vertical FlatList of 7 DayRow components.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register Plan tab + build DayRow + EmptyPlanState components</name>
  <files>apps/mobile/src/app/(tabs)/_layout.tsx, apps/mobile/src/components/plan/DayRow.tsx, apps/mobile/src/components/plan/EmptyPlanState.tsx</files>
  <action>
    1. Edit `_layout.tsx`: add a `<Tabs.Screen name="plan" options={{ title: 'Plan', tabBarIcon: ({ color }) => <Ionicons name="calendar-outline" size={24} color={color} /> }} />` entry. Match existing icon import style.

    2. Create `DayRow.tsx` as a pure presentational component:
       - Props: `entry: MealPlanEntry | null, dayLabel: string, isSwapping: boolean, isCooking: boolean, onSwap: () => void, onCook: () => void, onPress: () => void`
       - Left column: day label (Mon..Sun) with bold text
       - Main area: title (ellipsize), meta row (estimated_time_minutes + difficulty pill + kid-friendly emoji if true)
       - Right column: two icon buttons — Swap (Ionicons 'swap-horizontal') and Cook (Ionicons 'checkmark-circle' or 'flame')
       - Cooked state: reduced opacity, strikethrough title, cooked_at timestamp
       - Loading states: spinner replaces icon when isSwapping/isCooking
       - Null entry: show "No meal planned" muted text
       - NativeWind classes for spacing, borders, press states

    3. Create `EmptyPlanState.tsx`:
       - Large centered icon (calendar), headline "No plan yet", body "Generate a 7-day dinner plan from your pantry"
       - Primary button "Generate this week" with onPress prop
       - Loading state for when generation is in progress
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/mobile exec tsc --noEmit</automated>
  </verify>
  <done>Tab appears in _layout.tsx, DayRow + EmptyPlanState compile, accept typed props.</done>
</task>

<task type="auto">
  <name>Task 2: Build plan.tsx screen + SwapSheet + CookConfirm</name>
  <files>apps/mobile/src/app/(tabs)/plan.tsx, apps/mobile/src/components/plan/SwapSheet.tsx, apps/mobile/src/components/plan/CookConfirm.tsx</files>
  <action>
    1. Create `plan.tsx`:
       - useEffect on mount: `fetchCurrent()`
       - Render empty state when `currentPlan === null && !loading`
       - Render loading skeleton when `loading && !currentPlan`
       - Render 7-day FlatList when currentPlan exists:
         * Map entries to days 0..6 (fill gaps with null)
         * Day labels computed from plan.week_start using native Date offset
         * Each row: `<DayRow entry={...} dayLabel={...} onSwap={() => setSwapTarget(day)} onCook={() => setCookTarget(day)} onPress={() => navigateToDetail(day)} isSwapping={swappingDay===day} isCooking={cookingDay===day} />`
       - Header: week range text ("Mon Apr 13 – Sun Apr 19") + Regenerate button that calls `generate(currentWeekStart)` after confirm
       - Sticky bottom area: show error banner when store.error is set
       - Wire SwapSheet modal to swapTarget state: on confirm, store.swapDay(day), close sheet
       - Wire CookConfirm to cookTarget: on confirm, store.markCooked(day), show pantry delta toast on success

    2. Create `SwapSheet.tsx`:
       - Props: `visible, currentEntry, onConfirm, onCancel, loading`
       - Modal with "Swap this meal?" header, current title, "Pick something else" button
       - On confirm, calls onConfirm and shows spinner until parent closes

    3. Create `CookConfirm.tsx`:
       - Props: `visible, entry, onConfirm, onCancel, loading, pantryDelta`
       - Before confirm: "Mark {title} as cooked? This will deduct ingredients from your pantry."
       - After success: show list of deducted items
       - Close button

    Do NOT install a bottom sheet library. Use React Native's Modal component (consistent with existing screens — grep recipes.tsx if unsure).

    Empty pantry handling: if `generate` returns EMPTY_PANTRY error, show toast/banner pointing user to Pantry tab.
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/mobile exec tsc --noEmit && pnpm --filter @dinnertime/mobile test mealPlanStore</automated>
  </verify>
  <done>plan.tsx renders without errors, store actions wired, SwapSheet and CookConfirm handle their flows, typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Plan tab end-to-end</name>
  <files>apps/mobile/src/app/(tabs)/plan.tsx</files>
  <action>
    Claude has completed all automation. Hand control to the user for end-to-end visual verification. Do NOT attempt to automate simulator interaction. Ensure backend is running and migration is applied before prompting.
  </action>
  <what-built>
    Complete Phase 7: DB migration, mealPlanner service, routes, mobile store, Plan tab UI with generate/swap/cook flows.
  </what-built>
  <how-to-verify>
    1. Apply the migration locally: `pnpm --filter @dinnertime/server supabase:push` (or equivalent — check package scripts).
    2. Run backend: `pnpm --filter @dinnertime/server dev`
    3. Run mobile: `pnpm --filter @dinnertime/mobile start`, open in iOS simulator.
    4. Log in as a test user. Ensure pantry has at least 3 items and household preferences are set.
    5. Tap the new Plan tab. Should see the empty state.
    6. Tap "Generate this week". Should see a loading spinner, then 7 rows Mon..Sun with meals.
    7. Tap Swap on Wednesday. Should see SwapSheet, confirm, Wednesday's entry is replaced.
    8. Tap Cook on Monday. Should see CookConfirm, confirm, Monday marked cooked (strikethrough/faded), pantry delta toast appears.
    9. Navigate to Pantry tab — confirm the cooked ingredients were deducted.
    10. Tap Cook on Monday again. Should see "Already cooked" message (no double deduction).
    11. Kill and relaunch app. Plan tab should re-fetch and still show the cooked state (persistence).

    Expected outcomes:
    - All 7 days populated
    - Complexity seems reasonable (Mon-Thu simpler, Fri-Sun more ambitious)
    - Kid-friendly badge appears on at least some nights if household has kids
    - No flash of empty state after fetch
    - Error banner appears cleanly if pantry is empty (test by clearing pantry)
  </how-to-verify>
  <verify>Manual visual verification by user — no automated command.</verify>
  <done>User types "approved" after stepping through all 11 verification steps successfully.</done>
  <resume-signal>Type "approved" if the full flow works. Otherwise describe issue (which step, what happened).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm -r test` all green
- Manual visual verification passed
- No `react-native-calendars` or other new deps added (check package.json diff)
</verification>

<success_criteria>
- All 7 PLAN-* requirements demonstrably working in simulator
- Plan tab is registered and functional
- Generate then Swap then Cook flow works end-to-end
- Cooked state persists across app restart
</success_criteria>

<output>
Create `.planning/phases/07-meal-planning/07-05-SUMMARY.md` after completion.
</output>
