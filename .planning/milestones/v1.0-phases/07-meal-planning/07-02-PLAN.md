---
phase: 07-meal-planning
plan: 02
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/server/src/services/mealPlanner.ts
  - packages/server/src/services/__tests__/mealPlanner.test.ts
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04]
must_haves:
  truths:
    - "buildMealPlanPrompt includes pantry inventory, allergies, dislikes, cuisine preferences, recipe library, and AVOID REPEATING block"
    - "generateMealPlan returns exactly 7 entries, one per day_of_week 0..6"
    - "Recent meals query is capped at 21 entries"
    - "Prompt contains weeknight (Mon-Thu) vs weekend (Fri-Sun) complexity guidance"
    - "If household has kids, prompt requires at least 3 of 7 nights to be kid_friendly"
  artifacts:
    - path: packages/server/src/services/mealPlanner.ts
      provides: "buildMealPlanPrompt (pure), generateMealPlan (IO), generate_meal_plan Claude tool"
      exports: [buildMealPlanPrompt, generateMealPlan, generateMealPlanTool]
    - path: packages/server/src/services/__tests__/mealPlanner.test.ts
      provides: "Unit tests covering prompt assembly + generateMealPlan with mocked Claude/Supabase"
      contains: "describe('buildMealPlanPrompt'"
  key_links:
    - from: packages/server/src/services/mealPlanner.ts
      to: "@anthropic-ai/sdk tool_use"
      via: "generate_meal_plan tool with input_schema minItems:7 maxItems:7"
      pattern: "minItems:\\s*7"
    - from: packages/server/src/services/mealPlanner.ts
      to: packages/server/src/services/suggestions.ts
      via: "Mirror prompt block pattern (HARD CONSTRAINTS, SOFT PREFERENCES, pantry assembly)"
      pattern: "HARD CONSTRAINTS"
---

<objective>
Implement the core `mealPlanner` service: prompt assembly, Claude tool-use, and the generate-7-day flow — fully TDD.

Purpose: This is the AI brain of Phase 7. Everything downstream (routes, mobile store, UI) depends on a stable, tested contract here.
Output: Tested service exporting buildMealPlanPrompt, generateMealPlan, generateMealPlanTool.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-meal-planning/07-RESEARCH.md
@.planning/phases/07-meal-planning/07-01-SUMMARY.md
@packages/server/src/services/suggestions.ts
@packages/server/src/services/__tests__/suggestions.test.ts
@packages/server/src/types/mealPlan.ts

<interfaces>
Mirror the canonical pattern from suggestions.ts:
- Lazy singleton Anthropic client via env getter
- Pure `buildXPrompt(context): string` extracted for testing
- Async `generateX(supabase, profileId, ...): Promise<X>` does IO
- Replicate confidence decay via imported helper (or lift to shared util if not yet shared)
- HARD CONSTRAINTS block separates allergies from SOFT PREFERENCES (dietary restrictions)

New for this service:
- Fetch recipe library: `supabase.from('recipes').select('id, title').eq('profile_id', profileId).limit(100)`
- Fetch recent meals: `supabase.from('meal_plan_entries').select('title, cooked_at').eq('status','cooked')` joined via meal_plans.profile_id, ORDER BY cooked_at DESC, LIMIT 21
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD buildMealPlanPrompt (pure function)</name>
  <files>packages/server/src/services/mealPlanner.ts, packages/server/src/services/__tests__/mealPlanner.test.ts</files>
  <behavior>
    Pure function `buildMealPlanPrompt(context: MealPlanContext): string` where context = { pantryItems, preferences (allergies, restrictions, cuisines, dislikes, kidFriendlyNeeded, householdSize), recipeLibrary [{id,title}], recentMealTitles [string], weekStart }.

    Tests (write FIRST, must fail):
    - Test 1: Output contains every pantry item name
    - Test 2: Allergies appear under "HARD CONSTRAINTS" block (not mixed with SOFT PREFERENCES)
    - Test 3: Soft dietary restrictions appear under "SOFT PREFERENCES" block
    - Test 4: Recipe library titles appear in "RECIPE LIBRARY" block with their IDs so Claude can set recipe_id
    - Test 5: Recent cooked titles appear under "AVOID REPEATING" block
    - Test 6: Prompt includes explicit "Mon-Thu = weeknight simpler (15-30 min)" and "Fri-Sun = weekend ambitious allowed" guidance
    - Test 7: When kidFriendlyNeeded=true, prompt demands "at least 3 of 7 nights must be kid_friendly=true"
    - Test 8: Prompt demands "Return EXACTLY 7 days, one per day_of_week (0..6, 0=Monday)"
    - Test 9: Empty recipe library and empty recent meals do not crash — prompt still contains section headers (with a placeholder like "(none)")
  </behavior>
  <action>
    RED: Write all 9 tests against `buildMealPlanPrompt` (not yet implemented) in `mealPlanner.test.ts`. Run `pnpm --filter @dinnertime/server test mealPlanner` — all must fail.

    GREEN: Implement `buildMealPlanPrompt` in `mealPlanner.ts` by adapting `buildSuggestionPrompt` from suggestions.ts. Add the three new blocks (RECIPE LIBRARY, AVOID REPEATING, WEEK STRUCTURE). Run tests — all must pass.

    REFACTOR: If duplication with buildSuggestionPrompt is obvious, extract shared helpers for pantry/preferences block. Do NOT over-extract. Keep tests green.

    Commit after RED: `test(07-02): failing tests for buildMealPlanPrompt`
    Commit after GREEN: `feat(07-02): implement buildMealPlanPrompt`
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/server test mealPlanner</automated>
  </verify>
  <done>All 9 prompt assembly tests green. File exports buildMealPlanPrompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD generateMealPlan + generateMealPlanTool</name>
  <files>packages/server/src/services/mealPlanner.ts, packages/server/src/services/__tests__/mealPlanner.test.ts</files>
  <behavior>
    Export `generateMealPlanTool` (Anthropic tool definition) and `generateMealPlan(supabase, profileId, weekStart): Promise<MealPlan>`.

    Tool schema (Pattern 1 from research):
    - name: 'generate_meal_plan'
    - input_schema.properties.days: array minItems:7 maxItems:7
    - Each day: day_of_week enum ['mon','tue','wed','thu','fri','sat','sun'], title, description, recipe_id nullable, ingredients_used, ingredients_needed, estimated_time_minutes, difficulty enum, complexity_target enum ['weeknight','weekend'], kid_friendly, why_suggested
    - Required: day_of_week, title, description, estimated_time_minutes, difficulty, complexity_target, kid_friendly, why_suggested

    Tests (write FIRST, must fail):
    - Test 1: generateMealPlanTool has minItems:7 and maxItems:7 on days array
    - Test 2: Tool required includes 'complexity_target' and 'kid_friendly'
    - Test 3: generateMealPlan with mocked Supabase (pantry + prefs + recipes + recent meals) and mocked Anthropic returning 7 valid days → persists 1 meal_plans row + 7 meal_plan_entries rows, returns MealPlan with entries
    - Test 4: generateMealPlan throws 502-style error when Claude returns fewer than 7 days (Pitfall 1)
    - Test 5: generateMealPlan throws/returns 400 when pantry has < 3 items (reuse empty pantry guard from suggestions.ts)
    - Test 6: Recent meals fetch is capped at 21 entries (verify query .limit(21))
    - Test 7: Regenerate flow — if a meal_plan already exists for (profile_id, week_start), delete existing entries + plan in transaction then insert fresh (Pitfall 4)
    - Test 8: day_of_week strings ('mon'..'sun') are mapped to SMALLINT 0..6 on persistence

    Mock Anthropic SDK via vi.hoisted() (per Phase 01 decision). Mock Supabase builder chain.
  </behavior>
  <action>
    RED: Add 8 tests. Run — fail.

    GREEN: Implement `generateMealPlanTool` constant and `generateMealPlan` function:
    - Fetch pantry (reuse helper or query directly), preferences, recipe library (limit 100), recent meals (limit 21, status='cooked')
    - Guard: pantry items < 3 → throw error with code 'EMPTY_PANTRY'
    - Build context, call buildMealPlanPrompt
    - Call Anthropic with generate_meal_plan tool forced (tool_choice: { type: 'tool', name: 'generate_meal_plan' })
    - Extract tool_use block; validate days.length === 7, otherwise throw 'INVALID_PLAN_LENGTH'
    - Delete any existing plan for (profile_id, week_start) via cascade → insert fresh meal_plans row → insert 7 meal_plan_entries mapping day strings to 0..6
    - Return populated MealPlan

    Use lazy Anthropic singleton pattern (Phase 03 decision). Import confidence decay if needed — reuse from suggestions.ts.

    REFACTOR: extract dayStringToIndex helper if clearer.

    Commit: `test(07-02): failing tests for generateMealPlan` then `feat(07-02): implement generateMealPlan service`
  </action>
  <verify>
    <automated>pnpm --filter @dinnertime/server test mealPlanner</automated>
  </verify>
  <done>All generateMealPlan tests green; tool schema enforces 7 days; delete-then-insert on regenerate; empty pantry + invalid length errors raised.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @dinnertime/server test mealPlanner` passes
- No lint errors: `pnpm --filter @dinnertime/server lint`
</verification>

<success_criteria>
- mealPlanner.ts exports buildMealPlanPrompt, generateMealPlan, generateMealPlanTool
- Test file covers all 17 behaviors listed across Task 1 and Task 2
- Pure prompt function is fully unit-testable without mocks
- Tool schema enforces minItems:7/maxItems:7
</success_criteria>

<output>
Create `.planning/phases/07-meal-planning/07-02-SUMMARY.md` after completion.
</output>
</content>
</invoke>