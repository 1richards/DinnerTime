---
phase: 23-settings-auth-nfr
plan: 05
type: execute
wave: 2
depends_on: [23-00]
files_modified:
  - apps/mobile/src/components/ErrorBoundary.tsx
  - apps/mobile/src/components/__tests__/ErrorBoundary.test.ts
  - apps/mobile/src/components/NetworkErrorBanner.tsx
  - apps/mobile/src/components/__tests__/NetworkErrorBanner.test.ts
  - apps/mobile/src/lib/classifyNetworkError.ts
  - apps/mobile/src/lib/__tests__/classifyNetworkError.test.ts
  - apps/mobile/src/app/_layout.tsx
  - packages/server/src/middleware/rateLimitErrors.ts
  - packages/server/src/middleware/__tests__/rateLimitErrors.test.ts
  - packages/server/src/index.ts
autonomous: true
requirements:
  - NFR-12
  - NFR-13
  - NFR-14

must_haves:
  truths:
    - "Any uncaught render error in the app tree is caught by ErrorBoundary and rendered as a friendly fallback (logo + 'Something went wrong' + Restart button + Report issue button). The white-screen-of-death no longer occurs."
    - "ErrorBoundary calls Sentry.captureException(error, { contexts: { reactErrorInfo } }) on componentDidCatch — Sentry is lazy-imported so the error boundary still works if Sentry init failed."
    - "classifyNetworkError(err: unknown) returns a discriminated result: 'offline' (NetInfo isOnline false or TypeError matching /network/i), 'rate_limit' (status 429), 'timeout' (status 408 or AbortError), 'server' (5xx), 'unknown' (default). Exhaustive switch-friendly."
    - "NetworkErrorBanner renders null for null error, else an inline banner keyed by classification with a retry Button when onRetry is provided. Copy is user-facing ('We're a bit busy — try again in a moment' for rate_limit; 'Check your connection' for offline; 'Our kitchen timer slipped — one sec' for timeout; 'Something's odd on our end' for server)."
    - "Server middleware maps Anthropic/Supabase rate-limit responses (status 429 or error code 'rate_limit_exceeded') to a JSON body { error: 'rate_limit', message: 'We're a bit busy — try again in a minute', retryAfter?: <seconds> } and preserves the original Retry-After header. 5xx Anthropic errors become { error: 'ai_unavailable', message: 'Our AI service hiccuped — try again shortly' }."
  artifacts:
    - path: "apps/mobile/src/components/ErrorBoundary.tsx"
      provides: "React class-based ErrorBoundary wrapping the app root"
      exports: ["ErrorBoundary"]
    - path: "apps/mobile/src/components/NetworkErrorBanner.tsx"
      provides: "Inline banner component keyed on classifyNetworkError"
      exports: ["NetworkErrorBanner"]
    - path: "apps/mobile/src/lib/classifyNetworkError.ts"
      provides: "Pure classifier with discriminated union result"
      exports: ["classifyNetworkError", "NetworkErrorKind"]
    - path: "packages/server/src/middleware/rateLimitErrors.ts"
      provides: "Hono error-handling middleware that rewrites 429/5xx to friendly payloads"
      exports: ["rateLimitErrorsMiddleware"]
  key_links:
    - from: "apps/mobile/src/app/_layout.tsx"
      to: "apps/mobile/src/components/ErrorBoundary.tsx"
      via: "Wraps RootNavigator and every screen"
      pattern: "ErrorBoundary"
    - from: "apps/mobile/src/components/NetworkErrorBanner.tsx"
      to: "apps/mobile/src/lib/classifyNetworkError.ts"
      via: "Calls classifyNetworkError(error) to drive copy"
      pattern: "classifyNetworkError"
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/middleware/rateLimitErrors.ts"
      via: "app.use('*', rateLimitErrorsMiddleware)"
      pattern: "rateLimitErrors"
---

<objective>
Ship consistent error handling across client and server. Flips 2 red stubs green: ErrorBoundary.test.ts + NetworkErrorBanner.test.ts.

Purpose: NFR-12 (no white-screen crashes), NFR-13 (consistent offline pattern), NFR-14 (rate-limit errors become actionable copy). Users stop seeing raw error codes; Patrick stops debugging crash reports that just say "undefined is not a function."

Output: 1 React ErrorBoundary + 1 pure classifier + 1 banner component + 1 Hono middleware. No new screens — this is all infrastructure that existing screens consume.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-settings-auth-and-non-functional-requirements-account-management-session-lifecycle-security-performance-error-handling-observability/23-CONTEXT.md

<!-- Reference patterns -->
@apps/mobile/src/stores/networkStore.ts
@apps/mobile/src/components/OfflineBanner.tsx
@apps/mobile/src/app/_layout.tsx
@packages/server/src/index.ts
@packages/server/src/services/recipeParser.ts

<interfaces>
From @sentry/react-native (installed in 23-00, wired in 23-06):
```typescript
Sentry.captureException(error: unknown, options?: { contexts?: Record<string, unknown> }): void;
```
But 23-06 runs in parallel with 23-05. To avoid coupling, ErrorBoundary should lazy-import Sentry:
```typescript
try { const S = require('@sentry/react-native'); S.captureException(err, opts); } catch { /* Sentry not wired */ }
```

From apps/mobile/src/stores/networkStore.ts (existing):
```typescript
export const useNetworkStore: StoreApi<{ isOnline: boolean }>;
```

Hono middleware signature:
```typescript
async function rateLimitErrorsMiddleware(c: Context, next: Next) {
  try { await next(); }
  catch (err) { /* classify + rewrite */ }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classifyNetworkError pure helper + NetworkErrorBanner component</name>
  <files>
    apps/mobile/src/lib/classifyNetworkError.ts,
    apps/mobile/src/lib/__tests__/classifyNetworkError.test.ts,
    apps/mobile/src/components/NetworkErrorBanner.tsx,
    apps/mobile/src/components/__tests__/NetworkErrorBanner.test.ts
  </files>
  <behavior>
    - classifyNetworkError priority: 'offline' (err instanceof TypeError && /network/i.test(message)) OR !useNetworkStore.getState().isOnline → 'offline'; status === 429 → 'rate_limit'; status === 408 || err.name === 'AbortError' → 'timeout'; status >= 500 && status < 600 → 'server'; default → 'unknown'.
    - err shape support: accepts { status?: number; name?: string; message?: string } OR Response-like OR raw Error OR string. Best-effort extraction.
    - NetworkErrorBanner renders null when error === null OR classification === 'unknown' and no message. Otherwise: ErrorState primitive with icon + title + subtitle + (optional) retry Button.
    - Copy table: offline → "Check your connection — you're offline." | rate_limit → "We're a bit busy — try again in a minute." | timeout → "Our kitchen timer slipped — one sec." | server → "Something's odd on our end. Please try again." | unknown → "Something went wrong."
  </behavior>
  <action>
    classifyNetworkError.ts: pure function, no React or Zustand imports (test injectability). The "offline" branch that reads useNetworkStore.getState().isOnline — make this a SECOND pure helper `classifyWithNetwork(err, isOnline: boolean)` and have classifyNetworkError wrap it by reading the store. Keeps the test pure.

    NetworkErrorBanner.tsx: composes ErrorState (existing primitive from Phase 15-01). Uses Phase 19 tokens (colors.warning for offline + timeout, colors.destructive for rate_limit + server + unknown).

    Flip NetworkErrorBanner.test.ts (4 cases, shipped in 23-00) green. classifyNetworkError tests (new — not in 23-00; add in this plan): 6 cases (one per kind + one "precedence: offline wins over 429").
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/lib/__tests__/classifyNetworkError.test.ts src/components/__tests__/NetworkErrorBanner.test.ts</automated>
  </verify>
  <done>
    10+ test cases green across 2 files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ErrorBoundary component + _layout.tsx wiring + server rateLimitErrors middleware</name>
  <files>
    apps/mobile/src/components/ErrorBoundary.tsx,
    apps/mobile/src/components/__tests__/ErrorBoundary.test.ts,
    apps/mobile/src/app/_layout.tsx,
    packages/server/src/middleware/rateLimitErrors.ts,
    packages/server/src/middleware/__tests__/rateLimitErrors.test.ts,
    packages/server/src/index.ts
  </files>
  <behavior>
    - ErrorBoundary: class component. getDerivedStateFromError returns { error }. componentDidCatch(error, errorInfo) calls a local captureError(error, errorInfo) that lazy-imports @sentry/react-native and calls Sentry.captureException if available. Fallback render: centered View with app logo + "Something went wrong" Text + Restart Button (setState({ error: null }) to retry render) + "Report issue" Button (opens mailto:support@dinnertime.app?subject=Issue+in+DinnerTime&body=<stringified error>).
    - Wired at _layout.tsx root — wraps RootNavigator AND the AuthStateBanner. Outside BiometricGate (which 23-03 added) so the error boundary still shows if BiometricGate itself errors.
    - Server rateLimitErrorsMiddleware: wraps every request in try/catch. If caught error has status 429 OR message matches /rate.limit/i → c.json({ error: 'rate_limit', message: 'We're a bit busy — try again in a minute', retryAfter: parseInt(c.res.headers.get('Retry-After') || '60') }, 429). If 5xx from Anthropic (inspect error.cause or error.status) → { error: 'ai_unavailable', message: 'Our AI service hiccuped — try again shortly' }.
    - Middleware mounted via `app.onError(...)` in Hono (which is the correct Hono pattern for catch-all error handling).
    - Tests: ErrorBoundary.test.ts 3 cases (children render clean, throws → fallback + capture, Restart resets). rateLimitErrors.test.ts 3 cases (429 rewrites, 5xx from AI rewrites, 4xx passes through).
  </behavior>
  <action>
    ErrorBoundary.tsx: React class component (required — hooks can't catch errors). Lazy-import Sentry inside componentDidCatch to avoid coupling with 23-06's init:
    ```typescript
    componentDidCatch(error: Error, info: React.ErrorInfo) {
      try {
        // eslint-disable-next-line @typescript-eslint/no-require-imports
        const S = require('@sentry/react-native');
        S.captureException?.(error, { contexts: { react: { componentStack: info.componentStack } } });
      } catch { /* Sentry not wired yet */ }
    }
    ```
    Fallback UI uses colors from design tokens + the existing ErrorState primitive where possible.

    _layout.tsx wiring: wrap the main `<Stack>` inside `<ErrorBoundary>`. Keep BiometricGate outside ErrorBoundary (so gate survives child errors). Order: `<GestureHandlerRootView><QueryClientProvider><ErrorBoundary>... existing tree ...</ErrorBoundary></QueryClientProvider></GestureHandlerRootView>` with BiometricGate absolute-positioned sibling.

    rateLimitErrors.ts: for Hono, use `app.onError(handler)` rather than middleware — Hono's recommended pattern. Put this in `index.ts` after mounting routes:
    ```typescript
    app.onError((err, c) => rateLimitErrorHandler(err, c));
    ```
    And export `rateLimitErrorHandler(err, c)` from `middleware/rateLimitErrors.ts` so it's unit-testable.

    Per D-12, D-13, D-14.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/components/__tests__/ErrorBoundary.test.ts && cd ../../packages/server && pnpm test --run src/middleware/__tests__/rateLimitErrors.test.ts</automated>
  </verify>
  <done>
    6 tests green (3 mobile + 3 server). _layout.tsx wraps tree in ErrorBoundary. index.ts app.onError wired.
  </done>
</task>

</tasks>

<verification>
- Throw in a screen manually (temp `throw new Error('boom')` inside a useEffect of any tab) → error boundary fallback appears instead of white screen. Revert the throw after verify.
- Server: POST to a mock 429 handler → response body has `{ error: 'rate_limit' }`.
</verification>

<success_criteria>
NFR-12 (global boundary live), NFR-13 (consistent offline/network banner pattern live and ready for adoption in existing screens), NFR-14 (rate-limit copy user-friendly at server boundary). Future screens opting into NetworkErrorBanner is a quality-of-life migration, not required for this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settings-auth-and-non-functional-requirements-account-management-session-lifecycle-security-performance-error-handling-observability/23-05-SUMMARY.md`
</output>
