---
phase: 23-settings-auth-nfr
plan: 01
type: execute
wave: 2
depends_on: [23-00]
files_modified:
  - packages/server/src/routes/account.ts
  - packages/server/src/index.ts
  - packages/server/src/routes/__tests__/account.test.ts
  - apps/mobile/src/components/settings/AccountSection.tsx
  - apps/mobile/src/components/settings/AboutSection.tsx
  - apps/mobile/src/components/settings/ConnectedServicesSection.tsx
  - apps/mobile/src/app/settings/account/change-password.tsx
  - apps/mobile/src/app/settings/account/change-email.tsx
  - apps/mobile/src/app/settings/_layout.tsx
  - apps/mobile/src/app/(tabs)/settings.tsx
autonomous: true
requirements:
  - NFR-01
  - NFR-02
  - NFR-05
  - NFR-06

must_haves:
  truths:
    - "User can tap Settings → Change password, enter current + new password, and succeed: on submit the UI calls supabase.auth.reauthenticate(currentPassword) then supabase.auth.updateUser({ password }); on success shows a success toast and pops back to Settings."
    - "User can tap Settings → Change email, enter new email, submit, and see a 'Check your inbox — your new email stays in preview until you confirm' toast. Server hits supabase.auth.updateUser({ email }); old email stays active until Supabase confirmation email flow completes."
    - "Settings renders an About section at the bottom showing app version (from Constants.expoConfig.version), Privacy Policy + Terms of Service tappable rows (open via WebBrowser.openBrowserAsync to https://dinnertime.app/privacy and /terms), and a Support email row that opens mailto:support@dinnertime.app."
    - "Settings renders a Connected Services section with a single Instacart row showing 'Not connected' (v1 — anonymous link model per D-05). Row is visually present; tap is a no-op toast 'Instacart link handoff happens in Shopping.'"
    - "POST /api/v1/account/change-password returns 200 on success / 401 on wrong current password / 400 on missing/short new password (8+ chars)."
    - "POST /api/v1/account/change-email returns 200 on success and triggers Supabase's confirmation email; body validates email shape."
  artifacts:
    - path: "packages/server/src/routes/account.ts"
      provides: "Account management Hono router (change-password, change-email — export/delete added in 23-02)"
      exports: ["default"]
    - path: "apps/mobile/src/components/settings/AccountSection.tsx"
      provides: "Settings row group: Change password, Change email, Export data, Delete account (last two wired in 23-02)"
      exports: ["AccountSection"]
    - path: "apps/mobile/src/components/settings/AboutSection.tsx"
      provides: "App version, build number, Privacy/Terms/Support rows"
      exports: ["AboutSection"]
    - path: "apps/mobile/src/components/settings/ConnectedServicesSection.tsx"
      provides: "Instacart connection status placeholder"
      exports: ["ConnectedServicesSection"]
    - path: "apps/mobile/src/app/settings/account/change-password.tsx"
      provides: "Password change screen"
      exports: ["default"]
    - path: "apps/mobile/src/app/settings/account/change-email.tsx"
      provides: "Email change screen"
      exports: ["default"]
  key_links:
    - from: "packages/server/src/index.ts"
      to: "packages/server/src/routes/account.ts"
      via: "app.route('/account', account)"
      pattern: "account"
    - from: "apps/mobile/src/app/(tabs)/settings.tsx"
      to: "apps/mobile/src/components/settings/AccountSection.tsx"
      via: "<AccountSection /> rendered"
      pattern: "AccountSection"
    - from: "apps/mobile/src/app/settings/account/change-password.tsx"
      to: "supabase.auth"
      via: "signInWithPassword (re-auth) then updateUser({ password })"
      pattern: "updateUser"
---

<objective>
Ship the non-destructive half of account management: change password, change email, About section, Connected Services placeholder. Flips 5 red stubs green: `account.test.ts` (change-password + change-email cases), `AccountSection.test.ts`, `AboutSection.test.ts`.

Purpose: These are the daily-driver settings — low stakes, high trust. Patrick needs to change his password without hunting through Supabase Studio.

Output: 1 new Hono router + 3 Settings section components + 2 new modal-stack screens, wired into the existing Settings tab.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-settings-auth-and-non-functional-requirements-account-management-session-lifecycle-security-performance-error-handling-observability/23-CONTEXT.md

<!-- Reference patterns to clone -->
@apps/mobile/src/app/(tabs)/settings.tsx
@apps/mobile/src/components/settings/ShoppingHandoffSection.tsx
@apps/mobile/src/components/ui/Button.tsx
@apps/mobile/src/components/ui/Input.tsx
@packages/server/src/routes/pantry.ts
@packages/server/src/middleware/auth.ts

<interfaces>
From apps/mobile/src/stores/authStore.ts:
```typescript
export interface Profile {
  id: string;
  display_name: string | null;
  // ... other fields
}
export const useAuthStore: ...  // already imported in settings.tsx
```

From packages/server/src/middleware/auth.ts:
```typescript
export async function authMiddleware(c: Context, next: Next);
// c.get('user') returns Supabase User with .id, .email
// c.get('supabase') returns user-scoped supabase client
```

From apps/mobile/src/components/ui:
```typescript
// Button: title, onPress, loading, variant ('primary'|'secondary'|'destructive'|'ghost'|'icon')
// Input: label, value, onChangeText, secureTextEntry, keyboardType
```

From `expo-constants`:
```typescript
import Constants from 'expo-constants';
Constants.expoConfig?.version;   // e.g. '1.0.0'
Constants.expoConfig?.ios?.buildNumber;  // if set
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server /account route — change-password + change-email handlers</name>
  <files>
    packages/server/src/routes/account.ts,
    packages/server/src/index.ts,
    packages/server/src/routes/__tests__/account.test.ts
  </files>
  <behavior>
    - POST /account/change-password body { currentPassword, newPassword }: 401 no auth; 400 if currentPassword missing OR newPassword < 8 chars; 401 if supabase re-auth (signInWithPassword with current user email + currentPassword) fails; 200 on success after supabase.auth.updateUser({ password: newPassword }).
    - POST /account/change-email body { newEmail }: 401 no auth; 400 if newEmail missing or doesn't match email regex; 200 on success after supabase.auth.updateUser({ email: newEmail }). Response includes { emailConfirmationSent: true }.
    - Route mounted at /api/v1/account in index.ts (mirrors /telemetry mount pattern).
  </behavior>
  <action>
    Create `packages/server/src/routes/account.ts`: Hono router, applies authMiddleware to all routes. For change-password: read user from c.get('user'), read authed supabase from c.get('supabase'). Re-auth via `supabase.auth.signInWithPassword({ email: user.email!, password: currentPassword })` — if error → 401. Then `supabase.auth.updateUser({ password: newPassword })` — if error → 500. Return { success: true }.

    For change-email: zod schema `{ newEmail: z.string().email() }`. Call `supabase.auth.updateUser({ email: newEmail })`. Supabase automatically triggers its confirmation-email flow (the email is NOT live until confirmed — this is Supabase's default behavior). Return { success: true, emailConfirmationSent: true }.

    Mount in `index.ts`: `import account from './routes/account.js'` + `app.route('/account', account)` alongside existing mounts.

    Flip the change-password + change-email blocks of `account.test.ts` (shipped red in 23-00) green. Leave export + delete blocks RED — owned by 23-02.
  </action>
  <verify>
    <automated>cd packages/server && pnpm test --run src/routes/__tests__/account.test.ts -t "change-password|change-email"</automated>
  </verify>
  <done>
    2 describe blocks (change-password, change-email) green — 8 total cases. /account mounted in index.ts. Export + delete describes still RED (23-02 ships those).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mobile AccountSection + AboutSection + ConnectedServicesSection + change-password/change-email screens</name>
  <files>
    apps/mobile/src/components/settings/AccountSection.tsx,
    apps/mobile/src/components/settings/AboutSection.tsx,
    apps/mobile/src/components/settings/ConnectedServicesSection.tsx,
    apps/mobile/src/app/settings/account/change-password.tsx,
    apps/mobile/src/app/settings/account/change-email.tsx,
    apps/mobile/src/app/settings/_layout.tsx,
    apps/mobile/src/app/(tabs)/settings.tsx
  </files>
  <behavior>
    - AccountSection renders 4 Pressables in a bordered group: "Change password" → /settings/account/change-password, "Change email" → /settings/account/change-email, "Export data" (23-02), "Delete account" (23-02). Each uses ItemRow with `leading={{ kind: 'icon', name: <SF symbol> }}` + trailing chevron.
    - AboutSection renders 4 rows: Version (read-only, shows `Constants.expoConfig?.version ?? 'unknown'`), Privacy Policy (opens https://dinnertime.app/privacy via WebBrowser.openBrowserAsync), Terms of Service (opens /terms), Support (opens mailto:support@dinnertime.app via Linking.openURL).
    - ConnectedServicesSection renders a single Instacart row showing "Not connected — Instacart handoff happens in Shopping" as subtitle. Tap is a no-op.
    - change-password.tsx screen: 3 Inputs (current password, new password, confirm new password — all secureTextEntry in non-__DEV__), Button "Update password". On submit: validate client-side (new === confirm, new >= 8 chars), call authedFetch POST /account/change-password, on success show toast and router.back(), on 401 show "Current password incorrect" inline error, on other error show generic.
    - change-email.tsx screen: 1 Input (new email, keyboardType email-address), Button "Send confirmation". On submit: POST /account/change-email, on success show toast "Confirmation email sent to <email>. Your current email stays active until you confirm." + router.back().
    - settings/_layout.tsx: expo-router Stack with headerBackTitle:'Settings' — nests account/change-password + account/change-email + (23-02 export/delete) + (21) pantry-rules/staples.
  </behavior>
  <action>
    Build AccountSection as a `<View>` with section header "ACCOUNT" (Phase 19 token: `text-label text-text-secondary uppercase`) followed by 4 ItemRows. Each row uses the existing ItemRow primitive with `{ leading: { kind: 'icon', name: 'key' }, title: 'Change password', trailing: { kind: 'chevron' } }` pattern. Routes: `/settings/account/change-password`, `/settings/account/change-email`. Export + Delete rows wire to stubs `/settings/account/export` + `/settings/account/delete` that 23-02 will build.

    AboutSection: 4 rows under "ABOUT" header. Version is a display-only row (no chevron, right-aligns `Constants.expoConfig?.version` as `Text.font-semibold`). The Privacy/Terms/Support rows use `onPress: () => WebBrowser.openBrowserAsync(url)` or `Linking.openURL('mailto:…')`.

    ConnectedServicesSection: single display row under "CONNECTED SERVICES" header. Matches Phase 19 patterns.

    change-password.tsx: SafeAreaView + ScrollView + Stack.Screen title="Change password". Uses `useAuthStore(s => s.user)` to get email (not sent to server — server reads from auth context). Uses `authedFetch` from `../../../lib/authedFetch.ts` — **IMPORTANT**: 23-04 ships `authedFetch`. Until then, inline the fetch with `session.access_token` Bearer header lookup. Leave a TODO-23-04 comment.

    change-email.tsx: similar structure, single input.

    settings/_layout.tsx: if not present, create with `<Stack screenOptions={{ headerBackTitle: 'Settings' }} />`. Verify by checking if 21-05 already created it; if yes, just add the new routes. (Phase 21-05 shipped `/settings/pantry-rules` and `/settings/staples` — the layout should already exist.)

    Mount AccountSection + ConnectedServicesSection + AboutSection in `app/(tabs)/settings.tsx` in this order near the bottom: after existing Pantry + Cooking + Plan + Shopping sections, BEFORE the existing Account/Sign Out block. Keep the existing Sign Out button in place for now (23-04 will polish the sign-out copy; 23-02 will wrap Export+Delete into the same AccountSection).

    Per D-01, D-02, D-05, D-06.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/components/settings/__tests__/AccountSection.test.ts src/components/settings/__tests__/AboutSection.test.ts</automated>
  </verify>
  <done>
    AccountSection.test.ts + AboutSection.test.ts green. /settings/account/change-password and /settings/account/change-email routes exist and typecheck. (tabs)/settings.tsx renders 3 new sections without layout collapse.
  </done>
</task>

</tasks>

<verification>
- Server: 8/8 account test cases green for change-password + change-email.
- Mobile: AccountSection + AboutSection tests green.
- Manual-ish: Launch app, open Settings, scroll to bottom — see Account (Change password / Change email rows), Connected Services (Instacart Not connected), About (Version / Privacy / Terms / Support) sections.
</verification>

<success_criteria>
NFR-01 (change password with current-password verification), NFR-02 (change email triggers confirmation flow), NFR-05 (connected services section present), NFR-06 (About section with version + legal + support) all observable.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settings-auth-and-non-functional-requirements-account-management-session-lifecycle-security-performance-error-handling-observability/23-01-SUMMARY.md`
</output>
