---
phase: 16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display
plan: 03
type: execute
wave: 2
depends_on: [16-00]
files_modified:
  - apps/mobile/src/components/cooking/StickyCookingHeader.tsx
  - apps/mobile/src/components/cooking/VoiceWaveform.tsx
  - apps/mobile/src/components/cooking/StopTTSButton.tsx
  - apps/mobile/src/components/cooking/TimerBar.tsx
  - apps/mobile/src/cooking/haptics.ts
  - apps/mobile/src/cooking/useVoiceAmplitude.ts
autonomous: true
requirements: [COOK-UX-03, COOK-UX-04, COOK-UX-05]
user_setup: []
must_haves:
  truths:
    - "Sticky cooking header renders 64pt baseline / 112pt with timers (UI-SPEC lock)"
    - "VoiceWaveform shows 3 animated bars when listening, pulse dot idle-armed, mic-slash when voice off"
    - "StopTTSButton has accessibilityLabel='Stop reading' (UI-SPEC + VoiceOver clarity)"
    - "TimerBar uses Phase 19 tokens only — no #C2410C literal anywhere"
    - "TimerBar adopts bg-warning/20 background when a timer's remainingMs < 10000 (T-10s)"
    - "haptics.ts helpers map every UI-SPEC haptic event to expo-haptics API"
    - "useVoiceAmplitude hook drives waveform bars (amplitude event if available, cosmetic loop otherwise)"
  artifacts:
    - path: "apps/mobile/src/components/cooking/StickyCookingHeader.tsx"
      provides: "Top-sticky header composing Exit + title + VoiceWaveform + TimerBar + StopTTSButton"
      min_lines: 50
    - path: "apps/mobile/src/components/cooking/VoiceWaveform.tsx"
      provides: "Reanimated waveform + pulse dot + mic-slash (replaces VoiceStatusBadge in Wave 3 integration)"
      min_lines: 40
    - path: "apps/mobile/src/components/cooking/StopTTSButton.tsx"
      provides: "Icon-only stop button with accessibilityLabel='Stop reading'"
      min_lines: 20
    - path: "apps/mobile/src/components/cooking/TimerBar.tsx"
      provides: "Retokenized timer chip row with T-10s warning transition"
      contains: "bg-warning"
    - path: "apps/mobile/src/cooking/haptics.ts"
      provides: "Typed helpers for every UI-SPEC haptic event"
      exports: ["fireCommandHaptic", "fireIngredientHaptic", "fireTimerWarnHaptic", "fireTimerExpireHaptic", "fireExitConfirmHaptic", "fireStopTTSHaptic"]
    - path: "apps/mobile/src/cooking/useVoiceAmplitude.ts"
      provides: "Hook returning Reanimated SharedValue<number> driving waveform phase"
      exports: ["useVoiceAmplitude"]
  key_links:
    - from: "VoiceWaveform"
      to: "useVoiceAmplitude"
      via: "import + hook call driving 3 animated bar heights"
      pattern: "useVoiceAmplitude\\("
    - from: "StickyCookingHeader"
      to: "TimerBar"
      via: "render when timers.length > 0"
      pattern: "timers\\.length\\s*>\\s*0"
    - from: "TimerBar"
      to: "Phase 19 tokens"
      via: "className strings use bg-brand/15, border-brand, text-brand, bg-warning/20"
      pattern: "bg-(brand|warning)"
---

<objective>
Build the sticky header cluster: `StickyCookingHeader`, `VoiceWaveform`, `StopTTSButton`, retokenized `TimerBar`, plus the two supporting modules (`haptics.ts`, `useVoiceAmplitude.ts`). These are the always-visible controls users rely on while cooking — reaching them fast is COOK-UX-04 (at-a-glance) and COOK-UX-05 (voice-command clarity).

Purpose: Cooking at counter distance with greasy fingers means the header must show voice state, active timers, and TTS-interrupt without scrolling. Phase 19 tokens replace the last `#C2410C` literal in the cooking codebase.
Output: 6 files green against Wave 0 tests. `VoiceStatusBadge.tsx` untouched (deleted in 16-07 cleanup); old `TimerBar` rewritten in place, retaining import path so Wave 3 integration picks it up automatically.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-UI-SPEC.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-RESEARCH.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-00-PLAN.md
@apps/mobile/src/components/cooking/TimerBar.tsx
@apps/mobile/src/components/cooking/VoiceStatusBadge.tsx
@apps/mobile/src/design/tokens.ts
@apps/mobile/src/design/typography.ts
@apps/mobile/src/design/icons.ts
@apps/mobile/src/components/ui/SymbolIcon.tsx

<interfaces>
<!-- Phase 19 token imports every new cooking component MUST use -->

```typescript
// apps/mobile/src/design/tokens.ts
export const colors = {
  bg: '#FAF7F2',
  surface: '#FFFFFF',
  brand: '#C65D3A',
  brandPressed: '#A7492C',
  // ... text-primary/secondary/tertiary, warning, success, destructive, border
};
export const spacing = { /* 4, 8, 16, 24, 32, 48, 64 */ };

// apps/mobile/src/design/typography.ts — textStyles for StyleSheet consumers
// apps/mobile/src/design/icons.ts — iconSize helper + iconPropsForText('display') returns 28
```

```typescript
// NativeWind className conventions (from tailwind.config.js):
// bg-bg, bg-surface, bg-brand, bg-brand/15, bg-brand/30
// text-text-primary, text-text-secondary, text-text-tertiary
// text-display, text-title, text-body, text-label
// border-border, border-brand, border-warning
// bg-warning/10, bg-warning/20, text-warning
// bg-success/10, text-success
```

```typescript
// Timer type (existing)
interface Timer {
  id: string;
  label: string;
  endsAt: number;
  remainingMs: number;
}
```

```typescript
// Current TimerBar.tsx — read it fully before rewriting. Preserve props interface.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: haptics.ts helpers + useVoiceAmplitude hook</name>
  <files>
    apps/mobile/src/cooking/haptics.ts,
    apps/mobile/src/cooking/useVoiceAmplitude.ts
  </files>
  <behavior>
    Make green: `apps/mobile/src/cooking/__tests__/haptics.test.ts`, `apps/mobile/src/cooking/__tests__/useVoiceAmplitude.test.ts`.

    haptics.ts (COOK-UX-05) per UI-SPEC §Haptic contract:
    - `fireCommandHaptic()` → `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` (Next/Back/Repeat/Timer recognized)
    - `fireIngredientHaptic()` → `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` (Ingredient tap-to-check)
    - `fireTimerWarnHaptic()` → `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` (Timer at T-10s)
    - `fireTimerExpireHaptic()` → `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` (Timer expires)
    - `fireExitConfirmHaptic()` → `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning)` (Exit confirm destructive tap)
    - `fireStopTTSHaptic()` → `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` (Stop TTS button tapped)
    - All helpers swallow promises: `void Haptics.impactAsync(...)`. No throws.
    - Pitfall 9 guard: on iOS Simulator the native call is a no-op per docs; document inline: "Simulator: no-op. Device: Taptic Engine fires."

    useVoiceAmplitude (COOK-UX-05) per 16-RESEARCH.md Open Question 1:
    - Hook signature: `useVoiceAmplitude(listening: boolean): { phase: SharedValue<number> }`.
    - When `listening === true`: start a Reanimated repeating animation — `phase.value = withRepeat(withTiming(1, { duration: 600 }), -1, true)`.
    - When `listening === false`: `phase.value = 0`.
    - Attempts to subscribe to `@jamsch/expo-speech-recognition` volumechange event if that hook/event exists. If not (likely in 0.2.15), fall back to cosmetic loop. Wrap the probe in try/catch so a missing API never crashes.
    - Returns `{ phase }` object (so future extensions can add amplitude without breaking callers).
  </behavior>
  <action>
    Implements the haptic contract (UI-SPEC §Haptic contract) and the voice-amplitude hook (16-RESEARCH.md Open Question 1 fallback).

    1. Create `apps/mobile/src/cooking/haptics.ts`:
       - `import * as Haptics from 'expo-haptics';`
       - 6 exported void functions per the behavior block.
       - Each function uses `void` to fire-and-forget the promise.
       - Add a top-of-file JSDoc noting Pitfall 9 (simulator = no-op; physical device verification in DEVICE-TEST-16 §Haptics).

    2. Create `apps/mobile/src/cooking/useVoiceAmplitude.ts`:
       - `import { useSharedValue, withRepeat, withTiming, cancelAnimation } from 'react-native-reanimated';`
       - `import { useEffect } from 'react';`
       - Inside the hook:
         - `const phase = useSharedValue(0);`
         - `useEffect(() => { if (listening) { phase.value = withRepeat(withTiming(1, { duration: 600 }), -1, true); } else { cancelAnimation(phase); phase.value = 0; } }, [listening, phase]);`
       - Try/catch probe for amplitude events: attempt `const { useSpeechRecognitionEvent } = require('@jamsch/expo-speech-recognition');` and wire `volumechange` → update phase proportionally if available. Catch + ignore — cosmetic fallback is acceptable.
       - Return `{ phase }`.

    Per 16-RESEARCH.md Pattern 4: amplitude event is undocumented in 0.2.15; cosmetic loop satisfies COOK-UX-05 visual confirmation. Do NOT block this wave on the amplitude-driven path.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/cooking/__tests__/haptics.test.ts src/cooking/__tests__/useVoiceAmplitude.test.ts</automated>
  </verify>
  <done>
    Both test files green. Haptics helpers cover all 6 UI-SPEC events. useVoiceAmplitude returns SharedValue phase.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VoiceWaveform + StopTTSButton primitives</name>
  <files>
    apps/mobile/src/components/cooking/VoiceWaveform.tsx,
    apps/mobile/src/components/cooking/StopTTSButton.tsx
  </files>
  <behavior>
    Make green: `apps/mobile/src/components/cooking/__tests__/VoiceWaveform.test.tsx`, `StopTTSButton.test.tsx`.

    VoiceWaveform (UI-SPEC §Component Inventory, §Color "Accent reserved for → waveform mic fill"):
    - Props: `{ listening: boolean; enabled: boolean; onToggle: () => void }`.
    - When `!enabled`: renders mic-slash SF Symbol, `tintColor={colors.textTertiary}`, `accessibilityLabel="Voice commands: off"`, Pressable fires onToggle.
    - When `enabled && !listening`: pulse dot — outer `w-6 h-6 rounded-full bg-brand/30` + inner `w-2 h-2 rounded-full bg-brand`. `accessibilityLabel="Voice commands: on"`.
    - When `enabled && listening`: 3 animated bars — driven by `useVoiceAmplitude(listening).phase`. Each bar uses `useAnimatedStyle` returning height based on `Math.abs(Math.sin(phase.value * 2 * PI + offsetPi))`. `accessibilityLabel="Voice commands: listening"`.
    - All colors via `colors.*` or NativeWind token classes. No hardcoded hex. No import of anything outside Phase 19 tokens + expo-symbols + Reanimated.

    StopTTSButton (UI-SPEC §Copywriting "Stop reading" + §Component Inventory):
    - Props: `{ onPress: () => void }`.
    - Renders an SF Symbol (e.g., `stop.fill` or `speaker.slash.fill` — pick per existing `SymbolIcon` usage; `stop.circle.fill` is the preferred match for "stop reading" semantics).
    - `accessibilityLabel="Stop reading"` (UI-SPEC: "Stop" alone is ambiguous).
    - `accessibilityRole="button"`.
    - 44pt min tap target (`hitSlop`). Icon fill = `colors.brand`.
    - onPress fires haptic via `fireStopTTSHaptic()` + calls `onPress` prop.
  </behavior>
  <action>
    1. Create VoiceWaveform per the behavior block. Use the Reanimated example from 16-RESEARCH.md Pattern 4 lines 421-455 as the blueprint. Replace any inline color with `colors.*` import from `../../design/tokens` OR NativeWind className.

    2. Create StopTTSButton per the behavior block. Icon choice: `stop.circle.fill` (fills in the Stop metaphor cleanly; matches SF Symbols convention). Use `SymbolIcon` from `../ui/SymbolIcon` for consistency.

    Per UI-SPEC §Color §Accent reserved for: waveform mic fill and Stop button fill = `brand`; no other uses.

    Per Pitfall 7 (haptic on counter-face-down): acceptable — toast + waveform + TTS provide redundant confirmation channels.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/components/cooking/__tests__/VoiceWaveform.test.tsx src/components/cooking/__tests__/StopTTSButton.test.tsx</automated>
  </verify>
  <done>
    Both tests green. VoiceWaveform handles all 3 states. StopTTSButton declares correct accessibilityLabel.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retokenize TimerBar + build StickyCookingHeader</name>
  <files>
    apps/mobile/src/components/cooking/TimerBar.tsx,
    apps/mobile/src/components/cooking/StickyCookingHeader.tsx
  </files>
  <behavior>
    Make green: `apps/mobile/src/components/cooking/__tests__/TimerBar.test.tsx`, `StickyCookingHeader.test.tsx`.

    TimerBar (COOK-UX-03, §Component Inventory "retint hardcoded #C2410C"):
    - Props (preserve existing): `{ timers: Timer[]; onCancel: (id: string) => void }`.
    - Renders a horizontal row of chips — one per timer. Height 48pt (UI-SPEC §Spacing). `flex-row`, `gap-2`, `px-4`, `py-2`.
    - Each chip:
      - Default state: `bg-brand/15`, `border border-brand`, `text-brand-pressed` (or `colors.brandPressed`).
      - T-10s warn state (when `remainingMs < 10000`): chip background transitions to `bg-warning/20`, border `border-warning`.
      - Chip label: `text-label text-text-secondary uppercase` (e.g., "10 MIN").
      - Countdown: `text-body font-bold` — format as `9m 23s` or `23s` (if < 1 min). Use a `useEffect` + `setInterval(1000)` to update remainingMs each second, OR read from the store if cookingStore already ticks timers (check existing TimerBar.tsx first — preserve whatever ticker exists).
      - Tap on chip → `onCancel(timer.id)` with optional haptic (not in UI-SPEC for cancel; skip haptic for cancel to keep it reversible + low-friction).
    - **No `#C2410C` literal anywhere.** Grep-verifiable: `grep -n "#C2410C" apps/mobile/src/components/cooking/TimerBar.tsx` returns nothing.
    - All colors resolve via `colors.*` or Tailwind classes.

    StickyCookingHeader (COOK-UX-03, COOK-UX-04 §Layout structure, §Interaction Contract):
    - Props:
      ```typescript
      interface Props {
        recipeTitle: string;
        listening: boolean;
        voiceEnabled: boolean;
        ttsSpeaking: boolean;
        timers: Timer[];
        onExit: () => void;
        onToggleVoice: () => void;
        onStopTTS: () => void;
        onCancelTimer: (id: string) => void;
      }
      ```
    - Layout (per UI-SPEC §Layout structure + §Spacing):
      - Root `View`: `bg-surface border-b border-border`.
      - Base band (64pt = `h-16`): `flex-row items-center justify-between px-4`.
        - Left: Pressable with X SF Symbol + "Exit" text label (UI-SPEC copy). `accessibilityLabel="Exit cooking"`. Fires `onExit`.
        - Center: `<Text className="text-title text-text-primary flex-1 text-center mx-2" numberOfLines={1}>{recipeTitle}</Text>`.
        - Right: `<View className="flex-row items-center gap-2">` containing (conditionally) `<StopTTSButton onPress={onStopTTS} />` when `ttsSpeaking === true`, then always `<VoiceWaveform listening={listening} enabled={voiceEnabled} onToggle={onToggleVoice} />`.
      - Timer band (48pt = `h-12`): rendered **only** when `timers.length > 0`. Contains `<TimerBar timers={timers} onCancel={onCancelTimer} />`.
    - Total height behavior: 64pt default, 112pt with timers (UI-SPEC §Spacing Component-level).
    - No gestureEnabled prop — that's cook.tsx's responsibility (Wave 3).
    - No hardcoded colors; Phase 19 tokens only.
  </behavior>
  <action>
    1. Read existing `apps/mobile/src/components/cooking/TimerBar.tsx` fully. Preserve its Props interface. Rewrite the body to use Phase 19 tokens:
       - Replace every `#C2410C` with `colors.brandPressed` (or the `text-brand-pressed` Tailwind class — check `tailwind.config.js` for exact class name; if `brand-pressed` is not exposed, use inline `colors.brandPressed` via styles).
       - Replace any other hardcoded colors with tokens.
       - Add the T-10s warning transition: `timer.remainingMs < 10000` → swap to `bg-warning/20 border-warning text-warning`.
       - Ensure the 48pt chip height + 8pt gap matches UI-SPEC §Spacing.

    2. Create StickyCookingHeader per the behavior block. Import VoiceWaveform and StopTTSButton from adjacent files (no barrel re-export needed). Import TimerBar from `./TimerBar`. Use SymbolIcon for the Exit X icon.

    Per UI-SPEC §Color "Accent NEVER used for" → sticky-header background stays `bg-surface`, NOT `bg-brand`.

    Pitfall 10 documented: intentional color shift from `#C2410C` → `colors.brandPressed` (#A7492C). Close but not identical. No visual-diff regression is acceptable because this is the correct Phase 19 token.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/components/cooking/__tests__/TimerBar.test.tsx src/components/cooking/__tests__/StickyCookingHeader.test.tsx</automated>
  </verify>
  <done>
    TimerBar grep-clean: `grep -n "#C2410C" apps/mobile/src/components/cooking/TimerBar.tsx` empty. StickyCookingHeader renders all 4 slots + conditional timer band + conditional Stop button. All component tests green.
  </done>
</task>

</tasks>

<verification>
- `cd apps/mobile && pnpm test --run src/cooking src/components/cooking` (full mobile cooking suite green)
- `grep -rn "#C2410C" apps/mobile/src/components/cooking/ apps/mobile/src/cooking/` empty
- TypeScript compiles: `cd apps/mobile && pnpm tsc --noEmit`
- No new imports from outside Phase 19 design + expo-haptics + expo-symbols + Reanimated + Phase 9 cooking infra
</verification>

<success_criteria>
- 6 files exist on disk, all test suites green
- Sticky header renders at 64 or 112pt exactly per UI-SPEC
- VoiceWaveform cleanly handles all 3 states
- StopTTSButton has "Stop reading" accessibility label
- TimerBar has zero hardcoded colors
- Haptics helpers are typed and callable
- useVoiceAmplitude gracefully degrades if 0.2.15 lacks amplitude events
</success_criteria>

<output>
After completion, create `.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-03-SUMMARY.md` with:
- New primitives shipped + their line counts
- TimerBar retoken diff summary (removed hex literals, added warning-tone transition)
- Haptics API coverage (6/6 UI-SPEC events)
- useVoiceAmplitude fallback decision (amplitude event found or cosmetic?)
</output>
