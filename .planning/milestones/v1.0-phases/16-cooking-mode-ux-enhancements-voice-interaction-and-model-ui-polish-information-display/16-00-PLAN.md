---
phase: 16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/mobile/src/cooking/__fixtures__/sse-response.ts
  - apps/mobile/src/cooking/__fixtures__/recipe.ts
  - apps/mobile/src/cooking/__tests__/streamingAsk.test.ts
  - apps/mobile/src/cooking/__tests__/telemetry.test.ts
  - apps/mobile/src/cooking/__tests__/handleTranscript.test.ts
  - apps/mobile/src/cooking/__tests__/haptics.test.ts
  - apps/mobile/src/cooking/__tests__/useVoiceAmplitude.test.ts
  - apps/mobile/src/cooking/__tests__/useCurrentStepScroll.test.ts
  - apps/mobile/src/components/cooking/__tests__/ScrollableRecipe.test.tsx
  - apps/mobile/src/components/cooking/__tests__/StepCard.test.tsx
  - apps/mobile/src/components/cooking/__tests__/IngredientRow.test.tsx
  - apps/mobile/src/components/cooking/__tests__/StickyCookingHeader.test.tsx
  - apps/mobile/src/components/cooking/__tests__/VoiceWaveform.test.tsx
  - apps/mobile/src/components/cooking/__tests__/CommandToast.test.tsx
  - apps/mobile/src/components/cooking/__tests__/StopTTSButton.test.tsx
  - apps/mobile/src/components/cooking/__tests__/TimerBar.test.tsx
  - apps/mobile/src/stores/__tests__/cookingStore.test.ts
  - apps/mobile/src/stores/cookingStore.ts
  - apps/mobile/src/types/cooking.ts
  - apps/mobile/src/cooking/sse-smoke.ts
  - apps/mobile/package.json
  - packages/server/src/routes/__tests__/telemetry.test.ts
  - packages/server/src/routes/__tests__/cooking.test.ts
  - .planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/DEVICE-TEST-16.md
autonomous: true
requirements: [COOK-UX-01, COOK-UX-02, COOK-UX-03, COOK-UX-04, COOK-UX-05]
user_setup: []
must_haves:
  truths:
    - "All Phase 16 unit test suites exist (red) before production code arrives"
    - "cookingStore exposes darkMode, ingredientChecks, lastCommandToast, currentSessionId slices with persist(darkMode)"
    - "expo-haptics is installed and importable"
    - "SSE ReadableStream smoke test verifies RN 0.83 fetch compatibility"
    - "DEVICE-TEST-16.md checklist exists mirroring Phase 9 structure"
  artifacts:
    - path: "apps/mobile/src/stores/cookingStore.ts"
      provides: "Extended Zustand store (darkMode persisted, ingredientChecks ephemeral, lastCommandToast, currentSessionId)"
      contains: "persist, partialize"
    - path: "apps/mobile/src/cooking/__fixtures__/recipe.ts"
      provides: "Shared 8-ingredient / 6-step recipe fixture for Phase 16 component tests"
      exports: ["TEST_RECIPE"]
    - path: "apps/mobile/src/cooking/__fixtures__/sse-response.ts"
      provides: "Mock SSE event stream builder for streamingAsk tests"
      exports: ["buildSSEStream", "buildSSEError"]
    - path: "apps/mobile/src/cooking/sse-smoke.ts"
      provides: "One-shot smoke script to verify RN fetch ReadableStream parses SSE on iOS"
    - path: ".planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/DEVICE-TEST-16.md"
      provides: "Physical-iPhone manual verification checklist (latency, voice, haptics, telemetry, dark mode)"
  key_links:
    - from: "cookingStore persist"
      to: "AsyncStorage"
      via: "createJSONStorage(() => AsyncStorage)"
      pattern: "persist\\(.*partialize.*darkMode"
    - from: "Phase 16 component tests"
      to: "Phase 19 tokens"
      via: "imports tokens/typography/icons from src/design"
      pattern: "from\\s+['\"].*design/(tokens|typography|icons)"
---

<objective>
Establish Wave 0 test scaffolding for Phase 16 so every downstream wave has a red target to hit. Close every MISSING reference from 16-VALIDATION.md, extend cookingStore with the four Phase 16 state slices (with darkMode persistence), install expo-haptics, and stand up the physical-device verification checklist.

Purpose: The Nyquist rule (every `<automated>` in later plans must have a concrete target) only holds if the test files exist red before production code ships. This plan is that foundation.
Output: Red test suites for all 7 new components + 4 new cooking modules + telemetry + streaming server endpoint + SSE smoke script + DEVICE-TEST-16.md skeleton + extended cookingStore.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-CONTEXT.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-RESEARCH.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-UI-SPEC.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-VALIDATION.md
@apps/mobile/src/stores/cookingStore.ts
@apps/mobile/src/types/cooking.ts
@apps/mobile/src/cooking/handleTranscript.ts
@apps/mobile/src/cooking/useVoiceListener.ts
@apps/mobile/src/stores/progressionStore.ts

<interfaces>
<!-- Existing cookingStore interface (apps/mobile/src/stores/cookingStore.ts). -->
<!-- Wave 0 EXTENDS this — do NOT drop existing actions/state. -->

```typescript
// Current CookingState (src/types/cooking.ts):
interface CookingState {
  recipe: Recipe | null;
  stepIndex: number;
  voiceEnabled: boolean;
  ttsEnabled: boolean;
  listening: boolean;
  timers: Timer[];
  lastAssistantAnswer: string | null;
}

// Current CookingActions (cookingStore.ts):
interface CookingActions {
  enter: (recipe: Recipe) => void;
  exit: () => void;
  next: () => void;
  back: () => void;
  repeat: () => void;
  addTimer: (ms: number) => void;
  removeTimer: (id: string) => void;
  setListening: (listening: boolean) => void;
  setAssistantAnswer: (answer: string | null) => void;
}
```

```typescript
// progressionStore.ts persist precedent (mirror this shape):
persist(
  (set, get) => ({ /* ... */ }),
  {
    name: 'progression-store',
    storage: createJSONStorage(() => AsyncStorage),
    partialize: (s) => ({ cookStats: s.cookStats, ambitionSuggestions: s.ambitionSuggestions }),
  },
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend cookingStore with Phase 16 slices + persist(darkMode) + write fixtures + cookingStore tests</name>
  <files>
    apps/mobile/src/types/cooking.ts,
    apps/mobile/src/stores/cookingStore.ts,
    apps/mobile/src/stores/__tests__/cookingStore.test.ts,
    apps/mobile/src/cooking/__fixtures__/recipe.ts,
    apps/mobile/src/cooking/__fixtures__/sse-response.ts
  </files>
  <behavior>
    cookingStore tests (additive to existing — extend the existing __tests__/cookingStore.test.ts, do NOT delete current tests):
    - `toggleIngredient('rice-0')` flips entry in `ingredientChecks` map; second call flips back; multiple IDs accumulate.
    - `clearIngredientChecks()` resets to `{}`.
    - `enter(recipe)` clears `ingredientChecks` AND generates a fresh `currentSessionId` matching /^sess-[0-9a-z]+-[0-9a-z]{6}$/.
    - `exit()` clears `currentSessionId` (sets to null) in addition to existing behavior.
    - `setDarkMode(true)` sets state; `setDarkMode(false)` resets.
    - `showCommandToast('Next step')` sets `lastCommandToast` to `{ message: 'Next step', id: 't-<ms>' }` with a unique id.
    - `clearCommandToast()` sets `lastCommandToast: null`.
    - Persist middleware (mock AsyncStorage via vi.mock of '@react-native-async-storage/async-storage'): after `setDarkMode(true)`, the persist write call must include `{ darkMode: true }` and MUST NOT include `ingredientChecks` or `lastCommandToast` (verify via `partialize` output inspection or snapshot of mocked storage setItem call).
    - Rehydration: simulate AsyncStorage returning `{ state: { darkMode: true }, version: 0 }`; after rehydrate, store reads `darkMode === true` and all other fields use initial values.

    Fixture shape (src/cooking/__fixtures__/recipe.ts):
    - `TEST_RECIPE: Recipe` — 8 ingredients, 6 steps, realistic cook time + servings. Use real keys matching `Recipe` type. Consumers: ScrollableRecipe, StepCard, IngredientRow, StickyCookingHeader tests.

    Fixture shape (src/cooking/__fixtures__/sse-response.ts):
    - `buildSSEStream(chunks: string[]): ReadableStream<Uint8Array>` — encodes each chunk as `event: delta\ndata: <chunk>\n\n` then a final `event: done\ndata: <full>\n\n`. Returns a real ReadableStream.
    - `buildSSEError(code: string): ReadableStream<Uint8Array>` — emits a single `event: error\ndata: <code>\n\n` message.
  </behavior>
  <action>
    Implements D-01..D-23 telemetry+darkMode+ingredient-checks extensions per 16-CONTEXT.md and 16-RESEARCH.md Pattern 7.

    1. Extend `apps/mobile/src/types/cooking.ts`:
       - Add to `CookingState`: `ingredientChecks: Record<string, boolean>`, `darkMode: boolean`, `lastCommandToast: { message: string; id: string } | null`, `currentSessionId: string | null`.
       - Keep all existing fields.

    2. Rewrite `apps/mobile/src/stores/cookingStore.ts` with `persist + createJSONStorage(() => AsyncStorage) + partialize: (s) => ({ darkMode: s.darkMode })`, mirroring `progressionStore.ts` persist shape exactly. Preserve ALL existing actions (enter, exit, next, back, repeat, addTimer, removeTimer, setListening, setAssistantAnswer). ADD new actions:
       - `toggleIngredient(id: string)` — flips the entry in `ingredientChecks`.
       - `clearIngredientChecks()` — sets to `{}`.
       - `setDarkMode(on: boolean)` — sets `darkMode`.
       - `showCommandToast(message: string)` — sets `lastCommandToast = { message, id: 't-' + Date.now() }`.
       - `clearCommandToast()` — sets `lastCommandToast: null`.
       - `startSession()` — sets `currentSessionId` to `sess-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`.
       - Modify `enter(recipe)` to ALSO call `clearIngredientChecks()` logic inline AND set a fresh `currentSessionId` (inline, NOT via startSession call — one atomic set).
       - Modify `exit()` to clear `currentSessionId: null` and `lastCommandToast: null` in addition to existing behavior.

    3. Write `src/cooking/__fixtures__/recipe.ts` with `TEST_RECIPE: Recipe` (8 ingredients, 6 steps). Consult `apps/mobile/src/types/recipe.ts` for the exact shape — do NOT invent fields.

    4. Write `src/cooking/__fixtures__/sse-response.ts` with `buildSSEStream` and `buildSSEError` helpers (see behavior block). Use `new ReadableStream<Uint8Array>({ start(controller) { ... controller.enqueue(encoder.encode(msg)); controller.close(); } })`.

    5. Extend `src/stores/__tests__/cookingStore.test.ts` to cover all new behavior (see behavior block). Mock `@react-native-async-storage/async-storage` with an in-memory shim: `vi.mock('@react-native-async-storage/async-storage', () => ({ default: { getItem: vi.fn(async () => null), setItem: vi.fn(async () => undefined), removeItem: vi.fn() } }))`.

    Per D-03 (Background surface toggle): darkMode MUST persist across sessions. Per D-11 (Telemetry session IDs): currentSessionId MUST regenerate on every enter().
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/stores/__tests__/cookingStore.test.ts</automated>
  </verify>
  <done>
    cookingStore tests green (existing + new). TEST_RECIPE fixture and SSE helpers importable. Persist middleware wired with partialize: (s) => ({ darkMode }). All existing actions preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install expo-haptics + SSE smoke script + red test stubs for ALL Phase 16 modules (telemetry, streamingAsk, handleTranscript ext, haptics, useVoiceAmplitude, useCurrentStepScroll, 7 new components, TimerBar retoken)</name>
  <files>
    apps/mobile/package.json,
    apps/mobile/src/cooking/sse-smoke.ts,
    apps/mobile/src/cooking/__tests__/streamingAsk.test.ts,
    apps/mobile/src/cooking/__tests__/telemetry.test.ts,
    apps/mobile/src/cooking/__tests__/handleTranscript.test.ts,
    apps/mobile/src/cooking/__tests__/haptics.test.ts,
    apps/mobile/src/cooking/__tests__/useVoiceAmplitude.test.ts,
    apps/mobile/src/cooking/__tests__/useCurrentStepScroll.test.ts,
    apps/mobile/src/components/cooking/__tests__/ScrollableRecipe.test.tsx,
    apps/mobile/src/components/cooking/__tests__/StepCard.test.tsx,
    apps/mobile/src/components/cooking/__tests__/IngredientRow.test.tsx,
    apps/mobile/src/components/cooking/__tests__/StickyCookingHeader.test.tsx,
    apps/mobile/src/components/cooking/__tests__/VoiceWaveform.test.tsx,
    apps/mobile/src/components/cooking/__tests__/CommandToast.test.tsx,
    apps/mobile/src/components/cooking/__tests__/StopTTSButton.test.tsx,
    apps/mobile/src/components/cooking/__tests__/TimerBar.test.tsx,
    packages/server/src/routes/__tests__/telemetry.test.ts,
    packages/server/src/routes/__tests__/cooking.test.ts
  </files>
  <behavior>
    This task installs one dependency AND writes red test stubs. Tests call production modules that don't exist yet — Vitest reports "Cannot find module X" which IS the red signal. Execute-plan Wave 1/2/3 implementers then make them green.

    streamingAsk.test.ts (COOK-UX-01):
    - imports `streamAsk` from `../streamingAsk` (does not exist → red).
    - Plans: mocks fetch to return `{ ok: true, body: buildSSEStream(['Hello ', 'world. ', 'Done.']) }`. Asserts `onChunk` called 3 times with delta substrings. Asserts `onDone('Hello world. Done.')`. Asserts `onError` NOT called. Error path: fetch returns `ok: false, status: 503` → `onError('HTTP_503')`. Error-event path: `buildSSEError('CLAUDE_ERROR')` → `onError('CLAUDE_ERROR')`.

    telemetry.test.ts (COOK-UX-02):
    - imports `logCookingEvent, flushTelemetry, __resetForTests` from `../telemetry` (does not exist → red).
    - Asserts: 10 events trigger auto-flush (fetch called with POST + Authorization header + body.events.length === 10). Timer-based flush: 1 event + advance vitest fakeTimers 30s → flush fires. 5xx failure: batch re-queued; next flush retries. Queue cap: 201 enqueues drops oldest, queue.length === 200 after.
    - Ensures no PII leak: passing a payload containing `transcript: 'real words'` still posts (module is agnostic) — caller responsibility documented, but a dedicated assertion checks the `payload` key surface matches `['confidence', 'ms', 'intent_type', 'length', 'error_code', 'first_chunk_ms', 'total_ms', 'answer_length', 'session_id']` when the module's `sanitizePayload` helper is called.

    handleTranscript.test.ts (COOK-UX-05) — EXTEND existing:
    - Read current test file to understand existing shape. Add: `onCommandToast('Next step')` called when intent is `next`, NOT called when intent is `ask`. `onCommandHaptic()` called for next/back/repeat/timer, NOT for `ask`. Timer intent toast copy: `Timer set · 10 min`.

    haptics.test.ts (COOK-UX-05):
    - imports `fireCommandHaptic, fireIngredientHaptic, fireTimerWarnHaptic, fireTimerExpireHaptic, fireExitConfirmHaptic, fireStopTTSHaptic` from `../haptics` (does not exist → red).
    - Mocks `expo-haptics`. Asserts each helper calls the documented Haptics API (see UI-SPEC §Haptic contract):
      · fireCommandHaptic → `impactAsync(ImpactFeedbackStyle.Medium)`
      · fireIngredientHaptic → `impactAsync(ImpactFeedbackStyle.Light)`
      · fireTimerWarnHaptic → `impactAsync(ImpactFeedbackStyle.Light)`
      · fireTimerExpireHaptic → `notificationAsync(NotificationFeedbackType.Success)`
      · fireExitConfirmHaptic → `notificationAsync(NotificationFeedbackType.Warning)`
      · fireStopTTSHaptic → `impactAsync(ImpactFeedbackStyle.Medium)`

    useVoiceAmplitude.test.ts (COOK-UX-05):
    - imports `useVoiceAmplitude` hook from `../useVoiceAmplitude` (does not exist → red).
    - Asserts: when `listening=true`, returns a `phase: SharedValue<number>` that is not zero. When `listening=false`, phase is 0. Hook subscribes to speech-recognition amplitude event if available; falls back to cosmetic loop if not (per UI-SPEC fallback).

    useCurrentStepScroll.test.ts (COOK-UX-04):
    - imports `useCurrentStepScroll` hook from `../useCurrentStepScroll` (does not exist → red).
    - Asserts: given a ScrollView ref mock and a `stepYs.current = [0, 120, 260, 400]` and `currentStepIndex = 2`, the hook calls `scrollRef.current.scrollTo({ y: 140, animated: true })` (260 - 120 center offset per Pattern 2).

    Component tests (ScrollableRecipe, StepCard, IngredientRow, StickyCookingHeader, VoiceWaveform, CommandToast, StopTTSButton, TimerBar) — all red because components don't exist yet. Each test file asserts:
    - Module imports (red): `import { ScrollableRecipe } from '../ScrollableRecipe'` etc.
    - Render with TEST_RECIPE props. Assert className strings include expected Phase 19 tokens (`bg-bg`, `bg-surface`, `text-text-primary`, `text-body`, `text-display`, `text-title`, `text-label`, `border-border`, `bg-brand`, `bg-brand/15`, `bg-warning/10`, `text-warning`, `text-success`). Assert NO hardcoded hex (grep-style `.toEqual(expect.not.stringMatching(/#[0-9a-fA-F]{3,6}/))` on className prop).
    - StepCard: `isCurrent=true` → `text-display` class; `isCurrent=false` → `text-title` class; left rail visible only when current.
    - IngredientRow: onToggle fires on press; strike-through class applied when checked; success-tone check icon.
    - StickyCookingHeader: timer band renders when timers.length > 0; StopTTSButton renders only when ttsSpeaking=true.
    - VoiceWaveform: 3 animated bars when listening=true; single pulse dot when enabled && !listening; mic-slash when !enabled.
    - CommandToast: `accessibilityLiveRegion="polite"`; renders message text; auto-clears via `onClear` prop after 1.5s (use vi.useFakeTimers).
    - StopTTSButton: `accessibilityLabel="Stop reading"`; fires onPress.
    - TimerBar (retoken): no `#C2410C` substring in rendered className; uses token keywords (`brand`, `brandPressed`, `warning`). T-10s transition: when a timer's remainingMs < 10000, chip adopts `bg-warning/20` class.

    packages/server/src/routes/__tests__/telemetry.test.ts (COOK-UX-02):
    - imports telemetry route (does not exist → red). Asserts: POST /api/v1/telemetry/cooking requires Authorization bearer; on empty `events: []` returns 204; on 3 valid events returns 200 + inserts 3 rows via mocked Supabase client; on invalid schema returns 400.

    packages/server/src/routes/__tests__/cooking.test.ts — EXTEND existing:
    - Add describe block `/cooking/ask-stream SSE`. Red test: `POST /api/v1/cooking/ask-stream` with valid body streams `event: delta` events then `event: done`. Mock Anthropic stream builder. Error path: Anthropic throws → `event: error\ndata: CLAUDE_ERROR`. Preserve every existing `/ask` test untouched.

    sse-smoke.ts (not a test — a runnable script):
    - Imports `fetch` from react-native (Hermes globally-available). Calls `fetch('http://localhost:3000/api/v1/cooking/ask-stream', {...})` against a dev server. Reads `res.body.getReader()` and logs each chunk. Documents expected output and fallback behavior if `res.body` is null on RN 0.83. Run once in Wave 1 via `npx expo start --dev-client` + manually trigger.

    DEVICE-TEST-16.md skeleton (handled in this task's final step):
    - Mirrors Phase 9 DEVICE-TEST.md structure. Sections: §Latency (p95 TTS first-word), §Voice (next/back/repeat/timer reliability 0.5-1.5m distance), §Real-Kitchen Session (one recipe full cook, noise, telemetry upload), §Haptics (each of 6 haptic events felt on device), §TTS Audibility (counter/AirPods/CarPlay), §Dark Mode (OLED evaluation). Checkboxes unchecked; executor fills during Wave 5.
  </behavior>
  <action>
    1. Install expo-haptics:
       ```
       cd apps/mobile && npx expo install expo-haptics
       ```
       Verify in package.json dependencies — pin should match SDK 55 (~55.0.14). Per 16-RESEARCH.md §Standard Stack: no config plugin required.

    2. Write `apps/mobile/src/cooking/sse-smoke.ts` — a runnable spike script per Wave 0 gate in 16-VALIDATION.md. Does NOT run in CI; documents manual verification. 20–40 lines.

    3. Write all 17 test files listed in `<files>` as red stubs. For each file:
       - Use the existing repo vitest style (see `apps/mobile/src/cooking/__tests__/handleTranscript.test.ts` and `apps/mobile/src/stores/__tests__/cookingStore.test.ts` for patterns).
       - Mock native modules at the top: `vi.mock('expo-haptics', () => ({ impactAsync: vi.fn(), notificationAsync: vi.fn(), ImpactFeedbackStyle: { Light: 'light', Medium: 'medium' }, NotificationFeedbackType: { Success: 'success', Warning: 'warning' } }))`.
       - For component tests (.tsx), use `@testing-library/react-native` if already installed (check apps/mobile/package.json devDependencies) — if not, use vitest's node-env approach from Phase 19 patterns (static className inspection).
       - Each test file imports from modules that DO NOT YET EXIST — this is the red signal. Do NOT create stub source files. Execute-plan Wave 1/2/3 tasks create them.

    4. Extend handleTranscript.test.ts: read the existing file first (`apps/mobile/src/cooking/__tests__/handleTranscript.test.ts`), add new describe block `onCommandToast + onCommandHaptic callbacks` covering the deps-extension behavior. Do NOT touch existing tests.

    5. Extend packages/server/src/routes/__tests__/cooking.test.ts similarly — add describe block for `/cooking/ask-stream` SSE endpoint.

    6. Create `DEVICE-TEST-16.md` at phase-dir root with the sections enumerated in the behavior block. Mirror Phase 9's `DEVICE-TEST.md` — search the repo for it (likely at `.planning/phases/09-voice-cooking-mode/DEVICE-TEST.md` or similar) and match the structure.

    Per 16-VALIDATION.md Wave 0 Requirements: this task closes every MISSING reference. Per 16-RESEARCH.md Open Question 2: sse-smoke.ts is the Wave 0 gate for RN fetch streaming.
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm test --run src/cooking src/components/cooking src/stores 2>&1 | tee /tmp/p16-00-mobile.log; grep -qE "(no test files|Cannot find module|ReferenceError|0 passed)" /tmp/p16-00-mobile.log || (cd apps/mobile && pnpm list expo-haptics && test -f src/cooking/__tests__/streamingAsk.test.ts && test -f src/cooking/__tests__/telemetry.test.ts && test -f src/components/cooking/__tests__/ScrollableRecipe.test.tsx && test -f src/components/cooking/__tests__/StepCard.test.tsx && test -f src/components/cooking/__tests__/IngredientRow.test.tsx && test -f src/components/cooking/__tests__/StickyCookingHeader.test.tsx && test -f src/components/cooking/__tests__/VoiceWaveform.test.tsx && test -f src/components/cooking/__tests__/CommandToast.test.tsx && test -f src/components/cooking/__tests__/StopTTSButton.test.tsx && test -f src/components/cooking/__tests__/TimerBar.test.tsx && test -f src/cooking/__tests__/haptics.test.ts && test -f src/cooking/__tests__/useVoiceAmplitude.test.ts && test -f src/cooking/__tests__/useCurrentStepScroll.test.ts && test -f src/cooking/sse-smoke.ts) && test -f .planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/DEVICE-TEST-16.md && (cd packages/server && test -f src/routes/__tests__/telemetry.test.ts)</automated>
  </verify>
  <done>
    expo-haptics listed in apps/mobile/package.json. All 17 test files present (red). DEVICE-TEST-16.md exists with the 6 sections. sse-smoke.ts runnable. cookingStore tests green. handleTranscript + cooking.test.ts extensions added without breaking existing tests.
  </done>
</task>

</tasks>

<verification>
Plan-level verification:
- `cd apps/mobile && pnpm test --run src/stores/__tests__/cookingStore.test.ts` (all green — store ext is implemented here)
- `cd apps/mobile && pnpm list expo-haptics` (version ~55.0.x)
- Every file in `<files_modified>` exists on disk
- `ls .planning/phases/16-*/DEVICE-TEST-16.md` returns path
- grep -r `#C2410C` apps/mobile/src/components/cooking/ — still present in TimerBar.tsx (retoken happens in 16-03); TimerBar TEST asserts absence only at impl-time
</verification>

<success_criteria>
- cookingStore tests green, ingredientChecks/darkMode/lastCommandToast/currentSessionId wired, darkMode persisted
- expo-haptics installed
- All 17 Phase 16 test files exist (red for modules that ship in later waves)
- sse-smoke.ts exists for manual Wave 1 gate
- DEVICE-TEST-16.md skeleton exists
- handleTranscript + cooking route test extensions added non-destructively
- nyquist_compliant: true can be set in 16-VALIDATION.md after this plan ships
</success_criteria>

<output>
After completion, create `.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-00-SUMMARY.md` with:
- Files created (count + list)
- cookingStore deltas (actions added, persist shape)
- expo-haptics version pinned
- SSE smoke script status (ran manually? results?)
- DEVICE-TEST-16.md sections enumerated
- Any deviations from 16-VALIDATION.md Wave 0 Requirements
</output>
