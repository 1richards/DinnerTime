---
phase: 16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display
plan: 07
type: execute
wave: 4
depends_on: [16-06]
files_modified:
  - apps/mobile/src/app/(tabs)/settings.tsx
  - apps/mobile/.maestro/28-cooking-mode-ui.yaml
  - apps/mobile/src/components/cooking/StepDisplay.tsx
  - apps/mobile/src/components/cooking/VoiceStatusBadge.tsx
autonomous: false
requirements: [COOK-UX-03, COOK-UX-04]
user_setup: []
must_haves:
  truths:
    - "Settings Cooking section renders a Dark cooking mode toggle row"
    - "Toggling the switch calls setDarkMode and persists across app restarts"
    - "Maestro flow 28 runs green on iPhone 17 Pro sim: enter cooking, ScrollableRecipe visible, tap ingredient, tap Next, Exit"
    - "StepDisplay.tsx and VoiceStatusBadge.tsx are deleted with no consumers"
  artifacts:
    - path: "apps/mobile/src/app/(tabs)/settings.tsx"
      provides: "New Cooking section with dark-mode toggle row"
      contains: "Dark cooking mode"
    - path: "apps/mobile/.maestro/28-cooking-mode-ui.yaml"
      provides: "Maestro UAT flow for non-voice cooking UI (simulator-runnable)"
      contains: "Cook"
  key_links:
    - from: "settings.tsx Cooking section toggle"
      to: "cookingStore.setDarkMode"
      via: "direct store action call onValueChange"
      pattern: "setDarkMode\\("
    - from: "Maestro flow 28"
      to: "cook.tsx scrollable recipe"
      via: "launchApp + navigate to recipe + tap Cook + assert UI"
      pattern: "launchApp"
---

<objective>
Close out cooking UX with Settings integration (dark-mode toggle), a Maestro flow for simulator-runnable UI verification, and cleanup of Phase 9 components superseded by Phase 16 primitives.

Purpose: Dark-mode toggle lives in Settings per CONTEXT D-03. Maestro flow 28 covers every non-voice UI assertion for UAT. Deleting StepDisplay/VoiceStatusBadge keeps the cooking surface clean.
Output: Settings section renders + toggles + persists. Maestro flow 28 green. Two dead files removed. Human checkpoint confirms dark-mode toggle visual + Maestro run.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-UI-SPEC.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-CONTEXT.md
@.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-RESEARCH.md
@apps/mobile/src/app/(tabs)/settings.tsx
@apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
@apps/mobile/.maestro/27-something-new-search.yaml
@apps/mobile/.maestro/smoke.yaml

<interfaces>
Preserve existing Settings sections. ADD a "Cooking" section at the bottom or wherever fits the visual hierarchy.

Toggle row pattern (check existing settings.tsx for the convention):
- native iOS `<Switch value={darkMode} onValueChange={setDarkMode} />`
- row layout: flex-row items-center justify-between, py-4, border-b border-border

Maestro flow conventions (see existing 27-something-new-search.yaml):
- appId: com.dinnertime.app
- launchApp
- tapOn: (supports regex)
- assertVisible / assertNotVisible
- takeScreenshot: name
- Per CLAUDE.md UAT section: selectors are regex; prefer plain UI labels
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings Cooking section with dark-mode toggle + delete dead Phase 9 components</name>
  <files>
    apps/mobile/src/app/(tabs)/settings.tsx,
    apps/mobile/src/components/cooking/StepDisplay.tsx,
    apps/mobile/src/components/cooking/VoiceStatusBadge.tsx
  </files>
  <action>
    1. Read apps/mobile/src/app/(tabs)/settings.tsx fully. Understand the existing section pattern.

    2. Add a new "Cooking" section with one toggle row, per UI-SPEC §Component Inventory "Settings additions" + §Copywriting "Dark mode toggle label":
       - Section header: `<Text className="text-label text-text-secondary uppercase">COOKING</Text>` (UI-SPEC label role).
       - Toggle row:
         - Layout: flex-row items-center justify-between, py-4, border-b border-border.
         - Left View with two stacked Text nodes:
           - Title: `<Text className="text-body text-text-primary">Dark cooking mode</Text>`
           - Subtitle: `<Text className="text-body text-text-secondary">Darker background while cooking. Matches Spotify's Now Playing feel.</Text>`
         - Right: native iOS `<Switch value={darkMode} onValueChange={setDarkMode} />`.
         - accessibilityRole="switch", accessibilityState={{ checked: darkMode }}.
       - Wire `darkMode` and `setDarkMode` from `useCookingStore()`.

    3. Verify the Cooking section does not regress other Settings rows. If any existing Cooking section exists (should not per CONTEXT), augment rather than duplicate.

    4. Delete obsolete Phase 9 files:
       - `rm apps/mobile/src/components/cooking/StepDisplay.tsx`
       - `rm apps/mobile/src/components/cooking/VoiceStatusBadge.tsx`
       Verify no imports remain: `grep -rn "StepDisplay\|VoiceStatusBadge" apps/mobile/src/` returns empty (comments excluded). If any stray import exists, TypeScript will fail; remove or replace with the Phase 16 primitive.

    Per UI-SPEC §Component Inventory "Primitives to rework": StepDisplay -> ScrollableRecipe+StepCard (delete after migration). VoiceStatusBadge -> VoiceWaveform (delete file).

    Per UI-SPEC §Copywriting: use the exact toggle title and subtitle strings above (no em-dashes, no paraphrases).
  </action>
  <verify>
    <automated>cd apps/mobile && pnpm tsc --noEmit && pnpm test --run src/app src/stores && test ! -f src/components/cooking/StepDisplay.tsx && test ! -f src/components/cooking/VoiceStatusBadge.tsx</automated>
  </verify>
  <done>
    Settings renders the Cooking section with working toggle. Deleted files verified absent. TypeScript clean. No stray StepDisplay/VoiceStatusBadge imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Maestro flow 28 — non-voice cooking UI happy path</name>
  <files>
    apps/mobile/.maestro/28-cooking-mode-ui.yaml
  </files>
  <action>
    Create `apps/mobile/.maestro/28-cooking-mode-ui.yaml` covering the non-voice UAT path per 16-RESEARCH.md Open Question 6 and 16-VALIDATION.md Per-Task Verification Map.

    Pattern — mirror `apps/mobile/.maestro/27-something-new-search.yaml` structure.

    Flow steps:
    1. appId: com.dinnertime.app
    2. launchApp with stopApp: true
    3. Dismiss Expo dev-menu first-run banner (tapOn Continue if visible, tap 90%,32% to close dev menu) — copy from smoke.yaml.
    4. Sign in — reuse auth helper flow or inline tap "Sign In".
    5. Navigate: tapOn "Recipe Box" tab (or via Kitchen segment) → tap first recipe card.
    6. Assert visible: recipe title.
    7. Tap "Cook" button.
    8. Assert visible: "INGREDIENTS" section label (ScrollableRecipe rendered).
    9. Take screenshot: 28-01-cooking-mode-landing.
    10. Assert visible: "STEPS" section label.
    11. Assert visible: "STEP 1 of" substring (current step highlight).
    12. Tap on the first ingredient row (use first ingredient name from a seeded/known recipe, or use a positional tap on the first visible ingredient). Take screenshot 28-02-ingredient-checked.
    13. Tap "Next" nav button. Assert visible: "STEP 2 of". Take screenshot 28-03-step-advanced.
    14. Tap "Back" nav button. Assert visible: "STEP 1 of". Take screenshot 28-04-step-back.
    15. Tap "Exit" (top-left X + label). Assert visible: "End cooking session?". Take screenshot 28-05-exit-confirm.
    16. Tap "End cooking session". Assert back on recipe detail screen.
    17. Navigate to Settings tab. Tap "Cooking" section area (if section header is a tappable no-op, just assert visible). Assert visible: "Dark cooking mode".
    18. Tap the dark-mode Switch. Take screenshot 28-06-dark-mode-toggled.
    19. Re-enter cooking (back to recipe → tap Cook). Take screenshot 28-07-dark-mode-cooking (should show dark palette).
    20. Tap Exit → End cooking session.

    Selector caveats per CLAUDE.md UAT section:
    - Maestro selectors are regex. Avoid `=`, `(`, `?`, `·` (the middle dot from "Timer set · 10 min") in assertions.
    - Prefer plain UI labels over dynamic text. If a label contains a regex metachar, escape it or use a screenshot + visual verification instead of assertVisible.
    - Use `text: .*Cook.*` with regex mode if "Cook" button label ambiguates with "Cookbook" or similar.

    Add a top-of-file comment explaining the flow, the 3 reasons voice is not covered (physical-device only, no audio injection, DEVICE-TEST-16 covers), and the 7 screenshots produced.
  </action>
  <verify>
    <automated>cd apps/mobile && xcrun simctl list devices | grep -q "iPhone 17 Pro" && bash .maestro/scripts/uat.sh smoke && maestro test .maestro/28-cooking-mode-ui.yaml</automated>
  </verify>
  <done>
    Maestro flow 28 passes 20 steps, generates 7 screenshots at apps/mobile/28-01..28-07-*.png. Existing smoke.yaml unaffected.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human checkpoint — Settings dark-mode visual + Maestro flow 28 review</name>
  <files>
    apps/mobile/src/app/(tabs)/settings.tsx,
    apps/mobile/.maestro/28-cooking-mode-ui.yaml
  </files>
  <action>
    Human-in-the-loop verification checkpoint. Claude has already landed Settings Cooking section, Maestro flow 28, and deleted Phase 9 dead files. This task confirms visual + functional integrity before Phase 16 closeout.

    What was built:
    - Settings Cooking section with "Dark cooking mode" toggle row + subtitle
    - Maestro flow 28-cooking-mode-ui.yaml passing on iPhone 17 Pro simulator
    - StepDisplay.tsx + VoiceStatusBadge.tsx deleted

    How to verify:
    1. Boot simulator: `cd apps/mobile && bash .maestro/scripts/uat.sh boot` (or `xcrun simctl boot "iPhone 17 Pro" && open -a Simulator`).
    2. Start dev environment:
       - Terminal 1: `cd /Users/patrickrichards/DinnerTime && set -a && source .env && set +a && cd packages/server && pnpm dev`
       - Terminal 2: `cd apps/mobile && npx expo start --dev-client --lan --clear` (clear is important after installing expo-haptics).
    3. Install app on sim: `xcrun simctl install booted apps/mobile/ios/build/Build/Products/Debug-iphonesimulator/DinnerTime.app`.
    4. Open app. Navigate to Settings.
    5. Scroll to the Cooking section. Verify:
       - Section header "COOKING" in uppercase label typography.
       - Row title "Dark cooking mode".
       - Row subtitle "Darker background while cooking. Matches Spotify's Now Playing feel."
       - Switch toggles on tap and changes state visually.
    6. Toggle dark mode ON. Navigate to a recipe. Tap Cook. Verify the cooking screen uses dark palette (dark background, light text, brand accent on current step + mic + timer).
    7. Exit cooking (tap X, confirm "End cooking session"). Verify action sheet copy matches UI-SPEC.
    8. Run Maestro: `cd apps/mobile && maestro test .maestro/28-cooking-mode-ui.yaml`. All steps green. Screenshots at `apps/mobile/28-01..28-07-*.png` present.
    9. Force-quit app and relaunch. Re-enter cooking. Verify dark mode is still on (persist).
    10. Toggle dark mode OFF in Settings. Re-enter cooking. Verify light palette.

    Resume signal: Type "approved" or describe issues (e.g., "dark mode flashes light for ~200ms on cook-screen mount" — would be Pitfall 5 symptoms).
  </action>
  <verify>
    Human visual + functional verification per the steps above. No automated command — this is a checkpoint.
  </verify>
  <done>
    User types "approved" after completing the 10 verification steps, OR describes specific issues to address in a follow-up patch before Phase 16 closeout.
  </done>
</task>

</tasks>

<verification>
- Settings screen includes "Cooking" section with dark-mode toggle
- Maestro flow 28 green on iPhone 17 Pro simulator
- StepDisplay.tsx and VoiceStatusBadge.tsx absent from filesystem
- Full mobile test suite green: `cd apps/mobile && pnpm test --run`
- TypeScript clean: `cd apps/mobile && pnpm tsc --noEmit`
- Human checkpoint approves dark-mode toggle persistence + visual
</verification>

<success_criteria>
- Dark-mode toggle present in Settings, persists across app restarts
- Maestro flow 28 covers non-voice UAT for cooking (landing, ingredient check, step nav, exit confirm, dark-mode)
- Dead components removed (2 files)
- Human approval received
</success_criteria>

<output>
After completion, create `.planning/phases/16-cooking-mode-ux-enhancements-voice-interaction-and-model-ui-polish-information-display/16-07-SUMMARY.md` with:
- Settings Cooking section render confirmation
- Maestro flow 28 step count + screenshot count
- Deleted files (absolute paths)
- Human checkpoint outcome (approved/issues)
</output>
