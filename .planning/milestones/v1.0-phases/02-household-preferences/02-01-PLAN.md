---
phase: 02-household-preferences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00002_household_preferences.sql
  - apps/mobile/src/types/preferences.ts
  - apps/mobile/src/data/ingredients.ts
  - apps/mobile/src/data/dietary.ts
  - apps/mobile/src/stores/authStore.ts
autonomous: true
requirements: [FOUN-03, FOUN-04, FOUN-05]

must_haves:
  truths:
    - "Database schema supports per-member dietary restrictions and allergies"
    - "Database schema supports per-member disliked ingredients"
    - "Skill level column exists on profiles table"
    - "TypeScript types match database schema exactly"
  artifacts:
    - path: "supabase/migrations/00002_household_preferences.sql"
      provides: "household_members table, skill_level column on profiles"
      contains: "CREATE TABLE household_members"
    - path: "apps/mobile/src/types/preferences.ts"
      provides: "HouseholdMember, SkillLevel, AgeRange types"
      exports: ["HouseholdMember", "SkillLevel", "AgeRange", "DietaryOption", "CuisineOption"]
    - path: "apps/mobile/src/data/ingredients.ts"
      provides: "Curated ingredient list for dislike search"
      exports: ["INGREDIENTS"]
    - path: "apps/mobile/src/data/dietary.ts"
      provides: "Dietary and cuisine option constants"
      exports: ["DIETARY_OPTIONS", "CUISINE_OPTIONS", "SKILL_LEVELS", "AGE_RANGES"]
  key_links:
    - from: "apps/mobile/src/types/preferences.ts"
      to: "supabase/migrations/00002_household_preferences.sql"
      via: "type definitions match DB columns"
      pattern: "member_type.*adult.*kid"
---

<objective>
Create the database migration for household members and skill level, define all TypeScript types, and create the data constants (ingredient list, dietary/cuisine options) that the preferences store and settings UI will consume.

Purpose: Establish the data foundation so Plan 02 (store/hooks) and Plan 03 (UI) have stable contracts to build against.
Output: Migration SQL, TypeScript types, ingredient list, dietary/cuisine constants.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-preferences/02-CONTEXT.md
@.planning/phases/02-household-preferences/02-RESEARCH.md

<interfaces>
<!-- Existing schema from supabase/migrations/00001_profiles.sql -->
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  display_name TEXT,
  household_size INTEGER DEFAULT 2,
  dietary_preferences JSONB DEFAULT '[]'::jsonb,
  cuisine_preferences JSONB DEFAULT '[]'::jsonb,
  disliked_ingredients JSONB DEFAULT '[]'::jsonb,
  onboarding_complete BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Existing function reusable for new table trigger:
CREATE OR REPLACE FUNCTION public.update_updated_at() ...

<!-- Existing Profile type from apps/mobile/src/stores/authStore.ts -->
export interface Profile {
  id: string;
  display_name: string | null;
  household_size: number;
  dietary_preferences: string[];
  cuisine_preferences: string[];
  disliked_ingredients: string[];
  onboarding_complete: boolean;
  created_at: string;
  updated_at: string;
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and TypeScript type contracts</name>
  <files>supabase/migrations/00002_household_preferences.sql, apps/mobile/src/types/preferences.ts, apps/mobile/src/stores/authStore.ts</files>
  <action>
1. Create `supabase/migrations/00002_household_preferences.sql` with:
   - ALTER TABLE profiles ADD COLUMN skill_level TEXT DEFAULT 'beginner' CHECK (skill_level IN ('beginner', 'intermediate', 'confident', 'adventurous'))
   - CREATE TABLE household_members with columns: id (UUID PK, gen_random_uuid), profile_id (UUID NOT NULL FK to profiles(id) ON DELETE CASCADE), name (TEXT NOT NULL), member_type (TEXT NOT NULL, CHECK IN ('adult', 'kid')), age_range (TEXT, CHECK NULL or IN ('toddler', 'young_kid', 'older_kid', 'teen')), dietary_restrictions (JSONB DEFAULT '[]'), dietary_allergies (JSONB DEFAULT '[]'), disliked_ingredients (JSONB DEFAULT '[]'), created_at (TIMESTAMPTZ DEFAULT NOW()), updated_at (TIMESTAMPTZ DEFAULT NOW())
   - Enable RLS on household_members
   - Create 4 RLS policies (SELECT, INSERT, UPDATE, DELETE) all scoped to profile_id = auth.uid()
   - Create updated_at trigger reusing public.update_updated_at()

2. Create `apps/mobile/src/types/preferences.ts` with:
   - export type SkillLevel = 'beginner' | 'intermediate' | 'confident' | 'adventurous'
   - export type AgeRange = 'toddler' | 'young_kid' | 'older_kid' | 'teen'
   - export type MemberType = 'adult' | 'kid'
   - export type DietaryOption = (union of the 7 dietary strings)
   - export type CuisineOption = (union of the 10 cuisine strings)
   - export interface HouseholdMember { id: string; profile_id: string; name: string; member_type: MemberType; age_range: AgeRange | null; dietary_restrictions: DietaryOption[]; dietary_allergies: DietaryOption[]; disliked_ingredients: string[]; created_at: string; updated_at: string; }

3. Update `apps/mobile/src/stores/authStore.ts`:
   - Import SkillLevel from '../types/preferences'
   - Add skill_level: SkillLevel to the Profile interface

Note: dietary_restrictions = soft preferences (prefer to avoid), dietary_allergies = hard blocks (never suggest). This distinction is per user decision.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && cat supabase/migrations/00002_household_preferences.sql | grep -c "CREATE TABLE\|CREATE POLICY\|ALTER TABLE" && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Migration file creates household_members table with RLS and adds skill_level to profiles. TypeScript types export all interfaces matching the DB schema. Profile interface includes skill_level. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Data constants - ingredient list and dietary/cuisine options</name>
  <files>apps/mobile/src/data/ingredients.ts, apps/mobile/src/data/dietary.ts</files>
  <action>
1. Create `apps/mobile/src/data/dietary.ts`:
   - Import DietaryOption, CuisineOption, SkillLevel, AgeRange types from '../types/preferences'
   - export const DIETARY_OPTIONS: { value: DietaryOption; label: string; isAllergy: boolean }[] -- the 7 options. Mark 'Nut Allergy' with isAllergy: true, rest isAllergy: false (users can toggle any as allergy vs preference in the UI, but this provides a sensible default)
   - export const CUISINE_OPTIONS: { value: CuisineOption; label: string }[] -- the 10 options (Italian, Mexican, Chinese, Japanese, Indian, Thai, Mediterranean, American, Korean, French)
   - export const SKILL_LEVELS: { value: SkillLevel; label: string; description: string }[] -- 4 levels with short descriptions (e.g., Beginner: "Just getting started", Intermediate: "Comfortable with basics", Confident: "Can tackle most recipes", Adventurous: "Love trying new techniques")
   - export const AGE_RANGES: { value: AgeRange; label: string; range: string }[] -- 4 ranges with display labels and age ranges (Toddler: "1-3", Young Kid: "4-7", Older Kid: "8-12", Teen: "13+")

2. Create `apps/mobile/src/data/ingredients.ts`:
   - export const INGREDIENTS: string[] -- curated list of 200-300 common food ingredients organized by category in comments (Proteins, Seafood, Vegetables, Fruits, Dairy, Grains/Starches, Herbs/Spices, Condiments/Sauces, Nuts/Seeds, Other)
   - Focus on ingredients people commonly dislike: cilantro, olives, anchovies, liver, blue cheese, beets, brussels sprouts, oysters, tofu, eggplant, okra, etc. alongside common items
   - Alphabetically sorted within each category for maintainability
   - export function searchIngredients(query: string): string[] -- filters INGREDIENTS by case-insensitive substring match, returns top 10 results
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -10 && node -e "const m = require('./apps/mobile/src/data/ingredients.ts'); console.log('count:', m.INGREDIENTS?.length)" 2>/dev/null || echo "TS check only"</automated>
  </verify>
  <done>INGREDIENTS array has 200+ entries covering common food items. DIETARY_OPTIONS, CUISINE_OPTIONS, SKILL_LEVELS, AGE_RANGES all exported with proper types. searchIngredients function filters and returns top 10 matches. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- TypeScript compilation: `cd apps/mobile && npx tsc --noEmit` passes
- Migration file is syntactically valid SQL
- All type exports are importable from their respective modules
- INGREDIENTS array has >= 200 items
- All constants match the locked decisions from CONTEXT.md (7 dietary, 10 cuisine, 4 skill levels, 4 age ranges)
</verification>

<success_criteria>
- household_members table schema supports per-member dietary restrictions, allergies, and dislikes
- profiles table gains skill_level column
- TypeScript types provide strong contracts for store and UI layers
- Ingredient list is comprehensive enough for real-world dislike selection
- All data constants are typed and ready for consumption by preferences store and settings UI
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-preferences/02-01-SUMMARY.md`
</output>
