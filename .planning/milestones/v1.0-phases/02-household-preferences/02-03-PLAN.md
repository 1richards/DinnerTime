---
phase: 02-household-preferences
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/mobile/src/components/ui/ChipToggle.tsx
  - apps/mobile/src/components/ui/Toast.tsx
  - apps/mobile/src/components/settings/FamilyMembersSection.tsx
  - apps/mobile/src/components/settings/MemberCard.tsx
  - apps/mobile/src/components/settings/MemberFormModal.tsx
  - apps/mobile/src/components/settings/DietarySection.tsx
  - apps/mobile/src/components/settings/CuisineSection.tsx
  - apps/mobile/src/components/settings/DislikesSection.tsx
  - apps/mobile/src/components/settings/SkillLevelSection.tsx
  - apps/mobile/src/components/settings/IngredientSearch.tsx
  - apps/mobile/src/app/settings.tsx
  - apps/mobile/src/app/(tabs)/index.tsx
  - apps/mobile/src/app/(tabs)/_layout.tsx
  - apps/mobile/src/app/_layout.tsx
autonomous: false
requirements: [FOUN-03, FOUN-04, FOUN-05]

must_haves:
  truths:
    - "User can navigate to settings from Home tab header gear icon"
    - "User can add a family member with name, adult/kid type, and age range for kids"
    - "User can set per-member dietary restrictions and allergies separately"
    - "User can set per-member disliked ingredients via search/autocomplete"
    - "User can toggle cuisine preferences with chip toggles"
    - "User can set cooking skill level"
    - "All changes auto-save with toast confirmation"
    - "Settings screen shows grouped sections with warm orange/amber styling"
  artifacts:
    - path: "apps/mobile/src/app/settings.tsx"
      provides: "Settings screen with all preference sections"
      min_lines: 50
    - path: "apps/mobile/src/components/ui/ChipToggle.tsx"
      provides: "Reusable chip toggle component"
      exports: ["ChipToggle"]
    - path: "apps/mobile/src/components/settings/FamilyMembersSection.tsx"
      provides: "Family member list with add/edit/delete"
      exports: ["FamilyMembersSection"]
    - path: "apps/mobile/src/components/settings/IngredientSearch.tsx"
      provides: "Ingredient search autocomplete"
      exports: ["IngredientSearch"]
  key_links:
    - from: "apps/mobile/src/app/(tabs)/index.tsx"
      to: "apps/mobile/src/app/settings.tsx"
      via: "router.push('/settings') from gear icon"
      pattern: "router\\.push.*settings"
    - from: "apps/mobile/src/app/settings.tsx"
      to: "apps/mobile/src/stores/preferencesStore.ts"
      via: "usePreferencesStore for reading state"
      pattern: "usePreferencesStore"
    - from: "apps/mobile/src/app/settings.tsx"
      to: "apps/mobile/src/hooks/usePreferences.ts"
      via: "mutation hooks for auto-save"
      pattern: "useAddMember|useUpdateMember|useDeleteMember|useUpdateProfile"
    - from: "apps/mobile/src/components/settings/DislikesSection.tsx"
      to: "apps/mobile/src/hooks/useIngredientSearch.ts"
      via: "useIngredientSearch hook"
      pattern: "useIngredientSearch"
---

<objective>
Build the complete settings UI: reusable ChipToggle and Toast components, all settings sections (Family Members, Dietary, Cuisine, Dislikes, Skill Level), ingredient search with autocomplete, settings route, and gear icon navigation from Home tab.

Purpose: Give users the full settings experience to configure their household preferences. This is the user-facing layer that makes FOUN-03, FOUN-04, FOUN-05 tangible.
Output: Working settings screen accessible from Home tab, with auto-save on all changes.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-household-preferences/02-CONTEXT.md
@.planning/phases/02-household-preferences/02-RESEARCH.md
@.planning/phases/02-household-preferences/02-01-SUMMARY.md
@.planning/phases/02-household-preferences/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 - types/preferences.ts -->
export type SkillLevel = 'beginner' | 'intermediate' | 'confident' | 'adventurous';
export type AgeRange = 'toddler' | 'young_kid' | 'older_kid' | 'teen';
export type MemberType = 'adult' | 'kid';
export type DietaryOption = 'Vegetarian' | 'Vegan' | 'Gluten-Free' | 'Dairy-Free' | 'Nut Allergy' | 'Keto' | 'Paleo';
export type CuisineOption = 'Italian' | 'Mexican' | 'Chinese' | 'Japanese' | 'Indian' | 'Thai' | 'Mediterranean' | 'American' | 'Korean' | 'French';

export interface HouseholdMember {
  id: string; profile_id: string; name: string; member_type: MemberType;
  age_range: AgeRange | null; dietary_restrictions: DietaryOption[];
  dietary_allergies: DietaryOption[]; disliked_ingredients: string[];
  created_at: string; updated_at: string;
}

<!-- From Plan 01 - data/dietary.ts -->
export const DIETARY_OPTIONS: { value: DietaryOption; label: string; isAllergy: boolean }[];
export const CUISINE_OPTIONS: { value: CuisineOption; label: string }[];
export const SKILL_LEVELS: { value: SkillLevel; label: string; description: string }[];
export const AGE_RANGES: { value: AgeRange; label: string; range: string }[];

<!-- From Plan 02 - stores/preferencesStore.ts -->
export const usePreferencesStore: UseBoundStore<PreferencesState>;
// State: members, cuisinePreferences, skillLevel, isLoading
// Methods: loadPreferences, addMember, updateMember, deleteMember, updateCuisinePreferences, updateSkillLevel

<!-- From Plan 02 - hooks/usePreferences.ts -->
export function useAddMember(): UseMutationResult;
export function useUpdateMember(): UseMutationResult;
export function useDeleteMember(): UseMutationResult;
export function useUpdateProfile(): UseMutationResult;

<!-- From Plan 02 - hooks/useIngredientSearch.ts -->
export function useIngredientSearch(excludedItems: string[]): { query: string; setQuery: (q: string) => void; results: string[] };

<!-- Existing UI components -->
// Button: { title, variant?: 'primary'|'outline'|'ghost', onPress, loading?, disabled?, className? }
// Input: { label?, placeholder?, value, onChangeText, error?, ...TextInputProps }

<!-- Existing navigation -->
// app/(tabs)/_layout.tsx: Tabs with screenOptions
// app/(tabs)/index.tsx: Home screen with SafeAreaView
// app/_layout.tsx: Root Stack with Stack.Protected guards
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable UI components and settings section components</name>
  <files>apps/mobile/src/components/ui/ChipToggle.tsx, apps/mobile/src/components/ui/Toast.tsx, apps/mobile/src/components/settings/FamilyMembersSection.tsx, apps/mobile/src/components/settings/MemberCard.tsx, apps/mobile/src/components/settings/MemberFormModal.tsx, apps/mobile/src/components/settings/DietarySection.tsx, apps/mobile/src/components/settings/CuisineSection.tsx, apps/mobile/src/components/settings/DislikesSection.tsx, apps/mobile/src/components/settings/SkillLevelSection.tsx, apps/mobile/src/components/settings/IngredientSearch.tsx</files>
  <action>
1. Create `apps/mobile/src/components/ui/ChipToggle.tsx`:
   - Props: { label: string; selected: boolean; onToggle: () => void; variant?: 'default' | 'removable' }
   - Pressable with rounded-full, selected state uses bg-orange-500 text-white, unselected uses bg-warmGray-100 border border-warmGray-200 text-warmGray-700
   - 'removable' variant shows "x" suffix when selected
   - Extract the exact pattern from onboarding/index.tsx chip toggles

2. Create `apps/mobile/src/components/ui/Toast.tsx`:
   - Simple component: { message: string; visible: boolean; type?: 'success' | 'error' }
   - Positioned at top of screen, absolute. Uses Animated.View with fade in/out (2 second duration).
   - Success: bg-green-500 text-white. Error: bg-red-500 text-white.
   - Export a useToast() hook that returns { show: (message, type?) => void, ToastComponent }

3. Create `apps/mobile/src/components/settings/MemberCard.tsx`:
   - Shows member at a glance: name, type badge (Adult / Toddler / Young Kid / etc.), dietary icon indicators
   - Pressable card that calls onPress for editing. Shows delete icon (trash) on right side.
   - Warm styling: bg-white rounded-xl shadow-sm, border border-warmGray-100
   - Badge colors: Adult = warmGray-200, Toddler/Young Kid = orange-100, Older Kid/Teen = amber-100

4. Create `apps/mobile/src/components/settings/MemberFormModal.tsx`:
   - Modal (React Native Modal) for adding/editing a family member
   - Fields: name (Input), member_type (Adult/Kid toggle), age_range (only shown when Kid selected, using AGE_RANGES chips)
   - Per-member dietary restrictions: show DIETARY_OPTIONS as ChipToggles for preferences (soft)
   - Per-member allergies: separate section with same DIETARY_OPTIONS as ChipToggles for allergies (hard blocks). Use a different chip color (red-100 when selected) to visually distinguish from soft preferences.
   - Per-member disliked ingredients: embed IngredientSearch component, show selected as removable chips
   - Save and Cancel buttons at bottom
   - When editing, pre-populate all fields from existing member data
   - On save, call useAddMember or useUpdateMember as appropriate

5. Create `apps/mobile/src/components/settings/FamilyMembersSection.tsx`:
   - Section header "Family Members" with warmGray-900 font-bold text-lg
   - Lists MemberCard for each member in usePreferencesStore.members
   - "Add Member" button (outline variant) at bottom
   - Pressing a MemberCard opens MemberFormModal in edit mode
   - Delete confirmation uses Alert.alert before calling useDeleteMember

6. Create `apps/mobile/src/components/settings/DietarySection.tsx`:
   - Note: This section is informational, showing a summary of all members' combined dietary needs. Individual dietary settings are in MemberFormModal.
   - Section header "Dietary & Allergies"
   - Shows aggregated view: "Allergies (hard blocks):" lists union of all members' dietary_allergies. "Preferences (soft):" lists union of all members' dietary_restrictions.
   - If no members added yet, show prompt "Add family members to set dietary needs"

7. Create `apps/mobile/src/components/settings/CuisineSection.tsx`:
   - Section header "Cuisine Preferences"
   - ChipToggle for each CUISINE_OPTIONS item
   - Selected state reads from usePreferencesStore.cuisinePreferences
   - On toggle: call useUpdateProfile to save cuisine_preferences array to profiles table
   - Auto-saves immediately on toggle (no debounce needed for chip toggles)

8. Create `apps/mobile/src/components/settings/DislikesSection.tsx`:
   - Note: Like dietary, this shows an aggregated view. Individual dislikes are set per-member in MemberFormModal.
   - Section header "Disliked Ingredients"
   - Shows union of all members' disliked_ingredients as removable chips (read-only summary)
   - Prompt to edit via member profiles if user wants to change

9. Create `apps/mobile/src/components/settings/SkillLevelSection.tsx`:
   - Section header "Cooking Skill"
   - Radio-style selection: 4 options from SKILL_LEVELS, each showing label and description
   - Selected option has orange-500 left border and bg-orange-50
   - On select: call useUpdateProfile to save skill_level to profiles table

10. Create `apps/mobile/src/components/settings/IngredientSearch.tsx`:
    - Props: { selectedItems: string[]; onAdd: (item: string) => void; onRemove: (item: string) => void }
    - Uses useIngredientSearch(selectedItems) hook
    - TextInput for search query with magnifying glass icon
    - Dropdown below input showing filtered results (top 10)
    - Tapping a result calls onAdd and clears query
    - Selected items shown as removable ChipToggle (variant='removable') below the search
    - If no results found and query has 2+ chars, show "No matches. Press enter to add custom item." and allow free-text entry
    - Use KeyboardAvoidingView awareness -- position search results above keyboard if needed
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <done>All 10 components created with proper types, NativeWind styling in warm orange/amber palette, and correct hook integrations. ChipToggle extracted from onboarding pattern. Toast provides auto-save feedback. MemberFormModal handles full member CRUD with per-member dietary/allergy/dislike editing. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Settings route, gear icon navigation, and preferences loading</name>
  <files>apps/mobile/src/app/settings.tsx, apps/mobile/src/app/(tabs)/index.tsx, apps/mobile/src/app/(tabs)/_layout.tsx, apps/mobile/src/app/_layout.tsx</files>
  <action>
1. Create `apps/mobile/src/app/settings.tsx`:
   - Root-level route (outside tabs) so it pushes on top of tab navigator
   - Uses ScrollView with sections in order: FamilyMembersSection, DietarySection, CuisineSection, DislikesSection, SkillLevelSection
   - Each section separated by a horizontal divider (border-b border-warmGray-100 my-4)
   - Section headers use consistent styling: text-lg font-bold text-warmGray-900 mb-3
   - SafeAreaView wrapper, bg-warmWhite background
   - Loads preferences on mount: useEffect calling preferencesStore.loadPreferences(profile.id)
   - Shows ActivityIndicator while loading
   - Renders Toast component for auto-save feedback
   - keyboardShouldPersistTaps="handled" on ScrollView

2. Update `apps/mobile/src/app/(tabs)/_layout.tsx`:
   - Add headerRight to the "index" (Home) tab screen options:
     headerRight: () => Pressable with Ionicons "settings-outline" icon (size 24, color #1F2937), onPress => router.push('/settings'), className="mr-4"
   - Import router from expo-router and Pressable from react-native

3. Update `apps/mobile/src/app/_layout.tsx`:
   - Add a Stack.Screen entry for "settings" inside the isLoggedIn && isOnboarded guard:
     <Stack.Screen name="settings" options={{ headerShown: true, title: 'Settings', headerStyle: { backgroundColor: '#FFFBF5' }, headerTintColor: '#1F2937', headerShadowVisible: false }} />
   - This ensures the settings screen has a back button and header when pushed

4. Update `apps/mobile/src/app/(tabs)/index.tsx`:
   - No changes needed here if the gear icon is added via _layout.tsx headerRight. But verify the Home screen still renders correctly.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | tail -10 && cd apps/mobile && pnpm test 2>&1 | tail -10</automated>
  </verify>
  <done>Settings screen is accessible via gear icon on Home tab header. Settings route pushes on top of tabs with back navigation. All 5 sections render in a scrollable layout. Preferences load on mount from Supabase. Auto-save works with toast confirmation. TypeScript compiles and all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete settings screen</name>
  <files>apps/mobile/src/app/settings.tsx</files>
  <action>
    Human verifies the complete household preferences settings screen.

    Steps to verify:
    1. Start the app: cd apps/mobile and npx expo start
    2. Log in and complete onboarding if needed
    3. On the Home tab, tap the gear icon in the top-right header
    4. Settings screen should open with sections: Family Members, Dietary and Allergies, Cuisine Preferences, Disliked Ingredients, Cooking Skill
    5. Tap "Add Member" -- modal should open with name, adult/kid toggle, age range (for kids), dietary preferences, allergies (separate from preferences), and disliked ingredients search
    6. Add a family member (e.g., "Alex", Adult, Vegetarian preference, Nut Allergy as allergy)
    7. Verify the member card appears showing name, type badge, and dietary indicators
    8. Toggle some cuisine preferences -- they should auto-save with a brief toast
    9. Select a cooking skill level -- should auto-save
    10. Navigate back to Home tab and return to Settings -- all data should persist
    11. Edit the family member -- verify pre-populated fields
    12. Delete the family member -- confirm dialog should appear

    Resume: Type "approved" or describe issues to fix
  </action>
  <verify>Human confirms all 12 verification steps pass</verify>
  <done>User approves the settings screen UI and functionality</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd apps/mobile && npx tsc --noEmit`
- All tests pass: `cd apps/mobile && pnpm test`
- Settings screen renders without crashes
- All sections visible and interactive
- Auto-save works on cuisine toggles, skill level selection, and member CRUD
- Per-member dietary vs allergy distinction is visually clear
- Ingredient search filters correctly and allows free-text fallback
- Navigation: Home gear icon -> Settings -> Back works correctly
</verification>

<success_criteria>
- User can navigate to settings from Home tab header gear icon
- User can add/edit/delete family members with full profile (name, type, age range, dietary, allergies, dislikes)
- User can toggle cuisine preferences and cooking skill level
- Allergies (hard blocks) are visually distinct from dietary preferences (soft)
- Per-member disliked ingredients use search/autocomplete from curated list with removable chips
- All changes auto-save with toast confirmation
- Warm orange/amber visual style consistent with onboarding
- Data persists across navigation and app restarts
</success_criteria>

<output>
After completion, create `.planning/phases/02-household-preferences/02-03-SUMMARY.md`
</output>
