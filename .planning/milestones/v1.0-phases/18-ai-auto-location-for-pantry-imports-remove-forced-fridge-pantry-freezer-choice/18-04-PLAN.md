---
phase: 18-ai-auto-location-for-pantry-imports-remove-forced-fridge-pantry-freezer-choice
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - apps/mobile/src/app/scan/index.tsx
  - apps/mobile/src/app/scan/receipt.tsx
  - apps/mobile/src/app/scan/instacart.tsx
  - apps/mobile/src/components/pantry/LocationPicker.tsx
  - apps/mobile/.maestro/16-pantry-scan-stub.yaml
  - apps/mobile/.maestro/19-receipt-scan-stub.yaml
  - apps/mobile/.maestro/07-pantry-add.yaml
autonomous: false
requirements:
  - "Pantry UX improvement (post-v1)"
must_haves:
  truths:
    - "scan/index.tsx does not import or render LocationPicker"
    - "scan/receipt.tsx does not import or render LocationPicker"
    - "scan/instacart.tsx no longer hardcodes sourceLocation: 'pantry' in nav param"
    - "No file under apps/mobile/src/app/scan/ or apps/mobile/src/app/\\(tabs\\)/ imports LocationPicker"
    - "LocationPicker.tsx file is deleted from apps/mobile/src/components/pantry/"
    - "Maestro flows 07, 16, 19 pass on iOS Simulator after removal"
    - "Camera scan EmptyState copy is location-agnostic ('Take photos of your fridge, pantry, or freezer...')"
  artifacts:
    - path: "apps/mobile/src/app/scan/index.tsx"
      provides: "Camera scan screen with LocationPicker removed; cold-start → camera → review"
      contains: "EmptyState"
    - path: "apps/mobile/src/app/scan/receipt.tsx"
      provides: "Receipt scan entry with LocationPicker removed"
    - path: "apps/mobile/src/app/scan/instacart.tsx"
      provides: "Instacart import entry with no sourceLocation nav param"
  key_links:
    - from: "apps/mobile/src/app/scan/index.tsx"
      to: "apps/mobile/src/stores/pantryStore.ts"
      via: "startBatchScan(photos.map(p=>p.base64)) — no second arg"
      pattern: "startBatchScan\\([^,]+\\)"
    - from: "apps/mobile/src/app/scan/review.tsx"
      to: "apps/mobile/src/stores/pantryStore.ts"
      via: "confirmScan(profile.id) — no second arg"
      pattern: "confirmScan\\([^,]+\\)"
---

<objective>
Atomic removal of LocationPicker from all three scan entry points, delete the component file, strip `sourceLocation` nav params from the router calls, update EmptyState copy to be location-agnostic, and rebase the at-risk Maestro flows. Close with an iOS Simulator UAT checkpoint because this is the first surface where the user experiences the "no forced choice" UX.

Purpose: The prior plans built the plumbing. This plan removes the last piece of UX friction — the LocationPicker gating step. After this wave, tapping the camera FAB goes straight to camera; receipt scan goes straight to receipt picker; Instacart import goes straight to screenshot picker. The chip on the review screen is the single point of location choice, per-item.

Output: Three scan entry screens stripped of LocationPicker, a deleted component file, three rebased Maestro flows (two comment-only, one smoke verification), and a human UAT confirming flows 07/16/19 still pass on iOS Simulator.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-ai-auto-location-for-pantry-imports-remove-forced-fridge-pantry-freezer-choice/18-CONTEXT.md
@.planning/phases/18-ai-auto-location-for-pantry-imports-remove-forced-fridge-pantry-freezer-choice/18-RESEARCH.md
@.planning/phases/18-ai-auto-location-for-pantry-imports-remove-forced-fridge-pantry-freezer-choice/18-03-SUMMARY.md

@apps/mobile/src/app/scan/index.tsx
@apps/mobile/src/app/scan/receipt.tsx
@apps/mobile/src/app/scan/instacart.tsx
@apps/mobile/src/app/scan/review.tsx
@apps/mobile/src/components/pantry/LocationPicker.tsx
@apps/mobile/.maestro/16-pantry-scan-stub.yaml
@apps/mobile/.maestro/19-receipt-scan-stub.yaml
@apps/mobile/.maestro/07-pantry-add.yaml
@apps/mobile/.maestro/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LocationPicker from all scan entries + delete component</name>
  <files>
    apps/mobile/src/app/scan/index.tsx,
    apps/mobile/src/app/scan/receipt.tsx,
    apps/mobile/src/app/scan/instacart.tsx,
    apps/mobile/src/components/pantry/LocationPicker.tsx
  </files>
  <action>
Per D-Picker-Removal (CONTEXT.md) + RESEARCH Q9 + Q11.

1. `apps/mobile/src/app/scan/index.tsx` (single-photo / batch camera scan):
   - Remove the `import { LocationPicker } from '...'` line
   - Remove `selectedLocation` / `sourceLocation` state (`useState<SourceLocation>('pantry')`, etc.)
   - Remove the `<LocationPicker ... />` JSX block
   - Update the EmptyState subtitle copy to be location-agnostic: `"Take photos of your fridge, pantry, or freezer — we'll sort each item automatically."` (per RESEARCH Q11)
   - Update `handleSubmitBatch` (or equivalent) to call `startBatchScan(photos.map(p=>p.base64))` with one argument
   - Remove `sourceLocation` from any `router.push({...})` call that navigated to `/scan/review`
   - If a single-photo `startScan(base64)` call site exists, same — drop second arg

2. `apps/mobile/src/app/scan/receipt.tsx`:
   - Remove the `import { LocationPicker } from '...'` line
   - Remove state for selected location
   - Remove `<LocationPicker ... />` JSX
   - Update `startReceiptScan(base64)` call to drop second arg
   - Remove `sourceLocation` from any `router.push({...})` nav to /scan/review

3. `apps/mobile/src/app/scan/instacart.tsx`:
   - Remove the hardcoded `sourceLocation: 'pantry'` from any `router.push({ params: {...} })` call. File does NOT import LocationPicker (verified in RESEARCH Q9) — just drop the stale param.

4. DELETE `apps/mobile/src/components/pantry/LocationPicker.tsx` (per RESEARCH Q9 "Recommendation: DELETE the component"). Rationale: Phase 21 will build a fresh rules UI against its own schema; leaving dead code invites reintroduction.

5. Grep verify removal:
```bash
! grep -rn "LocationPicker" apps/mobile/src/
! grep -rn "from.*pantry/LocationPicker" apps/mobile/
! grep -rn "sourceLocation:\\s*['\"]pantry['\"]" apps/mobile/src/app/scan/instacart.tsx
```

6. Update any stale JSDoc or inline comments mentioning "user picks a location before scanning." The picker is gone; the chip replaces it.

Implements D-Picker-Removal from CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && npx tsc --noEmit -p . && pnpm test --run</automated>
  </verify>
  <done>
    - Zero `LocationPicker` imports anywhere in apps/mobile/src/
    - LocationPicker.tsx file deleted
    - scan/index.tsx EmptyState uses location-agnostic copy
    - scan/instacart.tsx no longer puts sourceLocation in nav params
    - tsc clean; full mobile test suite green (Wave 3's store tests already cover signature changes)
    - Grep gate passes: no LocationPicker references remain outside `.maestro/` / planning docs
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebase Maestro flows + run smoke suite</name>
  <files>
    apps/mobile/.maestro/16-pantry-scan-stub.yaml,
    apps/mobile/.maestro/19-receipt-scan-stub.yaml,
    apps/mobile/.maestro/07-pantry-add.yaml
  </files>
  <action>
Per RESEARCH Q14 "No flow rebase needed for functional testing. Comment-only updates on flows 16 + 19 to note the UX change."

1. `apps/mobile/.maestro/16-pantry-scan-stub.yaml`:
   - Update the header comment block at the top of the file to note: "# Phase 18: LocationPicker removed. Flow begins directly at camera (no gating step)."
   - Verify NO steps in the flow tap/assert against a LocationPicker element (RESEARCH Q14 confirms they don't). If any do, remove them.

2. `apps/mobile/.maestro/19-receipt-scan-stub.yaml`:
   - Add a header comment: "# Phase 18: LocationPicker removed from receipt scan. Flow begins directly at receipt picker."
   - RESEARCH Q14 confirms this flow "never asserts on LocationPicker, only on `.*Scan Receipt.*` / `.*Import from Instacart.*` / `.*Take Photo.*` / `.*Choose Screenshot.*`. All still present." No functional change needed — comment-only.

3. `apps/mobile/.maestro/07-pantry-add.yaml`:
   - Tests pantry-tab filter (Fridge/Pantry/Freezer pills at top of pantry list). Those still exist in the app — Phase 18 does not change the pantry tab's filter tabs, only the scan entry's LocationPicker.
   - No changes expected. Add a header comment confirming that "# Phase 18 verified: pantry tab filter tabs unchanged; only scan-entry LocationPicker was removed."

4. Check `apps/mobile/.maestro/README.md` — if it has a flow inventory noting LocationPicker anywhere, update that mention.

5. Run the targeted at-risk flows + smoke suite on iOS Simulator:
```bash
cd apps/mobile && xcrun simctl boot "iPhone 17 Pro" 2>/dev/null; open -a Simulator
cd apps/mobile && maestro test .maestro/07-pantry-add.yaml
cd apps/mobile && maestro test .maestro/16-pantry-scan-stub.yaml
cd apps/mobile && maestro test .maestro/19-receipt-scan-stub.yaml
cd apps/mobile && maestro test .maestro/smoke.yaml
```

If Metro is not running, see `CLAUDE.md` Dev Environment Startup. Use `--lan` mode for simulator (NOT `--tunnel`). If tests were recently run under a different NativeWind token or `.env`, clear Metro cache (`rm -rf .expo && npx expo start --dev-client --lan --clear`).

If any flow fails, the failure IS the gap — capture the screenshot, fix the selector, rerun. Do NOT mask a real regression with comment edits.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile && maestro test .maestro/07-pantry-add.yaml && maestro test .maestro/16-pantry-scan-stub.yaml && maestro test .maestro/19-receipt-scan-stub.yaml && maestro test .maestro/smoke.yaml</automated>
  </verify>
  <done>
    - Flows 07, 16, 19 all green on iOS Simulator
    - smoke.yaml green
    - Comments in flows 16 + 19 + 07 reflect Phase 18 UX change
    - No flow asserts against a LocationPicker selector
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: iOS Simulator UAT — per-item location UX end-to-end</name>
  <files>N/A — human verification, no file edits</files>
  <action>See <how-to-verify> below. Human runs through 7 behaviors in the iOS Simulator and types "approved" or reports issues.</action>
  <verify>Human confirmation in the chat after completing steps 1–7 below.</verify>
  <done>User types "approved" confirming all 7 behaviors verified; OR reports specific failures for follow-up.</done>
  <what-built>
Phase 18 complete stack: server classifies per-item source_location across all four scan flows; reconcileItems dual-writes item_attributes; /api/v1/pantry/override-events logs user corrections; mobile review screen shows a LocationChip per item with a 3-option bottom sheet on tap; LocationPicker is fully removed from scan entry screens. Camera FAB → camera → review (no gating step). Receipt scan → screenshot picker → review. Instacart import → screenshot picker → review.
  </what-built>
  <how-to-verify>
Start dev stack (see CLAUDE.md §Dev Environment Startup):
```
cd /Users/patrickrichards/DinnerTime && set -a && source .env && set +a && cd packages/server && pnpm dev
# In another shell:
cd /Users/patrickrichards/DinnerTime/apps/mobile && npx expo start --dev-client --lan
# Boot simulator if not running:
xcrun simctl boot "iPhone 17 Pro" 2>/dev/null; open -a Simulator
```

Then verify in the running simulator app (approximately 5-8 minutes):

1. **Camera scan — LocationPicker gone.** Tap camera FAB from Pantry tab. Expected: skips any location picker and goes straight to the photo capture / batch-photos screen. EmptyState copy reads "Take photos of your fridge, pantry, or freezer — we'll sort each item automatically." (or equivalent location-agnostic copy). No "Pick a location first" affordance.

2. **Review screen — per-item chips.** Submit a batch of 1-2 photos (or use the stub path if camera is unreachable on simulator). Review screen should render, each item row showing a LocationChip (SF Symbol + label Fridge/Pantry/Freezer). Tap any chip → a bottom sheet slides up showing 3 options (Fridge / Pantry / Freezer), with the current value ring-highlighted. Tap a different option → sheet dismisses, item row's chip updates to the new value.

3. **Override event logging — backend.** Before editing: note server logs should show a POST /scan-batch (or /scan). After tapping Confirm on review with at least one overridden item: server logs should show a fire-and-forget POST /api/v1/pantry/override-events. If you can tail the server logs, confirm a 200 with `{ data: { inserted: N } }` where N >= the number of items you edited.

4. **Receipt flow — fan-out.** Back on Pantry tab, tap the Bulk Import / BulkImportSheet affordance → "Scan Receipt". No LocationPicker appears. Submit a receipt photo (or stub). Review screen shows items with MIXED locations — if the receipt had dairy + chips + ice cream, you should see 2-3 different chip values.

5. **Instacart flow — fan-out.** Same BulkImportSheet → "Import from Instacart". No hardcoded 'pantry' anymore. Review screen chips should reflect per-item AI choices.

6. **Pantry tab unchanged.** Navigate to Pantry tab. Filter pills at top (All / Fridge / Pantry / Freezer) still work. Items are grouped as before.

7. **Existing items unchanged.** Any pantry items from before Phase 18 should still be visible and categorized correctly. Phase 18 is non-destructive for existing data (RESEARCH "Runtime State Inventory").

If any of these fail, capture a screenshot (`apps/mobile/.maestro/screenshots/` or `xcrun simctl io booted screenshot`) and describe the issue. Do NOT type "approved" until all 7 behaviors check out.
  </how-to-verify>
  <resume-signal>Type "approved" once steps 1-7 verified. Describe specifics if any step failed.</resume-signal>
</task>

</tasks>

<verification>
- Full mobile test suite green post-Task 1: `cd apps/mobile && pnpm test --run && npx tsc --noEmit -p .`
- All Maestro at-risk flows + smoke green post-Task 2: flows 07, 16, 19, smoke
- Human UAT approved post-Task 3 with all 7 behaviors confirmed
</verification>

<success_criteria>
- [ ] Zero LocationPicker imports in apps/mobile/src/
- [ ] LocationPicker.tsx file deleted
- [ ] scan/index.tsx, scan/receipt.tsx no longer import/render LocationPicker
- [ ] scan/instacart.tsx no longer hardcodes sourceLocation in nav params
- [ ] EmptyState copy on scan/index.tsx is location-agnostic
- [ ] Flows 07, 16, 19 + smoke.yaml all green on iOS Simulator
- [ ] Maestro flow comments reflect Phase 18 UX change
- [ ] Human UAT checkpoint approved — 7 behaviors all verified
- [ ] `npx tsc --noEmit -p .` clean across mobile
- [ ] `pnpm test --run` green across mobile
</success_criteria>

<output>
After completion, create `.planning/phases/18-ai-auto-location-for-pantry-imports-remove-forced-fridge-pantry-freezer-choice/18-04-SUMMARY.md` documenting the complete Phase 18 shipment: the classifier, the dual-write, the chip UX, and the LocationPicker deletion.
</output>
