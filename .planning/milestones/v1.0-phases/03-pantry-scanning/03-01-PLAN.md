---
phase: 03-pantry-scanning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00003_pantry_items.sql
  - apps/mobile/src/types/pantry.ts
  - packages/server/src/config/env.ts
  - packages/server/src/config/anthropic.ts
  - packages/server/vitest.config.ts
autonomous: true
requirements:
  - PANT-06

must_haves:
  truths:
    - "pantry_items table exists in Supabase with correct schema and RLS policies"
    - "TypeScript types for pantry domain are exported and usable by both mobile and server"
    - "ANTHROPIC_API_KEY is configured in server env and test env"
    - "expo-image-picker and @anthropic-ai/sdk are installed in their respective workspaces"
  artifacts:
    - path: "supabase/migrations/00003_pantry_items.sql"
      provides: "pantry_items table with RLS"
      contains: "CREATE TABLE pantry_items"
    - path: "apps/mobile/src/types/pantry.ts"
      provides: "PantryItem, ScanResult, ReviewItem, FoodCategory, SourceLocation, PantryItemStatus types"
      exports: ["PantryItem", "ScanResult", "ReviewItem", "FoodCategory", "SourceLocation", "PantryItemStatus"]
    - path: "packages/server/src/config/anthropic.ts"
      provides: "Anthropic client singleton"
      exports: ["anthropic"]
    - path: "packages/server/src/config/env.ts"
      provides: "ANTHROPIC_API_KEY env var"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "packages/server/src/config/anthropic.ts"
      to: "packages/server/src/config/env.ts"
      via: "imports ANTHROPIC_API_KEY from env config"
      pattern: "env\\.ANTHROPIC_API_KEY"
---

<objective>
Set up the foundation for pantry scanning: database schema, TypeScript types, Anthropic client config, and install required dependencies.

Purpose: All subsequent plans depend on the pantry_items table, shared types, and configured Anthropic SDK. This must be done first.
Output: Migration file, type definitions, Anthropic client config, installed dependencies.
</objective>

<execution_context>
@/Users/patrickrichards/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pantry-scanning/03-RESEARCH.md

@packages/server/src/config/env.ts
@packages/server/vitest.config.ts
@supabase/migrations/00001_profiles.sql
@supabase/migrations/00002_household_preferences.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Anthropic client</name>
  <files>packages/server/src/config/anthropic.ts, packages/server/src/config/env.ts, packages/server/vitest.config.ts</files>
  <action>
1. Install dependencies:
   - `cd packages/server && pnpm add @anthropic-ai/sdk`
   - `cd apps/mobile && pnpm add expo-image-picker`

2. Add ANTHROPIC_API_KEY to packages/server/src/config/env.ts:
   - Add a new lazy getter `get ANTHROPIC_API_KEY() { return requireEnv('ANTHROPIC_API_KEY'); }` following the existing pattern.

3. Create packages/server/src/config/anthropic.ts:
   - Import Anthropic from '@anthropic-ai/sdk'
   - Import env from './env.js'
   - Export a singleton: `export const anthropic = new Anthropic({ apiKey: env.ANTHROPIC_API_KEY });`

4. Add `ANTHROPIC_API_KEY: 'test-key'` to the env block in packages/server/vitest.config.ts so tests don't crash on missing env var.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && pnpm -r exec -- node -e "try{require.resolve('@anthropic-ai/sdk');console.log('anthropic-sdk: OK')}catch{}" 2>/dev/null; pnpm -r exec -- node -e "try{require.resolve('expo-image-picker');console.log('expo-image-picker: OK')}catch{}" 2>/dev/null; cd packages/server && pnpm vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>@anthropic-ai/sdk installed in server, expo-image-picker installed in mobile, ANTHROPIC_API_KEY in env config and test config, anthropic.ts client singleton exported.</done>
</task>

<task type="auto">
  <name>Task 2: Create pantry database migration and TypeScript types</name>
  <files>supabase/migrations/00003_pantry_items.sql, apps/mobile/src/types/pantry.ts</files>
  <action>
1. Create supabase/migrations/00003_pantry_items.sql following the exact schema from research:
   - Table: pantry_items with columns: id (UUID PK), profile_id (UUID FK to profiles), name (TEXT NOT NULL), normalized_name (TEXT NOT NULL), quantity (NUMERIC DEFAULT 1), unit (TEXT DEFAULT 'piece'), category (TEXT NOT NULL with CHECK constraint for: produce, dairy, protein, grain, condiment, beverage, frozen, snack, other), source_location (TEXT NOT NULL with CHECK for: fridge, pantry, freezer), confidence (NUMERIC DEFAULT 1.0, CHECK 0-1), status (TEXT DEFAULT 'available' with CHECK for: available, used, depleted), last_seen_at (TIMESTAMPTZ DEFAULT NOW()), created_at (TIMESTAMPTZ DEFAULT NOW()), updated_at (TIMESTAMPTZ DEFAULT NOW())
   - Create indexes: idx_pantry_items_profile (profile_id), idx_pantry_items_lookup (profile_id, normalized_name, source_location), idx_pantry_items_status (profile_id, status)
   - Enable RLS with 4 policies: SELECT, INSERT, UPDATE, DELETE all scoped to profile_id = auth.uid()
   - Add updated_at trigger using existing public.update_updated_at() function (same pattern as 00001_profiles.sql)

2. Create apps/mobile/src/types/pantry.ts with these exact types:
   - `FoodCategory` type union: 'produce' | 'dairy' | 'protein' | 'grain' | 'condiment' | 'beverage' | 'frozen' | 'snack' | 'other'
   - `SourceLocation` type union: 'fridge' | 'pantry' | 'freezer'
   - `PantryItemStatus` type union: 'available' | 'used' | 'depleted'
   - `PantryItem` interface: all columns from the DB table with string types for timestamps and id
   - `ScanResult` interface: { name: string, quantity: number, unit: string, confidence: number, category: FoodCategory } -- AI output before user review
   - `ReviewItem` interface extending ScanResult: adds { id: string (temp client UUID), accepted: boolean, userEdited: boolean }
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime && cat supabase/migrations/00003_pantry_items.sql | grep -c "CREATE TABLE\|CREATE INDEX\|CREATE POLICY\|CREATE TRIGGER" && npx tsc --noEmit --project apps/mobile/tsconfig.json 2>&1 | head -20 || echo "typecheck done"</automated>
  </verify>
  <done>Migration file creates pantry_items table with all columns, indexes, RLS policies, and updated_at trigger. TypeScript types exported for PantryItem, ScanResult, ReviewItem, FoodCategory, SourceLocation, PantryItemStatus.</done>
</task>

</tasks>

<verification>
- `pnpm -r test` passes (no regressions)
- Migration SQL is valid (correct syntax, references existing profiles table)
- TypeScript compiles with no errors
- Anthropic client config exports correctly
</verification>

<success_criteria>
Foundation artifacts exist: migration, types, Anthropic config, dependencies installed. All downstream plans (02, 03, 04) can proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-pantry-scanning/03-01-SUMMARY.md`
</output>
