---
phase: 19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash
plan: 06
type: execute
wave: 3
depends_on: [19-01, 19-02, 19-03, 19-04, 19-05]
files_modified:
  - apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
  - apps/mobile/.maestro/18-recipe-search-favorite.yaml
  - apps/mobile/.maestro/08-home-suggestions.yaml
  - apps/mobile/.maestro/11-shopping-list-generate.yaml
  - apps/mobile/.maestro/07-pantry-add.yaml
  - apps/mobile/.maestro/09-meal-plan-generate.yaml
  - apps/mobile/.maestro/21-design-buttons-visual.yaml
autonomous: false
requirements:
  - Design quality (post-v1)
must_haves:
  truths:
    - "Metro cache is cleared before Maestro run so NativeWind serves the Phase 19 tokens (Pitfall 2)"
    - "Every Maestro flow that asserted on orange-specific visuals or stale placeholders/selectors is updated"
    - "Sticky pill tap expands to /search modal and dismiss returns to Kitchen without z-order flash"
    - "Tab bar active tint visually reads as terracotta across all 4 tabs"
    - "FAB visual is terracotta across Kitchen (ImportFab / RegenerateFab) and Pantry (ScanButton)"
    - "Plan day card shows 7 days without scroll on iPhone 17 Pro simulator"
    - "Human UAT approves aesthetic direction per 19-VALIDATION.md Manual-Only Verifications"
    - "All Maestro flows that were green pre-Phase-19 are green post-Phase-19 (baseline screenshots refreshed)"
  artifacts:
    - path: "apps/mobile/.maestro/21-design-buttons-visual.yaml"
      provides: "New flow: launches app, navigates through primary-button surfaces (login CTA, onboarding CTA, scan FAB, sign out destructive), takes screenshot at each"
    - path: "apps/mobile/.maestro/20-kitchen-segment-toggle.yaml"
      provides: "Updated: asserts on new sticky pill (accessibilityLabel='Search recipes' or similar), re-baselines screenshot"
    - path: "apps/mobile/.maestro/18-recipe-search-favorite.yaml"
      provides: "Updated: tap sticky pill -> /search modal -> dismiss; re-baseline"
    - path: "apps/mobile/.maestro/08-home-suggestions.yaml"
      provides: "Updated: Kitchen suggestions segment FAB is terracotta; re-baseline"
  key_links:
    - from: "apps/mobile/.maestro/21-design-buttons-visual.yaml"
      to: "apps/mobile/.maestro/scripts/uat.sh"
      via: "uat.sh all iterates every .yaml in .maestro/"
      pattern: ".*\\.yaml"
    - from: "apps/mobile/.maestro/20-kitchen-segment-toggle.yaml"
      to: "new /search modal route"
      via: "tapOn: 'Search recipes' then assertVisible: 'Search' (modal title)"
      pattern: "assertVisible.*Search"
---

<objective>
Close Phase 19 with a Maestro flow update + screenshot-regen + human UAT gate. This is the ONLY plan in Phase 19 with a human-in-the-loop checkpoint — Block A's UAT Gate per EXECUTION-PLAN.md.

Purpose: Per 19-VALIDATION.md Sampling Rate + Manual-Only Verifications + Pitfall 5, every design sweep breaks Maestro text-literal selectors and screenshot baselines. This plan audits every at-risk flow, updates selectors, re-captures baselines, adds a new visual flow for buttons (per 19-VALIDATION.md Per-Task row 12), and pauses for the user to visually approve the terracotta palette + typography + sticky pill feel on device.

Output:
- 3 existing Maestro flows updated for new sticky pill / new tab tint / new FAB color.
- 3 existing Maestro flows re-baselined (pantry-add, shopping-list-generate, meal-plan-generate — screenshot assertions only).
- 1 new flow `21-design-buttons-visual.yaml` exercising every button variant.
- Human UAT checkpoint for aesthetic sign-off.
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-VALIDATION.md
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-05-SUMMARY.md
@/Users/patrickrichards/DinnerTime/CLAUDE.md
@apps/mobile/.maestro/README.md
@apps/mobile/.maestro/20-kitchen-segment-toggle.yaml
@apps/mobile/.maestro/18-recipe-search-favorite.yaml
@apps/mobile/.maestro/08-home-suggestions.yaml
@apps/mobile/.maestro/smoke.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear Metro cache + update at-risk Maestro flows + add buttons-visual flow + run full Maestro suite</name>
  <files>apps/mobile/.maestro/20-kitchen-segment-toggle.yaml, apps/mobile/.maestro/18-recipe-search-favorite.yaml, apps/mobile/.maestro/08-home-suggestions.yaml, apps/mobile/.maestro/11-shopping-list-generate.yaml, apps/mobile/.maestro/07-pantry-add.yaml, apps/mobile/.maestro/09-meal-plan-generate.yaml, apps/mobile/.maestro/21-design-buttons-visual.yaml</files>
  <action>
    Per 19-VALIDATION.md Per-Task rows 8-12 and Pitfall 5 in 19-RESEARCH.md:

    1. **Metro cache clear** (first — critical per CLAUDE.md + Pitfall 2):
       ```bash
       rm -rf /Users/patrickrichards/DinnerTime/apps/mobile/.expo
       ```
       Then restart Metro in background (if not already running):
       ```bash
       cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; (npx expo start --dev-client --lan --clear &amp;)
       ```
       Wait ~15 seconds for Metro to boot. Verify with `curl -sS http://localhost:8081/status` returning something metro-ish, or proceed to step 2.

    2. **Boot simulator + install + launch** (per CLAUDE.md UAT quick loop):
       ```bash
       xcrun simctl boot "iPhone 17 Pro" || true
       open -a Simulator
       xcrun simctl install booted /Users/patrickrichards/DinnerTime/apps/mobile/ios/build/Build/Products/Debug-iphonesimulator/DinnerTime.app
       ```

    3. **Audit + update at-risk flows** — read each flow file, audit for:
       - Text literals like `"Search recipes"` (placeholder may have changed — Plan 03 used "Search recipes" as default, but if the sticky pill's accessibility label differs or kitchen.tsx's sticky pill uses a different placeholder, update).
       - Tab bar selectors (`"Kitchen"`, `"Plan"`, `"Pantry"`, `"Shopping"`, `"Settings"`) — these text labels did NOT change, safe.
       - Screenshot assertions (`takeScreenshot` with filename) — baselines need regen because terracotta differs from orange.

       Per 19-VALIDATION.md, the high-risk flows are:

       **20-kitchen-segment-toggle.yaml:**
       - Add a step after landing on Library segment:
         ```yaml
         - assertVisible: ".*Search recipes.*"   # new sticky pill placeholder
         - takeScreenshot: kitchen-library-sticky-pill
         - tapOn: ".*Search recipes.*"            # open modal
         - assertVisible: "Search"                # modal title
         - takeScreenshot: kitchen-search-modal
         - back                                    # dismiss modal
         - assertVisible: ".*Kitchen.*"           # back on Kitchen
         ```
         The existing flow structure is preserved; these 6 lines get added per Pitfall 4 mitigation (verify modal z-order flash doesn't appear).

       **18-recipe-search-favorite.yaml:**
       - Search used to be inline on Library. Update to:
         - Tap sticky pill (`tapOn: ".*Search recipes.*"`).
         - In modal, input search query (modal's TextInput will be Phase 17's; for now it's the placeholder text from Plan 03's search.tsx — flow should NOT assert on search functionality here). Update to only verify the modal opens + can dismiss without crash.
         - Regen screenshot.

       **08-home-suggestions.yaml:**
       - Suggestions segment on Kitchen — FAB retinted terracotta. No selector change needed, but screenshot baseline must be regenerated.
       - Run the flow, capture `takeScreenshot: home-suggestions-terracotta`.

    4. **Regen baseline screenshots on these flows** (no selector changes, only visual change):
       - `11-shopping-list-generate.yaml` — buttons primary became terracotta.
       - `07-pantry-add.yaml` — scan button / FAB terracotta; Pantry rows now use ItemRow layout.
       - `09-meal-plan-generate.yaml` — Plan tab DayRow reshuffled; generate CTA now terracotta.

       Strategy: run each flow once with `maestro test --format junit > /dev/null`, capture new screenshot baselines, visually inspect that no interactions broke, commit screenshot diffs.

    5. **New flow: `21-design-buttons-visual.yaml`**:

       ```yaml
       appId: com.dinnertime.app
       name: 21-design-buttons-visual
       tags:
         - design
         - visual
       ---
       - runFlow: _ensure-logged-in.yaml

       # 1. Onboarding primary CTA — skipped if already onboarded, so bail on first screen
       - takeScreenshot: design-buttons-home-entry

       # 2. Kitchen -&gt; Library: Import FAB (terracotta)
       - tapOn: ".*Library.*"
       - takeScreenshot: design-library-import-fab

       # 3. Kitchen -&gt; Suggestions: Regenerate FAB (terracotta)
       - tapOn: ".*Suggestions.*"
       - takeScreenshot: design-suggestions-regen-fab

       # 4. Pantry scan button (ScanButton — terracotta)
       - tapOn: ".*Pantry.*"
       - takeScreenshot: design-pantry-scan-button

       # 5. Settings: destructive variant (Sign Out)
       - tapOn: ".*Settings.*"
       - scrollUntilVisible:
           element: ".*Sign Out.*"
           direction: DOWN
       - takeScreenshot: design-settings-sign-out-destructive

       # 6. Return to Kitchen (no-op ensure-logged-in exit cleanup)
       - tapOn: ".*Kitchen.*"
       - takeScreenshot: design-final-kitchen
       ```

       This doesn't assert button semantics programmatically (Maestro can't inspect button `variant` props) — it's purely a visual-regression flow. The human checkpoint in Task 2 reviews the 6 screenshots for: terracotta present, no orange leaks, Sign Out reads as red, FABs look premium.

    6. **Run full Maestro suite**:
       ```bash
       cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; .maestro/scripts/uat.sh all
       ```
       Expect all flows green. If any flow fails on a selector that hasn't been audited, update it and re-run.

    Per 19-VALIDATION.md rows 8-12 + Pitfall 5 (Maestro regex text selectors break on placeholder changes).
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; .maestro/scripts/uat.sh all 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>
    - Metro cache cleared; Metro restarted with `--clear`.
    - All 7 Maestro files (6 updated + 1 new) committed.
    - `uat.sh all` reports all flows GREEN (target: 21+ flows green, matching Phase 12's 20/21 baseline + new buttons-visual).
    - New screenshot baselines captured for the 6 visual-only flows.
    - Any flow that genuinely regresses is diagnosed (selector change? logic change?) and fixed OR escalated to user in Task 2 checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT Gate A — Aesthetic Sign-off on Phase 19 Design System</name>
  <files>apps/mobile (live on iPhone 17 Pro simulator — no code files modified; this is a verification gate)</files>
  <action>Present the built Phase 19 design system to the user via the running iOS Simulator. The user walks the Gate A checklist (see <how-to-verify> below) and either approves aesthetic direction or surfaces specific defects for gap closure. No automated work during this task — pause and wait for resume-signal.</action>
  <verify><automated>echo "Human UAT gate — no automated verify; user approval via resume-signal is the verification"</automated></verify>
  <done>User types "approved" in response to this checkpoint. If defects are reported, Plan 06 stays paused and a follow-up gap-closure plan is created via /gsd:plan-phase 19 --gaps.</done>
  <what-built>
    Phase 19 rebuilt the design substrate end-to-end:
    - Semantic design tokens (CSS vars + tokens.ts) replacing raw orange.
    - Rewritten Button (5 variants, 44pt), Chip (filter + display), Input primitives.
    - DoorDash-style StickySearchPill on Kitchen Library, routing to /search modal.
    - Mode-aware RecipeCard (grid + list).
    - Denser DayRow with status Chips.
    - Shared ItemRow across Shopping + Pantry.
    - Tab bar, FABs, collapsingHeader retinted via tokens.
    - ChipToggle + old components/recipes/SearchBar deleted.

    All automated gates (vitest + Maestro) are GREEN. This checkpoint verifies the SUBJECTIVE calls — does it LOOK like premium Spotify + DoorDash food design?
  </what-built>
  <how-to-verify>
    **Prerequisites (verify before starting):**
    1. Metro is running with --clear (if not: `cd apps/mobile &amp;&amp; rm -rf .expo &amp;&amp; npx expo start --dev-client --lan --clear`).
    2. iOS Simulator is running "iPhone 17 Pro" with the DinnerTime dev build installed.
    3. You are logged in (run `_ensure-logged-in.yaml` if needed).

    **Gate A Checklist — per 19-VALIDATION.md Manual-Only Verifications:**

    1. **Terracotta palette reads as premium + appetizing** (not generic SaaS orange). Compare side-by-side against a pre-Phase-19 screenshot if available.
       - Open each tab (Kitchen, Plan, Pantry, Shopping, Settings).
       - Confirm active tab tint is terracotta-ish (NOT pure orange).
       - Confirm primary CTAs (save buttons, FABs) are terracotta.
       - Confirm cream/warm-off-white backgrounds read as "warm" not "beige/dirty".

    2. **Typography hierarchy reads "Spotify-like premium"** on content-heavy screens.
       - Kitchen Library: recipe card titles bold + tight letter spacing, metadata smaller + muted.
       - Plan tab: day labels uppercase label style, meal names body weight.
       - Pantry: item names clear + scannable.

    3. **Sticky pill search feels DoorDash-like**.
       - On Kitchen Library, the sticky pill is visible above the collapsing header.
       - On scroll, the pill stays fixed and its shadow subtly grows.
       - Tap the pill -&gt; the /search modal opens with keyboard. Dismiss returns to Kitchen cleanly (NO z-order flash per Pitfall 4).

    4. **Chip density on Plan day card feels Strava-level** without cluttering.
       - Plan tab shows 7 days without scroll on iPhone 17 Pro.
       - Day cards render day label + thumbnail + meal name + status chip(s) compactly.

    5. **Buttons at 44pt tap target** — tap each variant on the screenshots from `21-design-buttons-visual.yaml`:
       - Primary (terracotta filled) — taps cleanly, reads as most important.
       - Secondary (cream with border) — clearly secondary, readable.
       - Ghost (transparent, terracotta text) — reads as action, less visually heavy.
       - Destructive (Sign Out) — unmistakably red.
       - Icon-only (header actions) — 44pt square, tappable without missing.

    6. **expo-glass-effect** (if Claude proposed in Plan 05): decide if it adds premium or gimmick. Flag any overuse.

    7. **Spot-check 5 screens** for any missed orange or hardcoded hex:
       - Login, Onboarding, Scan camera, Recipe detail, Shopping order detail.
       - Nothing should feel "stale 2025 orange".

    **If approved:** respond `approved` and Plan 06 completes.

    **If issues surfaced:** respond with specific defects. Common fix destinations:
       - Wrong shade of terracotta -&gt; edit `--color-brand` in `apps/mobile/src/global.css` + hex in `src/design/tokens.ts` (parity test will catch drift).
       - Typography too heavy/light -&gt; adjust `typography.ts` fontWeights.
       - Sticky pill too aggressive shadow -&gt; adjust `shadowOpacityConfig` outputRange in `src/components/ui/SearchBar.tsx`.
       - Destructive chip too faint -&gt; adjust `bg-destructive/15` -&gt; `/25` in `chipStyles.ts`.

    For each reported defect, create a follow-up gap-closure task (run `/gsd:plan-phase 19 --gaps` if checker-flagged).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 19, or describe specific visual defects to trigger gap closure.</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/mobile && pnpm test --run` — all tests green.
2. `cd apps/mobile && .maestro/scripts/uat.sh all` — all flows green.
3. Human verified aesthetic per Gate A checklist.
4. No orange hex or orange-[0-9]+ classes detected (tokens-purity.test.ts green).
5. 21-design-buttons-visual.yaml new flow exists and passes.
6. Screenshot baselines updated for 6 at-risk flows.
</verification>

<success_criteria>
- [ ] Metro cache cleared before Maestro run.
- [ ] 20-kitchen-segment-toggle.yaml updated for sticky pill.
- [ ] 18-recipe-search-favorite.yaml updated for modal search.
- [ ] 08-home-suggestions.yaml re-baselined.
- [ ] 11-shopping-list-generate.yaml re-baselined.
- [ ] 07-pantry-add.yaml re-baselined.
- [ ] 09-meal-plan-generate.yaml re-baselined.
- [ ] 21-design-buttons-visual.yaml new flow authored + green.
- [ ] `uat.sh all` returns all flows green.
- [ ] Human UAT Gate A APPROVED.
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-06-SUMMARY.md` and `.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-SUMMARY.md` (phase-level) capturing:
- Final Maestro flow count (target 22+ green).
- New screenshot baselines inventory (6 files).
- Human UAT outcome (approved / defects listed + follow-ups).
- Block A completion status (Phase 15 + Phase 19 per EXECUTION-PLAN.md).
- Handoff notes to Block B (Phase 18): tokens are now the single source of truth; any new component adds a semantic token in global.css + tokens.ts (parity test enforces).
</output>
