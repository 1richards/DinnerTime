---
phase: 19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash
plan: 05
type: execute
wave: 3
depends_on: [19-01, 19-02, 19-03, 19-04]
files_modified:
  - apps/mobile/src/components/shopping/ShoppingItemRow.tsx
  - apps/mobile/src/components/pantry/PantryItemCard.tsx
  - apps/mobile/src/components/ui/useCollapsingHeader.ts
  - apps/mobile/src/app/(tabs)/_layout.tsx
  - apps/mobile/src/app/(tabs)/kitchen.tsx
  - apps/mobile/src/app/(tabs)/pantry.tsx
  - apps/mobile/src/app/(tabs)/shopping.tsx
  - apps/mobile/src/app/(tabs)/plan.tsx
  - apps/mobile/src/app/(tabs)/settings.tsx
  - apps/mobile/src/app/_layout.tsx
  - apps/mobile/src/app/(auth)/login.tsx
  - apps/mobile/src/app/(auth)/register.tsx
  - apps/mobile/src/app/onboarding/index.tsx
  - apps/mobile/src/app/scan/index.tsx
  - apps/mobile/src/app/scan/receipt.tsx
  - apps/mobile/src/app/scan/instacart.tsx
  - apps/mobile/src/app/recipes/import.tsx
  - apps/mobile/src/app/recipes/discover.tsx
  - apps/mobile/src/app/recipes/import-url.tsx
  - apps/mobile/src/app/recipes/import-manual.tsx
  - apps/mobile/src/app/recipes/import-photo.tsx
  - apps/mobile/src/app/recipes/[id]/index.tsx
  - apps/mobile/src/app/shopping/order/[id].tsx
  - apps/mobile/src/app/shopping/orders.tsx
  - apps/mobile/src/components/settings/MemberFormModal.tsx
  - apps/mobile/src/components/settings/SkillLevelSection.tsx
  - apps/mobile/src/components/cooking/StepNavButtons.tsx
  - apps/mobile/src/components/cooking/AskSheet.tsx
  - apps/mobile/src/components/pantry/LocationPicker.tsx
  - apps/mobile/src/components/recipes/SearchBar.tsx
  - apps/mobile/src/design/tokens-purity.test.ts
autonomous: true
requirements:
  - Design quality (post-v1)
must_haves:
  truths:
    - "Zero `#F97316` hex literals remain in apps/mobile/src/**/*.{ts,tsx}"
    - "Zero `orange-[50-900]` Tailwind classes remain in apps/mobile/src/**/*.{ts,tsx}"
    - "Tab bar active tint uses colors.brand via tokens.ts (not hardcoded hex)"
    - "All FABs (ImportFab, RegenerateFab, ScanButton) retinted to terracotta via tokens"
    - "collapsingHeaderStyles (useCollapsingHeader.ts) migrates from hardcoded hex to typography + tokens"
    - "ShoppingItemRow and PantryItemCard adopt the shared ItemRow primitive from Plan 03"
    - "ChipToggle shim's last call sites migrate to Chip directly; ChipToggle.tsx is DELETED"
    - "Plan 01's tokens-purity.test.ts is flipped from describe.skip to describe (ON) and passes GREEN"
  artifacts:
    - path: "apps/mobile/src/app/(tabs)/_layout.tsx"
      provides: "Tab bar registers colors.brand as active tint (replaces '#F97316')"
      contains: "tabBarActiveTintColor: colors.brand"
    - path: "apps/mobile/src/components/shopping/ShoppingItemRow.tsx"
      provides: "Rewritten to use Plan 03's ItemRow with leading=checkbox + struck=checked"
    - path: "apps/mobile/src/components/pantry/PantryItemCard.tsx"
      provides: "Rewritten to use Plan 03's ItemRow with leading=stepper + trailingChip for stale/low-confidence state"
    - path: "apps/mobile/src/components/ui/useCollapsingHeader.ts"
      provides: "collapsingHeaderStyles uses typography tokens + tokens.ts colors (no hardcoded hex)"
    - path: "apps/mobile/src/design/tokens-purity.test.ts"
      provides: "describe.skip -> describe; test passes"
  key_links:
    - from: "apps/mobile/src/app/(tabs)/_layout.tsx"
      to: "apps/mobile/src/design/tokens.ts"
      via: "import { colors } from '../../design/tokens'"
      pattern: "tabBarActiveTintColor:\\s*colors\\.brand"
    - from: "apps/mobile/src/components/shopping/ShoppingItemRow.tsx"
      to: "apps/mobile/src/components/ui/ItemRow.tsx"
      via: "import { ItemRow } from '../ui/ItemRow'"
      pattern: "from ['\"]\\.\\./ui/ItemRow"
    - from: "apps/mobile/src/components/pantry/PantryItemCard.tsx"
      to: "apps/mobile/src/components/ui/ItemRow.tsx"
      via: "import { ItemRow } from '../ui/ItemRow'"
      pattern: "from ['\"]\\.\\./ui/ItemRow"
---

<objective>
One-pass sweep replacing every orange/hex/ad-hoc styling with Phase 19 tokens. Enforces CONTEXT's "one-pass token swap — no mixed orange+terracotta states" rule. Wires Shopping + Pantry rows to Plan 03's ItemRow. Retints tab bar, FABs, and collapsing-header chrome via tokens.ts imports. Finishes by flipping the purity test to ON.

Purpose: Implement D-01 (icon-size consistency), D-04 (nav headers inherit tokens), D-06 (shared ItemRow adoption, color semantics), the FAB retreatment default (terracotta token swap preserving 60x60 shadow FAB per CONTEXT Claude's Discretion default), and the tab bar active-tint retint (CONTEXT default: preserve translucent tab bar, terracotta active tint).

This is the largest plan in the phase by file count but each file's change is mechanical — one or two class/color swaps, no logic changes.

Output:
- ~25 files token-migrated.
- Shopping + Pantry rows using Plan 03's ItemRow primitive.
- Tab bar active tint = `colors.brand`.
- collapsingHeaderStyles migrated to typography tokens + tokens.ts colors (critical because navigator-style objects can't use className — Pitfall 6).
- Old `components/recipes/SearchBar.tsx` DELETED; Library call sites updated to import from `components/ui/SearchBar.tsx` (Plan 03's).
- ChipToggle.tsx DELETED; remaining call sites migrated to Chip directly.
- tokens-purity.test.ts FLIPPED ON and passes.
- Metro cache cleared after token swap (per CLAUDE.md gotcha + Pitfall 2).
</objective>

<execution_context>
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickrichards/DinnerTime/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-CONTEXT.md
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-RESEARCH.md
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-01-SUMMARY.md
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-02-SUMMARY.md
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-03-SUMMARY.md
@/Users/patrickrichards/DinnerTime/.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-04-SUMMARY.md
@apps/mobile/src/design/tokens.ts
@apps/mobile/src/components/ui/Button.tsx
@apps/mobile/src/components/ui/Chip.tsx
@apps/mobile/src/components/ui/ItemRow.tsx
@apps/mobile/src/components/ui/SearchBar.tsx
@apps/mobile/src/components/ui/useCollapsingHeader.ts
@apps/mobile/src/app/(tabs)/_layout.tsx
@apps/mobile/src/components/shopping/ShoppingItemRow.tsx
@apps/mobile/src/components/pantry/PantryItemCard.tsx
@apps/mobile/CLAUDE.md

<interfaces>
<!-- From prior waves -->
From src/design/tokens.ts: `colors`, `typography`, `spacing`, `radius`, `textStyles` (via typography.ts re-export).
From src/components/ui/Button.tsx: 5-variant Button.
From src/components/ui/Chip.tsx: `Chip`, `ChipKind`, `ChipTone`.
From src/components/ui/ItemRow.tsx: `ItemRow` with leading checkbox|stepper|icon variants.
From src/components/ui/SearchBar.tsx: `StickySearchPill`, legacy `SearchBar`.

<!-- Substitution map (exhaustive) -->
Orange -> brand:
- `bg-orange-500` -> `bg-brand`
- `bg-orange-600` (pressed / dark) -> `bg-brand-pressed`
- `text-orange-500` / `text-orange-600` -> `text-brand`
- `border-orange-500` -> `border-brand`
- `bg-orange-50` / `bg-orange-100` -> `bg-brand/10`
- `#F97316` (hex literal) -> `colors.brand`
- `#EA580C` (orange-600 hex) -> `colors.brandPressed`
- `#FED7AA` (orange-200) -> `colors.brand` at 15% alpha via `bg-brand/15`

Hex -> token:
- `#FAF7F2` -> `colors.bg`
- `#FFFBF5` -> `colors.bg` (close enough; accept small shade shift; warmWhite gone)
- `#FFFFFF` (explicit surface) -> `colors.surface` or `bg-surface` class
- `#1C1917` / `#1A140F` -> `colors.textPrimary` / `text-text-primary`
- `#7A6651` / `warmGray-600` -> `colors.textSecondary` / `text-text-secondary`
- `#A89178` / `warmGray-400` -> `colors.textTertiary` / `text-text-tertiary`
- `#E5D9CA` / `warmGray-200` -> `colors.border` / `border-border`
- `#F1EAE0` / `warmGray-100` -> `colors.surfaceSubtle` / `bg-surface-subtle`
- `#DC2626` / `red-500`/`red-600` -> `colors.destructive` / `bg-destructive`

Red chips (from old `colorScheme='red'` in ChipToggle): migrate to `<Chip kind='display' tone='destructive' />` where read-only (allergies in settings), or `<Chip kind='filter' selected={...} />` where toggleable (accept slight visual shift — brand replaces red for selected).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate ShoppingItemRow + PantryItemCard to ItemRow + migrate navigator-style chrome (tab bar, collapsingHeader, root layouts) to tokens</name>
  <files>apps/mobile/src/components/shopping/ShoppingItemRow.tsx, apps/mobile/src/components/pantry/PantryItemCard.tsx, apps/mobile/src/components/ui/useCollapsingHeader.ts, apps/mobile/src/app/(tabs)/_layout.tsx, apps/mobile/src/app/_layout.tsx</files>
  <behavior>
    - ShoppingItemRow renders via `<ItemRow leading={{kind:'checkbox', checked, onToggle}} title={...} subtitle={qty/unit} struck={checked} />`, with optional trailing-chip for "added from plan" indicator.
    - PantryItemCard renders via `<ItemRow leading={{kind:'stepper', quantity, unit, onInc, onDec}} title={...} trailingChip={staleChip} />` where staleChip is derived from confidence + last_seen.
    - `collapsingHeaderStyles` in useCollapsingHeader.ts uses `typography.display.fontSize/lineHeight/fontWeight/letterSpacing` for largeTitle, `typography.title` for compactTitle, `typography.caption` for largeSubtitle. Colors use tokens.ts (primary text, secondary text, border, surface).
    - Active actionBtn (currently `backgroundColor: '#F97316'`) uses `colors.brand`.
    - Tab bar active tint in `(tabs)/_layout.tsx`: replace `'#F97316'` literal with `colors.brand`. Inactive tint uses `colors.textTertiary`. Background uses `colors.bg`. Top border uses `colors.borderSubtle`.
    - Root Stack header tint in `_layout.tsx` (if any hardcoded) migrates to `colors.textPrimary` / `colors.bg`.
  </behavior>
  <action>
    Per D-06 (shared ItemRow) and Pitfall 6 (navigator can't use className — must use tokens.ts):

    1. **useCollapsingHeader.ts rewrite** — critical because it's imported by every tab:
       - Import `colors`, `typography` from `../../design/tokens`.
       - Replace every hardcoded hex in `collapsingHeaderStyles`:
         - `color: '#1A140F'` -> `color: colors.textPrimary`
         - `color: '#7A6651'` -> `color: colors.textSecondary`
         - `backgroundColor: 'rgba(255,251,245,0.95)'` -> use `colors.bg` with alpha (e.g., `colors.bg + 'F2'` or keep the rgba but derive from bg tokens). Simplest: compute `"rgba(250,247,242,0.95)"` as a constant inline, comment "derived from colors.bg".
         - `borderBottomColor: '#F1EAE0'` -> `colors.borderSubtle`
         - `backgroundColor: '#F97316'` (actionBtnActive) -> `colors.brand`
         - `borderColor: '#F97316'` -> `colors.brand`
         - `backgroundColor: '#DC2626'` (badge) -> `colors.destructive`
         - `borderColor: '#FFFBF5'` (badge border) -> `colors.bg`
         - `borderColor: '#E5D9CA'` -> `colors.border`
         - `color: '#FFFFFF'` (inside white texts) stays `'#FFFFFF'` (literal white is fine, not brand-coded).
       - Replace `largeTitle`: `fontSize: 34, fontWeight: '900', letterSpacing: -0.8` with `...typography.display`. The existing code uses `fontWeight: '900'` but typography.display is `'700'` — accept the change to match type scale, or export a variant if needed. Recommendation: accept `'700'` (matches iOS SF Pro Display bold, feels more premium).
       - Replace `compactTitle`: `fontSize: 17, fontWeight: '800'` with `...typography.title` (22pt/semibold). Actually compact headers are smaller than large titles — use `...typography.body` plus explicit `fontWeight: '600'` as override if font-body's 400 is too light. Use judgment; per 19-RESEARCH.md "premium feel comes from weight/size discipline" — bias to typography.title scaled down: `fontSize: 17, lineHeight: 22, fontWeight: '600'` inline, or port to a new `typography.subtitle` token if needed (optional — don't add tokens for single-use cases, just use inline).
       - Replace `largeSubtitle`: `fontSize: 14` with `...typography.caption` (13pt).

    2. **apps/mobile/src/app/(tabs)/_layout.tsx** — per Pitfall 6 use tokens, not className:
       - `import { colors } from '../../design/tokens';` at top.
       - Find current `tabBarActiveTintColor: '#F97316'` (or similar) and replace with `colors.brand`.
       - Add/update:
         - `tabBarInactiveTintColor: colors.textTertiary`.
         - `tabBarStyle: { backgroundColor: colors.bg, borderTopColor: colors.borderSubtle, borderTopWidth: StyleSheet.hairlineWidth }`.
         - Preserve translucent behavior if any — CONTEXT default is keep iOS translucent tab bar.
       - Header colors (if tab screens expose headers): `headerStyle: { backgroundColor: colors.bg }`, `headerTintColor: colors.textPrimary`.

    3. **apps/mobile/src/app/_layout.tsx** — root Stack:
       - Import `colors` if not already.
       - Replace any hardcoded hex in Stack.Screen options with tokens.
       - Verify the search modal registration from Plan 03 is present.

    4. **ShoppingItemRow.tsx rewrite** — thin wrapper over ItemRow:

       Read current implementation to capture props: likely `item`, `onToggle`, `onEdit`, etc.

       ```typescript
       import React from 'react';
       import { ItemRow } from '../ui/ItemRow';
       import type { ShoppingListItem } from '../../types/shopping';

       interface ShoppingItemRowProps {
         item: ShoppingListItem;
         onToggle: (id: string) =&gt; void;
         onLongPress?: (item: ShoppingListItem) =&gt; void;
       }

       export function ShoppingItemRow({ item, onToggle, onLongPress }: ShoppingItemRowProps) {
         const qtyText = item.quantity != null &amp;&amp; item.unit
           ? `${item.quantity} ${item.unit}`
           : item.quantity != null
           ? String(item.quantity)
           : undefined;

         return (
           &lt;ItemRow
             leading={{ kind: 'checkbox', checked: !!item.checked, onToggle: () =&gt; onToggle(item.id) }}
             title={item.name}
             subtitle={qtyText}
             struck={!!item.checked}
             onLongPress={onLongPress ? () =&gt; onLongPress(item) : undefined}
           /&gt;
         );
       }
       ```

       (Adapt prop names to actual type. Preserve any existing behavior — Swipeable wrappers, add-to-pantry actions — at the CALLER level, not inside this component.)

    5. **PantryItemCard.tsx rewrite** — ItemRow with stepper + staleness chip:

       ```typescript
       import React from 'react';
       import { ItemRow } from '../ui/ItemRow';
       import type { PantryItem } from '../../types/pantry';

       interface PantryItemCardProps {
         item: PantryItem;
         onIncrement: (id: string) =&gt; void;
         onDecrement: (id: string) =&gt; void;
         onPress?: (item: PantryItem) =&gt; void;
       }

       function deriveTrailingChip(item: PantryItem): { label: string; tone: 'warning' | 'destructive' | 'default' } | undefined {
         // Stale: confidence &lt; 0.3 or last_seen &gt; 14 days ago -&gt; destructive
         // Low-confidence: confidence &lt; 0.6 -&gt; warning
         // Otherwise no chip
         if (item.confidence != null &amp;&amp; item.confidence &lt; 0.3) return { label: 'Stale', tone: 'destructive' };
         if (item.confidence != null &amp;&amp; item.confidence &lt; 0.6) return { label: 'Low confidence', tone: 'warning' };
         return undefined;
       }

       export function PantryItemCard({ item, onIncrement, onDecrement, onPress }: PantryItemCardProps) {
         return (
           &lt;ItemRow
             leading={{
               kind: 'stepper',
               quantity: item.quantity ?? 0,
               unit: item.unit,
               onInc: () =&gt; onIncrement(item.id),
               onDec: () =&gt; onDecrement(item.id),
             }}
             title={item.name}
             subtitle={item.source_location}
             trailingChip={deriveTrailingChip(item)}
             onPress={onPress ? () =&gt; onPress(item) : undefined}
           /&gt;
         );
       }
       ```

       Adapt field names (`source_location`, `confidence`, `quantity`, `unit`) to actual PantryItem type.

    Per D-06 (shared ItemRow adoption — Shopping + Pantry) per 19-CONTEXT.md. Per Pitfall 6 (navigator options use tokens, not className) per 19-RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; npx tsc --noEmit -p .</automated>
  </verify>
  <done>
    - useCollapsingHeader.ts uses typography tokens + tokens.ts colors; zero hardcoded hex except literal `'#FFFFFF'`.
    - (tabs)/_layout.tsx: tab bar active tint is `colors.brand`; inactive tint + background + border all tokenized.
    - _layout.tsx root Stack options tokenized.
    - ShoppingItemRow rewritten over ItemRow with checkbox leading + struck.
    - PantryItemCard rewritten over ItemRow with stepper leading + derived staleness chip.
    - TypeScript clean across all touched files (zero errors).
    - Plan 01-04 tests still GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Sweep auth + onboarding + scan surfaces (blast-radius 1/3)</name>
  <files>apps/mobile/src/app/(auth)/login.tsx, apps/mobile/src/app/(auth)/register.tsx, apps/mobile/src/app/onboarding/index.tsx, apps/mobile/src/app/scan/index.tsx, apps/mobile/src/app/scan/receipt.tsx, apps/mobile/src/app/scan/instacart.tsx</files>
  <behavior>
    Same sweep rules as the combined sweep (see `<interfaces>` substitution map), scoped to these 6 files only. The tokens-purity.test.ts is NOT enabled yet — Task 2c flips it on after all 3 sub-sweeps land. This split exists to limit blast radius per commit (checker warning #6) without violating CONTEXT's one-pass-swap rule (the swap IS one pass — it just lands as three sequential sub-commits within Plan 19-05).
  </behavior>
  <action>
    For each file in this task's `files_modified` list, apply the substitution map from the plan's `<interfaces>` block:

    1. Read the file. Identify every:
       - `orange-[0-9]+` className usage (swap per map).
       - `#F97316`, `#FB923C`, `#EA580C` hex literal (swap to `colors.brand` / `colors.brandPressed`).
       - `#FAF7F2`, `#FFFBF5`, hardcoded warmWhite/warmGray hexes (swap to tokens).
       - `bg-warmGray-[0-9]+` / `text-warmGray-[0-9]+` / `border-warmGray-[0-9]+` classes (swap).
       - `<Button variant="outline" ...>` → `<Button variant="secondary" ...>`.
       - Destructive buttons (sign-out, delete-account variants on auth screens) → `<Button variant="destructive" ...>`.

    2. Import `colors` from the correct relative path:
       - `src/app/(auth)/**` → `'../../design/tokens'`
       - `src/app/onboarding/**` → `'../../design/tokens'`
       - `src/app/scan/**` → `'../../design/tokens'`

    3. For scan screens specifically: the ScanButton / camera overlay buttons often use `#F97316`. Retint to `colors.brand`. Preserve 60x60 dimensions + shadow.

    Commit message: `feat(19-05a): terracotta token swap — auth + onboarding + scan`.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; npx tsc --noEmit -p . &amp;&amp; ! grep -rE "orange-(50|100|200|300|400|500|600|700|800|900)\b|#F97316" apps/mobile/src/app/\(auth\) apps/mobile/src/app/onboarding apps/mobile/src/app/scan --include="*.ts" --include="*.tsx"</automated>
  </verify>
  <done>
    - All 6 files in this sub-sweep contain zero `orange-*` classes and zero `#F97316` literals.
    - `npx tsc --noEmit -p .` exits 0 (zero TypeScript errors globally).
    - Plan 01-04 tests still GREEN (tokens-purity.test.ts still `describe.skip` — flipped ON in Task 2c).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Sweep recipes + shopping + plan + suggestions + pantry tabs + sub-components (blast-radius 2/3)</name>
  <files>apps/mobile/src/app/(tabs)/kitchen.tsx, apps/mobile/src/app/(tabs)/pantry.tsx, apps/mobile/src/app/(tabs)/shopping.tsx, apps/mobile/src/app/(tabs)/plan.tsx, apps/mobile/src/app/recipes/import.tsx, apps/mobile/src/app/recipes/discover.tsx, apps/mobile/src/app/recipes/import-url.tsx, apps/mobile/src/app/recipes/import-manual.tsx, apps/mobile/src/app/recipes/import-photo.tsx, apps/mobile/src/app/recipes/[id]/index.tsx, apps/mobile/src/app/shopping/order/[id].tsx, apps/mobile/src/app/shopping/orders.tsx, apps/mobile/src/components/pantry/LocationPicker.tsx</files>
  <behavior>
    Same sweep rules, scoped to the content-heavy tabs + recipe/shopping routes + pantry sub-components. Kitchen.tsx Library SearchBar migration happens HERE (it's this task's largest structural change — bigger than a pure color swap).
  </behavior>
  <action>
    1. Apply token substitutions per the map (same as Task 2a) across all 13 files.

    2. **Kitchen Library SearchBar migration** (kitchen.tsx):
       - Remove `import { SearchBar } from '../../components/recipes/SearchBar'`.
       - Add `import { StickySearchPill } from '../../components/ui/SearchBar'`.
       - Inside Library segment render: remove inline SearchBar text input from the collapsing header; mount `<StickySearchPill context="library" placeholder="Search recipes" scrollY={scrollY} />` as a sibling to the FlatList (not inside it), above everything else so it sits absolute-positioned per Plan 03's design.
       - If Library currently has local `searchQuery` state filtering the list, that state becomes dead — the sticky pill routes to `/search` modal instead (Phase 17 will connect search back to the Library list).
       - Alternatively (simpler for Phase 19 only): keep existing inline filter functional AND mount the sticky pill that taps out to modal. Recommended — document chosen approach in commit.
       - NOTE: do NOT delete `components/recipes/SearchBar.tsx` here — Task 2c owns the file deletion so it happens atomically with ChipToggle removal.

    3. Import paths:
       - `src/app/(tabs)/**` → `'../../design/tokens'`
       - `src/app/recipes/**` → `'../../design/tokens'`
       - `src/app/recipes/[id]/**` → `'../../../design/tokens'`
       - `src/app/shopping/**` → `'../../design/tokens'`
       - `src/app/shopping/order/**` → `'../../../design/tokens'`
       - `src/components/pantry/**` → `'../../design/tokens'`

    4. FAB retreatment on kitchen.tsx / pantry.tsx / shopping.tsx: grep for `ImportFab`, `RegenerateFab`, `ScanButton` usages or inline `backgroundColor: '#F97316'` on 60x60 Pressables and swap to `colors.brand`.

    5. Button variant migration (outline → secondary, red destructive buttons → `variant="destructive"`).

    Commit message: `feat(19-05b): terracotta token swap — tabs + recipes + shopping + pantry; kitchen Library integrates StickySearchPill`.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; npx tsc --noEmit -p . &amp;&amp; ! grep -rE "orange-(50|100|200|300|400|500|600|700|800|900)\b|#F97316" apps/mobile/src/app/\(tabs\) apps/mobile/src/app/recipes apps/mobile/src/app/shopping apps/mobile/src/components/pantry --include="*.ts" --include="*.tsx"</automated>
  </verify>
  <done>
    - All 13 files in this sub-sweep contain zero `orange-*` classes and zero `#F97316` literals.
    - Kitchen Library mounts `StickySearchPill`.
    - `npx tsc --noEmit -p .` exits 0.
    - Task 2a files remain purity-clean (no regression).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2c: Sweep settings + cooking + DELETE ChipToggle + DELETE components/recipes/SearchBar + FAB retint finalization (blast-radius 3/3)</name>
  <files>apps/mobile/src/app/(tabs)/settings.tsx, apps/mobile/src/components/settings/MemberFormModal.tsx, apps/mobile/src/components/settings/SkillLevelSection.tsx, apps/mobile/src/components/cooking/StepNavButtons.tsx, apps/mobile/src/components/cooking/AskSheet.tsx, apps/mobile/src/components/recipes/SearchBar.tsx, apps/mobile/src/components/ui/ChipToggle.tsx</files>
  <behavior>
    Final sub-sweep. Completes settings + cooking token swap AND deletes the two deprecated files. After this task commits, every remaining `orange-*` and `#F97316` is gone — Task 3 flips tokens-purity.test.ts ON.
  </behavior>
  <action>
    1. Apply token substitutions to settings.tsx, MemberFormModal.tsx, SkillLevelSection.tsx, StepNavButtons.tsx, AskSheet.tsx.

    2. Import paths:
       - `src/components/settings/**` → `'../../design/tokens'`
       - `src/components/cooking/**` → `'../../design/tokens'`

    3. **ChipToggle removal** (the non-trivial part):
       - `grep -rl "ChipToggle" apps/mobile/src --include="*.tsx"` to enumerate call sites (settings, MemberFormModal, possibly others already touched by Task 2a/2b).
       - For each call site, replace:
         - `<ChipToggle label={x} selected={s} onToggle={t} />` → `<Chip kind="filter" label={x} selected={s} onPress={t} />`
         - Allergies/disliked in Settings stay as `kind="filter"` (toggleable multi-select).
         - Any read-only destructive chip (e.g., "High risk allergy" badge) → `<Chip kind="display" tone="destructive" label={x} />`.
       - Delete `apps/mobile/src/components/ui/ChipToggle.tsx` once all call sites are migrated.

    4. **Delete `apps/mobile/src/components/recipes/SearchBar.tsx`** — Kitchen Library was migrated in Task 2b; this is the last consumer.

    5. Settings "Sign out" / "Delete account" buttons: migrate to `<Button variant="destructive" ...>`.

    6. Cooking `StepNavButtons.tsx`: the primary/next CTA is currently `#F97316` filled — swap to `colors.brand` (or the component's existing Button invocation if already using `<Button variant="primary" />`).

    Commit message: `feat(19-05c): terracotta swap — settings + cooking; DELETE ChipToggle + recipes/SearchBar. tokens-purity.test.ts enablement lives in Task 3.`.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; npx tsc --noEmit -p . &amp;&amp; ! test -f apps/mobile/src/components/ui/ChipToggle.tsx &amp;&amp; ! test -f apps/mobile/src/components/recipes/SearchBar.tsx &amp;&amp; ! grep -rE "orange-(50|100|200|300|400|500|600|700|800|900)\b|#F97316" apps/mobile/src --include="*.ts" --include="*.tsx"</automated>
  </verify>
  <done>
    - All 5 settings/cooking files purity-clean.
    - `apps/mobile/src/components/ui/ChipToggle.tsx` DELETED.
    - `apps/mobile/src/components/recipes/SearchBar.tsx` DELETED.
    - Full `src/**` grep finds zero `orange-*` classes and zero `#F97316` literals.
    - `npx tsc --noEmit -p .` exits 0.
    - Full `pnpm test --run` GREEN (tokens-purity.test.ts is still `describe.skip` — Task 3 flips it on the next commit).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flip tokens-purity.test.ts ON; verify zero orange/hex regressions; clear Metro cache</name>
  <files>apps/mobile/src/design/tokens-purity.test.ts</files>
  <behavior>
    - Change `describe.skip(...)` to `describe(...)` in tokens-purity.test.ts.
    - Test now runs and asserts zero `#F97316` and zero `orange-[50-900]` classes in `apps/mobile/src/**/*.{ts,tsx}` (excluding `__tests__` and the purity test file itself).
    - Test passes GREEN.
    - Commit message includes explicit note that `rm -rf apps/mobile/.expo && (cd apps/mobile && npx expo start --dev-client --lan --clear)` is REQUIRED before Plan 06's Maestro run, per CLAUDE.md "Metro cache" gotcha and 19-RESEARCH.md Pitfall 2.
  </behavior>
  <action>
    1. Edit `apps/mobile/src/design/tokens-purity.test.ts`:
       - Change `describe.skip('token purity (Plan 05 enables this)', () =&gt; {` to `describe('token purity', () =&gt; {`.
       - Remove the "Plan 05 enables this" comment.

    2. Run the test:
       ```bash
       cd apps/mobile && pnpm test src/design/tokens-purity.test.ts --run
       ```

       If it fails (any offending file listed), go back and fix — the purity sweep is not done until this test is green.

    3. Commit with message:
       ```
       chore(19-05): enable token-purity enforcement

       All #F97316 and orange-* Tailwind classes swept from src/**.
       tokens-purity.test.ts flipped from describe.skip -&gt; describe.

       IMPORTANT: Next session MUST run:
         rm -rf apps/mobile/.expo
         cd apps/mobile &amp;&amp; npx expo start --dev-client --lan --clear

       Without this, NativeWind + Metro will serve stale cached classes
       per CLAUDE.md "Metro cache" gotcha (Pitfall 2 in 19-RESEARCH.md).
       Plan 06's Maestro UAT depends on the cleared cache.
       ```

    4. Do NOT run `expo start --clear` from inside this execution — that would start a long-running process. Instead, leave the clear instruction in the commit message and Plan 06's human UAT checkpoint.
  </action>
  <verify>
    <automated>cd /Users/patrickrichards/DinnerTime/apps/mobile &amp;&amp; pnpm test src/design/tokens-purity.test.ts --run</automated>
  </verify>
  <done>
    - tokens-purity.test.ts no longer `describe.skip`.
    - `pnpm test src/design/tokens-purity.test.ts --run` GREEN.
    - Full `pnpm test --run` GREEN (all Plans 01-05 tests + server untouched).
    - Commit message documents Metro cache-clear requirement.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/mobile && pnpm test --run` — FULL green, including `src/design/tokens-purity.test.ts`.
2. `cd apps/mobile && npx tsc --noEmit -p .` — zero errors.
3. `grep -rE "orange-[0-9]+|#F97316" apps/mobile/src --include="*.ts" --include="*.tsx"` returns NOTHING (or only matches inside test files / tokens-purity.test.ts regex literals).
4. `ls apps/mobile/src/components/ui/ChipToggle.tsx` returns "No such file" (deleted).
5. `ls apps/mobile/src/components/recipes/SearchBar.tsx` returns "No such file" (deleted).
6. Full typecheck + full test pass green.
</verification>

<success_criteria>
- [ ] ShoppingItemRow uses ItemRow with leading=checkbox.
- [ ] PantryItemCard uses ItemRow with leading=stepper + derived stale chip.
- [ ] useCollapsingHeader.ts migrated to typography + tokens (no hex except literal white).
- [ ] (tabs)/_layout.tsx tab bar uses colors.brand via tokens.ts.
- [ ] Every orange-[0-9]+ className and #F97316 literal removed from src/**.
- [ ] ChipToggle.tsx + components/recipes/SearchBar.tsx DELETED.
- [ ] Kitchen Library uses StickySearchPill.
- [ ] All Button variant="outline" call sites migrated to "secondary".
- [ ] All destructive/sign-out/delete buttons use variant="destructive".
- [ ] All FABs retinted terracotta via colors.brand.
- [ ] tokens-purity.test.ts flipped on and GREEN.
- [ ] Commit message documents Metro cache-clear requirement.
- [ ] Full typecheck + full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/19-design-professionalization-icons-buttons-navigation-search-bars-inspired-by-spotify-strava-doordash/19-05-SUMMARY.md` capturing:
- Count of files swept (should be ~25-30).
- Any files with non-trivial structural changes (e.g., Kitchen Library — document chosen search integration approach).
- FAB retreatment: terracotta swap applied to [list components]; no structural change (per CONTEXT default).
- Tab bar: translucent behavior preserved, active tint = colors.brand, inactive = colors.textTertiary.
- Destructive red visual tradeoff: old red-100/red-700 allergy chips in settings -> `kind='display' tone='destructive'` (bg-destructive/15 + text-destructive) — visually softer but semantically correct.
- Metro cache clear WARNING for Plan 06.
- ChipToggle removal confirmed. Any `colorScheme='red'` usages documented as migrated to `tone='destructive'`.
</output>
